# Codex Adversarial Review — Fable Reviewer Assessment (2026-08-01)

Model `gpt-5.6-sol`, reasoning effort `high`, run via
`/codex:adversarial-review --wait` against
`outputs/reviewer-workflow-stabilization-fable-assessment.md` on branch
`fable/reviewer-stabilization-assessment`.

**Reproduced verbatim.** Owner convention: Codex output is shared unedited. The
Fable author's response to each finding is in the assessment's §0, not here.

---

# Codex Adversarial Review

Target: branch diff against main
Verdict: needs-attention

Do not accept this assessment as implementation authority yet. The core current-source mechanics are largely CONFIRMED, but the “latent gap, not regression” conclusion is REFUTED by history, Finding A’s five-production-row claim is not causally proven, and the proposed W6 slice has atomicity, partial-success, and caller-contract holes.

Findings:
- [high] REFUTED — the “not a regression” reframe misses functional historical filters and a newly introduced 409 (outputs/reviewer-workflow-stabilization-fable-assessment.md:331-342)
  The cited `git log -S wmkf_invited/declined` check is too narrow. In `fe825933`, `pages/api/workbench/enrich-recommended.js:98-101` loaded `findByRequest(..., { selectedOnly:true })`, which excluded declined rows after decline archival set `selected=false`. Commit `ad8e0299` replaced that functional filter with disposition-only lookup while changing new applicant rows to `selected=false`, directly enabling declined rows to enter enrichment. Separately, `86d66f28` introduced `stored.candidateKey !== candidate.candidateKey` while ordinary enrichment DTOs remained keyless, creating the fresh-confirm/exclude 409. These are concrete change-induced regressions even though no literal invited/declined predicate was removed.
  Recommendation: Reframe as a latent lifecycle-model weakness exposed through specific regressions. Extend archaeology across behavioral symbols (`selectedOnly`, applicant row creation, candidate-key binding and DTO emission), and add regression tests pinned to both historical transitions.
- [high] MISSING — W6’s proposed guard is neither early enough nor concurrency-safe (outputs/reviewer-workflow-stabilization-fable-assessment.md:899-929)
  Placing the guard in `updateLifecycle` would reject only after `promoteApplicantReviewer` has already mutated global person/contact state at `promote-applicant-reviewer-service.js:430-503`; failure would leave partial contact changes without promotion. It also remains a TOCTOU check: `updateLifecycle` currently derives an ETag only for review-status changes at `reviewer-suggestion.js:1350-1368`, not `selected=true`, so a concurrent decline between guard read and PATCH can still produce the hybrid state. Restore is safely distinct because `restore()` performs its explicit reset with the row ETag at `reviewer-suggestion.js:1661-1686`. A predicate accepting only `(existing,payload)` also cannot treat “reset fields present” as authority, because door A automatically carries the same reset fields.
  Recommendation: Guard engagement before any contact mutation, then repeat the check atomically at the final selection write using the suggestion ETag. Keep `restore()` as the explicit scoped reset path, and test concurrent decline, zero pre-rejection contact writes, door A, door B, and Restore.
- [high] REFUTED — the probe does not prove door A fired five times (outputs/reviewer-workflow-stabilization-fable-assessment.md:562-582)
  The final-state signature `disposition=recommended + selected=true + staff_manual/referred source` is not directional evidence. `ensureApplicantRecommended` can encounter an already-selected manual/referred row, union `applicant`, set disposition to recommended, and deliberately preserve `selected` (`reviewer-suggestion.js:538-574`). That produces exactly the five-row signature without an applicant row ever being promoted through `ensureStaffManualCandidate`. The source-level bypass mechanism is CONFIRMED, but its measured production occurrence remains UNKNOWN; the probe itself correctly calls these rows candidates, while the assessment upgrades them to proven events.
  Recommendation: Downgrade “fired 5 times” to “5 ambiguous final-state candidates.” Establish mutation order from Dataverse audit history, logs, or timestamps before using the count as the strongest empirical argument for W6.
- [high] MISSING — the provenance-only door-A fix would falsely report a successful add (outputs/reviewer-workflow-stabilization-fable-assessment.md:899-910)
  The proposed minimal change leaves an existing applicant row unselected but does not revise the endpoint contract. `addManualReviewer` ignores `suggestion.selected`, returns `success:true`, labels the candidate `applicantRecommended:false`, and reports it as manually added (`manual-reviewer-service.js:275-300`). The referral UI then marks the action “added,” refreshes, and navigates to Invite where it expects the candidate to appear (`ReviewersTab.js:205-215`). Under the proposed engagement filtering, an engaged applicant row may instead be hidden entirely. This converts data corruption into a silent user-visible no-op.
  Recommendation: Define the caller-visible outcome: either route the action through the gated applicant promotion/explicit restore workflow, or return a typed `promotion_required`/`restore_required` response and render that remedy. Never return “added” unless the row is actually selected and visible in Invite.
- [high] OVERSTATED — the all-candidate monotonicity invariant is not implemented by the applicant-only slice (outputs/reviewer-workflow-stabilization-fable-assessment.md:781-803)
  I-1 explicitly covers all candidates, and the assessment itself notes that search-origin Wolberger would share the defect if her roster key differed. Yet the slice joins Dataverse engagement only for applicant-recommended rows. Current `displayRosterActive` restores every non-applicant active Postgres row without any Dataverse lifecycle check (`ReviewerSearchSection.js:1347-1371`). This split is reachable: general candidate saving treats roster finalization as non-fatal after Dataverse succeeds (`save-candidates-service.js:1517-1555`). Such a row can therefore remain actionable in Find despite durable engagement, and applicant-only projection does not repair it.
  Recommendation: Either narrow I-1 and the campaign-safety claim to applicant-origin rows, or add a bounded Dataverse reconciliation for every roster row carrying a suggestion anchor. Include roster-finalization failure and non-applicant key-drift workflows.
- [medium] MISSING — enrichment persistence failures are still presented as complete success (outputs/reviewer-workflow-stabilization-fable-assessment.md:854-858)
  `persistRecommendedRoster` catches per-row Postgres failures and reports only aggregate counts (`enrich-recommended-service.js:166-200`); the service still emits every candidate in `complete` (`:967-981`), and the client renders them and sets phase `done` (`ReviewerSearchSection.js:1288-1308`). Confirm/exclude then requires a matching stored row through `authoritativeApplicantCandidate` (`reviewer-roster.js:93-98`), so a failed insert or concurrency skip produces a card that immediately 409s and reruns enrichment after reload. Section 5’s “all-failed hydration” note does not cover this Postgres partial-success mode or identify failed rows.
  Recommendation: Return recorded, concurrency-skipped, and failed suggestion IDs separately. Re-read the roster after persistence, render only authoritative rows as actionable, and keep failed rows explicitly retryable. Add all-failed, mixed-failure, and concurrent-terminalization tests.
- [medium] OVERSTATED — Finding C’s incident attribution and the final Finding-D synthesis exceed the evidence (outputs/reviewer-workflow-stabilization-fable-assessment.md:640-658)
  The assessment admits Dataverse metadata cannot distinguish an app-created duplicate from direct Dynamics entry at lines 653-658, but elsewhere calls the duplicate confirmed as minted by the button and uses that attribution to prioritize the feature. Current source CONFIRMS that the button can send the whole referral and create a duplicate; it does not prove that this specific row came through that request. The verdict also resurrects Finding D as a common silent-divergence example at lines 1369-1376 after concluding at lines 1378-1383 that the observed real-world scenario count is zero. Those conclusions silently retain retracted or explicitly ambiguous premises.
  Recommendation: Label Finding C’s mechanism CONFIRMED but the specific production causation PLAUSIBLE/UNKNOWN unless request logs or audit records prove it. Remove Finding D from the final common-thread argument and ground any exclusion-copy recommendation solely in separately measured name-variant evidence.
- [medium] MISSING — “candidateKey one line ×2” overlooks the hydration-failure DTO (outputs/reviewer-workflow-stabilization-fable-assessment.md:886-888)
  `hydrationFailureCandidate` is a third emitted applicant DTO and omits `candidateKey` (`enrich-recommended-service.js:107-141`); those objects are appended directly to `out` at line 538 and sent in the completion payload. Adding the key only to the needs-review and resolved branches leaves I-4 (“every applicant candidate DTO”) false and preserves a fresh-window 409 for actions such as exclude unless every client happens to apply the proposed defensive mapper.
  Recommendation: Canonicalize keys once over the complete outgoing applicant DTO array, including preserved, hydration-failure, needs-review, resolved, and handled entries. Add branch-complete contract tests rather than two branch-local assertions.

Next steps:
- Correct the assessment’s regression history and downgrade unsupported production-causation claims.
- Redesign W6 around early validation plus ETag-bound final selection, while preserving explicit Restore semantics.
- Specify manual-add and enrichment partial-success response contracts before treating section 6 as implementation-ready.
- Decide whether engagement monotonicity is applicant-only or truly applies to every anchored roster row.
