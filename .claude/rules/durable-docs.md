---
paths:
  - "docs/**"
  - ".claude-memory/**"
  - "CLAUDE.md"
  - "SESSION_PROMPT.md"
---

# Durable Documents

Read the entire target before editing a fact, status, or claim. Search the live repo for restatements and reconcile frontmatter, summaries, body, tails, and linked docs in the same pass. Use `/sweep` for fact-level reconciliation and run the relevant drift gates before declaring completion. Historical/archive records may remain historical; classify them explicitly rather than silently rewriting them.
