---
title: "Service & Utility Catalog"
domain: docs-governance
kind: source-of-truth
status: canonical
summary: "If you're touching a service or utility, read its header before this catalog. If a header is sparse or stale, fix it in the same commit as your..."
canonical: true
cataloged: 2026-07-02
last_verified: 2026-09-05
owner: product-engineering
related:
  - lib/services/
  - lib/external/
  - lib/bill/
  - lib/utils/
  - lib/observability/
---

# Service & Utility Catalog

One-line lookup index for files under `lib/services/`, `lib/external/`, `lib/bill/`, `lib/utils/`, and `lib/observability/`. **Source-file headers are authoritative** for per-file contracts, safety posture, storage source-of-truth, and migration/drop history — this doc points there, it doesn't replace them.

If you're touching a service or utility, read its header before this catalog. If a header is sparse or stale, fix it in the same commit as your change rather than rely on this doc.

---

## `lib/services/`

### AI / prompt execution

- **`llm-client.js`** — Canonical Anthropic API wrapper (`complete()` + `stream()`). SSRF allowlist, abortable timeouts, 429/529 retry, single fallback-model swap, usage logging (including optional Explorer request/round correlation), API-key redaction. **Use this — not ad-hoc `fetch`** for new Anthropic API calls.
- **`model-capabilities.js`** — Reviewed Anthropic model capability registry for request shaping (`temperature`, `output_config.effort`) and response semantics (refusals, retention class, max tokens). Unknown runtime ids fail closed for optional params; configured ids are guarded by `check:model-registry`.
- **`model-review-validation.js`** — Shared write-time validator for tier keys and concrete Claude model ids. Admin model overrides and prompt publishes use it to reject unreviewed concrete Claude ids unless both capability and pricing entries exist.
- **`execute-prompt.js`** — Live prompt-execution Executor implementing `docs/EXECUTOR_CONTRACT.md`. Reads current prompt rows from Dataverse entity set `wmkf_ai_prompts`, rejects unreviewed concrete Claude model ids before execution, caps server-owned token overrides to the resolved model ceiling (while invalid prompt-row budgets fail closed), enforces optional final retry lower bounds, and attempts append-only audit rows in `wmkf_ai_runs`. Production consumers include Phase-I Dynamics summary, grantee title/abstract, field primer, peer-review summary, and review synthesis flows; inspect current callers before changing the contract.
- **`executor-budget-service.js`** — Strict schema and append-only publication/read contract for server-owned Executor standing/retry budgets in Dataverse `wmkf_appsystemsettings` (`executor.budgets.vNNNNNN`). Superuser publication uses reserved-revision optimistic concurrency, canonical UUID idempotency across every parseable row, reviewed model ceilings, paged reads, create-race reconciliation, and exact-row post-create verification. Malformed rows are skipped with Admin-visible warnings while their numeric keys remain reserved; unknown future schemas block older publishers. Governed prompt-model publications cross-check the current budget, while runtime reads fall back to bounded reviewed code defaults only on settings failure or when no valid revision exists.
- **`prompt-resolver.js`** — Legacy Session 103 holdover. Reads prompts from a scratch row on `wmkf_ai_runs`, 5-min cache, `{{var}}` interpolation, bundled `.js` fallback. `PROMPT_RESOLVER_STRICT=true` disables fallback. Currently used only by scripts; no live API route depends on it.
- **`model-resolver.js`** / **`model-override-loader.js`** — Per-app model overrides for `baseConfig.js`. Resolver computes effective model per app at call time and exposes `resolveModelWithCapabilities()` for coupled concrete-id + reviewed-capability lookup; loader caches DB-backed overrides.
- **`claude-reviewer-service.js`** — Live, legacy-named Reviewer
  Finder/Workbench orchestration service. It now delegates provider calls through
  `llm-client.js`; do not treat it as dead code.

### Dynamics / Dataverse

- **`dynamics-service.js`** — Dynamics 365 / Dataverse client (OAuth, OData, Dataverse Search, email activities, `updateIfEmpty`, `logAiRun`). Impersonation contract documented in `docs/DYNAMICS_IDENTITY_RECONCILIATION_PLAN.md`.
- **`dynamics-context.js`** — AsyncLocalStorage restriction context. `withDynamicsContext` / `bypassDynamicsRestrictions` for route + library callers; `enterDynamicsBypassForScript` for top-level scripts. **`DynamicsService.checkRestriction()` fails closed** when no context is set — every caller must opt in explicitly.
- **`dynamics-explorer-taxonomy.js`** — Dynamics Explorer A2 layer (S200): 6h-cached, **fail-loud** live resolution of program / grantprogram / type / `wmkf_request_type` / distinct `akoya_requeststatus` over a fixed whitelist, with table-restriction gate + GUID/int/label validation, into a server-resolved system-prompt block (replaces the old hardcoded program GUIDs). Wraps `dataverse-export/live-taxonomy.js`.
- **`dynamics-explorer-request-telemetry.js`** — Data-minimized one-row-per-request Explorer lifecycle writer. Starts `running`, atomically finalizes one terminal outcome, recovers a missing start with a conflict-safe terminal insert, and always fails soft toward the answer path. Stores no prompt, answer, tool output, query text, or exception message.
- **`dynamics-odata-validator.js`** — Dynamics Explorer OData pre-flight validator (S200): tolerant tokenizer + live `getEntityAttributes` checks, synthesized Lookup/Customer/Owner `_value` aliases, restriction enforcement across navigation-path roots, invalid Edm.Guid comparison checks, and rejects for provably wrong lookup / `$expand` forms. Every `$expand` fails closed while any field-level restriction is configured because the related target cannot be resolved here; otherwise unknown shapes pass through. Reject-with-hint only, **no auto-correct**. Historical design: `docs/DYNAMICS_EXPLORER_ODATA_VALIDATOR_DESIGN.md`.
- **`dataverse-identity-map.js`** / **`dynamics-identity-service.js`** — `user_profiles` ↔ Dynamics `systemuser` bridge; reconciliation CLI at `scripts/reconcile-dynamics-identities.js`.
- **`request-document-actor-service.js`** — **[PRODUCTION-PROVED FOR PRE-SITE CREATION 2026-08-31; WAVE 24 SCHEMA EXACT; PRODUCTION READINESS ON.]** Server-only Request Document business-actor resolver. It accepts only a session-derived Dynamics GUID, freshly rereads the exact enabled `systemuser`, enforces each writer's required vs availability-first policy, produces immutable create-time actor fields, and records a bounded `request_document_actor_not_captured` operational event for approved null-attribution outcomes. Commit `8ff4205a0ad43337cd987a4fc76639f936bab4bc` first reached Ready Production deployment `dpl_D94J9aRcfLfK81iBDsVYARVhZFPb`; signed-in health passed. Naturally generated Request `1002874` proved a Ready/Draft Pre-Site row with explicit Justin Gallivan origin actor/time, application built-in creator, exact current pointer, no missing-attribution event, and a deployment-boundary census of 1 attributed / 0 event-backed unattributed / 0 violations. Site Visit milestone and other path-specific first-write proofs remain opportunistic.
- **`program-director-resolver.js`** — Email → Dynamics `systemuser` bridge for Reviewer Finder's PD-filtered picker.
- **`grant-cycles-dataverse.js`** — Dataverse `wmkf_appgrantcycle` adapter (Migrates / Replaced `grant_cycles` at W3 cutover 2026-05-12 — drain-only thereafter). Consumed by Reviewer Finder + Review Manager (`render-emails`, `send-emails`) + `maintenance-service` blob cleanup.

### Wave 1 dispatch (Dataverse-default since 2026-05-12)

- **`settings-service.js`** / **`dataverse-settings-service.js`** — Dataverse `wmkf_appsystemsettings`. Legacy Postgres `system_settings` was dropped 2026-05-12 (Migrates / Replaced); dispatch retained as fail-loud opt-out. Exposes throwing metadata-prefix reads with `odata.maxpagesize`/`@odata.nextLink` paging and formatted lookup provenance, plus create-only writes for immutable versioned settings, in addition to the legacy fail-soft/upsert surface.
- **`app-access-service.js`** / **`dataverse-app-access-service.js`** — Dataverse `wmkf_appuserappaccesses`. Legacy Postgres `user_app_access` was dropped 2026-05-12 (Migrates / Replaced).
- **`dataverse-prefs-service.js`** — Dataverse `wmkf_appuserpreferences`
  adapter. Postgres `user_preferences` was dropped 2026-05-12 and its old
  database-service branch was removed; unsupported Postgres configuration fails
  loudly.
- **`email-signature.js`** — Unified per-user email-signature resolver. Reads the `email_signature` preference from Dataverse user preferences, falls back to legacy reviewer sender info, resolves request-scoped grantee signatures from the assigned Dataverse PD via `dataverse-identity-map`, and appends a Foundation-ending signature block for grantee invite/reminder mail.
- **`email-automation-preferences.js`** — Strict reader for the reserved per-PD `email_automation` review-all override (`{ reviewAll: boolean }`). Absence means the system default (VIP-only review); malformed persisted JSON throws so a read/shape failure can never silently weaken a PD's review posture.

### Storage / persistence

- **`database-service.js`** — Vercel Postgres operations; Wave 1 dispatch lives here.
- **`scheduled-email-store.js`** / **`scheduled-email-service.js`** — Source-built personalized-email ledger/coordinator plus per-(PD, contact) VIP flags (migration 036 applied 2026-08-26, branch code not yet deployed; VIP/digest decision layer per `docs/SCHEDULED_EMAIL_VIP_DIGEST_PLAN.md`). Freezes exact recipient/sender/draft/schedule/approval state at row creation, versions PD edit/approve/stop actions, refuses due-send claims on unapproved approval-required rows (send_now is the only bypass), builds the per-PD daily digest with receipted FYIs (claimed through the `scheduled_email_digest_runs` lease/membership ledger since the 2026-08-26 adversarial-review fixes), rebuilds unsent rows under the request's current PD on handoff, mints secure links only at real send, persists or correlation-recovers the Dynamics activity before transport, and repairs sent-but-unfinalized Dataverse state without re-sending. The store also holds the per-(lead PD, reviewer person) invitation VIP flags (`scheduled_email_reviewer_vip_flags`, person-keyed; synchronous Invite-Reviewers routing aid only, no ledger consumer). [RECHECKED after lib/services/scheduled-email-store.js change: reviewer VIP flag helpers added 2026-08-26] [RECHECKED after lib/services/scheduled-email-store.js change: claimDigestRun/reassignScheduledEmail added 2026-08-26] [RECHECKED after lib/services/scheduled-email-service.js change: sendScheduledEmailDigest rewritten onto the run ledger 2026-08-26]
- **`graph-service.js`** — Microsoft Graph (SharePoint files, listing/download, exact historical-version metadata/download/native restore, exact stable-item content replacement with required `If-Match`, current-item PDF conversion, content search, idempotent folder-path creation, stable path-to-item metadata, stable drive/item-ID metadata readback, and upload responses carrying site/drive/item/eTag/version metadata). Path uploads accept a closed `replace`/`fail` conflict behavior so ordinary retained snapshots remain create-only. Historical-version and conversion downloads manually follow Graph's pre-authenticated redirect without forwarding the bearer token.
- **`workbench-proposal-documents.js`** — Workbench document listing plus strict AI-input resolvers. Initial Assessment, request-bound Field Primer, and Pre-Site Visit use the exact active `AI Materials/ProposalNarrative_{Request#}.pdf`; Reviewer packages, Phase I files, archives, neighbors, and ambiguous active buckets do not substitute. The separate materials resolver still exposes the exact bibliography for the planned next-cycle Reviewer Finder contract and Proposal-tab display.
- **`workbench/request-search-service.js`** — **[PRODUCTION-LIVE 2026-09-04; SIGNED-IN SMOKE PASSED.]** Read-only active/historical Workbench request discovery. Text queries escape legacy Search operators, reject escaped expressions beyond the transport limit, and union up to 100 ranked Dataverse Search hits with a bounded, uniquely ordered Contact-name lookup (26 read to detect truncation; first 25 joined through authoritative `_wmkf_projectleader_value`). Project Leader matches rank before secondary full-text matches; the service hydrates Search IDs in 50-row chunks, deduplicates the union, discloses incomplete/capped sources, and returns stable 25-row pages under a 100-candidate ceiling. Filter-only queries use escaped OData; both legs apply the same cycle/status filter. Live cycle/status options use restriction-checked server aggregates rather than a hardcoded taxonomy. The service refuses an unfiltered scan and reports stale indexed matches that cannot be hydrated. Commit `cd2c73d5` is Ready in Production deployment `dpl_D6r4deRuAwe6xCgwGSmc5WTHWa7W`; signed-in smoke proved PI/title/exact-number/no-result behavior and stable 25→50 append with no duplicates.
- **`initial-assessment/artifact-service.js`** — Governed Initial Assessment producer + shared Workbench/pilot-locator read model. Requires exactly one active `AI Materials/ProposalNarrative_{Request#}.pdf` before any registry/AI/upload write, requires a positively resolved Dynamics-tracked request library, converges exact retries through `wmkf_requestdocument`, never overwrites Ready artifacts, and atomically commits replacement Ready + prior supersession (or exact-input reactivation) with the shared `akoya_request.wmkf_CurrentInitialAssessment` ETag fence. It preserves the canonical file while exposing a failed replacement separately, cleans up claim-lost uploads with character-bounded durable cleanup work on delete failure, writes DOCX bytes to the request's `Artifacts/Initial Assessment/` folder, and returns success only after stable SharePoint identity, canonical pointer, and lineage read-back are confirmed in Dataverse. Reads resolve the canonical editable artifact through the request pointer, refresh current Graph metadata, and return exact Ready/Board Ready snapshot rows separately. The exact snapshot producer is excluded from editable activation, supersession, and cycle discovery; unknown/lookalike producers remain ordinary fail-closed rows. **[VERIFIED IN SOURCE 2026-08-16 via focused tests and read-only live Request `1002788` extraction]** the new exact AI input resolves with non-empty text. The deployed locator is narrower than the planned full Editor Dashboard.
- **`initial-assessment/controls-service.js`** — Production-deployed superuser restore and Board-freeze coordinator (2026-08-30; signed-in read surface passed, POST writes not Production-exercised). Restore resolves the request pointer/stable item server-side, narrows stale races with exact version/metadata fences, invokes native Graph restore, requires byte-for-byte agreement with the selected version, ETag-patches the registry, and reconciles a lost response without a duplicate restore. Board freeze creates/reclaims a deterministic distinct Ready/Board Ready row/item, uses create-only path upload for the exact selected source buffer, and derives publication version/eTag metadata only from stable-ID Graph reads before and after normalized governed-content verification; upload/path `cTag` values are never persisted as versions. Exact Ready reuse may ETag-reconcile benign metadata drift only after the stable read proves the same governed content. Recovery removes the published drive/item from both cleanup queues while preserving unrelated cleanup work. Different governed content, unstable metadata, invalid cleanup JSON, and invalid DOCX packages fail closed; only items uploaded by the owned attempt can be recorded for cleanup, and a contested claim returns a retryable conflict.
- **`initial-assessment/template.js`** — Versioned `standard_business_brief` DOCX template. AI content is limited to Summary, Significance & Impact, Research Plan, and Team Expertise; Foundation Opportunity is always rendered as staff-required.
- **`pre-site-visit/proposal-core-service.js`** — Guarded Pre-Site proposal-core input/execution helper. It requires the exact active `AI Materials/ProposalNarrative_{Request#}.pdf`, composes authoritative request/PI/Co-PI context from Dataverse, and calls `pre-site-visit.proposal-core.generate` with bounded context and narrative overrides plus exported system assertions. `DV:InstitutionName` prefers the applicant account's `akoya_aka`, then account `name`, then the formatted applicant lookup; Request Bill.com organization fields are excluded. **[DEPLOYED TO PRODUCTION 2026-08-18; SIGNED-IN GENERATION + NO-DUPLICATE SMOKES PASSED 2026-08-27]** it deterministically normalizes the eight-field core, rejects trim-empty sections and unresolved reserved placeholders, drops undeclared output keys, and computes content-free editorial diagnostics including section/paragraph/combined targets, roster-name mismatches, and proposal-input truncation. Raw provider output remains on the AI run; only the canonical core reaches the artifact writer. The bibliography is deliberately excluded and remains reserved for next-cycle Reviewer Finder.
- **`pre-site-visit/artifact-service.js`** — Durable Pre-Site Request Document producer and read-only status model. **[VERIFIED IN PRODUCTION 2026-08-17.]** POST resolves and fingerprints the exact narrative before side effects, requires a governed prompt declaring exactly the context and `proposal_text` variables, claims/reclaims one deterministic Wave 19 row, persists all eight named fields plus immutable generated/input snapshots and prompt/run provenance, rereads Dataverse before rendering, uploads to `Artifacts/Pre-Site Visit/`, and atomically activates the Ready Word row with `akoya_request.wmkf_CurrentPreSiteVisit`. Request `1002379` proved generation and exact reuse. Bibliography changes do not affect generation identity; persisted-output retries avoid another Claude call; matching post-upload recovery reuses the same item; prompt/claim/cleanup races fail closed. **[DEPLOYED TO PRODUCTION 2026-08-18; SIGNED-IN GENERATION + NO-DUPLICATE SMOKES PASSED 2026-08-27]** a strict tracked-contract preflight runs before claim/model work; canonical envelope v3 persists diagnostics while v2 remains readable; named fields must reconcile exactly; every POST/GET/reuse path uses one warning projection; deterministic content failures block unchanged retry and expose a durable support reference. **[DEPLOYED TO PRODUCTION 2026-08-17; SIGNED-IN FEATURE SMOKE OPEN]** `getPreSiteVisitArtifactStatus` reads the current pointer plus latest newer pending operation without claims or writes and fails closed on invalid Ready lineage. The source-built correction-cycle branch also rejects final activation when the target cycle no longer equals the current Draft pointer, preventing an older in-flight generation from superseding a newer reopen. Exact frozen-distribution producers are excluded from editable status, activation cardinality, and supersession; unknown/lookalike producers retain normal fail-closed behavior.
- **`pre-site-visit/site-visit-transition-service.js`** — Guarded Pre-Site→Site Visit lifecycle transition. The request's current Pre-Site pointer is authority and a client artifact id is only an expected-value fence. It requires a same-request Ready/Draft Word row, verifies one stable Graph drive/item/publication version around a governed DOCX download/hash, then uses one ETag-conditional Dataverse PATCH to set lifecycle Review, persist the Site Visit handoff milestone, and refresh current file metadata without copying or changing SharePoint bytes. Exact completed Review retries are idempotent; stale UI, unknown states, file/version races, incomplete milestones, and concurrency conflicts fail closed. The Pre-Site generator separately refuses regeneration after promotion before any expensive or mutating work. Its API route strips nested correction audit details from the returned artifact unless the caller's fresh role is superuser.
- **`final-writeup/transition-service.js`** — **[PRODUCTION-PROVED 2026-08-30 PT / 2026-08-31 UTC; WAVE 22 EXACT, FLAG ON.]** Governed same-item Site Visit→Final Writeup group-review transition. It requires a session-derived actor and server-derived lead-PD/superuser authorization, verifies the current Ready/Review Pre-Site pointer plus one stable current Graph publication version and governed DOCX hash, and claims/reuses one deterministic Final row with the same drive/item identity. One three-ETag changeset moves the source to lifecycle Final, readies the Final row in Review with explicit group-review actor/time, retains Current Pre-Site, and sets Current Final. Request `1002788` proved one Ready/Review Final row and pointer, explicit Justin Gallivan attribution, and the unchanged 38,273-byte SharePoint item/version/hash with no new file. Exact retry, live lease, source/version races, atomic rejection, and ambiguous-response recovery are focused-test covered. It has no Graph upload/copy, acknowledgement, dashboard, leadership transition, or backward-stage action.
- **`final-writeup/acknowledgement-service.js`** — **[PRODUCTION-PROVED 2026-08-31.]** Wave 23 mark/read coordinator behind a distinct literal-on readiness interlock. It resolves the current Final and responsible PD server-side, validates the session-derived enabled `systemuser`, rejects PD self-review, records one stable-ID Graph publication observation, preserves same-version acknowledgement time, conditionally replaces later versions with `If-Match`/`noFallback`, and rereads ambiguous outcomes. Publication version owns freshness; eTag and last-modified remain diagnostic. Production readiness is exact `on` in Ready deployment `dpl_B9k3AprnYp5ExpkqpT3dUxCUZqWo`. An eligible colleague's pre-role POST exposed missing acknowledgement Create without partial persistence. After the dedicated role became effective for all 11 audience members, the retry succeeded, appeared in review history, and independent readback proved one complete acknowledgement row for Request `1002788`.
- **`final-writeup/dashboard-service.js`** — **[PERSONA LENSES PRODUCTION-LIVE 2026-09-03 PT.]** The bounded Final Writeups projection preserves the proved broad-Grant-Program superuser matrix and, from commit `213f6c34` / Ready deployment `dpl_HGrbWUNPJMJunVevYLVEmtn7He6a`, derives explicit PD, PC, Leadership, and overlapping queues from the published v2 assignments intersected with the current reviewer-role roster. Configured PCs receive the complete neutral matrix; superusers retain their complete view independent of persona. A read-only production-data smoke passed for PD, PC, Leadership, overlap, ineligible/unassigned, and superuser identities. V1/missing/unassigned/removed-role cases return no persona rows with bounded warnings rather than broadening access; rollout-off remains test-covered for rollback.
- **`final-writeup/matrix-audience-service.js`** — **[PRODUCTION-LIVE V2 2026-09-01 UTC.]** Strict v1/v2 `final_writeup.matrix_audiences` contract. V1 remains matrix-readable; only complete canonical v2 staffing replacements are writable. V2 combines explicit GUID-only persona/no-lens rows with broad Grant Program reviewer audiences under one ETag. Publication requires every current enabled reviewer-role member and rejects unknown/stale/incomplete references; runtime intersects stored references with current roles/programs, omits stale identities with warnings, and never treats program audience as persona authority. The dry-run-first operator command published once under ETag `W/"96930393"`; exact readback proved v2 at `W/"96944113"`, 11 complete assignments, zero stale/unassigned rows, and unchanged nine-person Research/six-person Southern California audiences. Repair/downgrade tooling remains the out-of-band malformed-value and rollback path.
- **`final-writeup/persona-service.js`** — **[PRODUCTION-LIVE / ROLLOUT ENABLED 2026-09-03 PT.]** Server-only viewer resolution from the published v2 assignment and current direct reviewer-role membership. The tracked source flag is true at `213f6c34`; disabled mode remains the rollback contract and returns the legacy empty result without reading the setting or roster. Explicit no-lens, missing row, removed-role, flag-on v1, and overlap semantics are separately tested.
- **`pre-site-visit/reopen-service.js`** — Production-proved superuser-only guarded correction service (**Wave 20 exact + literal-on Production readiness flag + Ready deployment `dpl_BbtmRghhSYa7EPiQkWxsmdkgRozp`, 2026-08-23**). After exact owner approval, signed-in Request `1002379` created one Ready/Draft successor and distinct SharePoint copy, atomically superseded the preserved Review row and moved the request pointer; exact unchanged retry reused the same row/item, and postcheck proved one cycle row plus matching governed source/successor bytes. It verifies the current Ready/Review pointer and exact unchanged handoff bytes; blocks a Final, any non-distribution derived Request Document, or a different editable generation with a live lease; and treats exact frozen-distribution producers as retained informational evidence rather than downstream/competing-generation blockers. It creates/reclaims one client-UUID-bound Ready/Draft successor and unique SharePoint copy, then atomically supersedes the preserved Review row and moves the request pointer under ETags. Successor reason fields plus existing lineage form the append-only correction event; exact unchanged retry and post-upload recovery reuse the same row/item. Failed rows remain evidence but do not block a distinct later client operation; expired reopen claims and Failed attempts superseded by a distinct operation record any retained copy as stable-identity cleanup work. Later Pre-Site generation is salted with the correction cycle so it cannot reactivate the preserved generation. **[CORRECTED 2026-08-31; WAVE 24 PRODUCTION-LIVE, CONTROLLED REOPEN PROOF PENDING]** Standard `createdby` may still name the service principal, but the runtime now projects immutable explicit origin actor/time and an honest “Not captured” fallback. No reopen was manufactured to prove the new field; the next naturally needed reopen must receive exact write/readback verification. The route fails closed while the Wave 20 readiness interlock is off, and response-boundary redaction limits history and nested correction details to superusers and omits their pending reopen-attempt row entirely.
- **`pre-site-visit/distribution-service.js` / `distribution-store.js`** — Production-proved frozen informational distribution (**2026-08-24, Request `1002379`, PDF-only operation `85f52fc5-fb48-4ceb-84d6-0f246af0b6fb`**). The path retained Ready/Board Ready DOCX and PDF rows, sent the selected 133,265-byte PDF to `jgallivan@wmkeck.org`, persisted one `sent` ledger row, and produced Dynamics activity `33ce6346-d89f-f111-b8db-6045bd07a06d` with Sent status, actor attribution, and exactly one attachment matching SHA-256 `574ac7b833801866c370a8056b7197933addfe3ea5dd535dcf4d29803c18f0c9`; history was visible and the bounded Production error-log scan was clean. Dynamics appended its CRM tracking token after pre-transport preview acceptance; inbox delivery is not independently verified. It captures an exact current Pre-Site Word version under before/after metadata and governed/raw hashes, always retains a deterministic Word snapshot from stable-ID native publication version/eTag, fences that Word identity around optional PDF conversion, and lets staff select DOCX, PDF, or both. Known staff/consultant addresses are staff-authoritative but syntax-normalized, deduplicated, and rejected on invalid/To-Cc conflict. A content-hashed exact preview binds actor/sender, recipients, message, source, mode, and selected attachment identities/bytes. Every non-sent attempt requires literal Dynamics impersonation and current pointer/source-version revalidation under the lease. Creation or unique correlation recovery persists the activity ID before exact assertions; ambiguity remains a failure. Per-kind attachment receipts, durable `send_requested`, lease renewal immediately before transport, status-before-retry, and exact sent receipt prevent blind duplicate activity/send after partial failure. History compares the captured source with the current pointer/version and marks drift. SharePoint/Request Document remain file authority; Postgres stores identities/hashes only, never attachment bytes.
- **`site-visit/curated-recipient-service.js`** — Admin-curated recipient options for the Pre-Site distribution composer. A versioned, 50-entry maximum `wmkf_appsystemsettings` value stores only active `user_profiles.id` and Dataverse Contact GUID references plus consultant/board category. It resolves active staff through the same exact profile→enabled-systemuser contract as the legacy Site Visit attendee directory, resolves external names/emails live from active Contacts, exposes stale references only to Admin for removal, and omits them from Workbench options. The superuser write is full-replacement and all-or-nothing; the read/search surface never creates or edits Contacts. **[VERIFIED IN SOURCE + LIVE FIELD-METADATA PROBE 2026-08-29.]**
- **`pre-site-visit/funding-history.js`** — Deterministic (no LLM) Institutional Funding History sentence for the Pre-Site writeup, matching the retired Power Automate template: `<AKA> has received N awards totaling $X million from WMKF. The most recent grant was awarded in <akoya_fiscalyear> <wmkf_wmkfprojectdescription>.` Count/sum come from the applicant account rollups `wmkf_countofprogramgrants`/`wmkf_sumofprogramgrants` and are cross-checked (`reconcileProgramGrantRollups`) against the live `akoya_request` rows matching the rollups' own predicate (`wmkf_typeforrollup eq 'Program' and akoya_grant gt 0`, recovered from their FormulaDefinition 2026-08-28; `loadProgramGrants` via `grantRequestAdapter.queryAllRequests`). The cited award is the newest **Research**-program grant (`researchProgramGrants`, matched on the `_wmkf_grantprogram_value` formatted label — owner decision 2026-08-28; count/sum still span all programs for parity with the form, and the sentence says "most recent research grant" when a newer non-research program grant exists). Recency is computed in code (`akoya_decisiondate`, else `wmkf_meetingdate`; `selectMostRecentProgramGrant`). `proposal-core-service.js` fails closed with 409 `pre_site_visit_funding_history_unavailable` on applicant read failure, query failure/cap, rollup↔live mismatch, or an undated grant. The sentence travels in the v3 input snapshot (so it participates in the generation key) and is never prompt input.
- **`pre-site-visit/docx-renderer.js`** — Template-preserving OOXML renderer for the tracked Phase II Pre-Site Word template. Replaces split-run Dataverse/AI placeholders across body, tables, and headers while retaining the complete package, page structure, PAGE field, and PD-owned graphical-abstract/recommendation/review slots. Since S467 (2026-08-28) `[[AI:InstitutionalFundingHistory]]` is a required filled placeholder (from `documentFields.institutionalFundingHistory`, produced by `pre-site-visit/funding-history.js`), not a PD slot; rendering without it throws. Production template v2 added the same 0.1-inch left cell padding to Recommendation used by the amount values and created a distinct Ready artifact. Its Word Online render exposed a width-sensitive Recommendation-label alignment defect. **[INFERRED FROM SCREENSHOT + OOXML WIDTH]** implicit wrapping was the remaining layout variable. **[DEPLOYED TO PRODUCTION 2026-08-17; SIGNED-IN FEATURE SMOKE OPEN]** template v3 makes that label explicitly non-wrapping. **[DEPLOYED TO PRODUCTION 2026-08-18; SIGNED-IN GENERATION + NO-DUPLICATE SMOKES PASSED 2026-08-27]** template v4 retains the no-wrap contract and changes only the first-page metadata rhythm: one blank line after Project Title, compact single-spaced Meeting Date/Staff Lead/Recommendation rows, and one blank line ending at the divider. **[DEPLOYED TO PRODUCTION 2026-08-18; SIGNED-IN GENERATION + NO-DUPLICATE SMOKES PASSED 2026-08-27]** render contract v5 keeps the exact v4 template bytes but renders any non-empty long-form paragraph count and treats exact roster-name underlining as best effort. Each contract version participates in the generation key, so later generation cannot collide with earlier Ready documents.

### Intake portal

- **`intake-draft-service.js`** — CRUD over `intake_drafts`. Drafts are Postgres-only; submission goes via `submission_jobs` drain queue; pending-attachments JSONB ops; 2h sweep. See header for state-machine summary + `docs/INTAKE_PORTAL_DRAIN_PLAN.md`.
- **`intake-audit-service.js`** — Append-only sha256-hashed audit; swallow-on-failure posture.

### Reviewer / honorarium

- **`review-manager/draft-fingerprint.js`** — Stage 6D (PR #159, `6606cc30`, 2026-09-06): `buildDraftFingerprintInputs` and `computeDraftFingerprint` — a sha256 over a stable-stringified record of the server-observed email body inputs (template type, lower-cased suggestion id, candidate name/affiliation, proposal title/abstract/honorific-stripped authors/institution/ordered co-PIs, due-date override, request due/meeting dates, cycle program name/deadline/raw custom fields, honorarium amount or null). `render-emails-service.js` stamps each draft; `send-emails-service.js` recomputes from fresh reads and skips `draft_stale` / `draft_fingerprint_missing` before any write. Golden vectors in `tests/unit/draft-fingerprint.test.js` pin the hashes; `tests/helpers/draft-fingerprint.js` is a deliberately independent reimplementation.
- **`auto-link-reviewer-contact-account.js`** — Acceptance-only Contact parent-Account enrichment. It requires a complete active Account scan and fills an empty `contact.parentcustomerid` only when the accepted self-reported affiliation has exactly one normalized exact match across Account name/AKA/legal/DC-AKA labels; existing parents, ambiguity, and misses abstain. Target freshness plus an ETag-conditional fill-only Contact write prevent stale or racing overwrites. Transient operational failures retry through `reviewer_acceptance_jobs`; a capped/incomplete scan instead abstains, raises one deduplicated operations warning, and preserves the reviewer-specific mismatch check. Successful/already-correct links auto-resolve the reviewer's standing mismatch warning.
- **`review-receipt-guard.js`** — Shared terminal/finality/accepted/ETag authorization for every request-time review-receipt sink; classifiers turn a lost If-Match race into `engagement_ended`, `review_received_locked`, or `conflict`.
- **`reviewer-engagement/correct-status.js`** — Stage 3C (PR #158 → `1c24e56f`, 2026-09-06): `patchReviewers` and `ReviewerStatusMutationError`, extracted byte-identically from `review-manager/reviewers-service.js`, which keeps `getReviewers` and re-exports both symbols so the route import is unchanged. Preserves the shipped 6A contract described below.
- **`review-manager/reviewers-service.js`** — Staff-shared reviewer GET DTO; since Stage 3C the preauthorized single/batch status correction is a compatibility re-export of `reviewer-engagement/correct-status.js`. Correction contract (unchanged): Nonempty batches trim/lowercase/deduplicate GUIDs in first-occurrence order, await adapter operations sequentially and stop at the first failure; single calls preserve the submitted ID and lifecycle object. Outcome arrays retain the confirmed prefix, unconfirmed adapter operation (including validation before lifecycle read/write), and unattempted suffix; they do not prove a failed operation reached HTTP persistence. Route validation/authorization and service complete/terminal-target prechecks remain error-only; the route owns HTTP mapping. Existing null/empty-string open-status clearing remains supported; no validation policy changes. **[VERIFIED source at `5b9964c8`; shipped in Production at `c19a16d8` on 2026-09-05 per the release receipt](audits/REVIEWER_LIFECYCLE_RELEASE_2026-09-05.md).**
- **`reviewer-engagement/close-review.js`** — Stage 3A (PR #154, `b7a04cd6`, 2026-09-05): the staff closeout command (`closeReview`, disposition/notes validation, ETag-conditional complete/honorarium-eligibility write, no-write repeat, correction without restamp), moved byte-identically out of `review-manager/close-review-service.js`; that module is now a two-symbol compatibility re-export and the route import is unchanged. Errors are plain `ServiceHttpError`. Caller census pinned by `tests/unit/reviewer-engagement-census.test.js` (helper `tests/helpers/import-census.js`). Later Stage 3 slices add sibling commands here.
- **`reviewer-engagement/terminal-transition.js`** — Stage 3B (PR #156, `4e7c378c`, 2026-09-05 PT): dedicated fail-closed post-accept `withdrew`/`released` transition, moved byte-identically from `review-manager/terminal-transition-service.js` (now a three-symbol compatibility re-export; route import unchanged). Fresh per-row eligibility read, explicit partial-success statuses, and ETag-conditional writes so a concurrent submission wins. Staff-recorded `withdrew` also corrects the response to declined, atomically deletes the exact linked honorarium request, and cancels acceptance follow-up; `released` remains a status-only WMKF outcome.
- **`review-manager/reviewer-vip-flags-service.js`** — Per-(lead PD, reviewer person) VIP flags for the invitation preview slice: flagged people's invite drafts render as full editable cards while the rest collapse. Resolves the lead PD server-side from the request row (never client input); flags key on `wmkf_potentialreviewersid` (candidates have no CRM contact pre-acceptance). Synchronous routing aid only — no send path is gated here.
- **`review-manager/manual-review-entry-service.js`** — Staff rescue for recording a complete structured review from the Workbench Reviews tab. Reuses the external review question set, sanitizer, validator, canonical submission producer, and atomic ETag-guarded parent/answer-row changeset; deletes a stale external draft only after commit.
- **`review-manager/export-reviews-service.js`** — Server-authoritative combined Word export. Accepts only a validated proposal GUID plus the session email, rereads request/submitted-review/synthesis data through the existing reviewers service, composes the shared report model, and renders the tracked combined template. Returns typed not-found/no-submission errors and a server-owned filename; no persistence or SharePoint write. **[PRODUCTION-LIVE + SIGNED-IN EXPORT PROVED 2026-09-03 at `3101f067`; Production Wave 25 is exact.]**
- **`review-documents/individual-review-builder.js`** — Shared answer-read → single-review composition → approved-template render seam. Its courtesy-attachment default remains `Review-{requestNumber}.docx`; the retained-file caller may supply the human-readable storage filename. Generation time stays caller-owned: thank-you attachments retain send-time metadata, while the Wave 2 retained-file service supplies the receipt timestamp. **[WAVE 1 SOURCE/TEST/RENDER VERIFIED AND PRODUCTION-LIVE 2026-09-03 AT `3ba2a6ad` / `dpl_22wUzC1cCi4nhKTSFQftfaycucSh`.]**
- **`review-documents/individual-file-service.js`** — Guarded retention and repair service for structured individual-review DOCX files. It freshly revalidates suggestion/request/reviewer/answer state, uses receipt time for stable generation, targets request-level `Reviews/Review-<request>-<sanitized reviewer name>.docx`, reconciles target content with the governed Word-part hash, conditionally commits both existing suggestion pointer fields through the narrow `attachReviewDocumentPointer` adapter op (ETag required since Stage 5, 2026-09-05), bounds pointer retry/cleanup, and returns per-suggestion statuses without logging review content. Scheduled discovery is exact-cycle-stamp-only and newest-first; mutation additionally requires the literal-on rollout flag, caller-specific deployment guard, enforcing Dataverse interlock, and canonical pre-resolved akoyaGO site/drive. The flag-off route creates no maintenance row. Existing legacy generated paths remain recognized; the backfill manifest rejects duplicate same-name destinations and ordinary scheduled/backfill filing stays create-only. A complete pointer can change only through an exact manifest-bound local repair: relocation creates/verifies a new item before an ETag pointer change, while content repair requires the exact current item ID/ETag/version/prior semantic hash and creates a retained SharePoint version without changing the pointer. **[WAVES 1–5 PRODUCTION-PROVED. WAVES 1–4 REACHED PRODUCTION AT `3ba2a6ad` / `dpl_22wUzC1cCi4nhKTSFQftfaycucSh`; REQUEST `1002874` IS PROVED AT THE NEW REVIEWS PATH AND VERSION `4.0`, AND THE EXACT REMAINING D26 MANIFEST COMPLETED 22 CREATED / 0 FAILED. WAVE 5 IS ACTIVE WITH EXACT PRODUCTION `WRITE=on`, `CYCLE=D26` AT `dpl_E6VKW5Wi8zDTfU1ZRhNsbH9yg9oM`. ON 2026-09-04, NATURAL MAINTENANCE RUN `70820` CREATED `Reviews/Review-1002959-Manuel Müller.docx` (VERSION `1.0`, 43,498 BYTES) WITHOUT ERROR AND DATAVERSE HELD THE COMPLETE POINTER.]**
  **[FOLLOW-UP VERIFIED 2026-09-04 UTC: THE `1003223` TEST SUGGESTION IS NOW
  `wmkf_selected=false`, WITH ITS RECEIVED-REVIEW HISTORY AND EMPTY POINTERS
  PRESERVED; THE IMMEDIATE ENABLED SWEEP RETURNED ZERO CANDIDATES AND ZERO
  ATTEMPTS.]**
- **`review-documents/backfill-service.js`** — Operator-only manifest and execution coordinator for missing historical individual-review retention. It unions exact cycle stamps with request meeting-cycle fallback, removes rows that acquired complete pointers before planning, emits a content-free hash-bound population/target manifest, blocks partial/divergent/unresolved state, rebuilds the complete unfinished population before write preflight, and binds every ensure call to the reviewed ETag, source fingerprint, semantic hash, exact Production Dataverse base, and exact SharePoint site/drive. Owner-confirmed test requests may be explicitly retained as hash-bound, non-writing `excluded_test_request` rows; unmatched exclusion inputs block. Row failures are retained individually and do not stop later rows. **[WAVE 3/4 SOURCE + FOCUSED TEST + ADVERSARIAL REVIEW + PRODUCTION EXECUTION VERIFIED. THE EXACT OWNER-APPROVED V4 D26 MANIFEST COMPLETED 22 CREATED / 0 FAILED; ROW-BY-ROW RECONCILIATION MATCHED EVERY REVIEWED IDENTITY, DESTINATION, HASH, AND UNIQUE SHAREPOINT ITEM. A FRESH SURVEY HAS ZERO ELIGIBLE OR RECONCILE ROWS, ZERO BLOCKERS, AND ONE VISIBLE TEST EXCLUSION.]**
- **`review-documents/repair-service.js`** — One-item operator repair coordinator for moving a retained structured review to the current template/path or versioning the exact current item without deleting prior bytes. Its schema-v2 dry-run manifest binds the exact suggestion, request, cycle, pointer, ETag, source fingerprint, old/new semantic hashes, stable item/version identity, Production Dataverse base, and canonical SharePoint site/drive. Relocation remains create-only and ETag-repoints Dataverse; content repair uses an exact-item `If-Match` replacement, re-verifies the current bytes, and downloads the prior SharePoint version to prove retention. **[SOURCE/FOCUSED-TEST VERIFIED; REQUEST `1002874` RELOCATION AND SAME-ITEM V3/V4 CONTENT REPAIRS EACH COMPLETED 1/1. LIVE ITEM `01G4GVMSZZ25YPTP3RGFEK6LCT64W3JPX2` IS VERSION `4.0`, POINTERS UNCHANGED BY CONTENT REPAIR, V4 HASH INDEPENDENTLY MATCHED, AND PRIOR VERSIONS REMAIN IN SHAREPOINT 2026-09-03.]**
- **`review-documents/docx-renderer.js`** — Template-preserving OOXML renderer and strict preflight for the approved individual-review and combined-review Word templates. Requires exactly one body marker, retains section/header/footer geometry, filters XML-invalid characters, escapes reviewer content, renders categorical options from the point-in-time snapshot, and distinguishes legacy selected-only from corrupt snapshot state. **[PRODUCTION-LIVE 2026-09-03 AT `3ba2a6ad` / `dpl_22wUzC1cCi4nhKTSFQftfaycucSh`. Word-for-the-web exposed defects in the legacy floating-image header despite v2 tab removal and v3 `wrapNone`; the owner-edited v4 templates replace the first-page drawing with an exact fixed 9360-DXA two-column Times New Roman text table. Focused tests and full rendered inspection pass. Request `1002874` contains the verified v4 governed bytes as SharePoint version `4.0`, with versions `1.0`–`3.0` retained, and the owner visually confirmed that version in Word Online.]**
- **`reviewer-manual-reminder.js`** — Authenticated per-reviewer reminder coordinator. Respond-by preview/send re-derives sender and recipient and deliberately mints/delivers the replacement pre-acceptance link; review-due sends re-read lifecycle/token state, fail closed on liveness/runway, and delegate to the shared link-free sender without rotating token authority.
- **`reviewer-reminder-sweep.js`** — Shared respond-by/review-due reminder engine and dormant cron sweeps. Both paths claim lifecycle markers with If-Match before transport; respond mints and injects a replacement link, while review-due rechecks token eligibility immediately before claim and never mints or carries a reviewer URL.
- **`reviewer-reminder-candidate.js`** / **`reviewer-reminder-eligibility.js`** — Canonical review-due candidate predicate plus pure fail-closed token/runway classifier shared by the scheduled sweep, manual sender, DTO, and read-only audit.
- **`reviewer-reminder-liveness-audit.js`** — Read-only current-cycle report builder over the shared candidate predicate and eligibility contract. Emits bounded identifiers/counts only; no token hash, reviewer email, maintenance row, mint, lifecycle write, or email transport.
- **`review-upload.js`** — Shared `writeReviewFiles` core for staff + reviewer-self upload paths; shared receipt authorization, unique per-attempt SharePoint folders persisted on the winning row, and an ETag-bound Dataverse write. Ordinary Dataverse failures clean up the caller's attempt; every 412 race loser is orphaned and never deleted because winner ownership cannot be inferred safely.
- **`reviewer-campaign-timeline.js`** — Dataverse `wmkf_appsystemsettings` reader/writer for current-cycle reviewer invitation timeline defaults (`reviewer.campaign_timeline_defaults`); admin-editable, read by `InviteEmailModal` before request-level campaign config.
- **`external-token.js`** — HS256 HMAC JWT primitive for external-reviewer magic links; hash-only storage for cheap revocation. 32+ char `EXTERNAL_LINK_SECRET`; rotation supported via `EXTERNAL_LINK_SECRET_PREVIOUS`.

### Multi-LLM panel (Virtual Review Panel)

- **`multi-llm-service.js`** — Claude / OpenAI / Gemini / Perplexity wrappers with normalized responses + retry/fan-out. Claude request shaping uses `model-capabilities.js`; Fable-style refusal metadata is surfaced in normalized results.
- **`panel-review-service.js`** — VRP pipeline: optional pre-review intelligence → optional claim verification → structured review → synthesis. Persistence in `panel_reviews` / `panel_review_items`. See `docs/VIRTUAL_REVIEW_PANEL.md`.
- **`literature-search-service.js`** — Stage 0 academic search orchestration; normalized results.

### Research-database clients

- **`pubmed-service.js`**, **`openalex-service.js`**, **`arxiv-service.js`**, **`biorxiv-service.js`**, **`chemrxiv-service.js`**, **`orcid-service.js`**, **`serp-contact-service.js`** — external research-DB clients (API shapes, rate limits, and tier positioning in each header). Europe PMC is queried by the scholarly-email helper rather than a standalone client.

### Reviewer Finder pipeline

- **`reviewer-finder/load-proposal-service.js`** — Default proposal ingestion
  prefers exactly one active `Reviewer Materials/Proposal_{Request#}.pdf`, then
  falls back only to exactly one active current-cycle
  `Phase I/ProjectDescription.pdf`; neither/ambiguity fails before
  download/Blob write and returns the server-listed picker data. An explicit
  authenticated, server-listed `fileKey` remains for deliberate
  historical/ad-hoc staff overrides. This compatibility fallback is confined
  to Reviewer Finder ingestion.
- **`discovery-service.js`** — Multi-database literature search orchestration.
- **`deduplication-service.js`** — Name matching, duplicate merge, COI filtering, ranking.
- **`institution-identity-resolver.js`** — W0 OpenAlex institution resolver with per-instance settled caching, same-abort-scope single-flight, immutable aggregate counters, unique-strongest name/country selection, structured associated-institution output, and `null` on ambiguity or provider failure. W1 callers use it to narrow pre-existing COI matches and corroborate affiliation consistency; provider failures and aborts never become cache entries.
- **`institution-affiliation-consistency.js`** — W1 direct-id or one-hop-associated institution consistency helper for identity corroboration and mismatch alerts. It is intentionally separate from COI and must never widen the hard-drop set.
- **`institution-affiliation-assessment.js`** — Source/currentness-aware organization relationship and total per-consumer policy contract. It preserves additional affiliations, distinguishes same/parent-child/sibling/related/distinct/unresolved, applies conditional neutrality only with an explicit affiliation-excluding identity result, and fails closed for unknown high-authority inputs. Stage 2 candidate-card and staff-notification presentation consume it behind an exact-on rollout flag (Production enabled 2026-08-19); high-authority consumers do not.
- **`institution-affiliation-stage2.js`** — Bounded Stage 2 evaluator and presentation projection for reviewer cards and post-acceptance staff notifications. It supplies source-aware copy and available remedies, treats provider failure as retryable/nonterminal, and never grants identity, selectability, or durable-write authority.
- **`ror-institution-candidate-contract.js`** — Verdict-free ROR candidate/input contract. Accepts only affiliation text plus optional trusted local country/domain evidence and cancellation, rejects email/arbitrary fields, normalizes ROR records and provenance, and forbids decision keys in provider output.
- **`ror-institution-evidence.js`** — Deterministic organization-span, fallback-query, text-evidence, and locality helpers shared by the production ROR retrieval and local decision layers.
- **`ror-institution-candidate-adapter.js`** — Request-scoped ROR API v2 candidate retrieval with explicit `single_search`, bounded fallback/contradiction probes, successor/parent hydration, cancellation-safe same-scope single-flight, settled cache, 429/5xx-only retry with capped `Retry-After`, request budget/deadlines, optional non-secret `ROR_CLIENT_ID`, and aggregate-only response-class/latency/reuse metrics. It returns evidence, never a winner.
- **`ror-institution-decision.js`** — Local veto-before-scoring institution decision. Multi-organization, sibling, domain, country, type, and granularity contradictions are non-overridable; ROR rank, aliases, scores, and `chosen` remain features rather than authority; ambiguity/provider failure abstains.
- **`ror-institution-identity-resolver.js`** — Request-scoped facade that resolves locally from verdict-free ROR candidates and only then hydrates the exact selected ROR through OpenAlex. Provider/bridge failures are retryable abstentions rather than cacheable misses; exact mismatches fail closed; metrics contain aggregate counts only.
- **`ror-affiliation-assertion-resolver.js`** — Assertion resolver that reuses verdict-free ROR candidates and the local decision contract while retaining source/canonical ids and one result per explicit organization segment. Partial provider failure preserves successful peers and never becomes `distinct`; Stage 2 low-authority presentation consumes it behind the exact-on rollout flag (Production enabled 2026-08-19) in addition to benchmark/test callers.
- **`reviewer-identity-runtime.js`** — Server-owned W2 runtime seam for non-biomedical/PubMed-off Track-A verification and server-computed enrichment reconciliation. Default and unknown modes return the legacy identity result exactly; `shadow` returns legacy after hard-bounded redacted comparisons; explicit owner-gated `combined` adapts W2 decisions with a `probable` ceiling. Batch W2 evaluation owns one request-bounded ROR candidate/decision/OpenAlex-bridge resolver and emits one PII-free aggregate cache/provider-call metric log; default comparison observers still await the non-throwing durable logger so normal inserts settle before function completion. A separate failure-isolated request-local observer can emit a bounded named comparison DTO to the current `/api/reviewer-finder/discover` request; that route supplies it only after a fresh superuser check, explicitly allowlists its SSE fields, and the Workbench renders it transiently without adding names to logs or durable comparison rows. Biomedical PubMed verification can also run a superuser-only compare-after-baseline diagnostic through this runtime; it returns the exact PubMed result array, emits no durable W2 telemetry, and the Workbench labels it as PubMed versus Works-first rather than Legacy/Combined.
- **`reviewer-identity-shadow-log.js`** — Best-effort Postgres persistence for resolver comparisons (`reviewer_identity_shadow_log`, migration 026). Whitelisted data-minimized scalars only (pseudonymous hashed candidate key, decisions, reason, anchors-agree, error code); awaited inserts are capped at 2 seconds, always resolve, and circuit-break after repeated failure. Non-authoritative: no application decision reader; the canonical operator report is `scripts/report-reviewer-identity-shadow-log.js`, and retention/cap cleanup runs in the daily maintenance cron.
- **`reviewer-works-first.js`** / **`reviewer-works-first-authoritative.js`** — Shared works-first resolver, exact sparse-fragment/byline-ORCID equivalence, W4.1 evidence-bundle builder/parser, and authoritative-result adapter used by the frozen evaluator and runtime seam. Uses byline-filtered works plus W0 institution identities; same-ORCID fragments may collapse, every distinct-ORCID set goes to review, and automated rescues cap at `probable`.
- **`contact-enrichment-service.js`** — Tiered contact lookup with identity anchoring, structured scholarly-email evidence, and Dataverse writeback.
- **`reviewer-contact-reconciliation.js`** — Sequential exact-email/ORCID reconciliation for enriched search cards. Produces bounded staff-facing Dataverse evidence; a still-matching server-bound identity decision plus trusted, name-consistent ORCID may also ETag-write person-scoped pending address-conflict state. Provider-only/provisional or browser-authored ORCIDs can require review but never establish a known identity or authorize a durable person write.
- **`reviewer-address-trust-service.js`** — Authenticated exact-address attestation, fresh conflict disclosure, active-roster retry, and durable repair orchestration. Roster receipts are request-scoped; person conflict/adjudication writes are stable-person-bound and ETag-conditional; receipt-first partial success is explicit.
- **`contact-enrichment/scholarly-email.js`** — Free NCBI PubMed + Europe PMC author-affiliation email resolver; requires full-forename or exact-ORCID identity plus affiliation corroboration, deduplicates the same work across providers, and abstains on tied addresses.
- **`reviewer-roster-store.js`** — Postgres operational roster for
  request-scoped Find state and server-owned promotion finalization. Stores
  actor-bound staff identity confirmation and roster-only manual
  website/affiliation drafts (never an unverified email). Ordinary edits invalidate the
  prior exact contact authority and expose explicit re-confirmation; only the successful Dataverse
  promotion service can finalize exact keys as `saved`, while authoritative
  applicant-excluded collisions become `blocked`.
- **`workbench/reviewer-roster-projection-service.js`** — Read-only Find-roster
  overlay that completely batches one request's visible suggestion anchors
  through Dataverse, removes already-engaged rows from working-state buckets,
  emits compact `handled` stage entries, and validates excluded→active
  promotion; missing suggestion anchors fail closed.
- **`workbench/reviewer-warm-validation-service.js`** — Read-only warm-revisit
  validator for the roster's reconciled mode. It reads only server-owned
  request inputs and exact Graph metadata for
  `Reviewer Materials/Proposal_{requestNumber}.pdf`, falling back only to
  `Phase I/ProjectDescription.pdf`; a binding is current only when Graph
  returns bounded drive/item identifiers plus a stable change token. It returns
  opaque content/input versions, bounded non-PII input counts, and
  server-derived invalidation plans. Its `current` state certifies only those
  bounded panel reads: it does not fabricate or certify candidate identity,
  COI, eligibility, contact, address, persistence, or promotion receipts.
  Applicant-anchor freshness is candidate-specific when an exact stored person
  id matches a current request slot; ordinary search candidates are explicitly
  not applicable to that stage. A suggestion-keyed row without an explicit
  applicant or non-applicant provenance fails closed rather than being inferred
  as not applicable. It never
  accepts a browser file key, downloads/uploads proposal content, materializes
  suggestions, parses exclusions, or calls evidence/model providers. A
  historical manual file binding is stale when it cannot be recovered from
  authoritative request persistence.
- **`workbench/reviewer-stage-refresh-service.js`** — Explicit one-candidate,
  one-stage staff repair for the executable `applicant_anchor` receipt. It
  derives the canonical suggestion key and input dependency from server-read
  roster, suggestion, and request records; candidate-key/updated-at/attempt
  CAS preserves prior evidence on failed refreshes. It never accepts
  name/receipt/dependency authority or calls the full applicant batch.
- **`reviewer-candidate-attestation.js`** — `NEXTAUTH_SECRET`-signed
  request/candidate receipt. New receipts mint projection v4; v3/v4 are
  contact-authoritative when their exact canonical contact projection is ready
  and matches, and v4 additionally binds `eligibilityCheckStatus`. v1/v2
  remain verifiable against their historical projections but are never
  contact-authoritative. A compact server-only roster receipt preserves a
  canonical digest of the accepted identity decision after ingestion and must
  still match the current row at use time. Mutable address/affiliation fields
  are deliberately excluded.
- **`reviewer-engagement/correct-response.js`** — Stage 3D (PR #160 → `081d558b`, 2026-09-06): `correctResponse({ suggestionId, lifecycle, authorizedRequestId, actingUserSystemId })`, the response-only invitation/response correction moved byte-identically out of `my-candidates-service.js` `patchMyCandidates`: authorized-request GUID guard, fresh non-excluded known-open row (Stage 2 predicates), exact concrete ETag, `updateLifecycle` with `ifMatch`, 412 → `correction_conflict`, then the nonfatal `accepted:true` `ensureToken` follow-up. `correctionError` lives here. Person edits stay in the caller.
- **`reviewer-engagement/record-email-outcome.js`** — Stage 3E (PR #161 → `01072571`, 2026-09-06): `recordDeliveredEmail({ suggestionId, originalSuggestion, templateType, sentAt, actingUserSystemId })`, moved byte-identically out of `send-emails-service.js`. Records an already-delivered materials/follow-up/thank-you message: up to three fresh reads, request/reviewer binding check, concrete-ETag requirement, closed-status refusal (thank-you exempt), newer-timestamp preservation, status bumps (`materials_sent`, `under_review`) and reminder count, retrying only a 412. The send loop still calls it after dispatch and maps a failure to the `updating_lifecycle` warning event. The inline invitation stamp stays in the send service (D1 in the Stage 7 plan).
- **`reviewer-engagement/expire-invitation.js`** — Stage 3E: `expireInvitation({ suggestion, cutoffIso, nowIso, actingUserSystemId })` → `{ outcome: 'swept' | 'skipped' }`, the per-row body of the stale-invite sweep (fresh read, pending/request/ETag guards, parent meeting-date revalidation, conditional write of `no_response`). `isPastCutoff` moved to `lib/utils/past-cutoff.js` (2026-09-06, S490); both this module and the sweep's discovery pass import it from there. The sweep keeps scan orchestration, counters and the 412/not-found catch.
- **`reviewer-engagement/claim-reminder.js`** — Stage 3G (PR #162 → `ec74c0d4`, 2026-09-06): `claimReviewDueReminder({ id, claimPatch, claimIfMatch, actingUserSystemId })`, the review-due reminder's fire-once ETag-conditional claim (`reminderSentAt` + `reminderCount` from the sweep's `claimPatch`), moved verbatim out of `reviewer-reminder-sweep.js`. The respond-kind claim stays coupled to `mintAndStore` in the sweep, which also keeps the 412 → `claimFailed` / else `prepareFailed` mapping and sends only after a successful claim.
- **`reviewer-engagement/change-review-deadline.js`** — Stage 3H (PR #163 → `81fdac43`, 2026-09-06): `changeReviewDeadline({ suggestionId, reviewDueDateOverride, ifMatch, actingUserSystemId })`, the ETag-conditional due-date override write (a `null` override clears it) moved verbatim out of `reviewer-due-extension.js`, which keeps eligibility, exact-date validation, the ETag presence check, `prepareNotification`, the 412 → `conflict` classification and the saved/not-notified envelope; persistence still precedes notification.
- **`reviewer-engagement/withdraw-pending-invitation.js`** — Stage 3I (PR #164 → `d47b07be`, 2026-09-06): `withdrawPendingInvitation({ id, nowIso, ifMatch, actingUserSystemId })`, the ETag-conditional pending → `withdrawn_sufficient` write (`responseType`, `withdrawnSufficientAt`, `respondReminderSentAt: null`) moved verbatim out of `review-manager/withdraw-sufficient-service.js`, which keeps the pending/override/sender/recipient checks, the per-id result mapping (412 → `changed_skipped`, else `write_failed`), the counter and the courtesy email that follows a successful write.
- **`reviewer-engagement/record-invitation.js`** — Stage 3F (PR #165 → `68198b2f`, 2026-09-06): two deliberately separate writers with no shared helper (a third, `markInvitationGenerated`, was retired with the generate-emails route 2026-09-06 under owner decision D2) — `recordDeliveredInvitation({ suggestionId, actingUserSystemId })` (the post-send `invited`/`emailSentAt` stamp from `send-emails-service.js`, unconditional as today: open owner decision D1 in the Stage 7 plan), `recordManualInvitation({ suggestionId, emailSentAt, ifMatch, actingUserSystemId })` (the verified-link manual record from `my-candidates-service.js`, which keeps its guards and 412 → `stale_manual_link` mapping). Delivered evidence and manual verified-link record are distinct contracts and must not be collapsed.
- **`reviewer-engagement/expire-invitation.js`** (Stage 7C update) — since PR #168 the sweep's expiry write goes through the adapter's named `expireInvitationResponse(id, nowIso, { ifMatch })` (concrete ETag required via `isConcreteEtag`; the sweep's own guard already guaranteed one), not the `patchFields` alias. That alias now has a single caller, `record-invitation.js` `markInvitationGenerated`, pending owner decision D2.
- **`reviewer-engagement/errors.js`** — Neutral leaf owner of `MyCandidatesError` (imports only `service-http-error`); `my-candidates-service.js` re-exports it for compatibility.
- **`reviewer-finder/my-candidates-service.js`** — Candidate reads and named
  edit/restore/manual-invite operations; since Stage 3D the response correction
  delegates to `reviewer-engagement/correct-response.js` (contract below unchanged); since Stage 3F the
  manual-invite record delegates to `reviewer-engagement/record-invitation.js`; the proposal-wide
  cycle/program-area fan-out (Stage 3K's `setRequestMetadata`, formerly the generic per-request bulk op)
  was REMOVED 2026-09-06 under owner decision D4 — no client had sent that PATCH since the standalone
  Reviewer Finder page was retired 2026-06-16, so a `proposalId`-only PATCH is now a plain 400. **[VERIFIED in source at `c51fa34d`,
  Stage 1D; shipped in Production at `c19a16d8` per the
  [release receipt](audits/REVIEWER_LIFECYCLE_RELEASE_2026-09-05.md)]** generic invitation/response
  corrections require a separately server-authorized Request binding, a fresh
  non-excluded known-open row without a completed-at marker, and its exact ETag.
  Conflicts stop without automatic retry before token follow-up or person edits;
  successful `accepted:true` corrections retain nonfatal token follow-up before
  person edits. Dedicated closeout owns eligibility/notes corrections.
- **`reviewer-finder/save-candidates-service.js`** — Server-owned Find→Invite
  promotion boundary: canonical contact projection, contact-bound v3/v4 or
  staff authority,
  exact-email owner reuse and race convergence, per-row result contract,
  bounded new-person compensation, and exact roster finalization.
- **`reviewer-promotion-repair-classifier.js`** — Pure read-only historical
  classifier (`D/C/U/E/N`) and redacted canonical manifest/hash builder.
  Classification D requires one active exact-email owner, independent
  same-person confirmation, and an unblocked ETag-complete merge plan; it does
  not execute repairs.
- **`capture-self-reported-orcid.js`** — Self-reported ORCID persistence seam. Accept-drain calls with a stable acceptance timestamp use the Wave 13 binding writer before contact fill; only typed `legacy_classification_required` falls back to the transitional person writes. Older/decline calls without a stable event retain the transitional path.
- **`reviewer-identity-binding-writer.js`** — Server-owned Wave 13 person-binding transition seam: fail-closed snapshot + ETag read, strict UTC timestamp normalization, lineage/source precedence, one complete conditional PATCH, and bounded 412 reread/recompute. Its first production caller is live since PR #57 / `00ffb09c`: acceptance-drain self-report only. Dirty legacy rows, revocation, unauthorized human correction, policy-reader migration, and automated writer migration remain blocked or deferred.

### Integrity Screener

- **`integrity-service.js`** — Retraction Watch + PubPeer + News + Haiku summarization.
- **`integrity-matching-service.js`** — Multi-tier name matching + Retraction Watch optimization.

### Admin / monitoring

- **`feedback-service.js`** — `dynamics_feedback` thumbs + auto-detected failures; optional Explorer request correlation is accepted only after authenticated-profile ownership and exact non-null session verification, otherwise feedback is saved uncorrelated.
- **`alert-service.js`** — `system_alerts` rows; health/maintenance/secret/log alerts; dedupe.
- **`notification-service.js`** — System-alert row + category-routed email;
  explicit-recipients union and HTML-escaped body. Callers must establish the
  trusted ambient Dataverse context; the service no longer creates a hidden
  bypass around its Dynamics email transport.
- **`maintenance-service.js`** — Cleanup operations; audit trail; Dataverse-configured retention, including Explorer request lifecycle rows on the query-log horizon with retained feedback correlations set null by FK and scheduled-email cleanup limited to finalized sends/explicit stops.
- **`portal-upload-staging.js`** — Actor/scope/resource-bound private Vercel Blob
  staging for external-grantee and staff replacement images. Owns short-lived
  client-token minting, atomic finalize leases, exact-path server reads, byte
  hash/size recording, durable SharePoint candidate identity, terminal result
  replay, and exact-path expiry cleanup. It never accepts a client pathname.
- **`portal-upload-client-failure.js`** — Closed-vocabulary parser and
  best-effort Operational Event writer for authenticated browser-direct upload
  failures. Stores only stage/category/status/declared size/type plus the
  server-resolved request identity and labels every row `clientReported:true`.

### Other

- **`irs-bmf-service.js`** — IRS Business Master File 501(c)(3) lookup for EIN verification. Used by `/api/irs/verify-ein` (PowerAutomate-callable via `IRS_VERIFY_SECRET`).
- **`dataverse-export/`** (subdirectory) — Dataverse Bulk Export (Track B) services. Deterministic QuerySpec→FetchXML translation, paging, trust-bounded Excel emission. See `docs/DATAVERSE_POWER_TOOLS_TRACK_B_BUILD_PLAN.md`.

---

## `lib/external/` — external-reviewer flow

- **`automated-email-notice.js`** — Shared personalized recipient-facing notice builder/renderer: identifies the message as automated, names the PD on whose behalf it was sent, states where replies go, and removes the legacy standalone marker that could split a closing from its signature.
- **`token-lifecycle.js`** — `mintAndStore` / `revoke` / `ensureToken` (idempotent) / `extendForPostSubmissionWindow` / `buildExternalUrl`.
- **`reviewer-token-state.js`** — Pure total classifier over stored reviewer-token hash/expiry/revocation metadata (`revoked`, `not_minted`, `invalid`, `expired`, `active`). It answers liveness only; review-due runway is deliberately evaluated by the separate reminder-eligibility helper.
- **`verify-suggestion-token.js`** — Combined JWT + suggestion-row check; discriminated result with reason codes.
- **`reviewer-materials.js`** — Enforces the exact reviewer-visible SharePoint contract at list + download: `Reviewer Materials/Proposal_{Request#}.pdf`. The folder is matched case-insensitively; the request-bound filename is exact. Neighboring files, including timestamped raw application exports, remain invisible.
- **`review-form-schema.js`** — static seed/shape source for affiliation plus the staged 11-question form: 2 core picklist ratings, the `impactAreas` multiselect, and 8 rich-text narratives. Only affiliation is parent-column-bound; structured and narrative answers map to `wmkf_appreviewanswer`. `validateReviewForm` supports partial legacy upload validation and delegates multiselect normalization to the canonicalizer.
- **`review-multiselect.js`** — authoritative pure canonicalizer for multiselect request values. Accepts numeric values only, rejects unknowns, deduplicates and orders by live options, constructs server-owned `{value,label}` pairs, and derives joined snapshot text.
- **`review-multipart-fields.js`** — strict Busboy field accumulator for review upload routes. Preserves scalar fields and repeated `field[]` values while rejecting ambiguous mixed scalar/array encodings before review validation.
- **`policy-fetcher.js`** — Policy-document fetcher with `bypassDynamicsRestrictions` wrapper.

---

## `lib/bill/` — BILL.com integration (S188+ slice)

- **`index.js`** — Public API: `createBillVendor`, `searchBillNetwork` (name + zip per BILL constraint, NOT email), `sendNetworkInvitation`, `verifyBillWebhook`. See `docs/BILL_LIB_DESIGN.md` + `docs/BILL_HONORARIUM_INTEGRATION_DESIGN.md`.
- **`session.js`** — Module-level session cache with cold-start serialization + failed-promise clearing. 30-min TTL ceiling under BILL's 35-min inactivity timeout.
- **`classify.js`** — BDC error classification (BDC_1144 hourly = fail-loud, BDC_1322 concurrent = retry, BDC_1109 session-expired = internal reauth-and-retry).
- **`errors.js`** — `BillError` + public subclasses (`BillAuthError` / `BillRateLimitError` / `BillValidationError` / `BillServerError`) + internal `BillSessionError`.
- **`redact.js`** — devKey / password / sessionId / JWT / base64 token sweep.

---

## `lib/utils/`

### Auth / cron

- **`auth-policy.js`** — proxy-bundle-safe `isAuthRequired()` shared between `proxy.js` (Node.js runtime in Next 16) and `lib/utils/auth.js`. Reads only `process.env`, no Node-only / `@vercel/postgres` / `next-auth` imports. **`NODE_ENV=production` fails closed** unless `EMERGENCY_AUTH_BYPASS=true`; the predicate is not a Vercel environment-name check and applies to production-mode Preview and Production runtimes. Misconfig warnings memoized once per process.
- **`cron-auth.js`** — Vercel cron `CRON_SECRET` verification (Bearer header). Dev-mode bypass.

### Versioning

- **`etag.js`** — `isConcreteEtag(value)`: true only for a strong or weak entity tag with a nonempty opaque body and no surrounding whitespace (`"…"`, `W/"…"`); rejects `*`, `W/""`, unquoted and control-character values. Stage 5 (2026-09-05) made it the `ifMatch` requirement behind `reviewer-suggestion.js` `attachReviewDocumentPointer` / `claimThankYou`, which fail with 400 `missing_version` otherwise. The four older inline copies of the same regex (`reviewer-suggestion.js`'s invitation-response guard, `reviewer-engagement/correct-response.js`, `reviewer-engagement/expire-invitation.js`, `reviewer-engagement/record-email-outcome.js`) were consolidated onto it 2026-09-06 (S490); `grep -rn 'W\\/)?"\[' lib/` now hits only this file.

### Health / files

- **`health-checker.js`** — Reusable health-check logic spanning all currently-monitored upstream services (incl. Microsoft Graph). Source file is the live list.
- **`guarded-reopen-readiness.js`** — Pure environment predicate for the Wave 20 guarded-reopen release interlock. Only literal `GUARDED_REOPEN_SCHEMA_READY=on` enables the extended Request Document projection and reopen route; unset/invalid values fail closed while leaving the base registry projection compatible with targets where Wave 20 is absent.
- **`final-writeup-acknowledgement-readiness.js`** — Separate Wave 23 release interlock. Only literal `FINAL_WRITEUP_ACKNOWLEDGEMENT_SCHEMA_READY=on` enables acknowledgement service access; unset/invalid values fail closed, and operators may set it only after hardened preflight proves the composite alternate-key index Active.
- **`request-document-explicit-actor-readiness.js`** — Wave 24 literal-on projection/write boundary. Off preserves the legacy Request Document shape; in Production it also reports an unhealthy `requestDocumentExplicitActorReadiness` health service. Production schema is exact 3/3 and the Production-only flag is exact `on` as of 2026-08-31; signed-in Admin health reports the service enabled. Preview remains unset.
- **`file-loader.js`** — Shared FileRef loader (upload/SharePoint → PDF/DOCX text) used by Grant Reporting and Phase I Dynamics.
- **`sharepoint-buckets.js`** — `getRequestSharePointBuckets(requestId, requestNumber)` — walks active + archive libraries for a request.
- **`cycle-code.js`** — Grant cycle code helpers (`Jxx`/`Dxx` from June/December meeting dates). Helpers: `meetingDateToCycleCode`, `parseCycleCode`, `cycleCodeToOdataFilter`.

### Upload validation

- **`file-magic.js`** — Magic-byte sniffing + extension/MIME validation. `validateReviewFile` (PDF/DOCX/DOC) for reviewer uploads; `validateIntakeAttachment` (PDF/DOCX/XLSX, parameterized per field `accept[]`) for the intake-portal three-call dance. **Shape only, not malware**: app-side Cloudmersive is primary defense — tenant has no MDO/Safe Attachments.
- **`blob-filename.js`** — `sanitizeBlobFilename` for intake-portal applicant-supplied filenames. NFKC normalize, reject `..` segments (incl. fullwidth), strip control + path separators, 200-codepoint cap.
- **`intake-blob.js`** — `getIntakeBlobToken()` reads `INTAKE_BLOB_RW_TOKEN` (**NOT** the shared `BLOB_READ_WRITE_TOKEN`). Single source of truth for the four intake Blob call sites; fail-loud on missing/whitespace.

### Form schema

- **`form-schema.js`** — Intake-portal form schema loader. Static import map (`SCHEMAS[formKey]`); `findFileField` walker; `countFieldEntries` cardinality helper.

### Collections

- **`past-cutoff.js`** — `isPastCutoff(meetingDate, cutoffIso)`: true only when a Date-parseable meeting date is strictly before the ISO cutoff instant; `null`/unparseable → `false`, so a missing meeting date can never make a row expiry-eligible. Moved out of `reviewer-engagement/expire-invitation.js` 2026-09-06 (S490); imported by that module and by the stale-invitation sweep's discovery pass.
- **`chunk.js`** — Canonical array-chunk helper: `chunk(array, size)` → in-order sub-arrays of ≤ size. Fail-closed: non-array → `TypeError`, non-positive-integer size → `RangeError`; `[]` → `[]`; input never mutated. Import under the alias `chunked` at call sites (per-iteration variables are commonly named `chunk`). **Use this — not a hand-rolled `for (i += N) slice` loop** (consolidated S331-332, `docs/CHUNK_CONSOLIDATION_PLAN.md`).
- **`reviewer-save-key.js`** — Shared browser/server partial-save correlation key; explicit client ids win, otherwise normalized submitted anchors distinguish same-name rows. Never use it for person merging.
- **`reviewer-identity-fields.js`** — Literal seven-field authority for resolver-sourced reviewer identity values that require durable lineage; shared by the legacy resolver and Wave 13 binding contract.
- **`reviewer-manual-confirmation.js`** — Canonical name/contact projection and exact matcher for request-scoped staff identity confirmation records.
- **`reviewer-vetted-email.js`** — Canonical pure reviewer contact projection
  shared by attestation mint/save, Find selectability, applicant backfill, and
  legacy reconciliation. Produces `ready`,
  `needs_identity_confirmation`, or `missing_email` with exact
  email/source/persistence authority; `pickVettedEmail` is its compatibility
  wrapper.
- **`reviewer-remediation.js`** — Total server-owned reviewer decision-code to
  executable remedy mapping. Unknown codes fail closed to retry plus durable
  repair; client-supplied action labels never grant authority.
- **`reviewer-address-trust.js`** — Pure bounded parser/builder for the
  versioned exact-address attestation, pending-conflict, and resolution bundle;
  malformed, oversized, unknown-version, and stored-email-mismatched state
  grants no send authority.
- **`reviewer-institution-account-match.js`** — Shared conservative exact matcher used by the accepted-reviewer runtime and the Contact→Account adjudication report. NFKC/case/punctuation/whitespace normalization only; indexes active Account `name`, `akoya_aka`, `wmkf_legalname`, and `wmkf_dc_aka`; duplicate targets remain ambiguous and no fuzzy/acronym expansion is inferred.

### Secrets

- **`tracked-secrets.js`** — Canonical `TRACKED_SECRETS` list for rotation/expiration alerting. Consumed by `pages/api/cron/secret-check.js` + `pages/api/admin/secrets.js`. **`docs/CREDENTIALS_RUNBOOK.md` mirrors this list by hand — this file is the canonical source.**

---

## `lib/observability/`

- **`request-correlation.js`** — Request-correlation ALS + the `workbench.dependency`
  telemetry emitter (Workbench Observability Stage 1). Owns a **dedicated
  `AsyncLocalStorage`, fully independent of the DAL restriction ALS** in
  `lib/services/dynamics-context.js` — nesting either inside the other leaks nothing.
  `withRequestCorrelation({correlationId, routeName}, fn)` returns `fn`'s value verbatim;
  `getRequestCorrelation()` is `undefined` outside any scope (the defined behavior for
  cron/scripts/cold start, not an error); `mintCorrelationId()` is `crypto.randomUUID()` —
  random, carries no user identity, never write authority. `emitDependencyEvent({url,
  method, ms, response, error})` **never throws** (whole body try/catch), never reads a
  request/response body, and emits exactly `console.log(JSON.stringify(event))` to the
  platform log stream — no durable write, no new table, and explicitly **not**
  `api_usage_log` (the LLM token/cost ledger). All classifiers use **closed value sets that
  fail closed to `'unknown'`**, so no URL, query string, path segment, GUID, filename,
  tenant id, token, header, or body material can reach an event field. `node:async_hooks`
  and `crypto` load lazily via guarded requires; note Turbopack statically resolves even
  variable-path requires, so browser-bundle safety rests on **no client bundle reaching
  this module** (verified by static-bundle scan, 2026-08-15), not on the require pattern.
  Extending the Dataverse entity-set allowlist is a reviewed commit, not an env or ad-hoc
  edit. Wired at `lib/services/dynamics/http.js`, `lib/services/graph-service.js`, and
  `lib/dataverse/client.js` (via a guarded lazy require that keeps the load off that
  module's import-time path). Source header is the contract.

---

## `shared/utils/` — lifecycle policy (browser-safe)

- **`shared/utils/reviewer-send-skip-reasons.js`** — Stage 6D: `SEND_SKIP_REASON` (the complete `skipped[].reason` vocabulary emitted by `send-emails-service.js`, every literal replaced; a grep test forbids bare literals) and `SEND_SKIP_REASON_LABEL` (staff copy rendered by `ReleaseMaterialsModal` and `InviteEmailModal`). `check:status-enum-parity` entry #5 requires every produced reason to have a label.
- **`shared/utils/reviewer-engagement-policy.js`** — Stage 2 (PR #155, `716bc558`, 2026-09-05): the
  one home for the duplicated invitation-correction policy. Exports three predicates over raw
  suggestion rows — `isClosedEngagementStatus(status)` (status-only), `isClosedEngagementRow(row)`
  (completion stamp or closed status), `isInvitationCorrectionSourceRow(row)` (explicit
  `undefined → false`); the status Sets are module-private. Consumers: `my-candidates-service.js`
  (response correction guard) and `reviewer-suggestion.js` `updateLifecycle`/`softDelete`
  (`updateLifecycle`'s leave-closed guard is the one remaining historical status-only site; `softDelete`
  moved to the row predicate 2026-09-06 under owner decision D0). Imports only `shared/config/`.
  Not a universal "done" predicate: materials, receipt, terminal-transition and rollup keep their
  own sets.

## Maintenance contract

- New service / utility files: add a one-line entry here in the same commit.
- Renaming or removing a service: edit / remove the line here in the same commit.
- A "load-bearing fact" (storage SoT, fail-loud behavior, env-var separation, auth boundary, drain posture, migration history) must live in the **source-file header**, not just here. This catalog is an index; the source header is the contract.
- This catalog is NOT auto-generated. Manual edits only.
