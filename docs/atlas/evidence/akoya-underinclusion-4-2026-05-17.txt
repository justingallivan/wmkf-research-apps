REDACTED AGGREGATE EVIDENCE
Probe date: 2026-05-17 (read-only production verification)
Subject: payee, applicant, primary-contact, and applicant-address coverage

Field metadata
--------------
- `akoya_payee`: Lookup ("Payee").
- `akoya_primarycontactid`: Lookup ("Primary Contact").
- `akoya_purpose`: Memo ("Purpose").

Fill observations
-----------------
| Field | Overall | Migrated | Native |
|---|---:|---:|---:|
| `akoya_payee` | 2,844/25,561 (11%) | 218/22,573 (1%) | 2,626/2,988 (88%) |
| `akoya_primarycontactid` | 18,078/25,561 (71%) | 15,766/22,573 (70%) | 2,312/2,988 (77%) |

In a 14-row newest-native sample, payee and applicant account matched in 13
rows and differed in one. Primary contact was often populated but represented
a person role distinct from the organizational payee/applicant.

Applicant-account address coverage
----------------------------------
Among 25,473 requests with an applicant account:

- `address1_city`: 25,382 populated.
- `address1_stateorprovince`: 25,381 populated.

Conclusion
----------
Payee is strongly native-era scoped and usually agrees with applicant account
in the inspected sample, while primary contact has different semantics.
Applicant-account city/state are broadly populated, but whether they belong in
a default analytical projection depends on the use case.

Redaction boundary
------------------
The original sample contained request numbers, organization and contact names,
and a state-frequency table. Those row-level and location-linked production
values are intentionally excluded. Re-run the read-only production probe when
current row-level substantiation is authorized.
