# API Route Security Matrix

Last updated: 2026-06-25

## Purpose

This document is the living authorization inventory for `pages/api`. It is meant to do two jobs:

1. Capture the current access-control posture route by route.
2. Provide a recurring control so new routes, changed routes, and data-scope assumptions are reviewed consistently.

The matrix focuses on authorization, data ownership, and persistence (what each route writes). It does not replace dependency scanning, secret scanning, CSP/header review, or AI data-minimization review.

**Column legend:** `(PG)` = Vercel Postgres, `(DV)` = Dataverse / Dynamics 365. SharePoint and Vercel Blob are named explicitly. The Persistence cell summarizes writes (and reads where notable); the canonical schema for Postgres tables is `lib/db/schema.sql`, for Dataverse entities `docs/atlas/dataverse-*.md`. "AI pass-through" means the route only invokes an LLM and returns the result without persisting business data; `api_usage_log` writes from the LLM client are still listed because they're the audit trail for spend monitoring.

## Access Classes

| Class | Meaning | Expected Guard |
|---|---|---|
| Public metadata | Safe to expose without a session. Should not reveal sensitive business data. | None, explicit rationale required |
| NextAuth | Framework-owned authentication endpoints. | NextAuth handler or `getServerSession` |
| Authenticated | Any signed-in user may call it. Use only for low-sensitivity shared utilities. | `requireAuth` |
| Profile | Signed-in user must have a linked active profile. User-owned data must be scoped to that profile. | `requireAuthWithProfile` |
| App | Signed-in user must have access to the named app. User-owned data must still be scoped where applicable. | `requireAppAccess(req, res, appKey)` |
| Superuser | Linked profile plus `dynamics_user_roles.role = 'superuser'`. | `requireAuthWithProfile` plus role check |
| Cron | Vercel scheduled/background endpoint. | `verifyCronSecret` |
| External token | Public magic-link route scoped by the token payload and server-side token verification. | `verifySuggestionToken` |
| Applicant session | NextAuth session minted by the `entra-external` provider (separate Entra External ID tenant). Carries `contactOid` + `contactEmail`. Routes scope writes to data the contact owns, and may additionally gate on `wmkf_portalmembership` role/state. | `getServerSession` + `session.user.userType === 'applicant'` (+ optional role/membership check) |

## Resolved Baseline Findings

These findings were identified in the first matrix pass and addressed in the follow-up hardening commits:

| Finding | Disposition | Current Control |
|---|---|---|
| P1: `/api/user-profiles` allowed standalone profile creation | Fixed | `POST` is no longer supported. Profile creation flows through `/api/auth/link-profile` during first-login Entra linking. |
| P1: `/api/blob-proxy` was authenticated but not ownership scoped | Accepted / documented | Blob proxy is explicitly limited to shared organizational assets. Future user-owned blob flows must use record-aware endpoints. |
| P1: `/api/review-manager/download-review` used app access rather than record ownership | Accepted / documented | Review Manager is a staff-shared workflow. `review-manager` access intentionally grants shared review-file access. |
| P2: Dynamics bypass routes needed explicit data boundaries | Fixed by documentation | Bypass routes now include inline data-boundary annotations describing org-wide, role-gated, PD-derived, or staff-shared scope. |
| P2: Superuser checks were duplicated | Fixed | Shared `requireSuperuser(req, res)` and `getUserRole(profileId)` helpers now live in `lib/utils/auth.js`; admin routes were migrated. |
| P3: `/api/auth/status` public contract could drift | Fixed by documentation | Endpoint is intentionally public and pinned to `{ enabled: boolean }` only. |

There are no open findings from the initial matrix pass as of this update. New findings should be added above the route matrix with priority, file/line references, disposition, and owner question. Medium-risk route notes in the matrix remain useful policy checkpoints, especially where an app intentionally has staff-wide access to Dynamics, SharePoint, reviewer, or file data.

## Route Matrix

| Route | Methods | Intended Class | Current Guard | Data Scope | Persistence | Risk | Notes |
|---|---:|---|---|---|---|---|---|
| `/api/admin/alerts` | GET, PATCH | Superuser | `requireSuperuser` | Global admin | Reads/updates `system_alerts` (PG) | Low | Shared helper. |
| `/api/admin/health-history` | GET | Superuser | `requireSuperuser` | Global admin | Reads `health_check_history` (PG) | Low | Shared helper. |
| `/api/admin/alert-recipients` | GET, PUT | Superuser | `requireSuperuser` | Global admin | Reads/writes `alertRecipientsByCategory` setting in `wmkf_appsystemsettings` (DV); reads superuser roster from `dynamics_user_roles` (PG) for fallback display | Low | Per-category routing config for system-alert emails. |
| `/api/admin/email-defaults` | GET, PUT | Superuser | `requireSuperuser` | Global admin | Reads/writes `email.grantee_invite.*` text defaults in `wmkf_appsystemsettings` (DV) via dispatcher | Low | Admin-editable email/text defaults catalog. GET uses strict reads and marks per-entry `unavailable` on settings fetch failure; PUT validates the catalog key and allows blank values. |
| `/api/admin/honorarium-amount` | GET, PUT | Superuser | `requireSuperuser` | Global admin | Reads/writes `honorarium.default_amount` in `wmkf_appsystemsettings` (DV) via dispatcher | Low | Single ground-truth reviewer-honorarium amount (S199); replaces the per-user preference. PUT validates a positive number. |
| `/api/admin/maintenance` | GET | Superuser | `requireSuperuser` | Global admin | Reads `maintenance_runs` (PG); reads `wmkf_appsystemsettings` (DV) for retention config | Low | Shared helper. |
| `/api/admin/reviewer-time-budget` | GET, PUT | Superuser | `requireSuperuser` | Global admin | Reads/writes `reviewer.time_budget_seconds` in `wmkf_appsystemsettings` (DV) | Low | Admin-editable reviewer-search wall-clock budget (S223; default 600s, clamped [120,800]). PUT clamps into range. Enforced in-app via an AbortSignal deadline because Vercel `maxDuration` is build-time-static. See docs/REVIEWER_TIMEOUT_BUDGET_PLAN.md. |
| `/api/admin/models` | GET, PUT | Superuser | `requireSuperuser` | Global admin | Reads/writes `wmkf_appsystemsettings` (DV) via dispatcher | Low | Also calls Anthropic model list. PUT accepts tier keys or reviewed concrete Claude ids only; unreviewed concrete ids are rejected before persistence. |
| `/api/admin/policies` | GET, POST | Superuser | `requireSuperuser` | Global admin | Reads/writes Dataverse `wmkf_policy` + `wmkf_policyversion`; writes `policy_publish_audit` (PG); writes `system_alerts` on audit-finalize failure | Medium | Three-step publish flow (create child, flip parent lookup, retire prior) with ETag concurrency + alt-key enforced uniqueness. See atlas page for state machine. |
| `/api/admin/prompts` | GET | Superuser | `requireSuperuser` | Global admin | Reads current (`iscurrent`) rows of Dataverse `wmkf_ai_prompt` | Low | Lists prompt rows for the /admin Prompt-templates editor (S222). |
| `/api/admin/prompts/[name]` | GET, PUT | Superuser | `requireSuperuser` | Global admin | Reads/writes Dataverse `wmkf_ai_prompt`; writes `prompt_publish_audit` (PG); writes `system_alerts` on audit-finalize / duplicate-current | Medium | Versioned publish (create new `iscurrent` row v+1, flip prior with If-Match, verify exactly-one-current). Validates body via prompt-validators and rejects unreviewed concrete Claude `wmkf_ai_model` values before create/resume. Mirrors the `policies` protocol; resume + idempotency by `request_id`. See `docs/atlas/dataverse-wmkf-ai-run-and-prompt.md`. |
| `/api/admin/reconcile-identities` | POST | Superuser | `requireSuperuser` | Global admin | Updates `user_profiles` (PG); reads Dynamics `systemuser` | Low | Manual equivalent of cron reconciliation. |
| `/api/admin/review-questions` | GET, POST | Superuser | `requireSuperuser` | Global admin | Reads/writes Dataverse `wmkf_reviewquestion` (active set); writes `review_question_audit` (PG); writes `system_alerts` on audit failure | Medium | Staff editor for the external-reviewer question set (staff-editable-questions epic Phase C). POST diffs the submitted full set vs current BY ROW ID and applies ONE atomic `executeChangeset` (create/update/soft-delete via statecode); enforces key-immutability (the `wmkf_questionkey` answer-join can't change) + optimistic concurrency via `questionSetVersion` (409 `set_changed`); pending→final audit with hard-abort if audit unavailable; calls `ReviewQuestionFetcher.invalidate()` on success. See `docs/atlas/dataverse-wmkf-reviewquestion.md`. |
| `/api/admin/secrets` | GET, PUT | Superuser | `requireSuperuser` | Global admin | Reads/writes `wmkf_appsystemsettings` (DV) via dispatcher | Low | Tracks metadata, not secret values. |
| `/api/admin/users` | DELETE | Superuser | `requireSuperuser` | Global admin | Updates `user_profiles.is_active` (PG); clears app-access cache | Medium | Soft-archive; refuses self-archive. Profile row preserved for audit FK integrity. |
| `/api/admin/stats` | GET | Superuser | `requireSuperuser` | Global admin | Read-only (PG SELECTs across usage tables) | Low | Usage statistics across users. |
| `/api/analyze-funding-gap` | POST | App | `requireAppAccess('funding-gap-analyzer')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | AI payload review still needed. |
| `/api/analyze-literature` | POST | App | `requireAppAccess('literature-analyzer')` | Request payload | Writes `api_usage_log` (PG); external lit-search APIs | Low | External research APIs / AI payload review. |
| `/api/api-capabilities` | GET | Authenticated | `requireAuth` | Shared metadata | Read-only | Low | Authenticated capability metadata. |
| `/api/app-access` | GET, POST, DELETE | Profile / Superuser | `requireAuthWithProfile` + `getUserRole` | Own grants; all grants for superuser | Reads/writes `wmkf_appuserappaccesses` (DV) via dispatcher | Low | Superuser branch uses shared role helper. |
| `/api/auth/[...nextauth]` | Framework | NextAuth | NextAuth callbacks | Session/profile linking | Inserts/updates `user_profiles` (PG); grants `wmkf_appuserappaccesses` (DV) | Medium | Identity linking remains a strategic hardening area. |
| `/api/auth/link-profile` | POST | NextAuth linking | `getServerSession` + `needsLinking` | Matching Azure email | Updates/inserts `user_profiles` (PG) | Low | Server-derived identity, email match enforced. |
| `/api/auth/status` | GET | Public metadata | None | None | Read-only | Low | Intentionally public if needed pre-login. |
| `/api/blob-proxy` | GET | Authenticated | `requireAuth` | Shared organizational blob assets only | Reads Vercel Blob (proxy) | Low | Host allowlist is the boundary; do not extend to user-owned blobs. |
| `/api/cron/auth-bypass-check` | GET, POST | Cron | `verifyCronSecret` | Global security posture | Writes `maintenance_runs`, `system_alerts` (PG); sends email | Low | Daily re-assert of the `EMERGENCY_AUTH_BYPASS` prod lever. Raises a CRITICAL alert while the lever is set; auto-resolves once cleared. Shares logic with the `instrumentation.js` cold-start hook via `lib/utils/auth-bypass-monitor.js`. |
| `/api/cron/generate-grantee-titles` | GET, POST | Cron | `verifyCronSecret` | Research awardees at `wmkf_phaseistatus=Invited` for the cycle whose `wmkf_wmkfprojectdescription` is empty | Reads `akoya_requests` (`akoya_title`, `wmkf_abstract`); generates via Executor (`grantee-title.generate`, Sonnet); ETag-conditional PATCH of `wmkf_wmkfprojectdescription` **only when empty** | Low | S269 chunk-7 edited-title generator. Idempotent write-when-empty (never overwrites manual curation); `queryAllRecords` (paginated, honors `capped`); bounded concurrency + soft time budget under the 120s cap; per-row fail-soft (failures left empty → retried next run). Seasonal daily (Apr–Jun/Oct–Dec). `?cycleCode=` overrides (one-off backfill / prior cycle / test). |
| `/api/cron/grantee-deliverable-reminders` | GET, POST | Cron | `verifyCronSecret` | `wmkf_granteedeliverable` rows still Invited 12+ days after first invite | Reads `wmkf_granteedeliverables` (paged, no `$expand`), then per-row reads `akoya_request`, PI/liaison `contact`, lead-PD `systemuser`, and the assigned PD's unified `email_signature` preference via `email-signature`; pre-send claim PATCH moves status out of Invited; sends Dynamics email as the PD with `noFallback:true`; final PATCH stamps `wmkf_remindeddate` | Low | Daily S271 grantee abstract reminder. Durable once-only behavior: claim before send means a post-send/finalize failure cannot double-send on the next run. Skips and reports missing PD name/email/systemuser (title is not required), missing recipient, claim failure, send failure, and capped query remainder. |
| `/api/cron/health-check` | GET, POST | Cron | `verifyCronSecret` | Global operational | Writes `health_check_history`, `system_alerts` (PG); sends email | Low | Stores health history and alerts. |
| `/api/cron/log-analysis` | GET, POST | Cron | `verifyCronSecret` | Vercel logs | Writes `api_usage_log` (PG) via llm-client; sends email | Low | Review redaction expectations. |
| `/api/cron/maintenance` | GET, POST | Cron | `verifyCronSecret` | Global operational | Writes `maintenance_runs`; deletes from `api_usage_log`, `dynamics_query_log`, `health_check_history`, `system_alerts`, `dynamics_feedback`, Vercel Blob | Low | Retention/cleanup job. |
| `/api/cron/reconcile-identities` | GET, POST | Cron | `verifyCronSecret` | Global identity | Updates `user_profiles` (PG); reads Dynamics | Low | Background identity sync. |
| `/api/cron/refresh-irs-bmf` | GET, POST | Cron | `verifyCronSecret` | Reference data only | Atomic-swap-replaces `irs_exempt_orgs` (PG); writes `maintenance_runs`, `system_alerts` on failure | Low | Quarterly IRS BMF refresh (15th of Jan/Apr/Jul/Oct). Downloads 4 IRS CSVs to /tmp, COPYs into staging, swaps to live. Atomic — failure leaves live table untouched. |
| `/api/cron/pricing-canary` | GET, POST | Cron | `verifyCronSecret` | `api_usage_log` distinct models + Anthropic `/v1/models` metadata | Reads `api_usage_log` (PG); calls Anthropic `/v1/models` with `CLAUDE_API_KEY`; writes `maintenance_runs` (PG) and `system_alerts` via NotificationService | Low | S181 weekly canary (Mon 10am UTC). Flags model ids in usage log that don't resolve via `lib/utils/model-pricing.js`, `LAST_REVIEWED_AT` older than 60 days, and newer live Claude ids not specifically covered by both capability/pricing registries. Routes to category `ops`; live model discovery degrades to skipped/unavailable without failing the whole cron. |
| `/api/cron/pricing-refresh` | GET, POST | Cron | `verifyCronSecret` | Anthropic `/cost_report` + `api_usage_log` summed tokens | Reads `api_usage_log` (PG); writes `model_pricing_audit` (PG); writes `system_alerts` via NotificationService; calls Anthropic Admin API (`ANTHROPIC_ADMIN_API_KEY`) | Low | S181 monthly drift check (1st of month, 11am UTC). Derives authoritative per-model price from Anthropic's cost report, compares to local table, alerts on >5% drift OR unknown-in-cost-report. Skips when admin key not set. Routes to category `ops`. |
| `/api/cron/reviewer-reminders` | GET, POST | Cron | `verifyCronSecret` | Invited non-responders at their per-reviewer respond-by deadline; accepted/materials-sent/not-submitted reviewers at review-due | Reads `wmkf_appreviewersuggestions` + per-request `akoya_request` campaign config + lead-PD `systemuser` + `email-signature`; claim-before-send conditional PATCH of `wmkf_respondremindersentat` (respond) / `wmkf_remindersentat`+`wmkf_remindercount` (review-due) via If-Match; re-mints token (`mintAndStore`); sends Dynamics email as the PD with `noFallback:true` | Low | Daily reviewer-engagement Phase-3 reminders (S275). Per-request opt-in (campaign-config enabled flags). Fire-once + at-most-once (claim before send). Knobs: `?maxBatch=N`, `?dryRun=1`. |
| `/api/cron/secret-check` | GET, POST | Cron | `verifyCronSecret` | Secret metadata | Writes `system_alerts` (PG); sends email | Low | Expiration metadata only. |
| `/api/cron/spend-check` | GET, POST | Cron | `verifyCronSecret` | Usage/spend logs | Reads `api_usage_log`; writes `system_alerts` (PG); sends email via Dynamics | Low | Global monitoring. |
| `/api/cron/sweep-stale-invites` | GET, POST | Cron | `verifyCronSecret` | Reviewer suggestions whose parent request's meeting date is past | Reads `akoya_requests` + `wmkf_appreviewersuggestions`; PATCHes stuck suggestions to `wmkf_responsetype=no_response` | Low | Daily sweep that closes silent reviewer invites past their meeting date. Idempotent; bounded by `maxBatch` (default 200). Ad-hoc backfill knobs: `?graceDays=N`, `?maxBatch=N`, `?dryRun=1`. |
| `/api/cron/drain-submissions` | GET, POST | Cron | Inline strict `CRON_SECRET` (no dev bypass — Codex round-12 Q5) | Up to `DRAIN_BATCH_SIZE` non-terminal jobs (claim-leased) | Claims + advances rows in `submission_jobs` (PG); writes `intake_audit` (PG); writes `system_alerts` (PG) on corruption/anomaly/build-pending; writes Dataverse `akoya_requests` (Create with pre-gen GUID, `Prefer: return=representation`) + `wmkf_proposalbudgetlines` children (per-child duplicate-PK skip); writes SharePoint files to `akoya_request/{requestnum}_{guidUpper}/Applicant_Uploads/` via Graph; reads private Blob (`INTAKE_BLOB_RW_TOKEN`) | Medium | Applicant intake portal drain. Two-phase claim (`FOR UPDATE SKIP LOCKED` + `lease_token`), transactional state-transition+audit, classifier-driven retry taxonomy. Wired: `queued → scanning → request_created → files_moved → dynamics_patched (budget-lines half)` with duplicate-PK recovery (read-back via GET for parent; per-child success-advance for budget-lines) + per-file `sharepoint_paths` JSONB skip + per-child `dynamics_patches.budget_lines` skip + post-download sha256 verification. BUILD-PENDING: `dynamics_patched` (persons + parent aggregates pending Connor Q2 + contact-resolution service), `status_flipped` (Connor Q1), `completed` (final cleanup). Schedule every 2 min (≪ `DRAIN_LOCK_TTL_SECONDS=600s`). |
| `/api/dataverse-export/download` | GET | App | `requireAppAccess('dataverse-bulk-export')` + signed `dvx-download` token | Private export artifact (own run) | Reads a PRIVATE Vercel Blob (no writes) | Medium | Gated proxy for the private .xlsx — two gates (app access + short-lived ≈1h signed token binding the blob pathname). Streams Content-Disposition: attachment. Closes the public-URL exposure gap (Codex S160 P1). |
| `/api/dataverse-export/metadata` | GET | App | `requireAppAccess('dataverse-bulk-export')` | Live Dataverse taxonomies (akoya_program / wmkf_type / wmkf_grantprogram / distinct akoya_requeststatus) | Read-only Dataverse; no PG/DV writes | Low | Fail-loud on taxonomy fetch (502), never a stale/partial list (Living-taxonomy §9). Distinct statuses page to completion, never one page. |
| `/api/dataverse-export/preview` | POST | App | `requireAppAccess('dataverse-bulk-export')` | Validated QuerySpec → compiled FetchXML + true aggregate count + era split | Read-only Dataverse (FetchXML aggregate counts only, NO rows); no writes | Low | §2.1 validation → 422 + violations. Mints a signed `resultToken` (HS256 / NEXTAUTH_SECRET) binding the exact validated spec. Never OData /$count. |
| `/api/dataverse-export/run` | POST | App | `requireAppAccess('dataverse-bulk-export')` | Confirmed QuerySpec (resultToken-gated) → akoya_request rows | Read-only Dataverse (backoff-hardened FetchXML paging); writes the .xlsx artifact to a PRIVATE Vercel Blob (access:'private', addRandomSuffix, contentDisposition:attachment); returns only a gated `/api/dataverse-export/download` proxy URL (signed short-lived dvx-download token) — never a public URL | Medium | SSE response. Stateless confirm gate: rejects any body whose `resultToken` was not minted by /preview; executes the token's embedded spec, never a body spec. Fails loud (422) if `excludeOperational` labels don't resolve against the live taxonomy. Terminal error writes NO Blob (a failure can never present as a short-but-complete file). Paging budget clamped to keep route headroom; loud truncation. |
| `/api/dynamics-explorer/chat` | POST | App | `requireAppAccess('dynamics-explorer')` | Org-wide CRM exploration shaped by Dynamics role and restrictions | Writes `dynamics_query_log`, `api_usage_log` (PG); reads Dynamics + SharePoint; ExcelJS export to Blob | Medium | Data boundary documented inline; AI minimization remains separate review area. |
| `/api/dynamics-explorer/download-document` | GET | App | `requireAppAccess('dynamics-explorer')` | Dynamics/SharePoint restrictions | Reads SharePoint (no write) | Medium | Boundary documented inline. |
| `/api/dynamics-explorer/feedback` | GET, POST, PATCH | App / Superuser | POST uses app access; GET/PATCH require superuser | Feedback records | Reads/writes `dynamics_feedback` (PG) | Low | Mixed route; current split is reasonable. |
| `/api/dynamics-explorer/restrictions` | GET, POST, DELETE | App / Role | `requireAppAccess('dynamics-explorer')` + `getUserRole` | Restriction config | Reads/writes `dynamics_restrictions` (PG) | Low | Read/write role handling in route. |
| `/api/dynamics-explorer/roles` | GET, POST, DELETE | App / Superuser | `requireAppAccess('dynamics-explorer')` + `getUserRole` | Own role or all roles | Reads/writes `dynamics_user_roles` (PG) | Low | Shared role helper. |
| `/api/email-defaults/grantee-invite` | GET | Profile | `requireAuthWithProfile` | Shared grantee invite defaults | Reads `email.grantee_invite.subject` and `email.grantee_invite.body` from `wmkf_appsystemsettings` (DV) via dispatcher | Low | Profile-readable defaults for PD grantee-invite composition. Uses strict reads so outages return `unavailable:true` instead of silently looking blank. |
| `/api/email-defaults/reviewer-templates` | GET | Profile | `requireAuthWithProfile` | Shared reviewer template defaults | Reads `email.reviewer_{invitation,materials,followup,thankyou}.{subject,body}` from `wmkf_appsystemsettings` (DV) via dispatcher | Low | Profile-readable org defaults for the four PD-composed reviewer templates (S297). Per-PD override is layered on top client-side (email-template-store.js). Strict reads → `unavailable:true` on outage. |
| `/api/evaluate-multi-perspective` | POST | App | `requireAppAccess('multi-perspective-evaluator')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | AI payload review. |
| `/api/expertise-finder/batch-match` | POST | App | `requireAppAccess('expertise-finder')` | Writes `user_profile_id` | Writes `expertise_matches`, `api_usage_log` (PG); reads Dynamics + SharePoint | Low | User-scoped usage/history writes. |
| `/api/expertise-finder/history` | GET | App | `requireAppAccess('expertise-finder')` | `user_profile_id` scoped | Read-only (`expertise_matches` SELECTs) | Low | Good scoping pattern. |
| `/api/expertise-finder/match` | POST | App | `requireAppAccess('expertise-finder')` | Writes `user_profile_id` | Writes `expertise_matches`, `api_usage_log` (PG) | Low | User-scoped usage/history writes. |
| `/api/expertise-finder/proposals` | GET | App | `requireAppAccess('expertise-finder')` | App-wide Dynamics proposal lookup | Reads Dynamics `akoya_request` | Medium | Staff-wide visibility policy checkpoint. |
| `/api/expertise-finder/roster` | GET, POST, PATCH, DELETE | App | `requireAppAccess('expertise-finder')` | Shared roster | Reads/writes `expertise_roster` (PG) | Medium | Shared mutable roster; confirm app-level access is sufficient. |
| `/api/field-primer/generate` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Two modes by body: `{proposalText}` (standalone) or `{requestId, regenerate?}` (Workbench, GUID-validated) | Writes `api_usage_log` (PG) via llm-client; writes `wmkf_ai_run` (DV) audit row via Executor. **requestId mode also persists** a JSON envelope to `akoya_request.wmkf_ai_fieldprimer` via explicit `updateRecord` (after expert grounding) — idempotent: returns the stored primer without a paid call unless `regenerate:true`; single-flight via an **ETag-conditional generation lease** (a racing request 412s on the stale ETag → `{status:'generating'}`), so only one cold generation runs per request; prior value restored on failure | Low | Standalone staff field-overview generator (S248) + Workbench Proposal-tab Field Primer (S258). Decoupled from reviewer origination — no discovery/save/COI write path. Untrusted `proposal_text` wrapped + A7-preambled by the Executor. requestId mode pulls `ProjectDescription` from SharePoint (`getProposalText`). |
| `/api/external/grantee/[token]/context` | GET | External token | `verifyGranteeToken` | Token-scoped `akoya_request` + related `wmkf_granteedeliverable` package row | Reads Dynamics `akoya_request` (abstract/header fields) + `wmkf_granteedeliverable` (status/image metadata); writes `external_rate_limit` (PG) | Low | Grantee deliverables portal (S268/S271). **Stateless** token — no stored hash/revocation; signature + expiry + `aud:'grantee'` claim (rejects reviewer-token replay, which carries no `aud`). Fail-closed: missing deliverable row or unknown/missing `wmkf_deliverablestatus` → read-only (NOT editable). No side-effect write (unlike the reviewer context route). Rate-limited per-token + per-IP (`external_rate_limit`); 429 + `Retry-After` on excess. |
| `/api/external/grantee/[token]/submit` | POST | External token | `verifyGranteeToken` | Token-scoped `akoya_request` + related `wmkf_granteedeliverable` package row | Multipart (busboy, `bodyParser:false`): `editedAbstract`+`caption` text, ONE `image` file (≤10 MB, `files:1`). Fail-closed **editable-status guard** from related row (refuse unless Drafted/Invited/Reminder Sent/Revision Requested → 409; missing row → 409). Validates image magic-bytes (`validateGranteeImage`) → virus-scans (Cloudmersive, soft-pass when disabled) → uploads to SharePoint `Grantee_Uploads/` (server-controlled unique filename, content-type from validated magic) → ETag-conditional PATCH of `akoya_request.wmkf_abstractapproved`, then package PATCH of `wmkf_imagecaption`/`wmkf_imagefileref`/status→Submitted via `patchDeliverable`; rollback deletes the new upload on PATCH failure; best-effort prunes the prior image on success. Writes `external_rate_limit` (PG). Whole body in `bypassDynamicsRestrictions('grantee-submit')` | Medium | Grantee Deliverables Portal chunk 5 (S268/S271). Closes the round-trip (the chunk-4 form posts here). Rate-limited per-token + per-IP; 429 + `Retry-After`. No staff identity (external). |
| `/api/external/review/[token]/context` | GET | External token | `verifySuggestionToken` | Token-scoped suggestion/request | Updates `wmkf_appreviewersuggestion` (DV) for first-view timestamp; reads SharePoint; writes `external_rate_limit` (PG) | Low | GET has a side-effect write (first-view timestamp). Public route protected by magic link verification. Rate-limited per-token + per-IP (`external_rate_limit`, security audit A6); 429 + `Retry-After` on excess. |
| `/api/external/review/[token]/proposal` | GET | External token | `verifySuggestionToken` | Token-scoped proposal files | Reads SharePoint; writes `external_rate_limit` (PG) | Medium | High-sensitivity payload; keep token tests strong. Rate-limited per-token + per-IP (`external_rate_limit`, security audit A6); 429 + `Retry-After` on excess. |
| `/api/external/review/[token]/upload` | POST | External token | `verifySuggestionToken` | Token-scoped upload | Writes SharePoint `Reviewer_Uploads/`; updates `wmkf_appreviewersuggestion` (DV) via writeReviewFiles; writes `external_rate_limit` (PG) | Low | Good candidate for replay/expiry regression tests. Rate-limited per-token + per-IP (`external_rate_limit`, security audit A6); 429 + `Retry-After` on excess. |
| `/api/external/review/[token]/respond` | POST | External token | `verifySuggestionToken` | Token-scoped accept/decline | Updates `wmkf_appreviewersuggestion` (DV) via `applyStage2aResponse`; reads `wmkf_policy`/`wmkf_policyversion` via `getActivePolicies`; writes `external_rate_limit` (PG) | Medium | Stage 2a slice 1 entry point. State machine + idempotency + active-child sanity. Optimistic locking via `If-Match`. Email triggers on decline-with-referral deferred. Rate-limited per-token + per-IP (`external_rate_limit`, security audit A6); 429 + `Retry-After` on excess. |
| `/api/external/review/[token]/draft` | GET, PUT | External token | `verifySuggestionToken` | Token-scoped review-form draft | Reads/writes `review_drafts` (PG) via `ReviewDraftService`; writes `external_rate_limit` (PG) | Medium | In-browser authoring autosave (reviewer review-form build, Phase 1). PUT sanitizes every rich-text answer server-side (`sanitize-review-html`) before persisting — stored-XSS boundary. Engagement gate reuses `computeEngagementState`: PUT 409 `materials_not_sent` outside the authoring stage and 409 `review_received_locked` once `wmkf_reviewreceivedat` is set; GET returns an empty draft post-submit. Rate-limited per-token + per-IP (`external_rate_limit`, security audit A6); 429 + `Retry-After` on excess. |
| `/api/external/review/[token]/submit` | POST | External token | `verifySuggestionToken` | Token-scoped final review submission | Atomic write to `wmkf_appreviewanswer` (DV, N answer rows upserted by alternate key) + parent `wmkf_appreviewersuggestion` PATCH (ratings/affiliation/receivedat) via `DynamicsService.executeChangeset` (one changeset, parent `If-Match`-guarded); deletes `review_drafts` (PG) post-commit; writes `external_rate_limit` (PG) | High | Final submit (reviewer review-form build, Phase 3). Submit is irreversible: form locks read-only, no re-submit. Server-sanitizes every rich-text answer (`sanitize-review-html`) before write — stored-XSS boundary. Finality precheck 409 `review_received_locked` if `wmkf_reviewreceivedat` already set (authoritative; the in-changeset `If-Match` 412 → 409 `conflict` is belt-and-suspenders). Engagement gate (`computeEngagementState`) 409 `materials_not_sent` outside the authoring stage. Atomic changeset = no partial/duplicate child rows; alternate-key upsert = retry-idempotent. Rate-limited per-token + per-IP (`external_rate_limit`, security audit A6); 429 + `Retry-After` on excess. |
| `/api/grant-reporting/extract` | POST | App | `requireAppAccess('grant-reporting')` | Request payload / Dynamics | Writes `api_usage_log` (PG); writes `wmkf_ai_run` (DV) via logAiRun | Medium | AI and Dynamics data-boundary review. |
| `/api/grant-reporting/lookup-grant` | POST | App | `requireAppAccess('grant-reporting', 'batch-phase-i-summaries')` | Staff app-wide Dynamics/SharePoint lookup by request number | Reads Dynamics + SharePoint (read-only) | Medium | Boundary documented inline. |
| `/api/health` | GET | Authenticated | `requireAuth` | Service status | Read-only | Low | Avoid exposing to unauthenticated users. |
| `/api/intake/draft` | POST | Applicant session | `getServerSession` + `userType === 'applicant'` + any-role membership guard via Dataverse `wmkf_portalmemberships` (live = approved + active; either Submitter `100000000` or Contributor `100000001`) | One own draft per `(contactOid, accountId, formKey)` triple | `IntakeDraftService.upsertDraftJson` writes only `draft_json` (never overwrites `attachments[]` — those are managed by `/api/intake/draft/attach`); preserves `draft_json.idempotency_key` across autosaves; best-effort `intake_audit` write (`draft.upsert`) | Low | Applicant intake portal — debounced autosave from the form UI. Strips caller-supplied `idempotency_key` (server owns that field's lifecycle). Mirrors `/submit`'s bridge + identity_conflict handling (409 + audit row on `bridge.conflict`). |
| `/api/intake/draft/attach` | POST | Applicant session | `getServerSession` + `userType === 'applicant'` + draft owner OR any-role membership against draft's `account_id` (live = approved + active; Submitter `100000000` or Contributor `100000001`) | One own draft (by id); rejects `request_id IS NOT NULL`; A2 dual-lookup (`attachments[]` first → 200 `already_attached` short-circuit, then `pending_attachments[]`) | `IntakeDraftService.promoteToClean` (atomic SQL move pending→attachments, gated on `attachmentId NOT in attachments[]` per chunk-3 design); `IntakeDraftService.removePending` on rejection paths; `@vercel/blob` `get(pathname, {access:'private', token: getIntakeBlobToken(), useCache:false})` then `del(pathname, {token})` on infected/oversized/cardinality-race; SHA-256 recomputed from bytes; magic-byte validated via `validateIntakeAttachment` against `field.accept[]`; size cross-checked against `pending.maxBytes`; `cloudmersive-scan` `scanBytes` gated by `isVirusScanEnabled()`; best-effort `intake_audit` writes (9 actions: `draft.attach` happy, `draft.attach_infected`, `draft.attach_infected_del_failed`, `draft.attach_scan_misconfigured`, `draft.attach_scan_unavailable`, `draft.attach_blob_misconfigured`, `draft.attach_magic_mismatch`, `draft.attach_size_exceeded`, `draft.attach.ownership_denied` — A3 metadata/payload field split) | High | S184 chunk 5. Third leg of the three-call attach dance. Magic mismatch returns 422 + Blob NOT deleted (operator-decision posture) + pending removed; size-exceeded / infected / cardinality-race return Blob deleted + pending removed. Scanner posture (A7): flag off → `scanner:'skipped'` clean path, flag on + key present → run scan, flag on + key missing → fail-loud 500 `scan_misconfigured` (pending intact). Blob errors wrapped via `buildNoResponseError('blob', err)` before classification (mirror of drain prior art). `INTAKE_BLOB_RW_TOKEN` enforced (NOT shared `BLOB_READ_WRITE_TOKEN` — A4). Forbidden body fields (400): `pathname`, `filename`, `contentType`, `fieldKey`, `accountId`, `formKey`, `requestId`, `contactOid`, `draftJson`. Allowed only `draftId` + `attachmentId`. |
| `/api/intake/draft/upload-token` | POST | Applicant session | `getServerSession` + `userType === 'applicant'` + draft owner (direct `contact_oid` match) OR any-role membership via Dataverse `wmkf_portalmemberships` against the draft's `account_id` (live = approved + active; Submitter `100000000` or Contributor `100000001`) | One own draft (by id); rejects `request_id IS NOT NULL` (submitted drafts frozen) | `IntakeDraftService.appendPending` writes only `pending_attachments[]` (server-managed; never overwritten by autosave); `@vercel/blob` `generateClientTokenFromReadWriteToken` mints a one-shot client-upload token scoped to opaque pathname `drafts/{draftId}/{attachmentId}` + the specific contentType + field maxBytes + 1h validUntil; best-effort `intake_audit` writes (`draft.upload_token.mint` happy path / `draft.upload_token.mint_failed` on Blob failure / `draft.upload_token.ownership_denied` on 403 — A3 metadata/payload field split) | Medium | S184 chunk 4. First leg of the three-call attach dance (browser-direct PUT in step 2; `/attach` finalizes in step 3). Per-field cardinality gate (single-valued: reject if already populated; multi-valued: enforce `maxFiles` across pending+clean). Filename sanitized via `sanitizeBlobFilename` (NFKC, `..` rejection, codepoint-aware truncation). Forbidden body fields (server-derived; reject 400): `attachmentId`, `pathname`, `accountId`, `formKey`, `requestId`, `contactOid`, `draftJson`. Blob token uses `INTAKE_BLOB_RW_TOKEN` (NOT shared `BLOB_READ_WRITE_TOKEN` — A4). `addRandomSuffix:false`, `allowOverwrite:false`. |
| `/api/intake/submit` | POST | Applicant session | `getServerSession` + `userType === 'applicant'` + Submitter-role guard via Dataverse `wmkf_portalmemberships` (live = approved + active + `wmkf_role=100000000`) | One own draft → one own `submission_jobs` row | Single PG txn: INSERT `submission_jobs` (idempotency-keyed) + UPDATE `intake_drafts.request_id = generated GUID`; best-effort `intake_audit` write (`submit` happy path / `submit.blocked_terminal` on 409) | Medium | Applicant intake portal — submission entry point. Pre-generates the `akoya_request` GUID written to PG immediately, materialized in Dataverse later by the drain. Idempotent via `idempotency_key UNIQUE` (`DO UPDATE … RETURNING`). 409 + `previous_submission_terminal` on collision against a terminal-status prior job. Attachment-shape validator (`lib/utils/intake-attachment-shape.js`) + all-clean scan invariant at submit-entry. |
| `/api/integrity-screener/dismiss` | POST, GET disallowed | App | `requireAppAccess('integrity-screener')` | Service layer | Writes `screening_dismissals`; updates `integrity_screenings` (PG) | Low | Method parsing looks safe; confirm service-level scope. |
| `/api/integrity-screener/history` | GET, PATCH | App | `requireAppAccess('integrity-screener')` | `access.profileId` passed to service | GET reads `integrity_screenings`; PATCH updates `integrity_screenings.status/notes/reviewed_at` (PG) | Low | Good scoping pattern if service enforces it. |
| `/api/integrity-screener/screen` | POST | App | `requireAppAccess('integrity-screener')` | `access.profileId` passed to service | Writes `integrity_screenings`, `api_usage_log` (PG) | Low | AI/search payload review. |
| `/api/irs/verify-ein` | GET | Shared secret | `x-irs-verify-secret` header == `IRS_VERIFY_SECRET` env var | EIN-keyed lookup | Reads `irs_exempt_orgs` (PG); read-only, stateless | Low | PowerAutomate-callable. Allowlisted in `proxy.js` (no NextAuth session). Returns IRS subsection/status/deductibility + derived `is501c3PublicCharity` flag. Never writes Dynamics — PA writes verified result back to `account` row itself. |
| `/api/phase-i-dynamics/summarize` | POST | App | `requireAppAccess('batch-phase-i-summaries')` | App-wide request ID writeback | Updates `akoya_request.wmkf_ai_summary`, writes `wmkf_ai_run` (DV); writes `api_usage_log` (PG) | Medium | Writes AI summary by request GUID; policy checkpoint for who may target records. |
| `/api/phase-i-dynamics/summarize-v2` | POST | App | `requireAppAccess('batch-phase-i-summaries')` | App-wide request ID writeback | Updates target field on subject entity, writes `wmkf_ai_run` (DV) via execute-prompt; `api_usage_log` (PG) | Medium | Shared executor path; policy checkpoint for who may target records. |
| `/api/process` | POST | App | `requireAppAccess('batch-proposal-summaries', 'phase-ii-writeup')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | AI payload review. |
| `/api/process-expenses` | POST | App | `requireAppAccess('expense-reporter')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | File/data minimization review. |
| `/api/process-peer-reviews` | POST | App | `requireAppAccess('peer-review-summarizer')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | AI payload review. |
| `/api/process-phase-i` | POST | App | `requireAppAccess('batch-phase-i-summaries')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | AI payload review. |
| `/api/process-phase-i-writeup` | POST | App | `requireAppAccess('phase-i-writeup')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | AI payload review. |
| `/api/qa` | POST | App | `requireAppAccess('phase-ii-writeup', 'batch-proposal-summaries')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | AI payload review. |
| `/api/refine` | POST | App | `requireAppAccess('phase-ii-writeup', 'batch-proposal-summaries')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | AI payload review. |
| `/api/review-manager/campaign-config` | GET, POST | App | `requireAppAccess('review-manager', 'reviewers')` | `requestId` GUID-validated before it becomes an `akoya_requests` selector | Reads/writes reviewer-engagement campaign-config columns on `akoya_request` (DV: `wmkf_respondoffsetdays`, `wmkf_reviewduedate`, reminder enabled/lead pairs, `wmkf_desiredcount`); `wmkf_quotanotifiedat` is read-only | Low | Per-request reviewer campaign config (Phase 1, S275). Editable-later surface for the config first written by send-emails on first invite. |
| `/api/review-manager/download-review` | GET | App | `requireAppAccess('review-manager', 'reviewers')` | Staff-shared review-manager access | Reads Dataverse + SharePoint | Low | Boundary documented inline; tighten to PD-only only if policy changes. |
| `/api/review-manager/mark-received-no-file` | POST | App | `requireAppAccess('review-manager', 'reviewers')` | Staff-shared review-manager access | Updates `wmkf_appreviewersuggestion` (DV) | Medium | Token/review lifecycle action. |
| `/api/review-manager/regenerate-token` | POST | App | `requireAppAccess('review-manager', 'reviewers')` | Staff-shared review-manager access | Mints token; updates `wmkf_appreviewersuggestion` (DV) via mintAndStore | Medium | Token lifecycle action. |
| `/api/review-manager/render-emails` | POST | App | `requireAppAccess('review-manager', 'reviewers')` | Staff-shared review-manager access | Reads Dataverse; mints tokens (writes `wmkf_appreviewersuggestion` token hash); reads cycle config from Dataverse `wmkf_appgrantcycle` via `lib/services/grant-cycles-dataverse` (W3 cutover 2026-05-12) | Medium | Token generation and email content review. |
| `/api/review-manager/reviewers` | GET, PATCH | App | `requireAppAccess('review-manager', 'reviewers')` | Staff-shared review-manager access with PD listing convenience | Reads Dataverse; GET attaches the narrative answer snapshot (`wmkf_appreviewanswer`, keyed child read by `_wmkf_appreviewersuggestion_value`, re-sanitized on read) per submitted reviewer (Phase 4); updates `wmkf_appreviewersuggestion` lifecycle (DV) on PATCH | Medium | Boundary documented inline. Reviewer-authored HTML is re-sanitized server-side on read before staff render (defense in depth). |
| `/api/review-manager/revoke-token` | POST | App | `requireAppAccess('review-manager', 'reviewers')` | Staff-shared review-manager access | Updates `wmkf_appreviewersuggestion` (DV) via revoke | Medium | Token lifecycle action. |
| `/api/review-manager/send-emails` | POST | App | `requireAppAccess('review-manager', 'reviewers')` | Staff-shared review-manager access | Sends email + creates email activity (DV `emails`); updates `wmkf_appreviewersuggestion`, `contact`, `wmkf_potentialreviewer` (DV); reads cycle config from Dataverse `wmkf_appgrantcycle` via `lib/services/grant-cycles-dataverse` (W3 cutover 2026-05-12) | Medium | Boundary documented inline; sends email and mints/uses tokens. |
| `/api/review-manager/upload-review` | POST | App | `requireAppAccess('review-manager', 'reviewers')` | Staff-shared review-manager access | Writes SharePoint `Reviewer_Uploads/`; updates `wmkf_appreviewersuggestion` (DV) via writeReviewFiles | Medium | Staff upload path. |
| `/api/review-manager/withdraw-sufficient` | POST | App | `requireAppAccess('review-manager', 'reviewers')` | `requestId` + every `suggestionId` GUID-validated; each row re-guarded still-pending from a fresh read | Updates `wmkf_appreviewersuggestion` (DV) → `withdrawn_sufficient` + `wmkf_withdrawnsufficientat`, clears `wmkf_respondremindersentat`; sends "no longer needed" Dynamics email as the lead PD | Medium | PD selective-decline (reviewer-engagement Phase 4, S275). Writes `withdrawn_sufficient` ONLY on still-pending rows (invited && !accepted && !declined) — never an accepted/honorarium row. State write precedes the courtesy email. |
| `/api/reviewer-finder/analyze` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | `access.profileId` passed | Writes Vercel Blob (summary upload); writes `api_usage_log` (PG) | Low | Existing auth tests cover representative route. |
| `/api/reviewer-finder/discover` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | `access.profileId` passed | AI pass-through + external research APIs; `api_usage_log` (PG) only | Low | External discovery APIs / AI review. |
| `/api/reviewer-finder/enrich-contacts` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Request payload | AI pass-through + external SERP/contact APIs; `api_usage_log` (PG) | Low | Rate-limited; server-side credentials only. |
| `/api/reviewer-finder/prompt-override` | GET, PUT, DELETE | App | `requireAppAccess('reviewer-finder', 'reviewers')` | `access.profileId` (own prefs only) | Reads/writes the per-user `PROMPT_OVERRIDES` key in `wmkf_appuserpreferences` (DV) via dispatcher; reads Dataverse `wmkf_ai_prompt` for the base template | Low | Sole write path for prompt overrides (the generic `/api/user-preferences` blocks that key). Validates the body via prompt-validators (S222). |
| `/api/reviewer-finder/contact-history` | GET | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Org-visible Dataverse data; reads junction + projectleader for `?contactId=<guid>` | Reads Dataverse `wmkf_apprequestperson` + `akoya_request` (read-only) | Low | UNION read of `wmkf_apprequestperson` + `akoya_request._wmkf_projectleader_value`. No write side. |
| `/api/reviewer-finder/generate-emails` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | `user_profile_id` scoped | Writes/updates `wmkf_appreviewersuggestion` (DV) via `reviewer-suggestion` adapter (W5 cutover 2026-05-12); `api_usage_log` (PG) | Low | Existing cross-user tests cover representative behavior. |
| `/api/reviewer-finder/cycle-material` | GET | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Record-scoped: the requested `pathname` must be the named cycle's review template or one of its attachments | Reads Dataverse `wmkf_appgrantcycle` (`findById`) + reads the **private** Vercel Blob store (`wmkf-uploads-private`, `UPLOADS_BLOB_RW_TOKEN`) server-side via `readUploadedBlobBuffer` | Low | Phase 1 record-scoped private download proxy (the record-aware pattern `blob-proxy.js` points to). Pathname must belong to the cycle (404 otherwise — no bearer-pathname read); forces `attachment` + `nosniff` + `no-store`; fail-closed 503 if the store token is unset. Legacy public materials still go through `blob-proxy.js`. |
| `/api/reviewer-finder/grant-cycles` | GET, POST, PATCH, DELETE | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Shared grant cycles | Reads/writes Dataverse `wmkf_appgrantcycle` via `lib/services/grant-cycles-dataverse` (W3 cutover 2026-05-12); admin UI for cycle management | Medium | Shared mutable records; confirm app-level access is enough. |
| `/api/reviewer-finder/load-proposal` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | App-wide proposal file load by request ID | Writes Vercel Blob; reads Dynamics + SharePoint | Medium | Policy checkpoint for who may load proposal source files. |
| `/api/reviewer-finder/my-candidates` | GET, PATCH, DELETE | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Org-visible Dataverse data; PD filter is UX default | Reads Dataverse; on PATCH updates `wmkf_appreviewersuggestion` + `wmkf_potentialreviewer` (DV; identity + bibliometric fields both on the person post-S213 collapse) | Medium | Boundary documented inline. |
| `/api/reviewer-finder/merge-candidates` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Org-visible Dataverse data; keeper/loser ids GUID-validated before becoming selectors | POST plan = read-only projected diff (`keeper`/`loser` display data, picker `fields`, sanitized reasons, `repointCount`, `collisionCount`; no suggestion/request/ETag internals). POST confirm = merges a PRE-ENGAGEMENT duplicate `wmkf_potentialreviewer` into the keeper (repoints/deletes `wmkf_appreviewersuggestion` rows, moves email, deactivates loser). Fail-closed block predicate refuses an engaged/promoted loser (409 `merge_blocked`). Step 3/4 Dataverse concurrency/duplicate-key failures return retryable 409 `merge_retryable_replan`; Step 6 email-transfer failures stay 500 `merge_email_move_failed` for orphan-email recovery. | Medium | Auth = same as my-candidates by design (S289): the block predicate, not a permission gate, restricts merge to the low-risk case. `docs/REVIEWER_MERGE_DESIGN.md`. |
| `/api/reviewer-finder/my-proposals` | GET | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Caller Azure email -> PD | Reads Dataverse (akoya_request + wmkf_appreviewersuggestion) | Low | Good identity-derived scope pattern. Postgres reader retired in W5 step 4 (2026-05-12). |
| `/api/reviewer-finder/save-candidates` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Trusted internal Dataverse writeback by request ID | Upserts `wmkf_potentialreviewer` + `wmkf_appreviewersuggestion` (DV; bibliometric fields land on the person post-S213 collapse, no separate sidecar) | Medium | Dataverse-only writeback (Postgres writes retired); policy checkpoint for who may write candidate suggestions to a request. |
| `/api/test-email` | POST | Superuser | `requireSuperuser` | Caller email | Sends email + creates email activity (DV `emails`) | Low | Superuser-only (tightened from `requireAppAccess('dynamics-explorer')`, eval #11). Privilege-verification test endpoint; consider disabling in prod if not needed. |
| `/api/upload-handler` | POST | Authenticated | `requireAuth` | Blob upload token | Mints Vercel Blob upload token (client uploads directly to Blob) | Medium | Any authenticated user can request a blob upload token; client-token flow shared by `FileUploaderSimple` + `SettingsModal` + `review-manager`. Blobs are `access: 'public'` — private-blob migration is the open residual of security-audit P2 (see `docs/archive/SECURITY_AUDIT_2026-05-21.md`). Legacy `/api/upload-file` retired 2026-05-21 (security audit A5) — `SettingsModal` migrated to this route. |
| `/api/user-preferences` | GET, POST, PUT, DELETE | Profile | `requireAuthWithProfile` | `profileId` | Reads/writes `wmkf_appuserpreferences` (DV) via dispatcher | Low | Intended user-owned settings. |
| `/api/user-profiles` | GET, PATCH, DELETE | Profile / Superuser | `requireAuthWithProfile`; `?all=true` uses `getUserRole` | Own profile; all profiles for superuser | Reads/updates/archives `user_profiles` (PG) | Low | `POST` intentionally unsupported; profile creation flows through `/api/auth/link-profile`. |
| `/api/virtual-review-panel` | GET, POST | App | `requireAppAccess('virtual-review-panel')` | Request payload | Writes `panel_reviews`, `panel_review_items`, `api_usage_log` (PG); calls multi-LLM providers | Low | AI payload review. |
| `/api/webhooks/bill` | POST | Shared secret (HMAC) | `x-bill-sha-signature` HMAC-SHA256 verified against `BILL_WEBHOOK_SECRET` env var (constant-time comparison) | BILL.com subscription scope | Writes `bill_webhook_events` (PG) for dedup; logs raw payload (sandbox-discovery slice); **no Dataverse writes yet** — `vendor.updated` → PATCH lands in follow-up slice once payload shape is sandbox-confirmed | Medium | BILL.com webhook scaffold (slice 1 of reviewer-honorarium onboarding). Allowlisted in `proxy.js` (no NextAuth session). 256 KB body cap; missing-correlator deliveries return 200 (to keep BILL from auto-disabling subscription). See `docs/BILL_LIB_DESIGN.md` v3. |
| `/api/bill/onboard-reviewer` | POST | Shared secret (HMAC) | `x-bill-internal-sig` HMAC-SHA256 over `v1:${timestamp}:${nonce}:${rawBody}` verified against `BILL_INTEGRATION_SECRET`; ±300s timestamp skew window; constant-time compare (see `lib/bill/internal-call-auth.js`) | Internal same-deployment call (respond.js → here) | Reads `contact` (Dataverse); writes `contact.wmkf_billcomid` + `akoya_isvendor` and `akoya_request.{wmkf_paymentnetworkidpni, wmkf_exisitngbillcomaccount}`; calls BILL.com vendor + network APIs via `lib/bill/` | Medium | BILL chunk 6. Always 200 on auth+validation success — outcome lives in `body.status` (onboarded / reused_existing / no_match / ambiguous_match / alert_only / deferred / bill_unavailable / partial / in_progress / resume_reconciled / invalid_input). Allowlisted in `proxy.js` (no NextAuth session). Gated by `BILL_ENABLED=true`; default false ships alert-only fallback. **No-BILL cycle (2026-07-01): `BILL_ONBOARDING_DEFERRED=true` short-circuits earlier to `deferred` (no BILL call, NO alert) — higher precedence than alert-only; the portal may still create the honorarium `akoya_request`, while payment is handled manually/offline.** **This endpoint is the boundary for EXTERNAL callers; the chunk-4 portal path (respond.js) deliberately calls `onboardReviewer()` IN-PROCESS instead of self-POSTing here (conscious design choice — see `docs/BILL_CHUNK_4_DESIGN.md` "In-process onboarding call"). Both entry paths share one validator, `validateOnboardInput()` in `onboard-reviewer-service.js`, so there is no validation drift between them.** Design: `docs/BILL_CHUNK_6_ENDPOINT_DESIGN.md`. |
| `/api/workbench/dashboard` | GET | App | `requireAppAccess('reviewers')` | Caller Azure email → lead PD; `scope=my` (default) filters to lead PD, `scope=all` drops it | Reads `akoya_request` + `wmkf_appreviewersuggestion` (DV, read-only); single cycle query gated by **triage** (S261): visibility = `akoya_requeststatus eq 'Phase II Pending' OR wmkf_triagestatus eq Advancing`, with `Set aside` hidden via the null-inclusive guard unless `?includeSetAside=1`; an untriaged row shows only via the Phase II Pending branch — untriaged non-Phase-II rows (incl. all Concept-stage) never shown. Triage clause parenthesized so it can't leak past cycle/scope ANDs | Low | Request Workbench tier-2 dashboard feed. Replaced the throwaway `d26Allowlist.js` union (S261); cycle picker derives from the PD's meeting-dated proposals (§5 allowlist retirement done, S261). Returns a server-computed `canManage` per row for the triage-flip control. `my-proposals.js` untouched/status-gated. |
| `/api/workbench/resolve-request` | GET | App | `requireAppAccess('reviewers')` | Org-open read by request number | Reads `akoya_request` (DV, read-only); `akoya_requestnum` (string) → GUID resolve; also returns Proposal-tab `proposalInfo` (PI/co-PIs/abstract/`akoya_request`/`akoya_expenses`) + `aiContent` (existing `wmkf_ai_*` fields) + Status-tab `statusClass` (derived) | Low | Number→GUID lookup + Proposal-tab top/AI data (Phase 1); `statusClass` for the Status tab (S260). |
| `/api/workbench/reviewer-rollup` | GET | App | `requireAppAccess('reviewers')` | `requestId` GUID-validated before it becomes a `_wmkf_request_value` OData selector | Reads `wmkf_appreviewersuggestion` (DV, read-only) via the shared lightweight rollup (counts only, no person/researcher fan-out); returns `{counts, needed, workRemaining, hint}` | Low | Per-request reviewer-stage rollup for the Overview tab (Group A, S260). Shared logic in `lib/services/reviewer-rollup.js`; cheaper than `/api/review-manager/reviewers`. |
| `/api/workbench/proposal-documents` | GET | App | `requireAppAccess('reviewers')` | `requestId` GUID-validated; scope derived only from the resolved record (`rec.akoya_requestid`), server resolves request number + cycle (not trusted from client) | Reads `akoya_request` (DV) + SharePoint via Graph (recursive list, read-only); no writes; per-bucket errors sanitized (logged server-side) | Low | Proposal-tab Phase I document listing (slot-matched + other). Interim filename-match bridge — `shared/config/workbenchProposalDocuments.js`. |
| `/api/workbench/download-proposal-document` | GET | App | `requireAppAccess('reviewers')` | Two-stage: (1) fast fail — folder top-level segment's GUID suffix MUST match `requestId`; (2) authoritative — requested `(library, folder, filename)` MUST be in the request's Phase I document set (re-derived via `listProposalDocuments`, not the raw query). Library allowlisted by `GraphService.getDriveId`; traversal rejected by `validatePath`. `disposition=inline` honored ONLY for a non-executable safe-mime allowlist (pdf/png/jpeg/gif/text) — never HTML/SVG; else `attachment`; `nosniff` always set | Reads SharePoint (Graph download, no write) | Medium | Proposal-tab scoped download proxy; mirrors `dynamics-explorer/download-document` but adds Phase-I membership scoping + safe inline view. |
| `/api/workbench/applicant-reviewers` | GET | App | `requireAppAccess('reviewers')` | Org-open, per-request scoped by `?requestId=` | Reads `akoya_request` (5 `wmkf_potentialreviewer1..5` slots + free-text `wmkf_excludedreviewers`); idempotently writes `wmkf_appreviewersuggestion` `disposition=recommended`, `wmkf_selected=false` rows (race-safe via `ensureApplicantRecommended`); one hardened Claude call (`reviewer-exclusion-parser.js`, A7-wrapped untrusted free-text) to parse excluded names. **No structured `disposition=excluded` rows written** (S210 option B) — excluded names returned for the search soft-block only; nothing global touched. | Medium | Request Workbench applicant-reviewer ingestion. Lazy on Find-tab open. Recommended rows appear in Find for PD review but do not enter the candidate pool until explicit promotion; excluded → soft-block names. |
| `/api/workbench/promote-applicant-reviewer` | POST | App | `requireAppAccess('reviewers')` | Org-open, per-request scoped by body `requestId`; both `requestId` and `suggestionId` GUID-validated before Dataverse selectors; verifies the suggestion row belongs to the request and has `wmkf_applicantdisposition=Recommended` | Reads `wmkf_appreviewersuggestion` via `findById`; updates the existing suggestion via `updateLifecycle(suggestionId, { selected: true })` with caller impersonation. No person upsert and no `save-candidates` path. Whole body in `bypassDynamicsRestrictions('workbench-promote-applicant-reviewer')`; errors sanitized. | Medium | S264 explicit promotion endpoint for applicant-suggested reviewers. Candidate pool membership is the PD-selected set; applicant-recommended rows are not auto-promoted by ingestion. |
| `/api/workbench/export-candidates` | POST | App | `requireAppAccess('reviewers')` | Org-open, per-request scoped by body `requestId`; `requestId` GUID-validated before it becomes an `akoya_request` record-id selector; `candidates` array capped (max 500); per-cell strings clamped | Reads one `akoya_request` (DV, read-only) for header metadata (number/institution/PI), fetched server-side — never trusted from the client; builds a two-sheet ExcelJS workbook (Request Info + Candidates) from the client-supplied slim candidate DTOs and streams it back as an `.xlsx` attachment. No writes. Whole body in `bypassDynamicsRestrictions('workbench-export-candidates')`; errors sanitized. | Low | Find-tab "Export to Excel" for the selected candidates. Derived columns (Source/Why/Conflicts/ORCID/Scholar) formatted in `lib/services/reviewer-candidate-export.js`. |
| `/api/workbench/manual-reviewer` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Org-open, per-request scoped by body `requestId`; GUID-validates before Dataverse use; 64kb body cap | Reads `akoya_request`; creates or fill-only reuses `wmkf_potentialreviewer` by email (name-only creates a new person); stamps staff-entered email provenance as `wmkf_emailsource=manual`; idempotently writes/reselects a non-excluded `wmkf_appreviewersuggestion` row via `ensureStaffManualCandidate`, unioning `staff_manual` into `wmkf_sources`. A staff-provided/looked-up ORCID is persisted **fill-only** (`wmkf_orcid`/`wmkf_orcidurl` via `upsertByPotentialReviewer` — never overwrites a resolver-sourced or reviewer-attested value, never touches `wmkf_identitystatus`). No candidate enrichment. | Medium | Workbench manual reviewer add Phase 1. Excluded rows fail closed with 409; manual emails flow into Slice-G LOW-confidence invite confirmation. |
| `/api/workbench/reviewer-lookup` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Org-open; not request-scoped; validates name/email/ORCID; 16kb body cap | Read-only Dataverse lookup across `wmkf_potentialreviewer` and `contact`: top-2 ambiguity-aware ORCID/email checks plus structured name search. No writes and no third-party calls. Returns `confident`, `candidates`, `conflict`, or `none`; ambiguous same-key rows return candidates, while cross-store splits/reverse-link collisions/email mismatches return conflict. | Low | Manual reviewer add dedup preflight and confirmation UI. |
| `/api/workbench/orcid-lookup` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Org-open; not request-scoped (name/affiliation lookup only); 16kb body cap | **No Dataverse read/write.** Calls ORCID public search via `ORCIDService.findContact` (name-match-gated, affiliation-narrowed; abstains on ambiguous/none rather than guess a namesake). Read-only convenience for the manual-add form; any chosen ORCID is persisted only on submit (`manual-reviewer.js`, fill-only). | Low | ORCID iD lookup for manual reviewer add. Degrades to `{found:false}` when `ORCID_CLIENT_ID/SECRET` absent. |
| `/api/workbench/enrich-recommended` | POST (SSE) | App | `requireAppAccess('reviewers')` | Org-open, per-request scoped by body `requestId`; whole body in `bypassDynamicsRestrictions('workbench-enrich-recommended')` | Reads the request's `disposition=recommended` `wmkf_appreviewersuggestion` rows + each person's `wmkf_potentialreviewer`; resolves `proposalInfo` from the client `analysisResult` or one `analyzeProposal` Claude call on `blobUrl`; PubMed author-verify + COI (institution + coauthorship) + contact/Scholar-bibliometrics enrich (`persist:false`); writes back per-person bibliometrics onto `wmkf_potentialreviewer` (race-safe `upsertByPotentialReviewer`, id-keyed; S213: the `researcher` adapter targets the person `wmkf_potentialreviewerses`, the sidecar is dropped) + deterministic COI tags to the junction `wmkf_matchreason` (atomic `setMatchReason`, ETag/If-Match, fail-closed on excluded). | Medium | Runs applicant-recommended reviewers through the full enrich pipeline on an explicit PD click (S211). See `docs/REQUEST_WORKBENCH_BUILD_PLAN.md` §Phase 3. |
| `/api/workbench/reviewer-roster` | GET / POST / PATCH | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Org-open, per-request scoped by GUID-validated `requestId` (query on GET, body on POST/PATCH); 2mb body cap; POST caps candidates ≤100 | Pure **Postgres** `reviewer_find_roster` (no Dataverse). GET → `listForRequest`; POST → `recordSurfaced` (bulk upsert status `active`, never-downgrade guard, per-request row cap); PATCH `exclude`/`promote`/`saved` → `setExcluded`/`promote`/`markSaved`. Stores a pruned render DTO (`pruneCandidateForRoster`, server-side too). | Low | Durable Find-tab candidate roster + cross-run dedup (S224). Operational roster state, NOT canonical reviewer identity (Dataverse). See `docs/atlas/postgres-reviewer-find-roster.md`. |
| `/api/workbench/grantee-deliverables/abstract` | GET / PUT | App | `requireAppAccess('reviewers')` | `requestId` GUID-validated off `req.query` (GET) / `req.body` (PUT) before record-id use; PUT body `text` (1..20000 chars, non-empty), `etag` (the client's loaded row etag — required), `baseField` (`approved`/`formatted`); 32kb body cap | GET reads one `akoya_request` (`wmkf_abstractformatted`/`wmkf_abstractapproved` + `_etag`) and the related `wmkf_granteedeliverable` status (read-only), returning the **effective** abstract (`approved ?? formatted`), its field, status, and an `editable` flag. PUT writes the PD's edited text to the **effective field decided from a fresh server read** (never the client's `baseField`): the grantee-approved field once it is non-empty, else the draft — so staff text never lands in the grantee-authored field before a submission. ETag-conditional `updateRecord` using the **client's loaded etag** as `If-Match` (a grantee submit / concurrent staff save / regenerate that landed during the edit window → 412 → 409 reload; a `baseField` flip also 409s). Status-gated (Safe default, S278): draft editable in null/Drafted/Invited/Reminder Sent; approved editable in Submitted/Staff Review only (Revision Requested / Complete / Closed No Response refused). **Never** changes `wmkf_deliverablestatus`; never blanks the field. Whole body in `bypassDynamicsRestrictions('grantee-abstract-load'/'grantee-abstract-save')`; `actingUserSystemId` = caller. | Medium | Grantee Deliverables Portal (S278). Awardee-tab PD review/edit/save of the publishable abstract — refine the AI draft before sending, and correct a grantee-returned abstract before it auto-assembles into the website HTML. See `docs/GRANTEE_PORTAL_BUILD_PLAN.md`. |
| `/api/workbench/grantee-deliverables/awardees` | GET | App | `requireAppAccess('reviewers')` | `cycleCode` (e.g. `J26`) validated via `cycleCodeToOdataFilter` (→ a meeting-date range filter, not a record selector); `scope` (`mine` default / `all`). No client GUID reaches a query — the mine-scope PD systemuserid is server-resolved from the session email (`resolveByEmail`), never client input | Reads `akoya_request` (DV, read-only) for the cycle, filtered to the research-awardee set: `akoya_requeststatus='Active'` + `akoya_programid` ∈ `GRANTEE_RESEARCH_PROGRAM_IDS` (editable config, GUID-keyed) + `wmkf_projectleader` present; **default scope=mine adds `_wmkf_programdirector_value eq <server-resolved session PD>`**, `scope=all` lists every PD's; per-row reads related `wmkf_granteedeliverable` for deliverable status. Whole body in `bypassDynamicsRestrictions('grantee-awardees')` | Low | Grantee Deliverables Portal awardee list (S268/S271). Fills the gap where the reviewer-finding dashboard doesn't surface awardees (it filters Phase-II-Pending/Advancing). Drives `/workbench/awardees` (defaults to the logged-in PD's awardees + "Show all programs" toggle; mine-scope with no resolvable PD → empty + `pdResolved:false`). Eligibility owner-validated against live J26 (=12). Missing deliverable row renders Not started. |
| `/api/workbench/grantee-deliverables/generate` | POST | App | `requireAppAccess('reviewers')` | `requestId` GUID-validated off `req.body` before record-id use; optional `regenerate` (strict boolean true); 8kb body cap | Reads one `akoya_request` (`wmkf_abstract`/`wmkf_abstractformatted` + `_etag`); staff write path ensures related `wmkf_granteedeliverable`; generates the house-style abstract via `generateGranteeAbstract` (Executor — writes `api_usage_log` PG + `wmkf_ai_run` DV audit); persists `wmkf_abstractformatted` to `akoya_request` via ETag-conditional `updateRecord`, and stamps package status→Drafted via `patchDeliverable` only from null/Drafted. Reuse-existing skips the paid call unless `regenerate===true`; status non-downgrade preserved. `loadModelOverrides()` warmed before model resolution. Whole body in `bypassDynamicsRestrictions('grantee-abstract-generate')`; `actingUserSystemId` = caller. | Medium | Grantee Deliverables Portal chunk 3 (S268/S271). Awardee-tab abstract generation. No lead-PD gate (draft generation, not authoritative visibility like triage). See `docs/GRANTEE_PORTAL_BUILD_PLAN.md`. |
| `/api/workbench/grantee-deliverables/recipients` | GET | App | `requireAppAccess('reviewers')` | `requestId` GUID-validated off `req.query` before record-id use | Reads `akoya_request` (`_wmkf_projectleader_value` + `_akoya_primarycontactid_value`) then the two `contact` rows (`emailaddress1` + name); read-only. Whole body in `bypassDynamicsRestrictions('grantee-recipients')`; contact GUIDs are server-sourced (from the request row), not client input | Low | Grantee Deliverables Portal chunk 3b (S268). Resolves the two invite recipients — PI (`wmkf_projectleader`) + liaison (`akoya_primarycontactid`, the foundation liaison/grant steward) — for staff confirm/override. Research-only scope. |
| `/api/workbench/grantee-deliverables/send-invite` | POST | App | `requireAppAccess('reviewers')` | `requestId` GUID-validated off `req.body`; staff-confirmed `toEmail`/`ccEmail` (format-validated), `subject`, `bodyText`; 64kb body cap | Reads `akoya_request` identity; staff write path ensures related `wmkf_granteedeliverable` and reads package status; resolves and appends the assigned PD's unified email signature server-side via `email-signature`; mints a stateless grantee magic-link SERVER-SIDE (chunk 1 `mintForRequest`) and injects the action button + fallback link into the body (never from staff input); sends via `DynamicsService.createAndSendEmail` from the staff member's mailbox (`azureEmail`) — PI in `To`, liaison in `Cc` — regarding the request; flips package status Drafted→Invited and stamps `wmkf_inviteddate` only on that first flip (non-downgrade, non-fatal). Whole body in `bypassDynamicsRestrictions('grantee-send-invite')` | Medium | Grantee Deliverables Portal chunk 3c (S268/S271). Requires the abstract generated first (status ≥ Drafted); refuses re-invite once Submitted+ (409). Re-sends while already Invited do not reset the invite clock. Phase 1 intentionally keeps From as the staff mailbox while the signature resolves from the assigned PD. |
| `/api/workbench/grantee-deliverables/preview-invite` | POST | App | `requireAppAccess('reviewers')` | `requestId` GUID-validated off `req.body`; `bodyText` (≥10 chars; 64kb body cap) | READ-ONLY RENDER — resolves and appends the assigned PD's unified email signature server-side, then returns the exact invitation email HTML (`renderGranteeInviteHtml`) for the staff-edited body with a clearly-marked PLACEHOLDER link. NEVER sends an email, mints a token, writes Dataverse, or changes status. Used by the Awardee-tab "Preview email" button. Whole body in `bypassDynamicsRestrictions('grantee-preview-invite')` | Low | Grantee Deliverables Portal (S271). Lets staff preview the invite formatting + assigned-PD signature + magic-link button before committing; the real per-request link is minted only by `send-invite`. |
| `/api/workbench/grantee-deliverables/website-html` | GET | App | `requireAppAccess('reviewers')` | `requestId` GUID-validated off `req.query` before record-id use | Reads one `akoya_request` + its related `wmkf_granteedeliverable` + applicant `account` (name/city/state) + the co-PI junction (`fetchCoPIs`) via `assembleGranteeDocument` (read-only); returns the assembled single-award HTML fragment (`renderAwardBlock`). STAFF/server-side surface → `includeImageRef` (image emitted as a `<figure>` placeholder carrying the SharePoint ref in a comment, NEVER a public `<img src>`). Whole body in `bypassDynamicsRestrictions('grantee-website-html')` | Low | Grantee Deliverables Portal chunk 8 output (b) (S270/S271). Website-ready HTML for one awarded research grant — replaces the staff member's manual HTML coding. |
| `/api/workbench/grantee-deliverables/cycle-export` | GET | App | `requireAppAccess('reviewers')` | `cycleCode` (e.g. `J26`) validated via `cycleCodeToOdataFilter` (meeting-date range filter, not a record selector — no client GUID reaches a query) | Reads the cycle's awarded-research `akoya_request` set (same filter as awardees: `Active` + research program + `wmkf_projectleader` present), assembles each via `assembleGranteeDocument` (bounded pool, fail-soft per row, including related `wmkf_granteedeliverable`), and returns a single standalone `text/html` page (`renderCyclePage`). STAFF/server-side → `includeImageRef`. Query failure → 503. Whole body in `bypassDynamicsRestrictions('grantee-cycle-export')` | Low | Grantee Deliverables Portal chunk 8 output (c) (S270/S271). Combined HTML export of all of a cycle's awarded abstracts — replaces the manual compile-to-PDF. On-demand v1. |
| `/api/workbench/triage` | POST | App + **hard manage gate** | `requireAppAccess('reviewers')` **AND** server-side manage check: superuser (`getUserRole(profileId) === 'superuser'`) OR request's lead PD (`_wmkf_programdirector_value` non-null AND equals caller `dynamicsSystemuserId`); null/absent lead PD → 403 (superuser-only) | `requestId` GUID-validated before record-id use; `triageStatus` validated via `isValidTriageValue` (numeric option set) → 400; 8kb body cap. Reads `akoya_request` (`_wmkf_programdirector_value`) then writes `wmkf_triagestatus` only (`actingUserSystemId` = caller). Whole body in `bypassDynamicsRestrictions('workbench-triage')`; errors sanitized | Medium | Sets the authoritative triage visibility field (Advancing/Set aside), replacing `d26Allowlist.js`. NOT the fail-open/UI-only `canManage` posture (S207) — this is a hard server gate. Constants: `shared/config/triageStatus.js`. See `docs/WORKBENCH_TRIAGE_FIELD_BUILD_PLAN.md`. |

## Regular Maintenance Process

### On every PR that touches `pages/api`

- Add or update the row in this matrix.
- State the intended access class in the PR description.
- Link the guard used in code.
- State the data boundary: own profile, app-wide, superuser global, token-scoped, cron-only, or intentionally public.
- Add or update tests when the route reads/writes user data, external reviewer data, tokens, files, CRM records, or admin settings.
- Treat new unlisted API routes as release-blocking once the route-inventory CI check exists.

### Weekly review

- Review Dependabot, CodeQL, Trivy, and Gitleaks results.
- Review health, secret, spend, and cron alerts.
- Review any owner questions or ambiguous matrix rows that were raised during PR review.

### Monthly review

- Re-run an API route inventory and compare it to this matrix.
- Confirm public routes are still intentionally public.
- Confirm app-level access still maps to current operational roles.
- Review high-sensitivity routes: external reviewer token routes, file downloads, profile/admin routes, and Dynamics bypass routes.

## Current Automation

`npm run check:api-routes` runs `scripts/check-api-route-security-matrix.js`. The script scans `pages/api/**/*.js` and fails when any API route is missing from this matrix. It also warns when a route does not contain one of the recognized guard tokens, unless the route is explicitly documented with `Current Guard = None`.

Recognized guard tokens:

- `requireAuth`
- `requireAuthWithProfile`
- `requireAppAccess`
- `requireSuperuser`
- `verifyCronSecret`
- `verifySuggestionToken`
- `getServerSession`
- `NextAuth`

The check runs in `.github/workflows/test.yml` before Jest. It should not try to prove authorization correctness. Its job is to prevent unreviewed routes from appearing silently.
