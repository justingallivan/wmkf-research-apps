---
title: API Route Security Matrix
domain: security-auth
kind: source-of-truth
status: canonical
summary: This document is the living authorization inventory for pages/api. It is meant to do two jobs:.
canonical: true
cataloged: 2026-07-02
last_verified: 2026-09-03
owner: product-engineering
related:
  - pages/api
  - lib/db/schema.sql
  - lib/utils/auth.js
  - docs/REVIEWER_TIMEOUT_BUDGET_PLAN.md
---

# API Route Security Matrix

Last updated: 2026-09-03

> **Wave 18 production boundary:** main includes a nullable per-engagement
> due-date override and the route behaviors noted below. [VERIFIED via production
> create, entity-scoped publish, typed metadata, and runtime `$select` on
> 2026-08-11 / 2026-08-12 UTC] the Wave 18 column is live and EXACT. The
> non-clobbering admin-body seed and runtime promotion completed in main
> `8647af33` / Vercel `dpl_AbTvWvMYb5inwPnYKTK2mkrkNXZz`; the route
> behaviors below are production-live.

> **Route→Service consolidation (Stage 7, 2026-07-05):** the business logic of
> the `pages/api` routes now lives in per-domain `lib/services/<domain>/`
> services; each route is a thin shell (guard → validate → DAL context →
> service call → HTTP mapping). Route URLs, guards, response envelopes, and
> the authorization posture recorded in the rows below are UNCHANGED by that
> campaign — only the location of the logic moved. Enforced by
> `check:route-service-boundary` (law mode: no route may import
> `lib/dataverse/adapters/*` or `lib/services/dynamics-service`); see
> `docs/ROUTE_SERVICE_CONSOLIDATION_PLAN.md`.

> **Reviewer request mutation boundary (2026-09-02):** Reviewer reads remain
> organization-wide after app access. Request-bound reviewer mutations now use
> one shared server gate: every target suggestion is resolved to its request,
> the complete batch is checked before any write/email, and only the request's
> lead Program Director or a superuser proceeds. Missing actor/owner data fails
> closed for non-superusers. Global reviewer-person merges and staff document
> reads remain organization-wide.

## Purpose

This document is the living authorization inventory for `pages/api`. It is meant to do two jobs:

1. Capture the current access-control posture route by route.
2. Provide a recurring control so new routes, changed routes, and data-scope assumptions are reviewed consistently.

The matrix focuses on authorization, data ownership, and persistence (what each route writes). It does not replace dependency scanning, secret scanning, CSP/header review, or AI data-minimization review.

**Column legend:** `(PG)` = Vercel Postgres, `(DV)` = Dataverse / Dynamics 365. SharePoint and Vercel Blob are named explicitly. The Persistence cell summarizes writes (and reads where notable); the canonical schema for Postgres tables is `lib/db/schema.sql`, for Dataverse entities `docs/atlas/dataverse-*.md`. "AI pass-through" means the route only invokes an LLM and returns the result without persisting business data; `api_usage_log` writes from the LLM client are still listed because they're the audit trail for spend monitoring.

## Access Classes

| Class | Meaning | Expected Guard |
|---|---|---|
| Public metadata | Safe to expose without a session. Should not reveal sensitive business data. | None, explicit rationale required |
| NextAuth | Framework-owned authentication endpoints. | NextAuth handler or `getServerSession` |
| Authenticated | Any signed-in user may call it. Use only for low-sensitivity shared utilities. | `requireAuth` |
| Profile | Signed-in user must have a linked active profile. User-owned data must be scoped to that profile. | `requireAuthWithProfile` |
| App | Signed-in user must have access to the named app. User-owned data must still be scoped where applicable. | `requireAppAccess(req, res, appKey)` |
| Superuser | Linked profile plus `dynamics_user_roles.role = 'superuser'`. | `requireSuperuser` |
| Cron | Vercel scheduled/background endpoint. | `verifyCronSecret` |
| External token | Public magic-link route scoped by the token payload and server-side token verification. | `verifySuggestionToken` |
| Applicant session | NextAuth session minted by the `entra-external` provider (separate Entra External ID tenant). Carries `contactOid` + `contactEmail`. Routes scope writes to data the contact owns, and may additionally gate on `wmkf_portalmembership` role/state. | `getServerSession` + `session.user.userType === 'applicant'` (+ optional role/membership check) |

## Resolved Baseline Findings

These findings were identified in the first matrix pass and addressed in the follow-up hardening commits:

| Finding | Disposition | Current Control |
|---|---|---|
| P1: `/api/user-profiles` allowed standalone profile creation | Fixed | `POST` is no longer supported. Profile creation flows through `/api/auth/link-profile` during first-login Entra linking. |
| P1: `/api/blob-proxy` was authenticated but not ownership scoped | Accepted / documented | Blob proxy is explicitly limited to shared organizational assets. Future user-owned blob flows must use record-aware endpoints. |
| P1: `/api/review-manager/download-review` used app access rather than record ownership | Accepted / documented | Review Manager is a staff-shared workflow. `review-manager` access intentionally grants shared review-file access. |
| P2: Dynamics bypass routes needed explicit data boundaries | Fixed by documentation | Bypass routes now include inline data-boundary annotations describing org-wide, role-gated, PD-derived, or staff-shared scope. |
| P2: Superuser checks were duplicated | Fixed | Shared `requireSuperuser(req, res)` and `getUserRole(profileId)` helpers now live in `lib/utils/auth.js`; admin routes were migrated. |
| P3: `/api/auth/status` public contract could drift | Fixed by documentation | Endpoint is intentionally public and pinned to `{ enabled: boolean }` only. |
| P1: `/api/auth/status` could disagree with fail-closed server enforcement | Fixed | The endpoint now delegates to `isAuthRequired()`, the same policy used by proxy and API guards; production misconfiguration reports `enabled:true`, while the explicit emergency bypass permits the base kill-switch/configuration predicate to report false. |

There are no open findings from the initial matrix pass as of this update. New findings should be added above the route matrix with priority, file/line references, disposition, and owner question. Medium-risk route notes in the matrix remain useful policy checkpoints, especially where an app intentionally has staff-wide access to Dynamics, SharePoint, reviewer, or file data.

## Route Matrix

Guarded-reopen response refinement (source-built 2026-08-22): the Pre-Site
GET/POST route emits reopen history and nested correction details only to
superusers, and it omits a pending reason-bearing reopen-attempt row entirely
for other roles while preserving ordinary pending-generation status. The
Start Site Visit transition applies the same nested-correction redaction to its
returned artifact for non-superusers. The
reopen writer preserves same-operation Failed recovery; when a distinct later
operation supersedes that attempt, any resolvable retained copy is added to its
stable-identity cleanup queue.

**Request Document actor correction (2026-08-31):** the Board-snapshot,
Start-Site-Visit, and guarded-reopen rows below describe a session-derived
Dynamics actor being passed to the write service. That remains true, but the
Request Document write census proved that these writes fall back and standard
`createdby`/`modifiedby` name the service principal. The former guarded-reopen
projection was therefore not reliable human attribution. The owner-selected
repair is Production-live: Wave 24 schema is exact,
Production readiness is literal `on`, and signed-in health passed. Naturally
generated Request `1002874` proved the first business-flow attribution on a
Ready/Draft Pre-Site row, with explicit Justin Gallivan origin actor/time,
application built-in creator, exact current pointer, no missing-attribution
event, and census 1 attributed / 0 event-backed unattributed / 0 violations:
`docs/REQUEST_DOCUMENT_EXPLICIT_ACTOR_PLAN.md` adds explicit row-origin
actor/time and a Site Visit milestone actor, with no staff-role expansion and
no new route or request-body identity field. Existing route guards and
session-derived identity boundaries remain unchanged. The Final
group-review fields and the Pre-Site distribution ledger are already explicit
and remain authoritative.

| Route | Methods | Intended Class | Current Guard | Data Scope | Persistence | Risk | Notes |
|---|---:|---|---|---|---|---|---|
| `/api/admin/alerts` | GET, PATCH | Superuser | `requireSuperuser` | Global admin | Reads/updates `system_alerts` (PG); repair-context GET re-reads one alert-correlated request, reviewer roster row, and potential reviewer from Dataverse | Low | Shared helper. `repairContext=<alert id>` accepts only an active/acknowledged reviewer-repair alert and derives all downstream identifiers from its server-owned metadata. |
| `/api/admin/health-history` | GET | Superuser | `requireSuperuser` | Global admin | Reads `health_check_history` (PG) | Low | Shared helper. |
| `/api/admin/alert-recipients` | GET, PUT | Superuser | `requireSuperuser` | Global admin | Reads/writes `alertRecipientsByCategory` setting in `wmkf_appsystemsettings` (DV); reads superuser roster from `dynamics_user_roles` (PG) for fallback display | Low | Per-category routing config for system-alert emails. |
| `/api/admin/site-visit-recipients` | GET, PUT | Superuser | `requireSuperuser`; `withDalContext` | Global admin. GET without a query returns the curated configuration and eligible staff; GET with a 2–100 character `search` query performs one bounded existing-Contact query, returns up to 50 rows, and explicitly signals when more matches exist. PUT accepts only a complete versioned reference configuration. | Reads active `user_profiles` (PG), enabled `systemusers`, existing `contacts`, and `site_visit.distribution_recipient_directory` in `wmkf_appsystemsettings` (DV). PUT writes only profile IDs, Contact GUIDs, and consultant/board categories to that setting; it never creates or edits Contacts and rejects the whole save if any reference is unavailable. | Medium | **[VERIFIED IN SOURCE 2026-08-29.]** Admin manages a capped directory of active staff and existing Dataverse Contacts. Names and email addresses remain source-owned and are resolved live. |
| `/api/admin/final-writeup-matrix-audiences` | GET, PUT | Superuser | `requireSuperuser`; `withDalContext` | Global admin. GET accepts no query parameters. PUT accepts only one complete version-2 `{config, expectedRevision}` replacement with exactly one explicit responsibility row (including allowed empty/no-lens rows) for every current reviewer-role member, at least one active broad Grant Program, and at least one reviewer per program; authenticated profile identity is server-derived. | Reads active `wmkf_grantprograms`, exact enabled members of `WMKF Final Writeup Reviewer`, and `final_writeup.matrix_audiences` in `wmkf_appsystemsettings` (DV). PUT writes one canonical GUID/role-enum configuration; the GET revision is round-tripped as a Dataverse `If-Match` precondition and stale editors receive `409`. Unknown, inactive, disabled, stale, duplicate, incomplete, empty, or over-cap references reject the whole save. Version 1 remains read-compatible and GET supplies a migration draft; malformed values require the ETag-guarded operator repair command. | Medium | **[PRODUCTION-LIVE V2 2026-09-01 UTC.]** Commit `84bf465b` is live in Ready deployment `dpl_41SybgPYfJXGarf7UqcMGCLMy4KS`. The dry-run-first operator command published the reviewed v2 draft once under ETag `W/"96930393"`; exact readback proved new ETag `W/"96944113"`, all 11 current reviewer-role assignments, one PD + Leadership overlap, zero stale/unassigned rows, and unchanged nine-person Research/six-person Southern California audiences. Signed-in Admin independently reloaded as **Published revision loaded** with Publish disabled. Runtime v2 resolution intersects stored references with current roles/programs and warns while omitting stale identities; publication remains strict. Names resolve live. |
| `/api/admin/email-defaults` | GET, PUT | Superuser | `requireSuperuser` | Global admin | Reads/writes cataloged email text defaults, including `email.grantee_invite.*` and the body-only `email.reviewer_extension.body`, in `wmkf_appsystemsettings` (DV) via dispatcher | Low | Admin-editable email/text defaults catalog. GET uses strict reads and marks per-entry `unavailable` on settings fetch failure; PUT validates the catalog key and required placeholders. Reviewer-extension subject is fixed in source, not editable. |
| `/api/admin/honorarium-amount` | GET, PUT | Superuser | `requireSuperuser` | Global admin | Reads/writes `honorarium.default_amount` in `wmkf_appsystemsettings` (DV) via dispatcher | Low | Single ground-truth reviewer-honorarium amount (S199); replaces the per-user preference. PUT validates a positive number. |
| `/api/admin/maintenance` | GET | Superuser | `requireSuperuser` | Global admin | Reads `maintenance_runs` (PG); reads `wmkf_appsystemsettings` (DV) for retention config | Low | Shared helper. |
| `/api/admin/reviewer-time-budget` | GET, PUT | Superuser | `requireSuperuser` | Global admin | Reads/writes `reviewer.time_budget_seconds` in `wmkf_appsystemsettings` (DV) | Low | Admin-editable reviewer-search wall-clock budget (S223; default 600s, clamped [120,800]). PUT clamps into range. Enforced in-app via an AbortSignal deadline because Vercel `maxDuration` is build-time-static. See docs/REVIEWER_TIMEOUT_BUDGET_PLAN.md. |
| `/api/admin/operational-events` | GET, PATCH | Superuser | `requireSuperuser` | Global admin | Reads/updates `operational_events` (PG) — GET is filtered/bounded reads; PATCH resolves/reopens one event, or a bounded batch (`events[]`, ≤500, per-row freshness snapshot requires status + both timestamps + occurrence count — "Resolve all shown" / "Resolve group"), with profile attribution; per-row database failures are reported separately from committed updates | Low | Admin surface for durable operational events (app-recorded failures + Vercel Log Drain rows, migration 030). Statuses: open/recovered/resolved/superseded/info. |
| `/api/admin/models` | GET, PUT | Superuser | `requireSuperuser` | Global admin | Reads/writes `wmkf_appsystemsettings` (DV) via dispatcher | Low | Also calls Anthropic model list. PUT accepts tier keys or reviewed concrete Claude ids only; unreviewed concrete ids are rejected before persistence. |
| `/api/admin/executor-budgets` | GET, PUT | Superuser | `requireSuperuser`; `withDalContext` | Global admin; accepts only the complete closed budget schema, an expected revision, and a UUID idempotency key. Authenticated profile identity is server-derived. | GET strictly reads append-only `executor.budgets.vNNNNNN` rows in `wmkf_appsystemsettings` (DV). PUT validates code-owned bounds and each current prompt's resolved model ceiling, creates exactly one next revision row, then rereads it before success. Existing rows are never updated. | Medium | **[PRODUCTION-DEPLOYED AND OWNER-VIEWED 2026-08-30; first publication not yet performed.]** The Admin read surface loaded the safe no-revision/code-fallback state. Stale editors, duplicate revision races, and request-id reuse with a changed payload return 409. Runtime callers do not accept client budget overrides and use bounded code fallback on settings failure. |
| `/api/admin/policies` | GET, POST | Superuser | `requireSuperuser` | Global admin | Reads/writes Dataverse `wmkf_policy` + `wmkf_policyversion`; writes `policy_publish_audit` (PG); writes `system_alerts` on audit-finalize failure | Medium | Three-step publish flow (create child, flip parent lookup, retire prior) with ETag concurrency + alt-key enforced uniqueness. See atlas page for state machine. |
| `/api/admin/prompts` | GET | Superuser | `requireSuperuser` | Global admin | Reads current (`iscurrent`) rows of Dataverse `wmkf_ai_prompt` | Low | Lists prompt rows for the /admin Prompt-templates editor (S222). |
| `/api/admin/prompts/[name]` | GET, PUT | Superuser | `requireSuperuser` | Global admin | Reads/writes Dataverse `wmkf_ai_prompt`; writes `prompt_publish_audit` (PG); writes `system_alerts` on audit-finalize / duplicate-current | Medium | Versioned publish (create new `iscurrent` row v+1, flip prior with If-Match, verify exactly-one-current). Production-live source adds prompt-specific model publishing, expected-version rejection, full-template/output-schema validation, native-structured model capability/token-limit checks, and payload-bound idempotency/recovery. Model-only changes create an immutable version. Commit `f10dff3f` is included in Production deployment commit `abfe5529`; this route still lacks a separate post-deploy runtime smoke. See `docs/atlas/dataverse-wmkf-ai-run-and-prompt.md`. |
| `/api/admin/reconcile-identities` | POST | Superuser | `requireSuperuser` | Global admin | Updates `user_profiles` (PG); reads Dynamics `systemuser` | Low | Manual equivalent of cron reconciliation. |
| `/api/admin/review-questions` | GET, POST | Superuser | `requireSuperuser` | Global admin | Reads/writes Dataverse `wmkf_reviewquestion` (active set); writes `review_question_audit` (PG); writes `system_alerts` on audit failure | Medium | Staff editor for the external-reviewer question set (staff-editable-questions epic Phase C). POST diffs the submitted full set vs current BY ROW ID and applies ONE atomic `executeChangeset` (create/update/soft-delete via statecode); enforces key-immutability (the `wmkf_questionkey` answer-join can't change) + optimistic concurrency via `questionSetVersion` (409 `set_changed`); pending→final audit with hard-abort if audit unavailable; calls `ReviewQuestionFetcher.invalidate()` on success. See `docs/atlas/dataverse-wmkf-reviewquestion.md`. |
| `/api/admin/secrets` | GET, PUT | Superuser | `requireSuperuser` | Global admin | Reads/writes `wmkf_appsystemsettings` (DV) via dispatcher | Low | Tracks metadata, not secret values. |
| `/api/admin/users` | DELETE | Superuser | `requireSuperuser` | Global admin | Updates `user_profiles.is_active` (PG); clears app-access cache | Medium | Soft-archive; refuses self-archive. Profile row preserved for audit FK integrity. |
| `/api/admin/stats` | GET | Superuser | `requireSuperuser` | Global admin | Read-only (PG SELECTs across usage tables) | Low | Usage statistics across users. |
| `/api/analyze-funding-gap` | POST | App | `requireAppAccess('funding-gap-analyzer')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | AI payload review still needed. |
| `/api/analyze-literature` | POST | App | `requireAppAccess('literature-analyzer')` | Request payload | Writes `api_usage_log` (PG); external lit-search APIs | Low | External research APIs / AI payload review. |
| `/api/api-capabilities` | GET | Authenticated | `requireAuth` | Shared metadata | Read-only | Low | Authenticated capability metadata. |
| `/api/app-access` | GET, POST, DELETE | Profile / Superuser | `requireAuthWithProfile` + `getUserRole` | Own grants; all grants for superuser | Reads/writes `wmkf_appuserappaccesses` (DV) via dispatcher | Low | Superuser branch uses shared role helper. |
| `/api/auth/[...nextauth]` | Framework | NextAuth | NextAuth callbacks | Session/profile linking | Inserts/updates `user_profiles` (PG); grants `wmkf_appuserappaccesses` (DV) | Medium | Identity linking remains a strategic hardening area. |
| `/api/auth/link-profile` | POST | NextAuth linking | `getServerSession` + `needsLinking` + locked live caller `is_active`/`needs_linking` check | Matching Azure email | Updates/deletes `user_profiles` (PG) in one transaction (no INSERT path) | Low | Server-derived identity, email match enforced. |
| `/api/auth/status` | GET | Public metadata | None | None | Read-only | Low | Intentionally public for pre-login UX; exact `{ enabled: boolean }` response reports shared `isAuthRequired()` enforcement state and exposes no configuration metadata. |
| `/api/blob-proxy` | GET | Authenticated | `requireAuth` | Shared organizational blob assets only | Reads Vercel Blob (proxy) | Low | Host allowlist is the boundary; do not extend to user-owned blobs. |
| `/api/cron/auth-bypass-check` | GET, POST | Cron | `verifyCronSecret` | Global security posture | Writes `maintenance_runs`, `system_alerts` (PG); sends email | Low | Daily re-assert of the `EMERGENCY_AUTH_BYPASS` prod lever. Raises a CRITICAL alert while the lever is set; auto-resolves once cleared. Shares logic with the `instrumentation.js` cold-start hook via `lib/utils/auth-bypass-monitor.js`. |
| `/api/cron/generate-grantee-titles` | GET, POST | Cron | `verifyCronSecret` | Research awardees at `wmkf_phaseistatus=Invited` for the cycle whose `wmkf_wmkfprojectdescription` is empty | Reads `akoya_requests` (`akoya_title`, `wmkf_abstract`); generates via Executor (`grantee-title.generate`, Sonnet); ETag-conditional PATCH of `wmkf_wmkfprojectdescription` **only when empty** | Low | S269 chunk-7 edited-title generator. Idempotent write-when-empty (never overwrites manual curation); `queryAllRecords` (paginated, honors `capped`); bounded concurrency + soft time budget under the 120s cap; per-row fail-soft (failures left empty → retried next run). Seasonal daily (Apr–Jun/Oct–Dec). `?cycleCode=` overrides (one-off backfill / prior cycle / test). |
| `/api/cron/grantee-deliverable-reminders` | GET, POST | Cron | `verifyCronSecret` | All `wmkf_granteedeliverable` rows still Invited with an invite date | Reads `wmkf_granteedeliverables` (paged, no `$expand`), then per-row reads `akoya_request`, PI/liaison `contact`, lead-PD `systemuser`, unified `email_signature`, the validated `email_automation` review-all override, and the PD's `scheduled_email_vip_flags` (PG). Every eligible row gets a `scheduled_email_messages` (PG) ledger entry on first sight with `approval_required` frozen from override/VIP state; the cron never sends recipient mail directly. Per-PD daily digests (correlation-keyed, FYIs receipted via `digest_fyi_at`), due sends (store claim refuses unapproved approval-required rows), and Dataverse finalization repair run from the ledger; the live grantee token is minted only at send, as the PD with `noFallback:true`. | Medium | VIP/digest decision layer (`docs/SCHEDULED_EMAIL_VIP_DIGEST_PLAN.md`), source-built on this branch. A review posture read failure fails closed (row skipped, never weakened to automatic). Existing frozen jobs still process when current shared defaults are unavailable. Due-send leases and a correlation-keyed Dynamics activity prevent blind duplicate creation; sent-but-unfinalized rows are repaired without re-sending. |
| `/api/cron/health-check` | GET, POST | Cron | `verifyCronSecret` | Global operational | Writes `health_check_history`, `system_alerts` (PG); sends email | Low | Stores health history and alerts. |
| `/api/cron/log-analysis` | GET, POST | Cron | `verifyCronSecret` | Vercel logs | Writes `api_usage_log` (PG) via llm-client; sends email | Low | Review redaction expectations. |
| `/api/cron/maintenance` | GET, POST | Cron | `verifyCronSecret` | Global operational | Writes `maintenance_runs`; deletes from `api_usage_log`, `dynamics_query_log`, `dynamics_explorer_requests`, `health_check_history`, `system_alerts`, `dynamics_feedback`, terminal `scheduled_email_messages`, and Vercel Blob | Low | Retention/cleanup job. Explorer request rows use the query-log retention setting (default 365d); their feedback FK is nulled rather than deleting retained feedback. Scheduled-email cleanup defaults to 365d and deletes only finalized sent rows or explicit stopped rows, never pending/failed/sent-but-unfinalized recovery work. Migration 033 applied and was schema/tracker-read-back in Production on 2026-08-21; migration 036 is source-built, not applied. |
| `/api/cron/reconcile-identities` | GET, POST | Cron | `verifyCronSecret` | Global identity | Updates `user_profiles` (PG); reads Dynamics | Low | Background identity sync. |
| `/api/cron/refresh-irs-bmf` | GET, POST | Cron | `verifyCronSecret` | Reference data only | Atomic-swap-replaces `irs_exempt_orgs` (PG); writes `maintenance_runs`, `system_alerts` on failure | Low | Quarterly IRS BMF refresh (15th of Jan/Apr/Jul/Oct). Downloads 4 IRS CSVs to /tmp, COPYs into staging, swaps to live. Atomic — failure leaves live table untouched. |
| `/api/cron/pricing-canary` | GET, POST | Cron | `verifyCronSecret` | `api_usage_log` distinct models + Anthropic `/v1/models` metadata | Reads `api_usage_log` (PG); calls Anthropic `/v1/models` with `CLAUDE_API_KEY`; writes `maintenance_runs` (PG) and `system_alerts` via NotificationService | Low | S181 weekly canary (Mon 10am UTC). Flags model ids in usage log that don't resolve via `lib/utils/model-pricing.js`, `LAST_REVIEWED_AT` older than 60 days, and newer live Claude ids not specifically covered by both capability/pricing registries. Routes to category `ops`; live model discovery degrades to skipped/unavailable without failing the whole cron. |
| `/api/cron/pricing-refresh` | GET, POST | Cron | `verifyCronSecret` | Anthropic `/cost_report` + `api_usage_log` summed tokens | Reads `api_usage_log` (PG); writes `model_pricing_audit` (PG); writes `system_alerts` via NotificationService; calls Anthropic Admin API (`ANTHROPIC_ADMIN_API_KEY`) | Low | S181 monthly drift check (1st of month, 11am UTC). Derives authoritative per-model price from Anthropic's cost report, compares to local table, alerts on >5% drift OR unknown-in-cost-report. Skips when admin key not set. Routes to category `ops`. |
| `/api/cron/reviewer-email-reconcile` | GET, POST | Cron | `verifyCronSecret` | Roster rows (active/saved) whose blob carries a `suggestionId` + an `emailPersistAllowed=true` email. Vetted rows always receive a live lifecycle check. Unvetted rows receive one only when their keyed `reviewer_email_reconcile_needs_merge` alert is still open (or when that key lookup fails), avoiding a full Dataverse scan without stranding standing alerts | Reads `reviewer_find_roster` + open keyed `system_alerts` (Postgres), then `wmkf_appreviewersuggestion` (`getForEmailReconcile`: only structured ObjectDoesNotExist `0x80040217` 404→gone; excluded rows readable; every other 404/error remains loud) and `wmkf_potentialreviewerses` (`getForEmailReconcile`, `findByEmailCandidates`) as needed. Gone/deselected/applicant-excluded suggestions auto-resolve their keyed alert before the vetted-email gate; a still-selected row must pass `pickVettedEmail` plus live Dataverse email-empty/ownership checks before WRITE ownerless email (`update` + `emailSource`), REPOINT to a single active keeper (`repointToPotentialReviewer`, guarded against `(person,request)` collision), or ALERT (`NotificationService`) for ambiguous/inactive/colliding. Missing person/personId and other non-conclusive states preserve the alert. Successful write/repoint and already-present email retract the keyed alert. Best-effort per row; `?dryRun=1` mutates nothing, returns only known-open retractions in `wouldRetract`, and sets `retractionPreviewComplete=false` when the open-key scan failed | Low | Backstop (Fix A, S317) for the roster-has-email/Dataverse-empty class. Retraction added S414; lifecycle-before-contact ordering was corrected by `3872d97c`, then hardened after Opus review for record-scoped Dataverse 404s, excluded lifecycle rows, open-alert gating, missing-person/probe parity, and accurate dry-run retractions. Promoted through `5f7baf9a` in Vercel Production deployment `dpl_ZyyAd6v77dDUq4kYscWsXMoikdq5` (READY, 2026-08-11). Knobs: `?maxBatch=N`, `?dryRun=1`. See `docs/REVIEWER_EMAIL_PERSIST_FIX_PLAN.md` and `docs/agent-wiki/topics/reviewer-identity.md`. |
| `/api/cron/reviewer-reminders` | GET, POST | Cron | `verifyCronSecret` | Invited non-responders at their per-reviewer respond-by deadline; accepted/materials-sent/not-submitted reviewers at their effective review-due date | Reads `wmkf_appreviewersuggestions` + per-request `akoya_request` campaign config + lead-PD `systemuser` + `email-signature`; claim-before-send conditional PATCH of `wmkf_respondremindersentat` (respond) / `wmkf_remindersentat`+`wmkf_remindercount` (review-due) via If-Match. Respond mints and delivers a replacement pre-acceptance token. Review-due performs fail-closed token-liveness/runway checks before hydration and again at the final sender, then sends a link-free Dynamics email without minting or rotating token authority. | Low | Phase-3 route/mechanism implemented (S275), but absent from the Vercel cron registry since 2026-09-01 under the reviewer-token incident hold. The liveness remediation is production-live at `4dd57369`, but the schedule remains held. The route remains callable with `CRON_SECRET`; `scripts/check-reviewer-reminder-hold.js` blocks accidental schedule restoration in CI and Vercel prebuild. Per-request enabled flags still gate the sweep. Knobs: `?maxBatch=N`, `?dryRun=1`. |
| `/api/cron/send-review-thankyous` | GET, POST | Cron | `verifyCronSecret` | Reviewers whose review was received (`wmkf_reviewreceivedat` set) but not yet thanked (`wmkf_thankyousentat` null), not excluded | Reads `wmkf_appreviewersuggestions` + lead-PD `systemuser` + `email-signature` + the reviewer's own answer snapshot (`wmkf_appreviewanswers` via `fetchAnswersBySuggestion`); builds the template-backed courtesy DOCX before any claim; conditionally PATCHes `wmkf_thankyousentat` via If-Match; renders the seeded `thankyou` template; sends Dynamics email as the PD with `noFallback:true` and the review attached as file bytes (`activitymimeattachments`, never Blob-staged) | Low | Daily reviewer thank-you sweep. A DOCX failure increments `attachmentFailed` and leaves the row unclaimed/unsent for a later complete retry. Fire-once + at-most-once still governs transport: the marker is claimed before email send and remains shared with the manual `thankyou` compatibility path. Template preflight occurs before the batch. A maintenance run with any send, attachment, or configuration failure is failed rather than reported healthy. Service `lib/services/reviewer-thankyou-sweep.js`. Knobs: `?maxBatch=N`, `?dryRun=1`. **[PRODUCTION-LIVE at `3101f067` 2026-09-03; source/test/render verified, but no release-smoke email was sent; Production Wave 25 is exact.]** |
| `/api/cron/file-review-docx` | GET, POST | Cron | `verifyCronSecret` | Bounded newest-first scan of received, selected, non-excluded reviewer suggestions carrying the exact configured cycle stamp and whose SharePoint pointer is absent or partial; every candidate is authoritatively re-read before action | With the write flag off, returns before persistence. When enabled, reads `wmkf_appreviewersuggestion`, `wmkf_appreviewanswer`, `akoya_request`, and `wmkf_potentialreviewers` (DV); create-only uploads one generated DOCX to the canonical `akoya_request` SharePoint drive, ETag-conditionally PATCHes both suggestion pointer fields, and records `maintenance_runs` plus deduplicated actionable `operational_events` (PG) | Medium | **[ROUTE PRODUCTION-DEPLOYED INERT 2026-09-03 AT `83da197f` / `dpl_F3oZ9MDbnyFox7S8Ekdos7423ece`; BOTH ROLLOUT VARIABLES ABSENT.]** An authenticated Production request returned `enabled:false` and the `file-review-docx` maintenance-run population remained zero before/after. The shared service's local operator backfill path is Production-proved for one exact owner-approved Request `1002874` row: create-only Graph upload, ETag pointer commit, semantic readback, and Workbench link visibility passed. The scheduled route itself remains unexercised. Requires exact `REVIEW_DOCX_SHAREPOINT_WRITE=on`, valid `REVIEW_DOCX_SHAREPOINT_CYCLE`, enforcing Dataverse target interlock, canonical akoyaGO site, and Production deployment. Create-only semantic reconciliation never replaces an existing item; partial/different pointers and divergent content fail visibly. Defaults `scanCap=50`, `attemptCap=5`; explicit route function duration is 300 seconds. |
| `/api/cron/secret-check` | GET, POST | Cron | `verifyCronSecret` | Secret metadata | Writes `system_alerts` (PG); sends email | Low | Expiration metadata only. |
| `/api/cron/spend-check` | GET, POST | Cron | `verifyCronSecret` | Usage/spend logs | Reads `api_usage_log`; writes `system_alerts` (PG); sends email via Dynamics | Low | Global monitoring. |
| `/api/cron/sweep-stale-invites` | GET, POST | Cron | `verifyCronSecret` | Reviewer suggestions whose parent request's meeting date is past | Reads `akoya_requests` + `wmkf_appreviewersuggestions`; PATCHes stuck suggestions to `wmkf_responsetype=no_response` | Low | Daily sweep that closes silent reviewer invites past their meeting date. Idempotent; bounded by `maxBatch` (default 200). Ad-hoc backfill knobs: `?graceDays=N`, `?maxBatch=N`, `?dryRun=1`. |
| `/api/cron/drain-submissions` | GET, POST | Cron | Inline strict `CRON_SECRET` (no dev bypass — Codex round-12 Q5) | Up to `DRAIN_BATCH_SIZE` non-terminal jobs (claim-leased) | Claims + advances rows in `submission_jobs` (PG); writes `intake_audit` (PG); writes `system_alerts` (PG) on corruption/anomaly/build-pending; writes Dataverse `akoya_requests` (Create with pre-gen GUID, `Prefer: return=representation`) + `wmkf_proposalbudgetlines` children (per-child duplicate-PK skip); writes SharePoint files to `akoya_request/{requestnum}_{guidUpper}/Applicant_Uploads/` via Graph; reads private Blob (`INTAKE_BLOB_RW_TOKEN`) | Medium | Applicant intake portal drain. Two-phase claim (`FOR UPDATE SKIP LOCKED` + `lease_token`), transactional state-transition+audit, classifier-driven retry taxonomy. Wired: `queued → scanning → request_created → files_moved → dynamics_patched (budget-lines half)` with duplicate-PK recovery (read-back via GET for parent; per-child success-advance for budget-lines) + per-file `sharepoint_paths` JSONB skip + per-child `dynamics_patches.budget_lines` skip + post-download sha256 verification. BUILD-PENDING: `dynamics_patched` (persons + parent aggregates pending Connor Q2 + contact-resolution service), `status_flipped` (Connor Q1), `completed` (final cleanup). Schedule every 2 min (≪ `DRAIN_LOCK_TTL_SECONDS=600s`). |
| `/api/cron/drain-reviewer-acceptances` | GET, POST | Cron | `verifyCronSecret` | Up to `limit` reviewer acceptance jobs (claim-leased) | Claims/updates `reviewer_acceptance_jobs` (PG); re-reads `wmkf_appreviewersuggestion` (DV) to verify accepted state; writes reviewer/contact/honorarium side effects in Dataverse via the existing honorarium, ORCID, identity, name/title, mismatch-alert, acceptance-email, and quota helpers; writes `system_alerts`/`maintenance_runs` through NotificationService/MaintenanceService | Medium | Reviewer Stage 2a accept fast-response drain. The public respond route stages a job before a fresh accept PATCH and returns after Dataverse accepts; this cron retries the slow post-accept tail. Jobs in `accept_pending` are processed only after the Dataverse row is actually accepted; stale intent-only jobs are cancelled after a grace window. Schedule every 2 min. |
| `/api/cron/drain-review-syntheses` | GET, POST | Cron | `verifyCronSecret` | Participating reviewer lifecycles across requests; bounded scan (default 25 ready requests) and claim (default 1, max 3) | Reads `wmkf_appreviewersuggestion`, `wmkf_appreviewanswer`, and `akoya_request` (DV); enqueues/claims/updates `review_synthesis_jobs` and records `maintenance_runs` (PG); calls `executePrompt('review-synthesis.generate')`, which writes `akoya_request.wmkf_reviewsynthesisjson` and `wmkf_ai_run` (DV) only after revalidating readiness and the exact fingerprint | Medium | Automatic all-in synthesis drain. Inert unless `REVIEW_SYNTHESIS_AUTOMATION_ENABLED` is exactly `true`; Production is deliberately enabled after the 2026-07-28 controlled smoke. Participant scans fail closed at the Dataverse cap; claims use `FOR UPDATE SKIP LOCKED` leases; lifecycle readiness is revalidated before content loading and a changed readiness/fingerprint cancels the claim; retryable failures are bounded to three attempts and terminal fingerprints are not silently reopened. **Production-proved by completed job `2`, maintenance run `27723`, and AI run `1b882cf6-bf8a-f111-ab0f-7ced8d3d15a6`; final PR #99 deployment `dpl_FdUJSjNwhbNWKWVzpyymiB2mpJo1` is Ready and a post-deploy zero-eligible drain was clean.** |
| `/api/dataverse-export/download` | GET | App | `requireAppAccess('dataverse-bulk-export')` + signed `dvx-download` token | Private export artifact (own run) | Reads a PRIVATE Vercel Blob (no writes) | Medium | Gated proxy for the private .xlsx — two gates (app access + short-lived ≈1h signed token binding the blob pathname). Streams Content-Disposition: attachment. Closes the public-URL exposure gap (Codex S160 P1). |
| `/api/dataverse-export/metadata` | GET | App | `requireAppAccess('dataverse-bulk-export')` | Live Dataverse taxonomies (akoya_program / wmkf_type / wmkf_grantprogram / distinct akoya_requeststatus) | Read-only Dataverse; no PG/DV writes | Low | Fail-loud on taxonomy fetch (502), never a stale/partial list (Living-taxonomy §9). Distinct statuses page to completion, never one page. |
| `/api/dataverse-export/preview` | POST | App | `requireAppAccess('dataverse-bulk-export')` | Validated QuerySpec → compiled FetchXML + true aggregate count + era split | Read-only Dataverse (FetchXML aggregate counts only, NO rows); no writes | Low | §2.1 validation → 422 + violations. Mints a signed `resultToken` (HS256 / NEXTAUTH_SECRET) binding the exact validated spec. Never OData /$count. |
| `/api/dataverse-export/run` | POST | App | `requireAppAccess('dataverse-bulk-export')` | Confirmed QuerySpec (resultToken-gated) → akoya_request rows | Read-only Dataverse (backoff-hardened FetchXML paging); writes the .xlsx artifact to a PRIVATE Vercel Blob (access:'private', addRandomSuffix, contentDisposition:attachment); returns only a gated `/api/dataverse-export/download` proxy URL (signed short-lived dvx-download token) — never a public URL | Medium | SSE response. Stateless confirm gate: rejects any body whose `resultToken` was not minted by /preview; executes the token's embedded spec, never a body spec. Fails loud (422) if `excludeOperational` labels don't resolve against the live taxonomy. Terminal error writes NO Blob (a failure can never present as a short-but-complete file). Paging budget clamped to keep route headroom; loud truncation. |
| `/api/dynamics-explorer/chat` | POST | App | `requireAppAccess('dynamics-explorer')` | Org-wide CRM exploration shaped by Dynamics role and restrictions | Writes one awaited fail-soft lifecycle row in `dynamics_explorer_requests`, plus best-effort per-tool `dynamics_query_log` and per-model `api_usage_log` rows correlated by nullable request/round fields (PG); reads Dynamics + SharePoint; ExcelJS export to Blob | Medium | Body-invalid requests create no lifecycle row. Terminal outcomes include completed/truncated/max-rounds/refused/error/client-disconnected; disconnect is marked before abort so the rejection cannot win as error. No prompt, answer, tool output, or raw error is stored in the lifecycle table. Production request `84aee86d-9c89-4434-9642-47ee6ccb4141` proved one completed lifecycle, one tool row, and two usage rows joined after migration 033 on 2026-08-21. |
| `/api/dynamics-explorer/download-document` | GET | App | `requireAppAccess('dynamics-explorer')` | Dynamics/SharePoint restrictions | Reads SharePoint (no write) | Medium | Boundary documented inline. |
| `/api/dynamics-explorer/feedback` | GET, POST, PATCH | App / Superuser | POST uses app access; GET/PATCH require superuser | Feedback records | Reads/writes `dynamics_feedback`; POST optionally verifies `dynamics_explorer_requests` correlation (PG) | Low | Client request IDs are evidence, not authority: correlation is retained only for an authenticated-profile-owned request with the same non-null session; mismatch/outage saves feedback uncorrelated. Mixed route; current split is reasonable. Migration 033 applied and was read back in Production on 2026-08-21; the release smoke deliberately created no feedback. |
| `/api/dynamics-explorer/restrictions` | GET, POST, DELETE | App / Role | `requireAppAccess('dynamics-explorer')` + `getUserRole` | Restriction config | Reads/writes `dynamics_restrictions` (PG) | Low | Read/write role handling in route. |
| `/api/dynamics-explorer/roles` | GET, POST, DELETE | App / Superuser | `requireAppAccess('dynamics-explorer')` + `getUserRole` | Own role or all roles | Reads/writes `dynamics_user_roles` (PG) | Low | Shared role helper. |
| `/api/email-automation-preferences` | GET, PUT | Profile | `requireAuthWithProfile` | The linked caller's own profile and Dataverse system-user preference row; no target profile/user is accepted from the request | Reads/writes only the reserved `email_automation` key in `wmkf_appuserpreferences` (DV) through the dispatcher | Medium | Dedicated validator for the `{ reviewAll: boolean }` review-all override (VIP/digest plan Decision 6). GET distinguishes no saved override from an explicit choice; the generic `/api/user-preferences` route blocks this reserved key. |
| `/api/scheduled-emails/vip-flags` | GET, PUT | Profile | `requireAuthWithProfile` | Caller profile resolves server-side to its own Dataverse system-user ID; flags are read/written only for that PD | Reads/writes that PD's `scheduled_email_vip_flags` rows (PG) | Medium | Per-(PD, contact) VIP review flags. PUT takes a GUID-validated `contactId` plus boolean `flagged`; the PD identity is never accepted from the request. Flagged recipients make future scheduled emails wait for that PD's approval. |
| `/api/email-defaults/grantee-invite` | GET | Profile | `requireAuthWithProfile` | Shared grantee invite defaults | Reads `email.grantee_invite.subject` and `email.grantee_invite.body` from `wmkf_appsystemsettings` (DV) via dispatcher | Low | Profile-readable defaults for PD grantee-invite composition. Uses strict reads so outages return `unavailable:true` instead of silently looking blank. |
| `/api/email-defaults/reviewer-templates` | GET | Profile | `requireAuthWithProfile` | Shared reviewer template defaults | Reads `email.reviewer_{invitation,materials,followup,thankyou}.{subject,body}` from `wmkf_appsystemsettings` (DV) via dispatcher | Low | Profile-readable org defaults for the four PD-composed reviewer templates (S297). Per-PD override is layered on top client-side (email-template-store.js). Strict reads → `unavailable:true` on outage. |
| `/api/evaluate-multi-perspective` | POST | App | `requireAppAccess('multi-perspective-evaluator')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | AI payload review. |
| `/api/expertise-finder/batch-match` | POST | App | `requireAppAccess('expertise-finder')` | Writes `user_profile_id` | Writes `expertise_matches`, `api_usage_log` (PG); reads Dynamics + SharePoint | Low | User-scoped usage/history writes. |
| `/api/expertise-finder/history` | GET | App | `requireAppAccess('expertise-finder')` | `user_profile_id` scoped | Read-only (`expertise_matches` SELECTs) | Low | Good scoping pattern. |
| `/api/expertise-finder/match` | POST | App | `requireAppAccess('expertise-finder')` | Writes `user_profile_id` | Writes `expertise_matches`, `api_usage_log` (PG) | Low | User-scoped usage/history writes. |
| `/api/expertise-finder/proposals` | GET | App | `requireAppAccess('expertise-finder')` | App-wide Dynamics proposal lookup | Reads Dynamics `akoya_request` | Medium | Staff-wide visibility policy checkpoint. |
| `/api/expertise-finder/roster` | GET, POST, PATCH, DELETE | App | `requireAppAccess('expertise-finder')` | Shared roster | Reads/writes `expertise_roster` (PG) | Medium | Shared mutable roster; confirm app-level access is sufficient. |
| `/api/field-primer/generate` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Two modes by body: `{proposalText}` (standalone) or `{requestId, regenerate?}` (Workbench, GUID-validated) | Writes `api_usage_log` (PG) via llm-client; writes `wmkf_ai_run` (DV) audit row via Executor. **requestId mode also persists** a JSON envelope to `akoya_request.wmkf_ai_fieldprimer` via explicit `updateRecord` (after expert grounding) — idempotent: returns the stored primer without a paid call unless `regenerate:true`; single-flight via an **ETag-conditional generation lease** (a racing request 412s on the stale ETag → `{status:'generating'}`), so only one cold generation runs per request; prior value restored on failure | Low | Standalone staff field-overview generator (S248) + Workbench Proposal-tab Field Primer (S258). Decoupled from reviewer origination — no discovery/save/COI write path. Untrusted `proposal_text` wrapped + A7-preambled by the Executor. requestId mode requires the exact active `AI Materials/ProposalNarrative_{Request#}.pdf`; missing or ambiguous input restores the prior lease value and stops before the model. Existing stored envelopes remain cached unless `regenerate:true`. |
| `/api/external/grantee/[token]/context` | GET | External token | `verifyGranteeToken` | Token-scoped `akoya_request` + related `wmkf_granteedeliverable` package row | Reads Dynamics `akoya_request` (abstract/header fields) + `wmkf_granteedeliverable` (status/image metadata); returns exact abstract/caption Markdown plus response-only sanitized `abstractHtml`/`captionHtml` for the restricted editors; on the edit view also resolves the `grantee-waiver` policy (`getActivePolicy`) and mints a signed waiver render token; writes `external_rate_limit` (PG) | Low | Grantee deliverables portal (S268/S271). **Stateless** token — no stored hash/revocation; signature + expiry + `aud:'grantee'` claim (rejects reviewer-token replay, which carries no `aud`). Fail-closed: missing deliverable row or unknown/missing `wmkf_deliverablestatus` → read-only (NOT editable). Waiver (2026-07-09): the edit view returns `{waiverPolicy, waiverToken}`; if the `grantee-waiver` slot can't resolve it fails CLOSED with `policy_unavailable` (503, transient) or `policy_misconfigured` (500, deterministic). Rate-limited per-token + per-IP (`external_rate_limit`); 429 + `Retry-After` on excess. |
| `/api/external/grantee/[token]/upload-token` | POST | External token | `verifyGranteeToken` | Token-scoped request/package; client supplies only filename, declared content type, and byte count | Re-verifies rate limit + external token + editable package status, then inserts an actor-hash/request/scope-bound `portal_upload_staging` row and returns a 15-minute Vercel Blob client token constrained to one server-generated pathname, private store, one allowed image type, 10 MiB, no overwrite/random suffix. Raw external token and client pathname are never persisted/accepted. | Medium | Direct-upload mint. Browser bytes go only to the private `UPLOADS_BLOB_RW_TOKEN` store; production preflight proves a client token from this store cannot override access to public. |
| `/api/external/grantee/[token]/upload-failure` | POST | External token | `verifyGranteeToken` | Token-scoped request; closed stage/category plus bounded status, declared byte count, and image type | Re-verifies rate limit + token and records a best-effort `portal_upload_client_failure` Operational Event labeled `clientReported:true`. The route rejects raw text and accepts no filename, pathname, URL, token, caption, abstract, or bytes. | Low | Restores visibility for browser→Blob failures that execute no application code; client evidence is diagnostic, not authoritative. |
| `/api/external/grantee/[token]/submit` | POST | External token | `verifyGranteeToken` | Token-scoped request/package plus optional actor-bound `stagingId`; 128kb JSON cap | JSON-only finalizer: `editedAbstract`, `caption`, signed `waiverToken`, optional `stagingId` (omitted when retaining the existing image). Atomically claims staging by id + scope + request + SHA-256 token binding; re-downloads the exact private pathname server-side, verifies Blob pathname/type/size/hash, then applies existing magic-byte/virus-scan/SharePoint/atomic-Dataverse writer. Candidate SharePoint identity is persisted before Dataverse; an expired-lease retry reconciles referenced candidate as committed or deletes the exact unreferenced item before retry. Consumed rows replay the durable result. Staged bytes are exact-path deleted after terminal commit/reject and by daily maintenance after expiry. Waiver/status/ETag/change-set/notification behavior remains fail-closed as before. | High | Large-image remediation (2026-08-19). The 9.12 MiB production failure no longer crosses a Function request-body seam. Rate-limited per token + IP; no staff identity. |
| `/api/external/review/[token]/context` | GET | External token | `verifySuggestionToken` | Token-scoped suggestion/request | Updates `wmkf_appreviewersuggestion` (DV) for first-view timestamp; reads the override-first effective review due date and SharePoint; writes `external_rate_limit` (PG) | Low | GET has a side-effect write (first-view timestamp). Wave 18 source exposes the suggestion override or request fallback as `reviewDeadline`. Public route protected by magic link verification. Rate-limited per-token + per-IP (`external_rate_limit`, security audit A6); 429 + `Retry-After` on excess. |
| `/api/external/review/[token]/proposal` | GET | External token | `verifySuggestionToken` | Token-scoped proposal files | Reads SharePoint; writes `external_rate_limit` (PG) | Medium | High-sensitivity payload; keep token tests strong. Rate-limited per-token + per-IP (`external_rate_limit`, security audit A6); 429 + `Retry-After` on excess. Served `inline` (no query param) when the file reports `application/pdf`, else `attachment`; `nosniff` always set — safe because the allow-set's only servable filename is `Proposal_{requestNum}.pdf`, so HTML/SVG cannot reach the response. Stricter than the `download-proposal-document` sibling, which allows a wider safe-mime set behind `?disposition=inline`. |
| `/api/external/review/[token]/upload` | POST | External token | `verifySuggestionToken` | Token-scoped upload; shared receipt guard freshly rejects terminal/already-received/non-accepted rows | Writes SharePoint `Reviewer_Uploads/`; ETag-conditionally updates `wmkf_appreviewersuggestion` (DV) via writeReviewFiles; writes `external_rate_limit` (PG) | Low | Rate-limited per-token + per-IP (`external_rate_limit`, security audit A6); 429 + `Retry-After` on excess. A terminal transition racing the upload makes the receipt changeset fail; uploaded SharePoint items are cleaned up. |
| `/api/external/review/[token]/respond` | POST | External token | `verifySuggestionToken` | Token-scoped accept/decline | Fresh accept: INSERT/UPSERT `reviewer_acceptance_jobs` (PG; raw token encrypted in payload) before updating `wmkf_appreviewersuggestion` (DV) via `applyStage2aResponse`; repeat accept queues the same follow-up job without restamping DV. Initial decline validates up to four structured referral rows (required published name, optional institution/email), stores a versioned envelope in the existing `wmkf_declinereferral` memo, and archives the suggestion from the proposal pool (`wmkf_selected=false`). Legacy scalar decline fields remain accepted for already-open browser sessions. Accepted→declined before materials atomically archives the suggestion + DELETEs its exact linked honorarium `akoya_request`, then cancels unlocked acceptance jobs and notifies the assigned PD. Reads `wmkf_policy`/`wmkf_policyversion` via `getActivePolicies`; writes `external_rate_limit` (PG). | Medium | Stage 2a accept/decline and pre-materials self-withdrawal entry point. State machine + idempotency + active-child sanity. Optimistic locking via `If-Match`; materials/review receipt remain hard locks. The slow accept tail (honorarium/contact/identity/email/quota) drains through `/api/cron/drain-reviewer-acceptances`; a leased worker re-checks and compensates for withdrawal races. Rate-limited per-token + per-IP (`external_rate_limit`, security audit A6); 429 + `Retry-After` on excess. |
| `/api/external/review/[token]/draft` | GET, PUT | External token | `verifySuggestionToken` | Token-scoped review-form draft | Reads/writes `review_drafts` (PG) via `ReviewDraftService`; writes `external_rate_limit` (PG) | Medium | In-browser authoring autosave (reviewer review-form build, Phase 1). PUT sanitizes every rich-text answer server-side (`sanitize-review-html`) before persisting — stored-XSS boundary. Engagement gate reuses `computeEngagementState`: PUT 409 `materials_not_sent` outside the authoring stage and 409 `review_received_locked` once `wmkf_reviewreceivedat` is set; GET returns an empty draft post-submit. Rate-limited per-token + per-IP (`external_rate_limit`, security audit A6); 429 + `Retry-After` on excess. |
| `/api/external/review/[token]/submit` | POST | External token | `verifySuggestionToken` | Token-scoped final review submission | Atomic write to `wmkf_appreviewanswer` (DV, N answer rows upserted by alternate key) + parent `wmkf_appreviewersuggestion` PATCH (ratings/affiliation/receivedat) via `DynamicsService.executeChangeset` (one changeset, parent `If-Match`-guarded); deletes `review_drafts` (PG) post-commit; writes `external_rate_limit` (PG) | High | Final submit (reviewer review-form build, Phase 3). Submit is irreversible: form locks read-only, no re-submit. Server-sanitizes every rich-text answer (`sanitize-review-html`) before write — stored-XSS boundary. Finality precheck 409 `review_received_locked` if `wmkf_reviewreceivedat` already set (authoritative; the in-changeset `If-Match` 412 → 409 `conflict` is belt-and-suspenders). Engagement gate (`computeEngagementState`) 409 `materials_not_sent` outside the authoring stage. Atomic changeset = no partial/duplicate child rows; alternate-key upsert = retry-idempotent. Rate-limited per-token + per-IP (`external_rate_limit`, security audit A6); 429 + `Retry-After` on excess. |
| `/api/grant-reporting/extract` | POST | App | `requireAppAccess('grant-reporting')` | Request payload / Dynamics | Writes `api_usage_log` (PG); writes `wmkf_ai_run` (DV) via logAiRun | Medium | AI and Dynamics data-boundary review. |
| `/api/grant-reporting/lookup-grant` | POST | App | `requireAppAccess('grant-reporting', 'batch-phase-i-summaries')` | Staff app-wide Dynamics/SharePoint lookup by request number | Reads Dynamics + SharePoint (read-only) | Medium | Boundary documented inline. |
| `/api/health` | GET | Authenticated | `requireAuth` | Service status | Read-only | Low | Avoid exposing to unauthenticated users. |
| `/api/intake/draft` | POST | Applicant session | `getServerSession` + `userType === 'applicant'` + any-role membership guard via Dataverse `wmkf_portalmemberships` (live = approved + active; either Submitter `100000000` or Contributor `100000001`) | One own draft per `(contactOid, accountId, formKey)` triple | `IntakeDraftService.upsertDraftJson` writes only `draft_json` (never overwrites `attachments[]` — those are managed by `/api/intake/draft/attach`); preserves `draft_json.idempotency_key` across autosaves; best-effort `intake_audit` write (`draft.upsert`) | Low | Applicant intake portal — debounced autosave from the form UI. Strips caller-supplied `idempotency_key` (server owns that field's lifecycle). Mirrors `/submit`'s bridge + identity_conflict handling (409 + audit row on `bridge.conflict`). |
| `/api/intake/draft/attach` | POST | Applicant session | `getServerSession` + `userType === 'applicant'` + draft owner OR any-role membership against draft's `account_id` (live = approved + active; Submitter `100000000` or Contributor `100000001`) | One own draft (by id); rejects `request_id IS NOT NULL`; A2 dual-lookup (`attachments[]` first → 200 `already_attached` short-circuit, then `pending_attachments[]`) | `IntakeDraftService.promoteToClean` (atomic SQL move pending→attachments, gated on `attachmentId NOT in attachments[]` per chunk-3 design); `IntakeDraftService.removePending` on rejection paths; `@vercel/blob` `get(pathname, {access:'private', token: getIntakeBlobToken(), useCache:false})` then `del(pathname, {token})` on infected/oversized/cardinality-race; SHA-256 recomputed from bytes; magic-byte validated via `validateIntakeAttachment` against `field.accept[]`; size cross-checked against `pending.maxBytes`; `cloudmersive-scan` `scanBytes` gated by `isVirusScanEnabled()`; best-effort `intake_audit` writes (9 actions: `draft.attach` happy, `draft.attach_infected`, `draft.attach_infected_del_failed`, `draft.attach_scan_misconfigured`, `draft.attach_scan_unavailable`, `draft.attach_blob_misconfigured`, `draft.attach_magic_mismatch`, `draft.attach_size_exceeded`, `draft.attach.ownership_denied` — A3 metadata/payload field split) | High | S184 chunk 5. Third leg of the three-call attach dance. Magic mismatch returns 422 + Blob NOT deleted (operator-decision posture) + pending removed; size-exceeded / infected / cardinality-race return Blob deleted + pending removed. Scanner posture (A7): flag off → `scanner:'skipped'` clean path, flag on + key present → run scan, flag on + key missing → fail-loud 500 `scan_misconfigured` (pending intact). Blob errors wrapped via `buildNoResponseError('blob', err)` before classification (mirror of drain prior art). `INTAKE_BLOB_RW_TOKEN` enforced (NOT shared `BLOB_READ_WRITE_TOKEN` — A4). Forbidden body fields (400): `pathname`, `filename`, `contentType`, `fieldKey`, `accountId`, `formKey`, `requestId`, `contactOid`, `draftJson`. Allowed only `draftId` + `attachmentId`. |
| `/api/intake/draft/upload-token` | POST | Applicant session | `getServerSession` + `userType === 'applicant'` + draft owner (direct `contact_oid` match) OR any-role membership via Dataverse `wmkf_portalmemberships` against the draft's `account_id` (live = approved + active; Submitter `100000000` or Contributor `100000001`) | One own draft (by id); rejects `request_id IS NOT NULL` (submitted drafts frozen) | `IntakeDraftService.appendPending` writes only `pending_attachments[]` (server-managed; never overwritten by autosave); `@vercel/blob` `generateClientTokenFromReadWriteToken` mints a one-shot client-upload token scoped to opaque pathname `drafts/{draftId}/{attachmentId}` + the specific contentType + field maxBytes + 1h validUntil; best-effort `intake_audit` writes (`draft.upload_token.mint` happy path / `draft.upload_token.mint_failed` on Blob failure / `draft.upload_token.ownership_denied` on 403 — A3 metadata/payload field split) | Medium | S184 chunk 4. First leg of the three-call attach dance (browser-direct PUT in step 2; `/attach` finalizes in step 3). Per-field cardinality gate (single-valued: reject if already populated; multi-valued: enforce `maxFiles` across pending+clean). Filename sanitized via `sanitizeBlobFilename` (NFKC, `..` rejection, codepoint-aware truncation). Forbidden body fields (server-derived; reject 400): `attachmentId`, `pathname`, `accountId`, `formKey`, `requestId`, `contactOid`, `draftJson`. Blob token uses `INTAKE_BLOB_RW_TOKEN` (NOT shared `BLOB_READ_WRITE_TOKEN` — A4). `addRandomSuffix:false`, `allowOverwrite:false`. |
| `/api/intake/submit` | POST | Applicant session | `getServerSession` + `userType === 'applicant'` + Submitter-role guard via Dataverse `wmkf_portalmemberships` (live = approved + active + `wmkf_role=100000000`) | One own draft → one own `submission_jobs` row | Single PG txn: INSERT `submission_jobs` (idempotency-keyed) + UPDATE `intake_drafts.request_id = generated GUID`; best-effort `intake_audit` write (`submit` happy path / `submit.blocked_terminal` on 409) | Medium | Applicant intake portal — submission entry point. Pre-generates the `akoya_request` GUID written to PG immediately, materialized in Dataverse later by the drain. Idempotent via `idempotency_key UNIQUE` (`DO UPDATE … RETURNING`). 409 + `previous_submission_terminal` on collision against a terminal-status prior job. Attachment-shape validator (`lib/utils/intake-attachment-shape.js`) + all-clean scan invariant at submit-entry. |
| `/api/integrity-screener/dismiss` | POST, GET disallowed | App | `requireAppAccess('integrity-screener')` | Service layer | Writes `screening_dismissals`; updates `integrity_screenings` (PG) | Low | Method parsing looks safe; confirm service-level scope. |
| `/api/integrity-screener/history` | GET, PATCH | App | `requireAppAccess('integrity-screener')` | `access.profileId` passed to service | GET reads `integrity_screenings`; PATCH updates `integrity_screenings.status/notes/reviewed_at` (PG) | Low | Good scoping pattern if service enforces it. |
| `/api/integrity-screener/screen` | POST | App | `requireAppAccess('integrity-screener')` | `access.profileId` passed to service | Writes `integrity_screenings`, `api_usage_log` (PG) | Low | AI/search payload review. |
| `/api/irs/verify-ein` | GET | Shared secret | `x-irs-verify-secret` header == `IRS_VERIFY_SECRET` env var | EIN-keyed lookup | Reads `irs_exempt_orgs` (PG); read-only, stateless | Low | PowerAutomate-callable. Allowlisted in `proxy.js` (no NextAuth session). Returns IRS subsection/status/deductibility + derived `is501c3PublicCharity` flag. Never writes Dynamics — PA writes verified result back to `account` row itself. |
| `/api/phase-i-dynamics/summarize` | POST | App | `requireAppAccess('batch-phase-i-summaries')` | App-wide request ID writeback | Updates `akoya_request.wmkf_ai_summary`, writes `wmkf_ai_run` (DV); writes `api_usage_log` (PG) | Medium | Writes AI summary by request GUID; policy checkpoint for who may target records. |
| `/api/phase-i-dynamics/summarize-v2` | POST | App | `requireAppAccess('batch-phase-i-summaries')` | App-wide request ID writeback | Updates target field on subject entity, writes `wmkf_ai_run` (DV) via execute-prompt; `api_usage_log` (PG) | Medium | Shared executor path; policy checkpoint for who may target records. |
| `/api/process` | POST | App | `requireAppAccess('batch-proposal-summaries', 'phase-ii-writeup')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | AI payload review. |
| `/api/process-expenses` | POST | App | `requireAppAccess('expense-reporter')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | File/data minimization review. |
| `/api/process-peer-reviews` | POST | App | `requireAppAccess('peer-review-summarizer')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | AI payload review. |
| `/api/process-phase-i` | POST | App | `requireAppAccess('batch-phase-i-summaries')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | AI payload review. |
| `/api/process-phase-i-writeup` | POST | App | `requireAppAccess('phase-i-writeup')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | AI payload review. |
| `/api/qa` | POST | App | `requireAppAccess('phase-ii-writeup', 'batch-proposal-summaries')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | AI payload review. |
| `/api/refine` | POST | App | `requireAppAccess('phase-ii-writeup', 'batch-proposal-summaries')` | Request payload | Writes `api_usage_log` (PG) via llm-client | Low | AI payload review. |
| `/api/review-manager/campaign-config` | GET, POST | App | `requireAppAccess('review-manager', 'reviewers')`; POST: lead PD or superuser | `requestId` GUID-validated; GET is staff-shared, while POST resolves request ownership server-side and fails closed on missing actor/owner data | Reads/writes reviewer-engagement campaign-config columns on `akoya_request` (DV: `wmkf_respondoffsetdays`, `wmkf_reviewduedate`, reminder enabled/lead pairs, `wmkf_desiredcount`); `wmkf_quotanotifiedat` is read-only | Low | Per-request reviewer campaign config (Phase 1, S275). Editable-later surface for the config first written by send-emails on first invite. |
| `/api/review-manager/campaign-timeline-defaults` | GET, PUT | App (GET) / Superuser (PUT) | `requireAppAccess('review-manager', 'reviewers')` (GET); `requireSuperuser` (PUT) | Staff-shared review-manager access (GET); global admin (PUT) | Reads/writes `reviewer.campaign_timeline_defaults` JSON in `wmkf_appsystemsettings` (DV) via `lib/services/reviewer-campaign-timeline.js` | Low | Current-cycle reviewer invitation timeline defaults. InviteEmailModal reads them fresh on open; `/admin` writes them from the Reviewer Campaign Timeline card. Request-level campaign config still wins for response offset and review due date. |
| `/api/review-manager/update-abstract` | POST | App | `requireAppAccess('review-manager', 'reviewers')` | `requestId` GUID-validated before it becomes an `akoya_requests` selector; abstract is staff-authored (trusted actor) | Overwrites `wmkf_abstract` (DV, canonical abstract of record) on `akoya_request` via `updateById`; existence-check-then-write; PD attribution via `actingUserSystemId` | Low | PD fixes a hard-wrapped abstract from the invite flow (S340). `wmkf_abstract` is GoApply write-once (not re-synced), so the edit is durable and corrects the abstract everywhere it is consumed. |
| `/api/review-manager/download-review` | GET | App | `requireAppAccess('review-manager', 'reviewers')` | Staff-shared review-manager access | Reads Dataverse + SharePoint | Low | Boundary documented inline; tighten to PD-only only if policy changes. |
| `/api/review-manager/export-reviews` | GET | App | `requireAppAccess('review-manager', 'reviewers')`; `withDalContext` | `proposalId` is a scalar GUID validated before lookup; staff-shared Review Manager read policy matches `/reviewers`. The browser supplies no report content. | Read-only: server-authoritatively reads the request, submitted reviewer rows, answer snapshots (including option snapshots), and stored synthesis through the existing reviewers service; returns private/no-store DOCX bytes rendered from the tracked combined template. No Dataverse, SharePoint, Blob, or Postgres write. | Low | Combined Word export. Returns 404 for an unknown request and 409 when no submitted review exists. **[PRODUCTION-LIVE + SIGNED-IN EXPORT PROVED on Request `1002903` 2026-09-03 at `3101f067`; valid 60,586-byte DOCX, 2xx dependencies, Production Wave 25 exact.]** |
| `/api/review-manager/manual-review-entry` | GET, POST | App | `requireAppAccess('review-manager', 'reviewers')` | `suggestionId` GUID-validated before it becomes a Dataverse selector; shared receipt guard re-reads and permits only accepted, non-terminal, not-yet-received rows | GET reads the current Dataverse review-question set and suggestion affiliation; POST atomically upserts the complete `wmkf_appreviewanswer` snapshot and ETag-patches the parent receipt/status/affiliation/staff marker | Medium | Reviews-tab rescue path when the external portal cannot be used. Reuses the external submission producer, full validation, and rich-text sanitizer; rejects stale question-set versions; post-commit stale-draft deletion is best effort. |
| `/api/review-manager/mark-received-no-file` | POST | App | `requireAppAccess('review-manager', 'reviewers')` | `suggestionId` GUID-validated; shared receipt guard freshly rejects terminal/already-received/non-accepted rows | ETag-conditionally updates `wmkf_appreviewersuggestion` and atomically upserts any supplied rating snapshot rows (DV) | Medium | Partial/no-file staff receipt path. A concurrent terminal transition or other row change returns 409 instead of overwriting the winner. |
| `/api/review-manager/materials-preflight` | GET | App | `requireAppAccess('review-manager', 'reviewers')` | `requestId` GUID-validated before it becomes an `akoya_requests` selector | Reads Dataverse (`akoya_requests`) + SharePoint via Graph (`lib/external/reviewer-materials.js`'s `listReviewerMaterials`, shared with the external portal's `context.js`) | Low | Warns the PD before a materials release when the reviewer-visible SharePoint download folder is empty. Returns `{ok:true,fileCount}` or a sanitized `{ok:false,reason}` — never raw Graph/Dataverse error text. `ReviewerManagePanel`'s `ReleaseMaterialsModal` fetches this fresh whenever it opens. |
| `/api/review-manager/regenerate-token` | POST | App | `requireAppAccess('review-manager', 'reviewers')`; lead PD or superuser | `suggestionId` GUID-validated, then resolved server-side to its request before token work | Reads suggestion + request default, server-derives expiry from accepted state and the effective reviewer due date, mints token, and updates `wmkf_appreviewersuggestion` (DV) via `mintAndStore` | Medium | Token lifecycle action. Client-supplied expiry is not authoritative; Wave 18 source uses the suggestion override first. |
| `/api/review-manager/review-due-extension` | POST | App | `requireAppAccess('review-manager', 'reviewers')`; lead PD or superuser | `suggestionId` GUID-validated and resolved server-side to its request before save or resend; service freshly permits only accepted, non-terminal, not-yet-received rows. Client supplies a date only for `action=save`; retry/resend re-reads durable state. | Preflights the admin body, Dynamics impersonation setting, assigned sender, confirmed recipient, signature, and calendar; then ETag-conditionally writes/clears `wmkf_appreviewersuggestion.wmkf_reviewduedateoverride` and dispatches the Dynamics email. Only an actual dispatch failure returns explicit saved/not-notified partial success. Retry/resend performs no date write. | Medium | Sole staff extension writer. Non-null date must be current/future and strictly after the request's original due date; no maximum. Null restores the original date. Existing overrides expose an explicit Resend deadline email action. There is no durable notification-owed marker for a failed restore send. Does not rotate a delivered token. |
| `/api/review-manager/release-settings` | GET, PUT | App (GET) / Superuser (PUT) | `requireAppAccess('review-manager', 'reviewers')` (GET); `requireSuperuser` (PUT) | Staff-shared review-manager access (GET); global admin (PUT) | Reads/writes `reviewer.release.attach_proposal_email` in `wmkf_appsystemsettings` (DV) via `lib/services/reviewer-release-config.js` | Low | Whether the materials/release email flow offers proposal/file attachments in addition to the reviewer's tokenized portal link. Default OFF (portal-link-only). `ReviewerManagePanel`'s `ReleaseMaterialsModal` GETs this fresh every time it opens (no build-time constant); the admin panel "Reviewer Release Attachments" section PUTs it. |
| `/api/review-manager/render-emails` | POST | App | `requireAppAccess('review-manager', 'reviewers')` | Staff-shared review-manager access | Read-only preview: reads Dataverse suggestion/request/reviewer/cycle data, renders the effective reviewer due date, and substitutes a clearly non-live JWT-shaped URL placeholder; no token mint or suggestion write | Medium | Email content review only. Wave 18 source resolves suggestion override before composer/request/cycle defaults. The final live reviewer token is minted and substituted by `send-emails` after recipient gates, immediately before dispatch. |
| `/api/review-manager/render-withdraw-emails` | POST | App | `requireAppAccess('review-manager', 'reviewers')` | `requestId` + every `suggestionId` GUID-validated before becoming a Dataverse selector; batch capped at 100 | Read-only — no writes, no email, no token minting. Reads `wmkf_appreviewersuggestion`, `wmkf_potentialreviewer` (name/email), `akoya_request` title/PD, the PD signature, and the withdraw email defaults | Low | Renders the "no longer needed" courtesy email so staff can edit before sending; the complete reviewed rows post back to `withdraw-sufficient` as `overrides`. Applies the same belongs-to-request + still-pending guards as the send and binds each preview to both recipient and Program Director sender identity. Missing reviewed rows or recipient/sender drift fail before the lifecycle write. |
| `/api/review-manager/reviewer-vip-flags` | GET, PUT | App | `requireAppAccess('review-manager', 'reviewers')` | `requestId` (GET query / PUT body) and PUT `potentialReviewerId` GUID-validated before any Dataverse selector; PUT `flagged` must be boolean; the lead-PD identity is resolved server-side from the request row's `_wmkf_programdirector_value` and never accepted from client input (a request without an assigned PD is a 409) | Reads the request row (DV) to resolve the lead PD, then reads/writes that PD's `scheduled_email_reviewer_vip_flags` rows (PG), keyed on `wmkf_potentialreviewersid` | Medium | Per-(lead PD, reviewer person) VIP flags for the Invite Reviewers panel. Any review-manager/reviewers staff may curate them on the PD's behalf (owner decision 2026-08-26). Synchronous routing aid only: flagged people's invitation drafts render as full editable preview cards in InviteEmailModal; no ledger workflow reads these flags and no send path is gated by this route. |
| `/api/review-manager/send-review-reminder` | POST | App | `requireAppAccess('review-manager', 'reviewers')`; send: lead PD or superuser | `requestId` + `suggestionId` GUID-validated; read-only respond preview remains staff-shared. Send resolves both targets server-side before any marker/token/email and also re-checks request/suggestion linkage in the reminder service. | Respond preview is read-only: reads defaults/request/PD/reviewer/signature and returns plain-text copy without minting a token, writing a marker, or sending. Send re-derives recipient/sender and treats preview identities only as expected-value guards; actual destinations remain server-derived. `respond` atomically writes `wmkf_respondremindersentat` plus replacement token authority and injects the live secure link after mint. `reviewdue` requires active token metadata whose expiry covers the effective deadline, writes only `wmkf_remindersentat`/`wmkf_remindercount`, and sends link-free without minting or rotating token authority. Both use the fresh suggestion ETag. | Medium | Per-reviewer staff action on the Invite and Reviews tabs. Invite Reviewers uses an editable respond-nudge preview modal; Track/Reviews uses the send-only omitted-kind review-due action. Reuses `lib/services/reviewer-reminder-sweep.js`'s send/template machinery via `lib/services/reviewer-manual-reminder.js`. Manual reminders resumed after production deployment `4dd57369`, authenticated smoke, and the D26 post-deploy liveness audit; the automatic cron remains separately held. Removed/revoked rows, token-liveness/runway refusals, recipient/sender drift, and ETag conflicts return typed no-send outcomes; transient reads and pre-email preparation failures return typed 502 reasons; a post-persistence email failure remains `send_failed`. |
| `/api/review-manager/synthesize-reviews` | POST | App | `requireAppAccess('review-manager', 'reviewers')` | `requestId` GUID-validated before it becomes a Dataverse selector; `confirmEarly` must be boolean and is required when readiness is not all-in | Reads `wmkf_appreviewersuggestion` + `wmkf_appreviewanswer` (DV, answerText only); creates/completes/fails a leased manual `review_synthesis_jobs` row (PG); calls `executePrompt('review-synthesis.generate')`, which writes `akoya_request.wmkf_reviewsynthesisjson` (DV, `guard: always-overwrite`) and a `wmkf_ai_run` audit row | Medium | Manual AI synthesis action. Requires at least one submitted review. When unresolved participants remain, staff must explicitly confirm the early run; regeneration remains deliberate via `overwrite`. The exact review digest plus lifecycle classification is fingerprinted before generation. A Dataverse write followed by ledger-finalization failure is returned as an explicit partial 502 (`writtenToDynamics:true`) rather than reported as success. The existing production prompt/column contract remains unchanged; migration 028 and the ledger/readiness code are production-deployed in `70956477`; signed-in manual verification remains. |
| `/api/review-manager/reviewers` | GET, PATCH | App | `requireAppAccess('review-manager', 'reviewers')`; PATCH: lead PD or superuser | GET is staff-shared and `scope=all` remains cycle-bounded. PATCH resolves all suggestion ids to server-owned request rows and authorizes the full batch before its first lifecycle write. | Reads Dataverse; GET attaches the narrative answer snapshot (`wmkf_appreviewanswer`, keyed child read by `_wmkf_appreviewersuggestion_value`, re-sanitized on read) per submitted reviewer and reads matching/latest `review_synthesis_jobs` state (PG, fail-soft); updates non-terminal `wmkf_appreviewersuggestion` lifecycle fields (DV) on PATCH | Medium | The all-request listing cannot fall through to an all-history scan. Reviewer-authored HTML is re-sanitized server-side on read. Generic PATCH rejects `withdrew`/`released`; terminal writes exist only at the dedicated fail-closed endpoint. |
| `/api/review-manager/revoke-token` | POST | App | `requireAppAccess('review-manager', 'reviewers')`; lead PD or superuser | Suggestion ownership is resolved server-side before revocation | Updates `wmkf_appreviewersuggestion` (DV) via revoke | Medium | Token lifecycle action. |
| `/api/review-manager/send-emails` | POST | App | `requireAppAccess('review-manager', 'reviewers')`; lead PD for every target or superuser | Every valid draft suggestion id is resolved server-side and the complete batch is authorized before SSE begins; malformed drafts retain the service's fail-closed no-send validation path | Sends email + creates email activity (DV `emails`); updates `wmkf_appreviewersuggestion` lifecycle fields. For a secure reviewer link, Wave 18 source computes token expiry from accepted state plus suggestion override/request default. It reads but does not write `wmkf_potentialreviewer`, and does not create/link/update `contact`; accepted-reviewer promotion occurs in the token-authenticated acceptance drain. | Medium | Thank-you sends never change a terminal status to `complete`. Invitation success does not establish a canonical CRM relationship. Saving a due-date override alone does not rotate a delivered token; send-time mint uses it. |
| `/api/review-manager/terminal-transition` | POST | App | `requireAppAccess('review-manager', 'reviewers')`; lead PD for every target or superuser | `requestId` + every `suggestionId` GUID-validated and ownership-authorized as a complete batch before transition; each row is then freshly re-guarded for lifecycle state | `withdrew`: one ETag-guarded DV changeset corrects the suggestion to `accepted=false` / `declined=true` / declined response type, stamps the terminal audit status, revokes the token, and deletes the exact server-read linked honorarium `akoya_request`; then cancels unlocked Postgres acceptance jobs. `released`: ETag-conditionally writes only the terminal status + token revocation. Per-row partial-success statuses; whole-request conflict when none transition. | Medium | Dedicated post-accept terminal seam. A concurrent review submission changes the ETag, so the transition returns `changed_skipped` and cannot overwrite the submitted review. A leased acceptance worker re-reads the declined row and compensates any honorarium created after the staff changeset. Production picklist values were provisioned 2026-07-23; runtime plus accepted/null repair shipped through PR #79 / merge `fd610837` on 2026-07-24. The full staff-withdrawal cleanup shipped in merge `70f51f45`, production deployment `dpl_9r2FYkAXhRqSXiJVCwevrXFZ5SzH`, on 2026-07-24. |
| `/api/review-manager/upload-review` | POST | App | `requireAppAccess('review-manager', 'reviewers')` | Staff-shared review-manager access; shared receipt guard freshly rejects terminal/already-received/non-accepted rows | Writes a unique per-attempt SharePoint `Reviewer_Uploads/.../attempt_<uuid>/` folder and persists that exact folder through an ETag-conditional `wmkf_appreviewersuggestion` (DV) write via `writeReviewFiles` | Medium | Staff upload path. A terminal transition or competing receipt that wins makes this attempt's changeset fail; cleanup is limited to the losing attempt and excludes item identities found in the winning persisted folder. |
| `/api/review-manager/withdraw-sufficient` | POST | App | `requireAppAccess('review-manager', 'reviewers')` | `requestId` + every `suggestionId` GUID-validated; each row re-guarded still-pending from a fresh read | Updates `wmkf_appreviewersuggestion` (DV) → `withdrawn_sufficient` + `wmkf_withdrawnsufficientat`, clears `wmkf_respondremindersentat`; sends "no longer needed" Dynamics email as the lead PD | Medium | PD selective-decline (reviewer-engagement Phase 4, S275). Writes `withdrawn_sufficient` ONLY on still-pending rows (invited && !accepted && !declined) — never an accepted/honorarium row. State write precedes the courtesy email. |
| `/api/reviewer-finder/analyze` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | `access.profileId` passed | Writes Vercel Blob (summary upload); writes `api_usage_log` (PG) | Low | Existing auth tests cover representative route. |
| `/api/reviewer-finder/discover` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | `access.profileId` passed | AI pass-through + external research APIs; `api_usage_log` (PG) only | Low | External discovery APIs / AI review. |
| `/api/reviewer-finder/enrich-contacts` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Request payload; optional `requestId` is GUID-validated before record use | AI pass-through + external SERP/contact APIs; `api_usage_log` (PG); after an exact receipt-bound ORCID person match, read-only capped Dataverse reads of `wmkf_appreviewersuggestion` request links plus legacy `akoya_request.wmkf_potentialreviewer1..5` links | Low | Rate-limited; server-side credentials only. Prior-request summaries are capped, omit the person GUID, use a 3-second presentation budget, and are display context only—not identity, email, promotion, or send authority. |
| `/api/reviewer-finder/prompt-override` | GET, PUT, DELETE | App | `requireAppAccess('reviewer-finder', 'reviewers')` | `access.profileId` (own prefs only) | Reads/writes the per-user `PROMPT_OVERRIDES` key in `wmkf_appuserpreferences` (DV) via dispatcher; reads Dataverse `wmkf_ai_prompt` for the base template | Low | Sole write path for prompt overrides (the generic `/api/user-preferences` blocks that key). Validates the body via prompt-validators (S222). |
| `/api/reviewer-finder/contact-history` | GET | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Org-visible Dataverse data; reads junction + projectleader for `?contactId=<guid>` | Reads Dataverse `wmkf_apprequestperson` + `akoya_request` (read-only) | Low | UNION read of `wmkf_apprequestperson` + `akoya_request._wmkf_projectleader_value`. No write side. |
| `/api/reviewer-finder/generate-emails` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | `user_profile_id` scoped | Writes/updates `wmkf_appreviewersuggestion` (DV) via `reviewer-suggestion` adapter (W5 cutover 2026-05-12); `api_usage_log` (PG) | Low | Existing cross-user tests cover representative behavior. |
| `/api/reviewer-finder/cycle-material` | GET | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Record-scoped: the requested `pathname` must be the named cycle's review template or one of its attachments | Reads Dataverse `wmkf_appgrantcycle` (`findById`) + reads the **private** Vercel Blob store (`wmkf-uploads-private`, `UPLOADS_BLOB_RW_TOKEN`) server-side via `readUploadedBlobBuffer` | Low | Phase 1 record-scoped private download proxy (the record-aware pattern `blob-proxy.js` points to). Pathname must belong to the cycle (404 otherwise — no bearer-pathname read); forces `attachment` + `nosniff` + `no-store`; fail-closed 503 if the store token is unset. Legacy public materials still go through `blob-proxy.js`. |
| `/api/reviewer-finder/grant-cycles` | GET, POST, PATCH, DELETE | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Shared grant cycles | Reads/writes Dataverse `wmkf_appgrantcycle` via `lib/services/grant-cycles-dataverse` (W3 cutover 2026-05-12); admin UI for cycle management | Medium | Shared mutable records; confirm app-level access is enough. |
| `/api/reviewer-finder/load-proposal` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | App-wide proposal file load by request ID; optional server-listed `fileKey` for deliberate staff override | Default prefers exactly one active `Reviewer Materials/Proposal_{Request#}.pdf`, then falls back only to exactly one active current-cycle `Phase I/ProjectDescription.pdf`; it writes the extracted proposal to Vercel Blob and reads Dynamics + SharePoint. Neither/ambiguity stops before download/Blob write and returns the server-listed picker data. | Medium | Exact two-path Reviewer Finder resolver; authenticated explicit `fileKey` is retained only for historical/ad-hoc staff analysis. This compatibility fallback does not alter external reviewer visibility or governed Initial Assessment input. |
| `/api/reviewer-finder/my-candidates` | GET, PATCH, DELETE | App | `requireAppAccess('reviewer-finder', 'reviewers')`; request-bound PATCH/DELETE: lead PD or superuser | Reads are org-visible and the PD filter is a UX default. Valid suggestion/request targets on PATCH and DELETE are ownership-authorized before service work. | Reads Dataverse; on PATCH updates supported `wmkf_appreviewersuggestion` lifecycle fields and `wmkf_potentialreviewer` identity/bibliometric fields. Wave 18 due-date values remain in GET DTOs but this generic route does not write them. | Medium | Global reviewer-person merge routes remain separately organization-wide. Per-reviewer deadline changes use the dedicated post-accept `/api/review-manager/review-due-extension` contract. |
| `/api/reviewer-finder/merge-candidates` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Org-visible Dataverse data; keeper/loser ids GUID-validated before becoming selectors | POST plan = read-only projected diff (`keeper`/`loser` display data, picker `fields`, sanitized reasons, `repointCount`, `collisionCount`; no suggestion/request/ETag internals). POST confirm = merges a PRE-ENGAGEMENT duplicate `wmkf_potentialreviewer` into the keeper (repoints/deletes `wmkf_appreviewersuggestion` rows, moves email, deactivates loser). Fail-closed block predicate refuses an engaged/promoted loser (409 `merge_blocked`). Step 3/4 Dataverse concurrency/duplicate-key failures return retryable 409 `merge_retryable_replan`; Step 6 email-transfer failures stay 500 `merge_email_move_failed` for orphan-email recovery. | Medium | Auth = same as my-candidates by design (S289): the block predicate, not a permission gate, restricts merge to the low-risk case. `docs/REVIEWER_MERGE_DESIGN.md`. |
| `/api/reviewer-finder/my-proposals` | GET | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Caller Azure email -> PD | Reads Dataverse (akoya_request + wmkf_appreviewersuggestion) | Low | Good identity-derived scope pattern. Postgres reader retired in W5 step 4 (2026-05-12). |
| `/api/reviewer-finder/save-candidates` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Trusted internal Dataverse writeback by request ID; automated identity/eligibility evidence is server-attested and the verified immutable surfaced roster key is re-read before writes | Reads `reviewer_find_roster` identity/address receipts and eligibility state (PG), then upserts `wmkf_potentialreviewer` + `wmkf_appreviewersuggestion` (DV; bibliometric fields land on the person post-S213 collapse, no separate sidecar) | Medium | Policy checkpoint for who may write candidate suggestions to a request. New roster-managed rows without a valid key-bound receipt reject as `identity_attestation_required`; when an exact-address adjudication intentionally invalidates the older token's contact digest, recovery is allowed only from the current request-scoped active/saved row whose server-owned identity digest still matches the submitted identity **and** whose exact address receipt is bound to that request/key/email. Identity receipt alone never authorizes contact. Only bare pre-roster payloads with neither an explicit roster candidate key nor an automated receipt retain the compatibility path. Direct official deceased evidence rejects pre-write with `rejectedIneligible`/`candidate_ineligible`; unknown/lookup failure does not invent an exclusion. The service also recomputes institution COI server-side from trusted request/applicant/PI context, performs `lookupReviewerIdentity` before the COI gate/upsert so CRM affiliation participates, rejects same-institution rows with `rejectedInstitutionCOI`, and fails closed 503 if complete applicant institution context cannot be loaded. |
| `/api/scheduled-emails` | GET | Profile | `requireAuthWithProfile` | Caller profile resolves server-side to its own Dataverse system-user ID; results are restricted to that PD | Reads that PD's `scheduled_email_messages` rows (PG), bounded to 200 | Medium | Returns editable subject/body plus exact recipient/sender context, action/version state, automation notice, and a placeholder-link preview. It does not expose a live grantee token. |
| `/api/scheduled-emails/[id]` | GET, PATCH | Profile | `requireAuthWithProfile` | GUID message ID plus server-derived caller system-user ID; every read/action requires exact PD ownership. PATCH actions are allowlisted and version-fenced. | Reads/writes one owned `scheduled_email_messages` row (PG). `edit` changes bounded subject/body only and clears any prior approval; `approve` releases an approval-required row for its scheduled send; `stop` prevents send; `send_now` leases the row (the PD's version-fenced action is itself the approval bypass), freshly rechecks Dataverse eligibility, mints the live token, creates/recovers and sends the Dynamics email as the PD, then finalizes `wmkf_granteedeliverable`. | High | Recipients, sender, workflow/source IDs, signature, and schedule are server-owned and cannot be edited through this route. Stale versions return 409. A secure token is minted only for the real send. The Dynamics activity identity is persisted before transport and correlation-recovered on ambiguous creation. |
| `/api/test-email` | POST | Superuser | `requireSuperuser` | Caller email | Sends email + creates email activity (DV `emails`) | Low | Superuser-only (tightened from `requireAppAccess('dynamics-explorer')`, eval #11). Privilege-verification test endpoint; consider disabling in prod if not needed. |
| `/api/upload-handler` | POST | Authenticated | `requireAuth` | Blob upload token | Mints Vercel Blob upload token (client uploads directly to Blob) | Medium | Any authenticated user can request a blob upload token; client-token flow shared by `FileUploaderSimple` + `SettingsModal` + `review-manager`. Blobs are `access: 'public'` — private-blob migration is the open residual of security-audit P2 (see `docs/archive/SECURITY_AUDIT_2026-05-21.md`). Legacy `/api/upload-file` retired 2026-05-21 (security audit A5) — `SettingsModal` migrated to this route. |
| `/api/user-preferences` | GET, POST, PUT, DELETE | Profile | `requireAuthWithProfile` | `profileId` | Reads/writes `wmkf_appuserpreferences` (DV) via dispatcher | Low | Intended user-owned settings. Reserved prompt-override and `email_automation` keys are rejected so their dedicated validation routes cannot be bypassed. |
| `/api/user-profiles` | GET, PATCH, DELETE | Profile / Superuser | `requireAuthWithProfile`; `?all=true` uses `getUserRole` | Own profile; all profiles for superuser | Reads/updates/archives `user_profiles` (PG) | Low | `POST` intentionally unsupported; profile creation flows through `/api/auth/link-profile`. |
| `/api/virtual-review-panel` | GET, POST | App | `requireAppAccess('virtual-review-panel')` | Request payload | Writes `panel_reviews`, `panel_review_items`, `api_usage_log` (PG); calls multi-LLM providers | Low | AI payload review. |
| `/api/webhooks/bill` | POST | Shared secret (HMAC) | `x-bill-sha-signature` HMAC-SHA256 verified against `BILL_WEBHOOK_SECRET` env var (constant-time comparison) | BILL.com subscription scope | Writes `bill_webhook_events` (PG) for dedup; logs raw payload (sandbox-discovery slice); **no Dataverse writes yet** — `vendor.updated` → PATCH lands in follow-up slice once payload shape is sandbox-confirmed | Medium | BILL.com webhook scaffold (slice 1 of reviewer-honorarium onboarding). Allowlisted in `proxy.js` (no NextAuth session). 256 KB body cap; missing-correlator deliveries return 200 (to keep BILL from auto-disabling subscription). See `docs/BILL_LIB_DESIGN.md` v3. |
| `/api/webhooks/vercel-log-drain` | POST | Shared secret (HMAC) | `x-vercel-signature` HMAC-SHA1 of raw body verified against `VERCEL_LOG_DRAIN_SECRET` env var (constant-time comparison; fail-closed 500 when unset outside dev) | Vercel Log Drain deliveries for this project | Writes selected entries to `operational_events` (PG) with `vercel:<log id>` dedup (`ON CONFLICT DO NOTHING`); summaries redacted via `log-redactor`, metadata allowlisted (never clientIp/userAgent/referer/JA3/JA4/headers/bodies) | Medium | Vercel Log Drain ingestion (durable operational observability). Allowlisted in `proxy.js` (no NextAuth session). 4 MB body cap (413), non-identity content-encoding rejected (415), batch selection/storage caps with loud dropped counts. Parse problems (malformed/skipped/invalid) return 200; Postgres STORAGE failures return 503 so Vercel redelivers (per-entry dedup makes retry safe; a sustained outage surfaces as an errored drain instead of silent evidence loss). Selection: error/fatal levels, 5xx, crashes (`statusCode -1`), failed structured dependency events. See `docs/OPERATIONAL_EVENTS_AND_LOG_DRAIN.md`. |
| `/api/bill/onboard-reviewer` | POST | Shared secret (HMAC) | `x-bill-internal-sig` HMAC-SHA256 over `v1:${timestamp}:${nonce}:${rawBody}` verified against `BILL_INTEGRATION_SECRET`; ±300s timestamp skew window; constant-time compare (see `lib/bill/internal-call-auth.js`) | Internal same-deployment callers / automation | Reads `contact` (Dataverse); writes `contact.wmkf_billcomid` + `akoya_isvendor` and `akoya_request.{wmkf_paymentnetworkidpni, wmkf_exisitngbillcomaccount}`; calls BILL.com vendor + network APIs via `lib/bill/` | Medium | BILL chunk 6. Always 200 on auth+validation success — outcome lives in `body.status` (onboarded / reused_existing / no_match / ambiguous_match / alert_only / deferred / bill_unavailable / partial / in_progress / resume_reconciled / invalid_input). Allowlisted in `proxy.js` (no NextAuth session). Gated by `BILL_ENABLED=true`; default false ships alert-only fallback. **No-BILL cycle (2026-07-01): `BILL_ONBOARDING_DEFERRED=true` short-circuits earlier to `deferred` (no BILL call, NO alert) — higher precedence than alert-only; the portal may still create the honorarium `akoya_request`, while payment is handled manually/offline.** **The reviewer acceptance drain path deliberately calls `onboardReviewer()` IN-PROCESS through `ensureHonorariumOnboarding()` instead of self-POSTing here (conscious design choice — see `docs/BILL_CHUNK_4_DESIGN.md` "In-process onboarding call"). Both entry paths share one validator, `validateOnboardInput()` in `onboard-reviewer-service.js`, so there is no validation drift between them.** Design: `docs/BILL_CHUNK_6_ENDPOINT_DESIGN.md`. |
| `/api/workbench/dashboard` | GET | App | `requireAppAccess('reviewers')` | Cycle discovery is organization-wide. Selected-cycle `scope=my` (default) filters to the email-resolved lead PD; `scope=all` drops it. `canManage` uses the session-minted Dynamics actor and is true only for lead PD or superuser. | Reads `akoya_request` + `wmkf_appreviewersuggestion` (DV, read-only); cycle discovery and selected-cycle reads share the triage visibility predicate. The picker includes set-aside-only cycles, reports active and set-aside counts separately, and fails 503 if its paginated scan is capped. Reviewer rollup reads active rows plus archived declines. | Low | The default cycle prefers the caller's newest non-set-aside assignment, then the newest organization-wide active cycle, then a set-aside-only cycle. `my-proposals.js` remains untouched/status-gated. |
| `/api/workbench/initial-assessment` | GET, POST | App | `requireAppAccess('reviewers')` | GET without filters returns artifact-backed cycle options; filtered GET accepts exactly one GUID-validated `requestId` or shape-validated `cycleCode`; POST accepts only a GUID `requestId`, then resolves title, institution, cycle, proposal source, active request folder, prompt/template contract, and acting user server-side | GET reads `wmkf_requestdocument` + the canonical `akoya_request.wmkf_CurrentInitialAssessment` pointer (DV), then performs a read-only Graph lookup by each file's stable drive/item identity. Request-specific GET also returns Ready/Board Ready Initial Assessment snapshot rows separately from the editable canonical artifact. Successful lookups replace response-only name/size/link/version/eTag/last-modified values and report `metadataStatus=current`; 404/error/incomplete-identity cases retain the registry snapshot with `missing`/`unavailable` rather than writing Dataverse or guessing by path. POST reads `akoya_request` + exactly one active `AI Materials/ProposalNarrative_{Request#}.pdf` from the positively resolved `akoya_request` SharePoint bucket, executes governed prompt `initial-assessment.generate` with the request GUID for AI-run lineage, writes DOCX bytes to the existing request folder under `Artifacts/Initial Assessment/`, and atomically writes stable Graph identity/provenance, prior-Ready supersession, and the request pointer in one ETag-guarded changeset. Missing/ambiguous exact AI input returns 409 before registry, AI, upload, or artifact writes. Exact retries use a deterministic alternate key and Ready rows never re-run or overwrite. Deployed recovery hashes normalized governed Word parts so SharePoint packaging metadata does not produce a false mismatch. | Medium | Governed Initial Assessment producer and shared Workbench/pilot-locator read contract. The exact Board-snapshot producer is excluded from editable Ready cardinality, supersession, and cycle discovery; unknown/lookalike producers retain ordinary fail-closed behavior. The locator is the narrow cycle discovery/Open-in-Word slice, not the full Editor Dashboard contract. Foundation Opportunity is never a prompt output and remains visibly staff-required in the versioned DOCX template. **[VERIFIED IN SOURCE 2026-08-16 via focused tests and read-only live Request `1002788` extraction]** the exact AI narrative resolves with non-empty text. Earlier production proofs using Phase I/Reviewer Materials remain historical producer-mechanics evidence. GET current-metadata read-through uses bounded concurrency and duplicate identity coalescing, and both consumers share current/missing/unavailable/unchecked display semantics. Native version restore and first-stage recycle recovery passed in a separate production-library audit. **[PRODUCTION-DEPLOYED + SIGNED-IN READ-SMOKED 2026-08-30]** restore/snapshot controls shipped through PR #138; Request `1003109` loaded the canonical artifact, snapshot control, and native version history. Their POST writes remain unexercised and owner-gated. |
| `/api/workbench/initial-assessment/versions` | GET | App | `requireAppAccess('reviewers')` (matches the sibling `initial-assessment` route) | Requires GUID-validated `requestId` **and** `expectedArtifactId`; rejects either missing/non-GUID value with 400 before any adapter call. `expectedArtifactId` is the artifact DTO id already displayed by the Workbench and binds this lazy read to the request pointer's exact canonical editable row. SharePoint `driveId`/`itemId` are **never** accepted from the caller | **Read-only.** Resolves `akoya_request.wmkf_CurrentInitialAssessment`, requires exactly one active non-snapshot Ready row, and compares its id with `expectedArtifactId`; invalid cardinality/pointer state or a regeneration race returns 409 and no version data. On a match, lists native SharePoint versions via Graph using **that row's** stable drive/item identity — a client cannot redirect the read at an arbitrary SharePoint item. Graph first reads the drive item's authoritative `publication.versionId`, then fetches **that one version directly, BEFORE paginating**, so no scan outcome can omit or discard the current editor. It follows version `@odata.nextLink` pages up to a **3-page list-scan cap** and only while the shared timeout leaves room for another page, skipping the page copy of the current version rather than listing it twice. The result is sorted current-first and capped at the default 20. Continuation degradation returns accumulated entries with honest `hasMore`; first-page and identity failures remain fail-loud. Returns `lastModifiedBy` display name only, never UPN/email. A stable-item/list 404 degrades to `missing`; no Dataverse write occurs on any path. | Medium | Human-edit audit surface for the exact governed Initial Assessment artifact staff are viewing. Pilot owner-decision 6 settled that SharePoint native version history — not a Dataverse mirror — is the record of who edited it. Fetched lazily on staff disclosure. Built S413 (2026-08-10); superusers gain write controls through the separate Production-deployed, owner-gated routes below. |
| `/api/workbench/initial-assessment/restore-version` | POST | Superuser | `requireAppAccess('reviewers')` + fresh `getUserRole`; `withDalContext` | Exact 8kb body: GUID `requestId`, GUID `expectedArtifactId`, bounded `targetVersionId`, and `expectedCurrentVersionId`. The request pointer and stable Graph drive/item identity are resolved server-side; every caller value is validation or a stale-view fence. A linked Dynamics staff identity is required for ordinary profile-backed sessions. | **[PRODUCTION-DEPLOYED + READ-SMOKED 2026-08-30; POST NOT PRODUCTION-EXERCISED.]** Downloads the selected historical version, verifies a stable canonical current identity immediately before invoking Graph `restoreVersion`, then requires a new current version whose bytes exactly match the target. ETag-conditionally refreshes the canonical Request Document's current Graph metadata/governed hash and rereads it. A retry after Graph success/Dataverse response loss reconciles only on byte-for-byte current-content agreement instead of restoring twice. | High | Native restore preserves every existing version. Microsoft Graph exposes no conditional-write header for `restoreVersion`, so the final-call race cannot be eliminated: an edit after the stable precheck is retained as an intermediate version and the selected version then becomes current. Production mutation requires explicit owner authorization. |
| `/api/workbench/initial-assessment/board-snapshot` | POST | Superuser | `requireAppAccess('reviewers')` + fresh `getUserRole`; `withDalContext` | Exact 8kb body: GUID `requestId`, GUID `expectedArtifactId`, and bounded `expectedCurrentVersionId`. Canonical source row/item/path and snapshot destination are server-derived. A linked Dynamics staff identity is required for ordinary profile-backed sessions. | **[PRODUCTION-DEPLOYED + READ-SMOKED 2026-08-30; POST NOT PRODUCTION-EXERCISED.]** Reads one stable exact current source version, creates/reclaims one deterministic Request Document row, and create-only uploads the exact selected source buffer into a distinct `Board Milestones` item. Post-ingestion readback hashes normalized governed Word content rather than requiring ZIP-container byte identity, so legitimate SharePoint repackaging is accepted while changed or invalid content fails closed. Distinct item identity, Ready/Board Ready lifecycle, source row/version/hash, and post-write reread are required before success. Wave 24 now records immutable explicit row-origin actor/time for a newly created snapshot; standard `createdby` is not accepted as human attribution. The first Production snapshot remains separately owner-gated and will supply the natural controlled proof. Interrupted uploads recover only from a deterministic governed-equivalent file; only an item uploaded by the owned attempt can enter orphan cleanup. The canonical request pointer is never moved. | High | Implements the owner’s 2026-08-10 copy decision. Exact snapshot producer rows are excluded from editable activation/supersession/cycle discovery; unknown/lookalike producers remain ordinary rows and fail closed. Exact Ready retries return the same row/item. First Production snapshot requires explicit owner authorization. |
| `/api/workbench/pre-site-visit` | GET, POST | App | `requireAppAccess('reviewers')`; both methods resolve `getUserRole` before deciding whether to include guarded-reopen audit details | GET accepts only a GUID-validated `requestId` query; POST's exact-body allowlist accepts only GUID-validated `requestId`. Request metadata, proposal path, prompt name, model, template, and destination are server-resolved. Reopen attempt history and nested artifact `correction` details are emitted only to superusers; other authorized readers receive the ordinary artifact/status fields on both methods. | **POST [VERIFIED IN PRODUCTION 2026-08-17]:** reads `akoya_request`, applicant `account`, Co-PI junction, the exact active Proposal Narrative, current governed prompt, and Request Document state; it never reads the bibliography. A new generation writes the append-only `wmkf_ai_run`, eight Pre-Site fields, immutable snapshots, provenance, stable SharePoint identity, Ready/superseded state, and `akoya_request.wmkf_CurrentPreSiteVisit`. Request `1002379` proved generation and exact reuse. **GET [DEPLOYED TO PRODUCTION 2026-08-17; SIGNED-IN FEATURE SMOKE OPEN]:** reads only the request pointer and Pre-Site Request Document rows, returns separate current/pending artifact DTOs, and performs no claim/model/render/upload/write. | Medium | The tab loads GET status on entry and, after a lost POST response or 202, performs bounded GET polling; it never automatically repeats POST. Current Ready pointers fail closed unless they resolve to a same-request, non-superseded Ready Word row. Production template v2 added Recommendation-cell padding and created Ready artifact `76a0d4b2-8b9a-f111-b8db-7ced8d3d15a6`; Word Online exposed a width-sensitive Recommendation-label alignment defect. **[INFERRED FROM SCREENSHOT + OOXML WIDTH]** Implicit wrapping was the remaining layout variable. **[DEPLOYED TO PRODUCTION 2026-08-17; SIGNED-IN FEATURE SMOKE OPEN]** template v3 makes that label explicitly non-wrapping under a new generation identity. The public sign-in/auth boundary passed after deployment; signed-in status loading, compact actions/download, and Word Online v3 proof remain open. |
| `/api/workbench/pre-site-visit/recipient-options` | GET | App | `requireAppAccess('reviewers')`; `withDalContext` | Staff-shared curated distribution directory. Returns only currently available configured recipients as category, display name, and email; stable source IDs and unavailable entries are not exposed. | Reads active `user_profiles` (PG), enabled `systemusers`, configured `contacts`, and `site_visit.distribution_recipient_directory` in `wmkf_appsystemsettings` (DV). No writes. | Medium | **[VERIFIED IN SOURCE 2026-08-29.]** The composer loads this route only when the picker opens. A directory failure does not disable manual To/Cc entry or preview creation. This is separate from `/api/workbench/site-visit/recipients`, which remains the compatibility directory for saved Site Visit attendee references. |
| `/api/workbench/pre-site-visit/start-site-visit` | POST | App | `requireAppAccess('reviewers')` | Exact-body allowlist accepts only GUID-validated `requestId` and `expectedArtifactId`. The expected artifact is a stale-screen guard; the server independently resolves `akoya_request.wmkf_CurrentPreSiteVisit` and never accepts drive/item/version authority from the caller. | **[PRODUCTION-PROVED 2026-08-21.]** Reads the current Ready Pre-Site row, then reads the same stable SharePoint drive/item metadata before and after downloading the Word bytes. A stable exact publication version is required. One ETag-conditional Dataverse PATCH changes lifecycle Draft→Review, records the governed content hash plus handoff version/time in `wmkf_milestone*`, and refreshes the row's current Graph metadata. It never copies or mutates SharePoint bytes. A completed matching Review state is idempotent; a stale artifact, file/version race, unknown lifecycle, incomplete prior milestone, or ETag conflict fails closed. The authenticated session's Dynamics system-user id is passed to the write service, but the Request Document census proved fallback makes standard `modifiedby` the service principal; Wave 24 now writes explicit `MilestoneCreatedBy` on a new transition, with controlled Production proof pending the next natural handoff. Signed-in Request `1002379` completed the pre-Wave-24 controlled transition after exact owner approval; a fresh authenticated load retained the same SharePoint Edit/Download identity, returned Review and the handoff timestamp, and showed regeneration locked. | Medium | This is the Pre-Site→Site Visit workspace handoff, not a Board distribution snapshot. Once Review is current, the Pre-Site generator rejects regeneration before source/prompt/AI/claim/render/upload work. The merged Staff Deliberations tab (S466) continues to Edit/Download the same SharePoint item. |
| `/api/workbench/final-writeup` | GET, POST | App + **hard manage gate on POST** | `requireAppAccess('reviewers')` + fresh `getUserRole`; `withDalContext`; literal-on `FINAL_WRITEUP_SCHEMA_READY`. POST service requires session-derived Dynamics system-user identity and either superuser status or exact equality with the request's non-null lead-PD lookup; missing lead PD is superuser-only. | GET accepts only GUID `requestId`. POST exact body accepts only GUID `requestId` and `expectedArtifactId`; the latter is a stale-screen fence. Request pointer, lead PD, Final pointer, source row, Graph drive/item/version, source hash, transition actor, and time are server-derived. | **[PRODUCTION-PROVED 2026-08-30 PT / 2026-08-31 UTC; WAVE 22 EXACT, FLAG ON.]** POST verifies the current Ready/Review Pre-Site row and one stable current Word version around governed DOCX hashing; creates/reclaims one deterministic Final row that carries the same stable drive/item; then atomically moves the source lifecycle to Final, readies the Final row in Review, records explicit group-review actor/time, and sets `wmkf_CurrentFinalWriteup` under source/Final/request ETags while retaining `wmkf_CurrentPreSiteVisit`. Request `1002788` proved the explicit actor/time, one current Final pointer, retained Pre-Site pointer, and unchanged SharePoint item/version/hash/file cardinality. Exact committed retry performs no Graph or write work; live claims return 202; changed files, unknown states, pointer mismatches, claim loss, and partial/ambiguous changesets fail closed or reconcile from durable state. No Graph upload/copy exists. | High | Slice 1 group-review handoff and separate-tab Word launch. Personal acknowledgement is isolated in the sibling route below; dashboard, PC backup, leadership transition, backward-stage UI, and board package remain absent. Production deployment `dpl_7kzQ1v7XGtyNx4Fady2JxMrTxQEJ` is Ready on `ebb147bb`; the neutral acknowledgement role is live while persona-specific rollout remains separately gated. |
| `/api/workbench/final-writeup/acknowledgement` | GET, POST | App | `requireAppAccess('reviewers')`; `withDalContext`; service-level literal-on `FINAL_WRITEUP_ACKNOWLEDGEMENT_SCHEMA_READY` | GET accepts only GUID `requestId`. POST exact body accepts only GUID `requestId` and `expectedFinalArtifactId`; the latter fences the exact current Final row. Reviewer identity is always the session-derived Dynamics system-user ID; reviewer ID, lead PD, Final pointer, stable file identity, and Graph publication metadata are never accepted from the client. | **[PRODUCTION-PROVED 2026-08-31.]** GET reads the current Final, enabled reviewer, current Graph publication, all positive acknowledgement initials, and the caller's version-aware state. POST rejects responsible-PD self-review, then creates or conditionally replaces one `wmkf_finalwriteupreviewacknowledgement` row under the active Final-document + reviewer alternate key; same-version repeat preserves the original timestamp and ambiguous writes are reread. Writes request no-fallback actor impersonation. | High | Wave 23 is exact/Active and Production readiness is exact `on` in Ready deployment `dpl_B9k3AprnYp5ExpkqpT3dUxCUZqWo`. Signed-in Request `1002788` first proved retained Word access and responsible-PD exclusion. An eligible colleague's pre-role POST then exposed missing acknowledgement Create without partial persistence. After the dedicated role became effective for all 11 audience members, the colleague retry succeeded and appeared in review history; independent readback proved exactly one complete acknowledgement row. |
| `/api/workbench/final-writeups` | GET | App; superuser-only matrix branch | `requireAppAccess('reviewers')` + fresh `getUserRole`; `withDalContext`; service-level literal-on `FINAL_WRITEUP_ACKNOWLEDGEMENT_SCHEMA_READY` | Accepts no query parameters or one GUID `requestId`; all request rows, broad Grant Program lookups, current-Final pointers, reviewer identity, lead-PD relationship, acknowledgement rows, stable file identities, publication metadata, stage, queues, expected-reviewer roster, and previous/next navigation are server-derived. The caller identity is only the session-derived Dynamics system-user ID. The index response includes the complete matrix only when the fresh Postgres role is `superuser`; focused and ordinary responses return `coordinatorMatrix:null`. | **[PRODUCTION-LIVE V2 / PERSONAS DISABLED 2026-09-01 UTC.]** Commit `84bf465b` and Ready deployment `dpl_41SybgPYfJXGarf7UqcMGCLMy4KS` read the published v2 setting while preserving the neutral matrix. The route caps current-Final rows at 100, batch-reads bounded exact state, and refreshes stable Graph identities with concurrency four. The matrix groups by broad `wmkf_grantprogram`; current stable references resolve to live names, stale v2 references are omitted with warnings, and unconfigured programs remain explicit. Ordinary/focused responses receive neither matrix nor staffing configuration. It performs no write. | Medium | Signed-in post-migration Request `1002788` smoke proved the nine-column Research matrix and all prior states/actions still render correctly. Production v2 also stores a six-person Southern California audience, although the current request set supplied no Southern California group to render. Persona lenses remain disabled pending representative Word-access proof; no team provisioning is required. The matrix carries no approval, compliance, deadline, denominator, or reviewer-order semantics. |
| `/api/workbench/site-visit/logistics` | GET, PATCH | App | `requireAppAccess('reviewers')`; `withDalContext`; literal-on `SITE_VISIT_LOGISTICS_SCHEMA_READY`; PATCH additionally requires the session-derived Dynamics system-user id | GET accepts only a GUID `requestId`. PATCH uses an exact field allowlist; request/activity IDs are GUID-validated, organizer must be a server-resolved WMKF staff profile, Board/Consultant recipients resolve by immutable `expertise_roster.id`, and manual recipients are syntax-bounded. The server independently requires the current Pre-Site artifact to be Ready/Review and re-resolves the active Site Visit. | **[PRODUCTION-PROVED 2026-08-25.]** Wave 21 is exact in sandbox and Production and the literal-on flag is active in Preview/Production. GET reads the current Pre-Site stage, the single active `wmkf_sitevisit`, its ActivityParty rows, and eligible governed Request Document material links. More than one active Site Visit fails closed. PATCH creates one request-bound custom Activity; later field-only edits use ETag-fenced parent PATCH. Because Dataverse rejects direct ActivityParty create/update/delete (`0x80040800`), attendee-role edits use an atomically proved ETag-fenced delete plus nested-party create of the same Activity GUID. It persists standard UTC schedule fields and additive format/IANA-zone/location/immutable-reference-map fields; it never upserts by caller-selected identity. Read-only Production readback for Request `1002379` confirmed Site Visit `11b41d73-02a0-f111-b8dc-6045bd018a07`, exact Request binding, active state, persisted logistics fields, and five ActivityParty rows. | High | Local wall times are resolved against the named IANA zone; nonexistent DST times fail. Since S466 (2026-08-28) the in-app logistics editor is removed: GET's only UI caller is the headless `useSiteVisitContext` read and PATCH currently has no in-app UI caller. The attendee reference JSON is server-owned identity metadata, while ActivityParty remains the actual organizer/attendee representation; a visit scheduled directly in Dynamics (no map) projects read-only refs from its ActivityParty emails. Tracked sandbox proof created/replaced/deleted one marked sentinel and independently confirmed absence. |
| `/api/workbench/site-visit/recipients` | GET | App | `requireAppAccess('reviewers')`; `withDalContext` | Staff-shared directory only. Returns active WMKF profiles that resolve uniquely to an enabled Dataverse system user plus active Board/Consultant roster rows and their maintained preferred email. Dataverse system-user IDs are removed from the response. | **[PRODUCTION-PROVED 2026-08-25.]** Reads active `user_profiles`, enabled Dataverse `systemusers`, and active Board/Consultant `expertise_roster` rows. No writes. The signed-in logistics flow loaded and used the directory for Request `1002379`. | Medium | Staff identity requires exact mapped ID or a unique same-email join. External identity remains the roster row ID; names are never used as lookup keys. |
| `/api/workbench/pre-site-visit/reopen` | POST | App + Superuser | `requireAppAccess('reviewers')` + fresh `getUserRole(profileId) === 'superuser'`; linked callers also require a session-derived Dynamics system-user id; literal-on `GUARDED_REOPEN_SCHEMA_READY` interlock | Exact six-field body: GUID `requestId`, expected current artifact GUID, client operation UUID, exact typed request number, allowlisted reason code, and bounded reason note. All request/pointer/Graph/source/downstream authority is re-resolved server-side. | **PRODUCTION-PROVED 2026-08-23.** Wave 20 readback is 3 exact/0 divergent, the Production flag is literal `on`, and merge `af986d92` is Ready in deployment `dpl_BbtmRghhSYa7EPiQkWxsmdkgRozp`. After exact owner approval, signed-in Request `1002379` preserved source `76a0d4b2-8b9a-f111-b8db-7ced8d3d15a6`, created Ready/Draft successor `888982b6-0a9f-f111-b8dc-7ced8d3d15a6` and one distinct SharePoint copy, atomically moved the request pointer, and left no Final pointer or cleanup work. Exact unchanged retry returned that same row/item with `reused: true`; postcheck found one cycle row and identical governed source/successor bytes. The route reads the request pointer and Pre-Site rows; verifies the current Ready/Review row's exact milestone bytes through stable Graph identity; rejects a current Final, any non-distribution derived Request Document, or a different editable generation with a live lease. Frozen distribution snapshots are preserved informational evidence and are excluded from both downstream and competing-generation blockers. Creates/reclaims one successor registry row under the generation-key alternate key, copies exact bytes to one deterministic new SharePoint filename, persists stable target identity, then atomically supersedes the source, marks the successor Ready/Draft, and moves `wmkf_CurrentPreSiteVisit` under three ETags. It passes the session actor into the write service and stores bounded reopen cycle/reason fields plus lineage, but standard `createdby` may still name the service principal after fallback; Wave 24 now records immutable explicit origin actor/time, with controlled proof pending the next naturally needed reopen. Exact unchanged retry and post-upload recovery reuse the same row/item; Failed attempts remain audit evidence but do not block a distinct later operation. An expired competing reopen is first failed, and any retained copy is recorded as stable-identity cleanup work. | High | Schema-first promotion and durable mutation proof are complete. The UI control is superuser-only; server enforcement is authoritative. Any later reopen or Site Visit handoff remains a separately approval-gated business mutation. |
| `/api/workbench/pre-site-visit/distribution/prepare` | POST | App | `requireAppAccess('reviewers')`; sender email and required Dynamics system-user ID come only from the authenticated session | Exact-body allowlist accepts request/current-artifact/client-operation GUIDs, attachment mode `docx`/`pdf`/`both`, known-recipient To/Cc text, subject, plain-text body, optional saved Site Visit GUID/calendar selection, and at most 25 governed material artifact GUIDs. Server independently resolves the current Ready/Review Word pointer, Site Visit ETag/content, and material link identities; drive/item/version, snapshot paths, hashes, sender, actor, and preview identity are never client-selected. | **[PRODUCTION-PROVED BASE AND CALENDAR/MATERIAL EXTENSION 2026-08-25.]** Migration 035 is applied/read back. (PG) creates/resumes one `pre_site_distribution_attempts` row and persists exact source version/hash before later steps. SharePoint + (DV) Request Document retain deterministic Word/PDF snapshots. The extended preview hash also binds server-resolved material links and the exact informational calendar bytes, Site Visit ID/ETag, and bounded schedule/organizer snapshot. No email activity is created and nothing is sent. | High | Request `1002379` operation `f497643a-2e9e-4032-a323-1e40874d16f1` proved a calendar-enabled, one-material preview and send. The calendar is `METHOD:PUBLISH`: add-to-calendar only, with no RSVP or reliable update/cancellation semantics. Any changed compose field, attachment/link selection, or Site Visit ETag requires a new preview. |
| `/api/workbench/pre-site-visit/distribution/send` | POST | App | `requireAppAccess('reviewers')`; exact sender/actor must match the prepared attempt; every non-sent attempt requires literal `DYNAMICS_IMPERSONATION_ENABLED=true` before lease claim, while an exact sent retry remains a read-only receipt | Exact-body allowlist accepts only request GUID, operation UUID, and 64-hex preview hash. Server reloads every recipient/body/link/attachment identity from the prepared ledger. Under the lease it re-resolves the current request pointer/source version, every linked material, and the Site Visit ETag before Dynamics work and again immediately before transport. | **[PRODUCTION-PROVED BASE AND CALENDAR/MATERIAL EXTENSION 2026-08-25.]** Reads frozen SharePoint bytes and verifies hashes; deterministically rebuilds and verifies calendar bytes from the bounded stored Site Visit snapshot. (DV) creates or uniquely recovers one correlation-keyed email activity; (PG) persists that ID before exact activity-content assertions, then adds/reconciles each selected DOCX/PDF/calendar attachment by deterministic filename and records each attachment receipt. Send intent, lease renewal, repeated freshness checks, status reconciliation, and exact sent retry retain the proved base behavior. | High | Request `1002379` operation `f497643a-2e9e-4032-a323-1e40874d16f1` reached `sent` with calendar enabled, one material, the saved Site Visit ID, and no final error. `sent` asserts Dynamics transport acceptance, not inbox/calendar-client delivery. |
| `/api/workbench/pre-site-visit/distribution/history` | GET | App | `requireAppAccess('reviewers')`; Dataverse/Graph current-source comparison runs inside `withDalContext` | Requires a GUID requestId query. Staff-shared Workbench scope is intentional; no caller-supplied actor or recipient selector is accepted. | **[PRODUCTION-PROVED 2026-08-24.]** Reads (PG) `pre_site_distribution_attempts` for that request, then read-only current Pre-Site pointer/Graph version state, and returns exact recipients, compose text, selected snapshot links/identities, source freshness, Dynamics activity/transport receipt, retry state, and bounded last error. No writes. | Medium | The signed-in Workbench surfaced the `1002379` row as Accepted by Dynamics with the PDF selection and Dynamics ID. History is visible to the same authorized Workbench staff who can prepare/send. It reports transport acceptance separately from inbox delivery and marks older frozen previews after the working Word source advances. |
| `/api/workbench/resolve-request` | GET | App | `requireAppAccess('reviewers')` | Org-open read by request number | Reads `akoya_request` (DV, read-only); `akoya_requestnum` (string) → GUID resolve; also returns Proposal-tab `proposalInfo` (PI/co-PIs/abstract/`akoya_request`/`akoya_expenses`) + `aiContent` (existing `wmkf_ai_*` fields) + Status-tab `statusClass` (derived) | Low | Number→GUID lookup + Proposal-tab top/AI data (Phase 1); `statusClass` for the Status tab (S260). |
| `/api/workbench/reviewer-rollup` | GET | App | `requireAppAccess('reviewers')` | `requestId` GUID-validated before it becomes a `_wmkf_request_value` OData selector | Reads active and archived-declined `wmkf_appreviewersuggestion` rows (DV, read-only) via the shared lightweight rollup (counts only, no person/researcher fan-out); returns `{counts, needed, workRemaining, hint}`. Existing lifecycle fields inside `counts` remain active-pool-only except `declined`; nested `counts.progress` is the exclusive accepted/pending/declined/uninvited display rollup. | Low | Per-request reviewer-stage rollup for the Overview tab (Group A, S260) and shared dashboard cards. Shared logic in `lib/services/reviewer-rollup.js`; cheaper than `/api/review-manager/reviewers`. |
| `/api/workbench/decline-referrals` | GET, PATCH | App | `requireAppAccess('reviewer-finder', 'reviewers')` | GET `requestId`; PATCH GUID-validates `requestId` + `suggestionId`, requires the GET-issued exact-content `referralVersion`, and accepts only an optional integer `referralIndex` from 0–3. Before PATCH, the adapter freshly verifies that the source belongs to the request, is still declined, and has the same underlying referral payload; the exact parsed index must exist for structured dismissal, while an omitted index is legacy-only. | GET reads all request `wmkf_appreviewersuggestion` rows, resolves linked people, expands structured envelopes, and omits a structured person when its persisted prefix mask marks that index dismissed or an exact-name `referred` candidate (plus exact email when supplied) is selected/engaged; resolved legacy notes are also omitted. PATCH ETag-conditionally marks one exact target resolved and retries one 412 only while the underlying payload remains byte-identical (mask-only races can merge); changed content returns 409. It preserves the structured JSON payload or legacy prose and never mutates `wmkf_sources`. No third-party calls. | Low | Staff-side decline-referral inbox (S349). Current rows surface published name, institution, and email separately with **Dismiss** beside **Add as candidate**; failed/ambiguous adds and promotion/restore remedies remain visible. Legacy prose stays display-only and exposes **Dismiss resolved note** after staff handles its people separately only when the preserved marker fits the 2,000-character memo. Overlength legacy values and malformed/future reserved envelopes remain visible but non-dismissible for administrator repair. Independent of the review-manager GET so referrals surface even before any reviewer has accepted. |
| `/api/workbench/proposal-documents` | GET | App | `requireAppAccess('reviewers')` | `requestId` GUID-validated; scope derived only from the resolved record (`rec.akoya_requestid`), server resolves request number + cycle (not trusted from client) | Reads `akoya_request` (DV) + SharePoint via Graph (recursive list, read-only); no writes; per-bucket errors sanitized (logged server-side) | Low | Proposal-tab document listing: Reviewer Materials reuses the external portal's exact `Reviewer Materials/Proposal_{Request#}.pdf` predicate; other files beside it remain excluded. Phase I is slot-matched plus unmatched files; Phase II returns every file under the exact folder; canonical AI Materials remain exact-name matched. The Phase I filename bridge remains in `shared/config/workbenchProposalDocuments.js`. |
| `/api/workbench/download-proposal-document` | GET | App | `requireAppAccess('reviewers')` | Two-stage: (1) fast fail — folder top-level segment's GUID suffix MUST match `requestId`; (2) authoritative — requested `(library, folder, filename)` MUST be in the request's re-derived Proposal-tab Reviewer Materials, Phase I, Phase II, or canonical AI Materials document set, never merely present under the raw client path. Reviewer Materials membership is exact-name/request-bound through the external portal's shared predicate. Library allowlisted by `GraphService.getDriveId`; traversal rejected by `validatePath`. `disposition=inline` honored ONLY for a non-executable safe-mime allowlist (pdf/png/jpeg/gif/text) — never HTML/SVG; else `attachment`; `nosniff` always set | Reads SharePoint (Graph download, no write) | Medium | Proposal-tab scoped download proxy; mirrors `dynamics-explorer/download-document` but adds exact listed-document membership scoping + safe inline view. |
| `/api/workbench/applicant-reviewers` | GET | App | `requireAppAccess('reviewers')` | Org-open, per-request scoped by `?requestId=` | Reads `akoya_request` (5 `wmkf_potentialreviewer1..5` slots + free-text `wmkf_excludedreviewers`); idempotently writes `wmkf_appreviewersuggestion` `disposition=recommended`, `wmkf_selected=false` rows (race-safe via `ensureApplicantRecommended`); one hardened Claude call (`reviewer-exclusion-parser.js`, A7-wrapped untrusted free-text) to parse excluded names. **No structured `disposition=excluded` rows written** (S210 option B) — excluded names returned for the search soft-block only; nothing global touched. | Medium | Request Workbench applicant-reviewer ingestion. Lazy on Find-tab open. Recommended rows appear in Find for PD review but do not enter the candidate pool until explicit promotion; excluded → soft-block names. |
| `/api/workbench/promote-applicant-reviewer` | POST | App | `requireAppAccess('reviewers')` | Org-open, per-request scoped by GUID-validated `requestId` + `suggestionId`; verifies Recommended disposition and requires the canonical request/`suggestion:<id>` roster row | Reads suggestion, roster receipt, exact linked reviewer person, email ownership, and request COI context. Missing identity/address evidence, deceased status, pending contradiction, duplicate ownership, or unavailable authoritative reads block before selection. An exact server receipt may atomically write email + `staff_verified` + versioned trust bundle under the person ETag; only after canonical re-read does the route select the suggestion and finalize the roster. No client person id is trusted. | Medium | Applicant promotion now shares the exact-address contract with ordinary save. Contact creation/linking is out of scope; invitation sending remains non-promoting. |
| `/api/workbench/reviewer-address-trust` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Per-request GUID scope plus a bounded server roster candidate key or a GUID suggestion re-read and verified against the request; closed action/target schema and 64kb cap. It rejects client `stage`, receipt, source/provider, version, person-id, authority, and mode fields. Structural retry accepts only a server-allowlisted reason code and re-reads the current record; the code itself grants no authority. | [PRODUCTION-LIVE 2026-08-20 in Ready deployment `dpl_9yZ9xTHqfNgLcbZxJDekZkAjqpPS`; source/tests plus signed-in read-only card smoke, with the earlier exact-choice modal smoke through Cancel] `verify_person_and_address` re-resolves canonical roster identity, the fresh pending stored/found tuple, and current person ETag before accepting `staff_address_choice`; it ETag-writes the exact selected email plus non-null `keep_stored`/`use_found` trust resolution, then CAS-projects the matching roster receipt/choice. `retry_check` freshly verifies inactive, duplicate-owner, or Contact-linkage corrections before clearing structural state. Canonical conflict success best-effort auto-resolves only server-correlated legacy alerts using their persisted keys; closeout failure does not roll back the domain decision. `create_repair_request` remains a bounded terminal fallback and deduplicates active/acknowledged rows. Never creates or links Contacts. The live write branch was not exercised during the read-only smoke. | Medium | Routine stored/found choices and structural record fixes are staff Workbench/AkoyaGO actions, not Admin actions. Client labels, submitted codes, addresses outside the fresh pair, and alert keys grant no authority. A lost CAS reports no contact/address success; retained repair alerts never unblock promotion. |
| `/api/workbench/export-candidates` | POST | App | `requireAppAccess('reviewers')` | Org-open, per-request scoped by body `requestId`; `requestId` GUID-validated before it becomes an `akoya_request` record-id selector; `candidates` array capped (max 500); per-cell strings clamped | Reads one `akoya_request` (DV, read-only) for header metadata (number/institution/PI), fetched server-side — never trusted from the client; builds a two-sheet ExcelJS workbook (Request Info + Candidates) from the client-supplied slim candidate DTOs and streams it back as an `.xlsx` attachment. No writes. Whole body in `bypassDynamicsRestrictions('workbench-export-candidates')`; errors sanitized. | Low | Find-tab "Export to Excel" for the selected candidates. Derived columns (Source/Why/Conflicts/ORCID/Scholar) formatted in `lib/services/reviewer-candidate-export.js`. |
| `/api/workbench/manual-reviewer` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Org-open, per-request scoped by body `requestId`; GUID-validates before Dataverse use; 64kb body cap | Reads `akoya_request`; creates or fill-only reuses `wmkf_potentialreviewer` by email (name-only creates a new person); stamps staff-entered email provenance as `wmkf_emailsource=manual`; idempotently writes/reselects a non-excluded `wmkf_appreviewersuggestion` row via `ensureStaffManualCandidate`, unioning `staff_manual` into `wmkf_sources`. A staff-provided/looked-up ORCID is persisted **fill-only** (`wmkf_orcid`/`wmkf_orcidurl` via `upsertByPotentialReviewer` — never overwrites a resolver-sourced or reviewer-attested value, never touches `wmkf_identitystatus`). No candidate enrichment. | Medium | Workbench manual reviewer add Phase 1. Excluded rows fail closed with 409; manual emails flow into Slice-G LOW-confidence invite confirmation. |
| `/api/workbench/reviewer-lookup` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Org-open; not request-scoped; validates name/email/ORCID; 16kb body cap | Read-only Dataverse lookup across `wmkf_potentialreviewer` and `contact`: top-2 ambiguity-aware ORCID/email checks plus structured name search. No writes and no third-party calls. Returns `confident`, `candidates`, `conflict`, or `none`; ambiguous same-key rows return candidates, while cross-store splits/reverse-link collisions/email mismatches return conflict. | Low | Manual reviewer add dedup preflight and confirmation UI. |
| `/api/workbench/orcid-lookup` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Org-open; not request-scoped (name/affiliation lookup only); 16kb body cap | **No Dataverse read/write.** Calls ORCID public search via `ORCIDService.findContact` (name-match-gated, affiliation-narrowed; abstains on ambiguous/none rather than guess a namesake). Read-only convenience for the manual-add form; any chosen ORCID is persisted only on submit (`manual-reviewer.js`, fill-only). | Low | ORCID iD lookup for manual reviewer add. Degrades to `{found:false}` when `ORCID_CLIENT_ID/SECRET` absent. |
| `/api/workbench/enrich-recommended` | POST (SSE) | App | `requireAppAccess('reviewers')` | Org-open, per-request scoped by body `requestId`; whole body in `bypassDynamicsRestrictions('workbench-enrich-recommended')` | Reads the request's Recommended suggestions + each person; resolves proposal context; runs PubMed verify, COI, contact/bibliometrics, and identity-bound first-party eligibility evidence. Ordinary rows receive the existing id-keyed Dataverse writeback; direct-deceased rows skip all person/COI writeback and are recorded only as request-scoped roster `ineligible` evidence. | Medium | Auto-runs applicant-recommended reviewers through enrichment. Direct official deceased evidence is visible/deduped but never selectable or promoted; unknown/provider failure preserves prior behavior. |
| `/api/workbench/reviewer-stage-refresh` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | [VERIFIED via source/tests] Closed 16kb target-only body: GUID `requestId`, canonical `candidateKey`, allowlisted executable `stage`, and server-issued `expectedUpdatedAt`. It rejects client evidence, receipts, dependencies, authority, source/provider, proposal/version, and plan fields; the server re-reads the exact roster row and any needed Dataverse/Graph authority. | Dispatches exactly one candidate/stage: `applicant_anchor`, `identity`, `institution_domains`, `institution_coi`, `coauthor_coi`, `eligibility`, `contact`, or `roster_persistence`. Start/complete/fail/recovery use an exact roster-token CAS and **candidate-wide** lease. A live other-stage owner returns `refresh_in_progress`; an expired owner is persisted as a retryable recovery outcome. Completion atomically applies the named projected receipt and `roster_persistence` when its upstream set is complete. `address_trust` is rejected here for the structured address route. | Medium | Explicit staff retry with no name lookup or batch repair. Proposal-dependent identity/coauthor work has Graph metadata pre/post authority checks; changed authority yields no current receipt. |
| `/api/workbench/reviewer-reconcile` | POST | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Closed 256kb body: GUID `requestId` plus an optional bounded (at the stored active-roster cap of 300) unique list of canonical stored roster keys. This admits every valid continuation (a canonical key may be 560 characters) but rejects candidate blobs, stage receipts, provider input, authority claims, and all unknown fields. It re-reads active request-scoped roster rows server-side. | Runs a bounded, sequential recovery pass through existing one-stage refreshes. The durable coordination remains each row's exact `updated_at` CAS and candidate-wide lease; repeats re-plan from current receipts. The in-memory request context amortizes initial request/proposal metadata and exact-version proposal analysis, but every producer still performs a fresh pre-write authority comparison. For a legacy row with a verified ORCID but no stored person authority, this private reconciler may bind one exact, active, name-consistent Dataverse reviewer match using its current person ID + ETag and then project deterministic `contact`/`address_trust` evidence only for ready, quick-check, staff-verified, or not-applicable address outcomes. Ambiguous, conflicting, inactive, research-only, or concurrently changed matches remain staff action. An applicant suggestion with no linked reviewer person likewise returns terminal `action_required`; no person anchor is fabricated. No cold search, candidate creation/selection, promotion, invitation, or email send occurs. | Medium | Explicit operational recovery / promotion-preflight primitive. The browser sends exact server-rendered active roster keys and requires a one-for-one response before it reloads the parent snapshot; the response and scrubbed server log expose bounded aggregate outcome/code counts, including rejected rows, but no candidate PII. Active-producing roster writes await cap enforcement before success. Only retryable or work-budget-exhausted rows receive continuation keys; terminal action-required, blocked, rejected, and policy/COI rows do not. An observed over-cap roster is a fail-closed integrity fault (`roster_active_cap_exceeded`), not a continuation: reload the roster and contact an administrator if it persists; do not rerun discovery. |
| `/api/workbench/reviewer-roster` | GET / POST / PATCH | App | `requireAppAccess('reviewer-finder', 'reviewers')` | Org-open, per-request scoped by GUID-validated `requestId` (query on GET, body on POST/PATCH); GET authenticates and validates scope before dispatching its allowlisted mode. `mode=cached` is Postgres-only; `mode=reconciled` requires one opaque snapshot token and runs engagement plus request/input/Graph-metadata reads in trusted DAL context; missing mode retains the temporary compatibility response. It accepts no file key or client authority. Repeated/unknown modes and invalid/repeated reconciliation tokens fail closed. 2mb body cap; POST caps candidates ≤100; browser eligibility fields are cleared unless a request/candidate/evidence-bound server receipt verifies them; browser POST rejects applicant/suggestion-shaped rows, applicant PATCH mutations must bind an existing canonical server row, and browser-authored POST/exclude/saved blobs cannot supply staff-confirmation authority. [VERIFIED via source/tests] `confirm_identity` is additionally a strict server action: it re-resolves the authoritative applicant/non-applicant row and derives the canonical `candidateKey`; browser data can only provide bounded manual contact details, never canonical identity. `update_contact_draft` requires an exact active roster `candidateKey` and accepts only bounded `website`/`affiliation` strings or null; `email` and unknown fields fail closed. | `reviewer_find_roster` is the durable Postgres working roster. GET also reads open `reviewer_address_repair_requested` rows from `system_alerts`, returning only active/acknowledged alerts whose server-owned request/candidate keys match visible roster rows; if that supplemental read fails, the roster still returns with `repairRequestsUnavailable=true`, preserving Confirm identity while suppressing duplicate repair creation. GET `mode=cached` returns its render DTO plus an opaque correlation/concurrency snapshot token and makes no Dataverse/provider call. GET `mode=reconciled` verifies the supplied snapshot before and after read-only engagement plus metadata-only warm validation; either mismatch returns `409 roster_snapshot_changed` plus a fresh cached snapshot. Warm validation resolves only server-owned exact canonical/fallback proposal paths, Graph item metadata, and five request recommendation slots/exclusion/PI/applicant-org fingerprints. A current proposal binding requires bounded Graph drive/item identifiers and a stable eTag/version/last-modified token. It returns opaque versions and bounded non-PII invalidation output; no proposal bytes, Blob writes, applicant materialization, exclusion parsing, model, or evidence provider call. `authorityState:'current'` requires all bounded panel reads but is never candidate-stage/promotion authority: stages whose own person/institution/proposal-author input was not read stay stale. Applicant anchors are per exact stored person-id/slot only; general search rows are explicitly not applicable. POST → `recordSurfaced` (`active` or attested direct-deceased `ineligible`, monotonic ineligible/curation guards, active/saved row cap); PATCH supports exclude/promote/saved/confirm-identity/remove-previous-results/update-contact-draft. Applicant exclude/saved/confirm actions reuse the stored server blob. Ordinary candidate POST/exclude/saved strips client authority and carries forward only a request/candidate-key-matched server confirmation plus its canonical manual contact. `update_contact_draft` merges only website/affiliation into the exact active request roster row; it never writes Dataverse or accepts an unverified email. Stores only a pruned render/evidence DTO. | Low | Durable Find-tab candidate roster + cross-run dedup. Operational roster state, NOT canonical reviewer identity. `savedKeys` exposes only canonical saved applicant suggestion keys so terminal staff actions do not retrigger enrichment; `ineligible` remains visible with its official source and cannot be excluded/promoted/saved. See `docs/atlas/postgres-reviewer-find-roster.md`. |
| `/api/workbench/grantee-deliverables/abstract` | GET / PUT | App | `requireAppAccess('reviewers')` | `requestId` GUID-validated off `req.query` (GET) / `req.body` (PUT) before record-id use; PUT body `text` (1..20000 chars, non-empty), `etag` (the client's loaded row etag — required), `baseField` (`approved`/`formatted`); 32kb body cap | GET reads one `akoya_request` (`wmkf_abstractformatted`/`wmkf_abstractapproved` + `_etag`) and the related `wmkf_granteedeliverable` (read-only), returning the **effective** abstract (`approved ?? formatted`), its field, status, and an `editable` flag, **plus the grantee-submission record** (2026-07-29): `caption` (`wmkf_imagecaption`), `imageRef` (`wmkf_imagefileref` — the **private SharePoint reference**, STAFF-only; the external grantee surface still never receives it), `imageUrl` (the same ref echoed **only** when it parses as absolute http(s), so the writer's relative-path fallback and any hostile scheme resolve to `null` and can never reach an `href`), `hasImage`, and `submittedAt` (`wmkf_waiverackedat`, surfaced as the de-facto submission time since no submitted-date field exists), **plus the outbound lifecycle timestamps** (2026-08-09): `invitedAt` (`wmkf_inviteddate`) and `remindedAt` (`wmkf_remindeddate`), which drive the Awardee tab's status header and its derived invite+14d response deadline — both were already inside `DELIVERABLE_SELECT` for the reminder cron, so this exposes existing reads rather than widening the projection, and neither is grantee-authored or private-material data. **Plus the published PI/Co-PI byline** (2026-08-13): `pi`, `coPIs`, `bylineNames`, and `bylineUnavailable`, so a PD can eyeball the names before sending — an incorrect Co-PI in Dataverse previously reached a grantee inside a draft abstract with no surface displaying it. This widens the GET's reads by `_wmkf_projectleader_value` on the request row and one `wmkf_apprequestperson` Co-PI query (`resolveByline`, the same producer the grantee portal / website HTML / cycle export render, so the staff check cannot disagree with the published output — [VERIFIED via `lib/services/grantee-document-assembly.js:85-86` as the sole `joinNames` caller, consumed by `pages/api/external/grantee/[token]/context.js:65`, `pages/api/workbench/grantee-deliverables/website-html.js:45`, and `lib/services/workbench/grantee-deliverables/cycle-export-service.js:41`]). Both are staff-visible CRM names, not grantee-authored or private-material data. The Co-PI read is **fail-soft but never fail-silent**: a throw is caught so display-only data cannot 500 the abstract editor, and it sets `bylineUnavailable:true` so the UI reports *unverified* rather than rendering an empty roster — a failed lookup must not read as a clean check. All read-only — the GET adds no writes. PUT writes the PD's edited text to the **effective field decided from a fresh server read** (never the client's `baseField`): the grantee-approved field once it is non-empty, else the draft — so staff text never lands in the grantee-authored field before a submission. ETag-conditional `updateRecord` using the **client's loaded etag** as `If-Match` (a grantee submit / concurrent staff save / regenerate that landed during the edit window → 412 → 409 reload; a `baseField` flip also 409s). Status-gated (Safe default, S278): draft editable in null/Drafted/Invited/Reminder Sent; approved editable in Submitted/Staff Review only (Revision Requested / Complete / Closed No Response refused). **Never** changes `wmkf_deliverablestatus`; never blanks the field. Whole body in `bypassDynamicsRestrictions('grantee-abstract-load'/'grantee-abstract-save')`; `actingUserSystemId` = caller. | Medium | Grantee Deliverables Portal (S278). Awardee-tab PD review/edit/save of the publishable abstract — refine the AI draft before sending, and correct a grantee-returned abstract before it auto-assembles into the website HTML. See `docs/GRANTEE_PORTAL_BUILD_PLAN.md`. |
| `/api/workbench/grantee-deliverables/awardees` | GET | App | `requireAppAccess('reviewers')` | `cycleCode` (e.g. `J26`) validated via `cycleCodeToOdataFilter` (→ a meeting-date range filter, not a record selector); `scope` (`mine` default / `all`). No client GUID reaches a query — the mine-scope PD systemuserid is server-resolved from the session email (`resolveByEmail`), never client input | Reads `akoya_request` (DV, read-only) for the cycle, filtered to the research-awardee set: `akoya_requeststatus='Active'` + `akoya_programid` ∈ `GRANTEE_RESEARCH_PROGRAM_IDS` (editable config, GUID-keyed) + `wmkf_projectleader` present; **default scope=mine adds `_wmkf_programdirector_value eq <server-resolved session PD>`**, `scope=all` lists every PD's; per-row reads related `wmkf_granteedeliverable` for deliverable status. Whole body in `bypassDynamicsRestrictions('grantee-awardees')` | Low | Grantee Deliverables Portal awardee list (S268/S271). Fills the gap where the reviewer-finding dashboard doesn't surface awardees (it filters Phase-II-Pending/Advancing). Drives `/workbench/awardees` (defaults to the logged-in PD's awardees + "Show all programs" toggle; mine-scope with no resolvable PD → empty + `pdResolved:false`). Eligibility owner-validated against live J26 (=12). Missing deliverable row renders Not started. |
| `/api/workbench/grantee-deliverables/generate` | POST | App | `requireAppAccess('reviewers')` | `requestId` GUID-validated off `req.body` before record-id use; optional `regenerate` (strict boolean true); 8kb body cap | Reads one `akoya_request` (`wmkf_abstract`/`wmkf_abstractformatted` + `_etag`); staff write path ensures related `wmkf_granteedeliverable`; generates the house-style abstract via `generateGranteeAbstract` (Executor — writes `api_usage_log` PG + `wmkf_ai_run` DV audit); persists `wmkf_abstractformatted` to `akoya_request` via ETag-conditional `updateRecord`, and stamps package status→Drafted via `patchDeliverable` only from null/Drafted. Reuse-existing skips the paid call unless `regenerate===true`; status non-downgrade preserved. `loadModelOverrides()` warmed before model resolution. Whole body in `bypassDynamicsRestrictions('grantee-abstract-generate')`; `actingUserSystemId` = caller. **Regenerate is refused once the grantee has returned the package** (2026-08-09): `regenerate: true` at status >= Submitted throws 409 before the paid call and before any write, mirroring the range guard in `send-invite-service`. Rationale: it would burn an LLM call and overwrite `wmkf_abstractformatted` — the historical draft, i.e. what was actually sent to the grantee — while the published text is `wmkf_abstractapproved`, which this path never touches, so nothing visible would change and the loss would be silent. The reuse path (`regenerate` absent/false) stays open at any status: it performs no write and doubles as the Awardee tab's load. | Medium | Grantee Deliverables Portal chunk 3 (S268/S271). Awardee-tab abstract generation. No lead-PD gate (draft generation, not authoritative visibility like triage). See `docs/GRANTEE_PORTAL_BUILD_PLAN.md`. |
| `/api/workbench/grantee-deliverables/recipients` | GET | App | `requireAppAccess('reviewers')` | `requestId` GUID-validated off `req.query` before record-id use | Reads `akoya_request` (`_wmkf_projectleader_value` + `_akoya_primarycontactid_value`) then the two `contact` rows (`emailaddress1` + name); read-only. Whole body in `bypassDynamicsRestrictions('grantee-recipients')`; contact GUIDs are server-sourced (from the request row), not client input | Low | Grantee Deliverables Portal chunk 3b (S268). Resolves the two invite recipients — PI (`wmkf_projectleader`) + liaison (`akoya_primarycontactid`, the foundation liaison/grant steward) — for staff confirm/override. Research-only scope. |
| `/api/workbench/grantee-deliverables/send-invite` | POST | App | `requireAppAccess('reviewers')` | `requestId` GUID-validated off `req.body`; staff-confirmed `toEmail`/`ccEmail` (format-validated), `subject`, `bodyText`; 64kb body cap | Reads `akoya_request` identity; staff write path ensures related `wmkf_granteedeliverable` and reads package status; resolves and appends the assigned PD's unified email signature server-side via `email-signature`; mints a stateless grantee magic-link SERVER-SIDE (chunk 1 `mintForRequest`) and injects the action button + fallback link into the body (never from staff input); sends via `DynamicsService.createAndSendEmail` from the staff member's mailbox (`azureEmail`) — PI in `To`, liaison in `Cc` — regarding the request; flips package status Drafted→Invited and stamps `wmkf_inviteddate` only on that first flip (non-downgrade, non-fatal). Whole body in `bypassDynamicsRestrictions('grantee-send-invite')` | Medium | Grantee Deliverables Portal chunk 3c (S268/S271). Requires the abstract generated first (status ≥ Drafted); refuses re-invite once Submitted+ (409). Re-sends while already Invited do not reset the invite clock. Phase 1 intentionally keeps From as the staff mailbox while the signature resolves from the assigned PD. |
| `/api/workbench/grantee-deliverables/preview-invite` | POST | App | `requireAppAccess('reviewers')` | `requestId` GUID-validated off `req.body`; `bodyText` (≥10 chars; 64kb body cap) | READ-ONLY RENDER — resolves and appends the assigned PD's unified email signature server-side, then returns the exact invitation email HTML (`renderGranteeInviteHtml`) for the staff-edited body with a clearly-marked PLACEHOLDER link. NEVER sends an email, mints a token, writes Dataverse, or changes status. Used by the Awardee-tab "Preview email" button. Whole body in `bypassDynamicsRestrictions('grantee-preview-invite')` | Low | Grantee Deliverables Portal (S271). Lets staff preview the invite formatting + assigned-PD signature + magic-link button before committing; the real per-request link is minted only by `send-invite`. |
| `/api/workbench/grantee-deliverables/image` | GET | App | `requireAppAccess('reviewers')` (matches the other grantee-deliverable workbench routes) | `requestId` GUID-validated off `req.query` before record-id use; no request body (`bodyParser: false`) | **Private-material egress path** (2026-08-09). Reads one `akoya_request` (`akoya_requestid,akoya_requestnum`) and its related `wmkf_granteedeliverable` (`wmkf_imagefileref`), then streams the grantee-uploaded award image **bytes** out of the private `akoya_request` SharePoint library so the Awardee tab can render it inline instead of only linking out. Read-only — no writes. The stored ref carries no drive/item identity, so the folder is RE-DERIVED from the request row via the writer's own exported `granteeUploadFolder` (`lib/services/grantee-upload.js`) and only the FILENAME comes from the ref — matched against the writer's exact server-controlled pattern (`{reqNum}_grantee_image_{8 hex}.{png|jpg|webp}`) and refused otherwise, so traversal and surprise extensions are impossible by construction rather than by escaping. `Content-Type` is derived from that validated extension (never from the ref, never from Graph's reported mimeType) and the bytes are re-sniffed (`sniffImageType`) before serving, so a later out-of-band replacement in the library cannot make the route mislabel content. Response carries `X-Content-Type-Options: nosniff`, `Cache-Control: private, no-store`, and `Content-Disposition: inline` with the server-minted filename. Graph error detail never reaches the client (404 no request/package/image/unrecognized ref; 502 fetch or content-check failure). Whole body in `withDalContext('grantee-image-load')`. | Medium | Grantee Deliverables Portal (S411 increment 2). Renders the grantee's uploaded image on the staff Awardee tab; the SharePoint link is retained as the fallback when this route cannot serve. See `docs/GRANTEE_SUBMIT_VISIBILITY_SPEC.md`. |
| `/api/workbench/grantee-deliverables/replacement-upload-token` | POST | App | `requireAppAccess('reviewers')` | `requestId` GUID, filename/type/size, required current package `etag`; 16kb JSON cap | Freshly reads package, requires Submitted/Staff Review and exact ETag, then inserts a profile/request/scope-bound staging row and returns a private, single-path, 10 MiB client token. | Medium | Staff half of the direct-upload replacement flow; no image bytes enter the route. |
| `/api/workbench/grantee-deliverables/replacement-upload-failure` | POST | App | `requireAppAccess('reviewers')` | `requestId` GUID plus closed/bounded client-failure telemetry; 4kb JSON cap | Records only a sanitized, best-effort `portal_upload_client_failure` Operational Event labeled `clientReported:true`; no raw exception, filename, pathname, URL, content, or token field is accepted. | Low | Staff visibility for browser→Blob failures; Origin/session enforcement remains in `requireAppAccess`. |
| `/api/workbench/grantee-deliverables/replace-submission` | POST | App | `requireAppAccess('reviewers')` | `requestId` GUID, required package `etag`, optional caption ≤2000, optional actor-bound `stagingId`; 64kb JSON cap | **Private-material WRITE path.** Caption-only behavior is unchanged. Image replacement claims and verifies the exact private staged object, persists candidate SharePoint identity before the conditional package PATCH, and durably completes/reconciles the staging result. Existing fresh status gate (Submitted/Staff Review), magic validation, scan, server-controlled filename, waiver non-write, status non-write, ETag, response-drop re-read, and exact prior-image prune remain in the narrow domain service. | High | Direct-upload revision of S412 (2026-08-19); image bytes no longer traverse the Function body. |
| `/api/workbench/grantee-deliverables/website-html` | GET | App | `requireAppAccess('reviewers')` | `requestId` GUID-validated off `req.query` before record-id use | Reads one `akoya_request` + its related `wmkf_granteedeliverable` + applicant `account` (name/city/state) + the co-PI junction (`fetchCoPIs`) via `assembleGranteeDocument` (read-only); returns the assembled single-award HTML fragment (`renderAwardBlock`). STAFF/server-side surface → `includeImageRef` (image emitted as a `<figure>` placeholder carrying the SharePoint ref in a comment, NEVER a public `<img src>`). Whole body in `bypassDynamicsRestrictions('grantee-website-html')` | Low | Grantee Deliverables Portal chunk 8 output (b) (S270/S271). Website-ready HTML for one awarded research grant — replaces the staff member's manual HTML coding. |
| `/api/workbench/grantee-deliverables/cycle-export` | GET | App | `requireAppAccess('reviewers')` | `cycleCode` (e.g. `J26`) validated via `cycleCodeToOdataFilter` (meeting-date range filter, not a record selector — no client GUID reaches a query) | Reads the cycle's awarded-research `akoya_request` set (same filter as awardees: `Active` + research program + `wmkf_projectleader` present), assembles each via `assembleGranteeDocument` (bounded pool, fail-soft per row, including related `wmkf_granteedeliverable`), and returns a single standalone `text/html` page (`renderCyclePage`). STAFF/server-side → `includeImageRef`. Query failure → 503. Whole body in `bypassDynamicsRestrictions('grantee-cycle-export')` | Low | Grantee Deliverables Portal chunk 8 output (c) (S270/S271). Combined HTML export of all of a cycle's awarded abstracts — replaces the manual compile-to-PDF. On-demand v1. |
| `/api/workbench/triage` | POST | App + **hard manage gate** | `requireAppAccess('reviewers')` **AND** server-side manage check: superuser (`getUserRole(profileId) === 'superuser'`) OR request's lead PD (`_wmkf_programdirector_value` non-null AND equals caller `dynamicsSystemuserId`); null/absent lead PD → 403 (superuser-only) | `requestId` GUID-validated before record-id use; `triageStatus` validated via `isValidTriageValue` (numeric option set) → 400; 8kb body cap. Reads `akoya_request` (`_wmkf_programdirector_value`) then writes `wmkf_triagestatus` only (`actingUserSystemId` = caller). Whole body in `bypassDynamicsRestrictions('workbench-triage')`; errors sanitized | Medium | Sets the authoritative triage visibility field (Advancing/Set aside), replacing `d26Allowlist.js`. NOT the fail-open/UI-only `canManage` posture (S207) — this is a hard server gate. Constants: `shared/config/triageStatus.js`. See `docs/WORKBENCH_TRIAGE_FIELD_BUILD_PLAN.md`. |

**Reviewer-roster contact-draft detail (2026-08-18):** `update_contact_draft`
rejects non-HTTP(S), document, directory, and candidate-inconsistent profile URLs.
For ordinary non-applicant rows it clears stale automated/staff exact-contact
authority and stamps the explicit `manual_contact_changed` identity remedy before
the request-scoped Postgres CAS write. Applicant-recommended rows retain their
separate server-owned promotion contract. No branch writes Dataverse or accepts email.

### Grantee abstract and caption response rendering

- `/api/external/grantee/[token]/context` keeps `abstractFormatted`, `abstractApproved`, and `caption` as exact Markdown and adds response-only `deliverable.abstractHtml` / `deliverable.captionHtml`, rendered through the shared server-side body/inline sanitizers. No HTML is persisted and the token scope is unchanged.
- `/api/workbench/grantee-deliverables/abstract` GET keeps exact Markdown in `effective` and `caption`, and adds response-only sanitized `effectiveHtml` / `captionHtml` for the staff editor seeds and caption display. PUT still accepts and persists only abstract Markdown, with the existing ETag and effective-field guards; caption writes remain on the separate ETag-conditional `replace-submission` route.
- The abstract PUT reconciles an ambiguous non-412 Dataverse write by re-reading both abstract fields. An exact value on the same effective field returns 200 with the fresh ETag; a field flip remains a 409 stale conflict; a mismatched or unavailable confirmation returns a typed 503. The Awardee tab preserves the working copy and renders that save-specific failure beside the Save button.

## Regular Maintenance Process

### On every PR that touches `pages/api`

- Add or update the row in this matrix.
- State the intended access class in the PR description.
- Link the guard used in code.
- State the data boundary: own profile, app-wide, superuser global, token-scoped, cron-only, or intentionally public.
- Add or update tests when the route reads/writes user data, external reviewer data, tokens, files, CRM records, or admin settings.
- Treat new unlisted API routes as release-blocking once the route-inventory CI check exists.

### Weekly review

- Review Dependabot, Semgrep, Trivy, and Gitleaks results.
- Review health, secret, spend, and cron alerts.
- Review any owner questions or ambiguous matrix rows that were raised during PR review.

### Monthly review

- Re-run an API route inventory and compare it to this matrix.
- Confirm public routes are still intentionally public.
- Confirm app-level access still maps to current operational roles.
- Review high-sensitivity routes: external reviewer token routes, file downloads, profile/admin routes, and Dynamics bypass routes.

## Current Automation

`npm run check:api-routes` runs `scripts/check-api-route-security-matrix.js`. The script scans `pages/api/**/*.js` and fails when any API route is missing from this matrix. It also warns when a route does not contain one of the recognized guard tokens, unless the route is explicitly documented with `Current Guard = None`.

Recognized guard tokens:

- `requireAuth`
- `requireAuthWithProfile`
- `requireAppAccess`
- `requireSuperuser`
- `verifyCronSecret`
- `verifySuggestionToken`
- `getServerSession`
- `NextAuth`

The check runs in `.github/workflows/test.yml` before Jest. It should not try to prove authorization correctness. Its job is to prevent unreviewed routes from appearing silently.
