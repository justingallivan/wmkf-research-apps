---
title: Reviewer UI surfacing handoff 2026-09-er
status: active
summary: "Reviewer extension history plus a local Workbench filter/search branch. Meeting-date cycles and compact controls are candidates to retain; the hard-coded Research-only scope is owner-superseded and must be redesigned before release."
---

# Reviewer UI surfacing handoff

## Issue 1 — due-date extension missing from Last Action

- Screenshot: Reviewer Follow-up Request `1003046`; Liang Huang showed an extended due date of Sep 13, 2026, while Last Action remained the Sep 6 reminder.
- Root cause: `wmkf_reviewduedateoverride` stored only the new DateOnly deadline; the extension writer emitted no event timestamp.
- Fix: add `wmkf_reviewduedateextensiongrantedat`, write/clear it atomically with the override, project it through reviewer reads, and render a dated “Review due date extended” history event.
- Sibling surfaces checked: main FIELD_SELECT, token-status, token-regeneration, acceptance-drain, merge predicate, engagement reset, reviewer DTO, Last Action, and History drawer.
- Owner smoke: on signed-in Request `1003046`, confirm Liang’s Last Action is “Review due date extended” dated Sep 7, 2026 and History includes the new due date.
- Historical backfill: owner-authorized, owner-run exact-row backfill is required for Liang because the pre-field write has no durable grant timestamp.

## Issue 2 — Request Workbench cycle dropdown has irregular gaps/order

- Screenshot: Request Workbench → Request list → Cycle dropdown showed scattered fiscal-year labels (December 2026, June 2020, December 2017, etc.) instead of a recent-first timeline.
- Root causes: the original service grouped and filtered on sparse legacy `akoya_fiscalyear`. The first meeting-date correction (`ffeafc47`) remained on `codex/reviewer-ui-surfacing`, so the compact-controls review branch did not include it. After that commit was cherry-picked as `9cab8c3e`, signed-in local review exposed a second defect: `buildHeaders` requested 100 aggregate rows and the adapter returned that incomplete page as successful options.
- Current branch implementation: aggregate `wmkf_meetingdate` in UTC for the two canonical Research program IDs, sort by full year/month descending, and offer June/December meetings only. The dropdown omits off-cycle and no-date groups. Request up to 5,000 month groups explicitly; reject continuation metadata, a full page, or malformed responses. Table/field restrictions (including `akoya_programid`), target interlock, and shared timeout remain enforced. Existing filter error/retry UI handles aggregate failures.
- **OWNER-SUPERSEDED DESIGN, 2026-09-08 — do not promote the branch as-is:** the hard-coded Research-only scope is temporary and needs reconciliation in the Cycle Dossier session. The intended design is a live **Grant Program** dropdown. It defaults to the signed-in PD's program and allows the user to select other programs. Do not infer a fixed suite allowlist from `shared/config/researchPrograms.js`. Before implementation, verify the signed-in PD → program source, the live program-option query, non-PD fallback, and how changing program recomputes cycle/status/results. Preserve the owner's separate requirement that off-cycle meetings are absent from this Workbench cycle view unless the owner changes it.
- Temporary Research scope in this branch: `shared/config/researchPrograms.js` owns the Medical Research and Science and Engineering Research GUIDs. `granteeResearchPrograms.js` re-exports the same set, preserving Awardees' separate Active-status and PI requirements. Request discovery currently preserves active/historical statuses while excluding Directors' Directed, Staff Directed, reviewer honoraria, and unknown program IDs. Commit `9cab8c3e` first hard-coded the two Research IDs for cycle aggregation; commit `889ddf2b` extended that hard-coded scope across options, search, and dashboard visibility.
- Search options, filter-only and exact-number queries, PI joins, and dashboard visibility use the canonical `_akoya_programid_value`. Numeric Request Locator searches now use the scoped search endpoint before navigating, rather than the unrestricted request-detail resolver. If the scoped exact-number query is empty, one bounded exact read selects only the request number and program lookups and can return `{requestNumber, program}` for a request in another program; the locator names that program and directs the user to search AkoyaGO, without exposing request details or a Workbench link. The versioned session cache (v3) discards previous broad-program results and off-cycle selections on reload and sanitizes the acknowledgement shape.
- Text-search contract: a sandbox probe found that Search filtering on the program lookup silently returned zero for a known matching Research record. Fetch bounded ranked Search candidates, then apply Research/cycle/status during canonical hydration via `findByIds`' optional filter. Its ID OR group and caller scope are parenthesized independently. Canonical hydrated program IDs are checked again before projection. Totals exclude filtered-out indexed candidates; the index ceiling still triggers the limited-results warning. A broad term can exhaust the 100-candidate index window before all Research matches are found; users must narrow it. Status/cycle filtering on the PI query remains server-side.
- Meeting-date result labels remain accurate for historical discovery: off-cycle/no-date Research records can still be found by text, number, or status without being offered as cycle choices. Explicit legacy API cycle filters retain the earlier date-range/no-date/escaped-fiscal-year behavior. No source dates or programs are rewritten.
- Sibling surfaces: the shared dashboard service also narrows Reviewer follow-up's cycles and request feed. Its merge is anchored to dashboard requests, so broader reviewer rows cannot reintroduce excluded programs. Other `findByIds` callers (reviewer authorization and initial-assessment artifacts) supply no added filter and preserve their prior queries. Direct request-detail resolution and Final writeups are separate discovery surfaces.
- [VERIFIED via signed-in local browser, 2026-09-07] `localhost:3000/workbench` shows 92 June/December cycle choices plus All cycles, descending from December 2026. December 2026 returns 212 Research matches (previous broad result: 618), with 25 displayed. Reload discards the previous broad result cache. After `903de6b9`, exact search for known Directors' Directed request `1003278` acknowledges that it is a valid request in `Directors' Directed Grant Program` and directs the user to AkoyaGO without exposing its details or a Workbench link. Exact Research request `1002915` still navigates to its Workbench detail. These observations apply to the local branch, not production.
- Dataverse Search transport: `codex/compact-controls` now classifies only the exact normalized `POST /api/search/v1.0/query` pathname as a semantic read. Local/Preview access to production remains denied unless `DATAVERSE_ALLOW_PROD_READS=yes`; all other POST paths remain writes. [VERIFIED via signed-in local browser and dependency logs, 2026-09-07] With that explicit production-read acknowledgement, institution (`University of Washington`), title (`Nanopore tweezers`), and PI (`Jens Gundlach`) searches completed successfully through the Search POST and canonical hydration. Sandbox full-service rehearsal still encounters missing legacy contact/request columns (`wmkf_orcid`, `_wmkf_researchleader_value`).
- [VERIFIED via read-only production probe and guarded apply logs, 2026-09-08] The exact `akoya_request` Quick Find view is `Quick Find Active Requests` (`08b51bf9-47c5-4ef5-a1a5-6af4713bca5b`, query type 4, managed=true, customizable=true). `akoya_programid` is absent from both FetchXML projection and layout. Two production PATCH attempts, first without and then with canonical `MSCRM.SolutionUniqueName=wmkfResearchReviewAppSuite`, both returned HTTP 400 / Dataverse `0x80040216`; exact readback was unchanged and PublishXml was never called. The metadata update is BLOCKED; do not blindly retry until the platform error is diagnosed.
- [VERIFIED via read-only sandbox probes] Actual service Search options (including `SEARCH_ORDER`) returned HTTP 200 before unrelated missing-column errors. A reduced-select canonical query retained the known Medical Research Search hit (three matching rows, all in that program); the same query with a nonmatching program returned zero. This verifies the transport/filter mechanism, not a complete sandbox UI flow.
- Regression coverage: 235 tests across search service/route, Request Locator, dashboard integration, adapter aggregation/scoped hydration, the existing Awardees/export/cron consumers, and reviewer-authorization/initial-assessment consumers. Cases include excluded and absent program IDs present in fixtures, stale requests, old-cache invalidation, exact-number scope, pagination/counts, 100+ aggregate groups, continuation rejection, UTC grouping, and restrictions. Types, focused lint, API, Atlas, DAL, context-boundary, route-service, OData, and GUID gates pass with paired self-tests.
- Release follow-up: retain and re-evaluate the compact controls (`58f0a191`), meeting-date derivation and complete bounded aggregate (`9cab8c3e`, `3472367c`), guarded exact Dataverse Search read classification (`4fdd8217`), and safe outside-program acknowledgement behavior (`903de6b9`). Revise the hard-coded program filtering introduced by `9cab8c3e` and `889ddf2b` into the owner-directed live Grant Program selector before any promotion. Update the cache key, API contract, option loading, dashboard/search predicates, copy, and regression tests together. No production promotion, merge, or deployment has occurred from this branch.

## Compact-controls branch handoff

- Branch/worktree: `codex/compact-controls` at `/Users/gallivan/Code/WMKF_Apps/.claude/worktrees/compact-controls`.
- Branch work before stop documentation: `58f0a191^..9ecdab1b` (equivalently `30ef052b..9ecdab1b`).
- Compact UI: `58f0a191` gives the Request Locator cycle/status selects the shared compact `ToolbarSelect` treatment and adds option-load recovery, disabled/loading states, keyboard-preserving native selects, cache recovery, and focused control tests.
- Cycle corrections: `9cab8c3e` changes cycle derivation from sparse `akoya_fiscalyear` to `wmkf_meetingdate`; `3472367c` makes the aggregate complete-or-error, restriction-checked, UTC-grouped, and bounded at 5,000 groups.
- Program-scope/search work: `889ddf2b` applies the temporary Research-only scope and June/December cycle policy; `4fdd8217` permits only exact `POST /api/search/v1.0/query` as an interlocked semantic read and adds guarded Quick Find inspection/apply tooling; `903de6b9` safely acknowledges exact requests outside the temporary scope.
- Design documentation: `9ecdab1b` refreshes only `.impeccable/design.json` metadata; `DESIGN.md` is unchanged.
- Live external state: read-only production/sandbox Dataverse probes were run. Two explicitly authorized production Quick Find PATCH attempts failed with HTTP 400 / `0x80040216`; exact readback proved no metadata change and PublishXml was never called. Power Apps was viewed earlier in the session but nothing was changed; the owner requires explicit permission before any future Power Apps/admin-interface inspection. No deployment, merge, promotion, database migration, or other successful production write occurred.
- Known limits: full text search is bounded to the top 100 indexed candidates before canonical hydration; broad terms can omit eligible matches below that window. Sandbox full-service rehearsal is blocked by unrelated missing legacy columns. The live Grant Program option source, signed-in PD default mapping, non-PD behavior, and cross-program cycle/status refresh contract remain unverified and must be designed before replacing the temporary scope.
