# Test Request Factory — admin-only design

Status: **AMENDED DESIGN; SANDBOX REQUEST AND APP-OWNED FOLDER/LOCATION CREATE PROVEN; READ-ONLY ADMIN PREVIEW DEPLOYED — DOCUMENT-READY AND PRODUCTION ENABLEMENT BLOCKED.** A second bounded sandbox rehearsal created Request 1000339 and its exact physical folder and Dataverse location under the app-suite identity; see `evidence/test-request-factory/app-owned-location-rehearsal-2026-09-23.json`. The earlier Request 1000338 gained a later staff-created empty folder, but its automatic provisioner remains unidentified. The deployed Admin preview remains read-only. No production schema/data write or production deployment was performed. Deterministic meeting date and production isolation remain open. Owner scope confirmed 2026-09-19: admin-only synthetic test requests, not operational duplication. **Owner amendment 2026-09-23 (below) extends the scope to later-stage clones with linked documents and synthetic reviewers; where an older passage conflicts, the amendment wins.**

## Owner amendment — 2026-09-23 (Session 535)

This section records owner decisions made after the original design and the read-only feasibility investigations that informed them. Where a later section of this document still says otherwise, this section is authoritative; conflicting lines are marked in place.

### Owner decisions

1. **[OWNER DECISION] Numbered proposal PDFs are copied and renamed.** `Reviewer Materials/Proposal_<n>.pdf`, `AI Materials/ProposalNarrative_<n>.pdf` and `AI Materials/ProposalBibliography_<n>.pdf` are copied byte-for-byte under the destination request number. The owner reports that the request number is not currently written into these generated PDFs; if it is in future, a mismatch inside a test fixture is acceptable. No identity-rewriting transformer is required for them. Contents matter only insofar as the files exist for workflows to process (for example emailing them).
2. **[OWNER DECISION] Later-stage clones.** The administrator chooses how far along the Workbench (left to right) the clone should reach, and the factory brings the documents created along the way. Documents are **linked** where feasible (registry row, request pointer and any parent records, so the app treats them as the new request's documents); where a copy cannot be linked, the factory drives the app's own no-AI step to produce the document in the new request. See *Later-stage recipes* below.
3. **[OWNER DECISION] Reviews are cloned onto synthetic reviewers** so they appear as ordinary reviews in the new request. Synthetic reviewers are seeded 1:1 from the source request's reviewers (the source is expected to be a test request itself).
4. **[OWNER DECISION] Synthetic reviewers use real, staff-controlled throwaway inboxes** so staff can exercise reviewer interactions (invitation, portal, submission, thank-you). Each staff member has different throwaways, so the addresses are **editable per run in the creation form**. When the source reviewer is already synthetic, the field defaults to that reviewer's address; when the source reviewer is a real person, the field starts empty and is required — a real reviewer's address is never used as a default. The per-run assignment is recorded in the run ledger. There is no central address list to maintain.
5. **[OWNER DECISION] Sandbox first, production soon.** Live runs start in the Dataverse sandbox. The source for sandbox testing is production test request **1003222** (ZZTEST-03), because no production request has completed the whole workflow; the owner authorized read-only production reads scoped to that request (2026-09-23).
6. **[OWNER DECISION] Cross-environment source bundle (option A).** For sandbox runs, a read-only production step exports what the factory needs from the source into a private local bundle; a sandbox step builds the new request from that bundle. The same reader and builder serve the eventual same-environment production path; neither is sandbox-only scaffolding. SharePoint is the shared akoyaGO site in both cases, so file copy is not cross-environment.
7. **[OWNER DECISION] Build ownership.** Claude (root) now builds this workstream in the `codex/test-request-preview-integration` branch (worktree `WMKF_Apps-factory`). Codex performs an adversarial review of each stage before root accepts it. This replaces the Luna-builds / Sol-reviews arrangement below.

### Later-stage recipes (dependency order)

[VERIFIED via read-only source investigation on `main`, 2026-09-23; file:line anchors are in the Session 535 investigation reports] Workbench tab order is Overview, Proposal, Initial Assessment, Reviewers, Reviews, Review Panel (grant-gated), Staff Deliberations, Final Writeup, Status, Awardee (`pages/workbench/[requestId].js`). Overview and Status create nothing.

| Tab | Document | Recipe |
|---|---|---|
| Proposal | Phase I slots; numbered proposal PDFs | Copy; rename numbered PDFs to the destination number (decision 1). |
| Proposal | Field Primer (`akoya_request.wmkf_ai_fieldprimer`) | Copy the field value. |
| Initial Assessment | IA DOCX (`wmkf_requestdocument` type Initial Assessment + `wmkf_CurrentInitialAssessment`) | Copy and link: `requestDocumentAdapter.create` with an identity computed by `buildInitialAssessmentIdentity` over the **destination** request context and copied narrative, upload bytes, recompute the governed content hash, then `commitReadyLineage`. No AI call. The document body still shows the source request's number/title/institution (accepted by decision 1's rationale). A later "Generate" click on the test request creates a new IA rather than reusing the copy; that is harmless. |
| Initial Assessment | IA board snapshot | App step `createInitialAssessmentBoardSnapshot` from the linked IA (no AI). |
| Reviewers / Reviews | Reviewer suggestions, review answers, review DOCX | Synthetic reviewers (below). Review DOCX is produced by the app's filing path, not copied. |
| Review Panel | AI panel run | Not cloned; regenerate only if needed (paid AI, spans several requests). |
| Staff Deliberations | Pre-Site Visit DOCX | Seed a registry row with the source draft (core JSON, section fields) and an input snapshot rebuilt against the destination request and copied narrative; the app then renders without an AI call. Must follow the reviews (its referee section reads submitted reviews). The row requires an AI-run link; whether to point at the source run or a stub run is an open implementation decision. |
| Staff Deliberations | Applicant slides / bios / other materials | Copy and link registry rows. Visible in the tab only with a fresh site-visit activity and materials collection created by a no-send preparation operation; never copy tokens. |
| Staff Deliberations | Recording / transcript / transcript summary | Copy and link (row + bytes). |
| Staff Deliberations | Consultant feedback | Copy and link, with a synthetic consultant, because the `consultant_feedback` row binds a roster consultant (a real person). |
| Staff Deliberations | Pre-RP brief | App step (deterministic render, no AI) after reviews exist. |
| Staff Deliberations | Distribution snapshots and attempts | Not cloned — they record an actual email send. |
| Final Writeup | Final Writeup row + `wmkf_CurrentFinalWriteup` | App steps `start-site-visit` then `startFinalWriteup` (no AI; reuses the Pre-Site item). |
| Awardee | Formatted abstract, images, invite | Out of scope (post-award). |

Every generation key includes the request GUID, so keys are always recomputed for the destination; nothing is reused from the source row.

### Synthetic reviewers

[VERIFIED via read-only source investigation, 2026-09-23] A review "exists" through a `wmkf_potentialreviewers` person row, a per-request `wmkf_appreviewersuggestion` row, `wmkf_appreviewanswer` rows keyed by (suggestion, question key), and a review DOCX pointer on the suggestion.

- **Person:** a `wmkf_potentialreviewers` row named `TEST · <source reviewer name>` with the run's throwaway address (decision 4), created through the existing find-or-create by email so re-cloning reuses it. A person-level test marker is required; the address alone is not a safety boundary.
- **Suggestion:** copy candidate content, lifecycle and received state; regenerate `wmkf_grantcyclecode` from the destination; never copy portal tokens, proposal URL/password or `wmkf_honorariumrequest` (it points at the source's BILL request).
- **Answers:** copy, bound to the new suggestion.
- **Writer:** the repository's reviewer-engagement boundary gate permits generic suggestion writers only inside `lib/services/reviewer-engagement/`, so the seeder is a new named command there. Order: preallocate and journal the suggestion GUID; create it without the received stamp; upsert answers; then stamp received (and thank-you, when the thank-you email is not wanted) with If-Match. Stamping received before answers exist reproduces the zero-answer snapshot left by test Request 1003223.

### Isolation additions required by this amendment

The original isolation section stands and gains these consumers. [VERIFIED via source] Four scheduled jobs scan all suggestion rows without a request or test filter and act on cloned state: the thank-you sweep (sends email), the review-synthesis drain (spends AI credits), review DOCX filing (writes SharePoint) and the stale-invite sweep (changes status). Staff can also now **accept through a throwaway inbox**, which promotes the synthetic person to a CRM Contact and queues honorarium/BILL onboarding. In production the request marker must suppress acceptance-side Contact promotion and payment onboarding, and the scheduled jobs must skip marked requests or be explicitly limited to allowed test behavior. Email concerning a marked request is default-deny at the shared delivery seam. The sole exception is a trusted, server-owned reviewer-engagement purpose—not client input—bound to one suggestion and synthetic person on the regarding request; every To/Cc recipient must equal that person's recorded address. The run ledger's per-run assignment is the address authority once it exists, never a reviewer-editable field. Reviewer sends without the regarding request, with unknown marker state, or with missing/forged purpose fail closed. Materials-contributor and grantee email remain denied; grantee invite/reminder paths deny before token minting, claiming or activity creation. The mechanically derived Dynamics-email caller census is authoritative; the schema proposal's hand-written sender table is only the implementation checklist. [ASSUMED — Vercel platform behavior, not re-verified this session] Vercel runs scheduled jobs only on the production deployment, so in sandbox testing automatic reviewer emails do not fire; only staff-triggered sends are exercised there.

### Revised build order

1. This amendment (documentation).
2. File plan: allow copy-and-rename for the three numbered PDFs.
3. Stage 1 isolation: request and person markers, scheduled-job and acceptance-side exclusions, recipient confinement, and ordinary queue/report exclusion.
4. Source bundle export (production, read-only) and sandbox Basic-clone proof with file copy. The one narrow pre-ledger exception is the existing, already-reviewed sandbox-only `scripts/rehearse-test-request-sandbox.mjs`: it reserves an exclusive receipt before the write and recovers an ambiguous create by exact preallocated Request ID rather than retrying (`scripts/rehearse-test-request-sandbox.mjs:3-15,921-923`). The exception covers that script's existing Request-plus-folder/location path; the script does not copy files today. Adding file copy to it stays inside the exception only if each file's intended destination is journaled in the receipt before the write, the write is create-only with conflict refusal, and an ambiguous outcome is recovered by exact item rather than retried; otherwise file copy waits for the ledger. No other recipe or production execution uses this exception.
5. Durable run ledger and numbered migrations, atomic reservation, resource journaling, and the bounded resumable runner described in *Operation contract and recovery*. These land before any synthetic-reviewer or later-stage recipe and before production.
6. Later-stage recipes one at a time: IA → synthetic reviewers and reviews → site-visit materials and transcripts → Pre-Site seed and render → app-driven Pre-RP brief, site-visit start and Final Writeup.
7. Admin creation form (including editable reviewer addresses), resume/retire operations, and production release.

Each stage is a separate commit with a Codex adversarial review before acceptance.

**Source bundle export (build-order item 4, first half), built Session 536 and hardened after review (2026-09-23).** [VERIFIED via source and unit tests; the initial Session 536 production read was v1, and its later v2 export predates the final origin and archive-miss checks described here] `scripts/export-test-request-source-bundle.mjs` reads one production Grant Request by number (refusing any host but the registered production host, and requiring `DATAVERSE_ALLOW_PROD_READS=yes`), lists its SharePoint folders, and downloads each recognized source document to hash it, reusing the admin preview's classification, read limits and hashing (`lib/services/test-requests/admin-preview-service.js`). Its exporter-only dependency set requires positively resolved parent libraries and complete bounded Dataverse location results; parent-resolution failure or any location continuation aborts. Archive discovery ignores only a structured Graph `itemNotFound` 404 raised by listing the computed request folder; a missing or renamed archive library and every other 404 become `SOURCE_BUCKET_UNAVAILABLE`, which rejects strict export. The shared lenient Admin preview uses the same narrower folder-miss rule and surfaces those other failures as inventory errors. After sequential hydration, the exporter repeats strict discovery, resolves each drive again, reads fresh item metadata, and compares the complete canonical inventory (library/folder, drive/item, kind/name/size/MIME type, eTag and version ID); any addition, removal, move or metadata drift aborts before the Request revision check and before the shell writes a file. It then re-reads the Request's `versionnumber` and refuses if it changed. `lib/services/test-requests/source-bundle.js` defines bundle version 2 and rejects version 1: build and read accept only a host in the tracked production Dataverse registry, require every document to identify the tracked shared SharePoint site, and reject mixed-site documents. The bundle stores the Request projected by the existing `projectCloneSource` (identity, number, type, purpose, requested amount, fiscal year, meeting date, revision), and each document's inventory ID, kind, library, folder, registered SharePoint site identity, Graph drive and item IDs, size, MIME type, eTag, version ID and SHA-256. Files are referenced, not embedded, because SharePoint is shared across environments. The stored drive ID is snapshot identity only: the sandbox clone consumer must re-resolve the document library's drive on the registered site immediately before copy, then re-verify the item eTag and SHA-256. The bundle holds source text, so it is written once (mode 0600) to a new absolute path outside the repository and only a summary is printed. The Session 536 live run found Request 1003222 at `e43ae6ea-698f-f111-8076-6045bd018a07`, revision `W/"98622844"`, December 2026 / 2026-12-11, no purpose text, requested amount present, and two recognized documents: `ProposalNarrative_1003222.pdf` and `Proposal_1003222.pdf`; no Phase I document or bibliography matched. That v1 artifact cannot be consumed by the hardened reader. [VERIFIED via owner-authorized live run, Session 536] Request 1003222 was then re-exported as version 2 with strict discovery: same GUID, revision `W/"98622844"`, and the same two documents with identical SHA-256 prefixes (`813a7a6e64b2`, `65b4395d7d10`), each carrying drive ID and SharePoint site identity; the obsolete v1 file was deleted. [VERIFIED via owner-authorized live run, Session 536] After the origin and archive-miss checks landed (`4b8ebec7b`, `aba21a5a9`), Request 1003222 was re-exported again with identical results (same GUID, revision and two SHA-256 prefixes); it passed strict discovery, which shows the archive lookups that failed in the first run were folder-level misses, not missing libraries. A third Codex review found that a missing child folder mid-walk was also tagged as a folder miss; Claude fixed it so only the requested root folder qualifies, and strict discovery now clears the Graph drive cache and re-confirms the library exists before accepting even a root miss (regression tests fail when either fix is reverted). A final live re-export after that fix again matched. **Exporter ACCEPTED by the owner (Session 536) without a fourth review round.** Earlier bundle files were deleted; the current bundle lives in Session 536's scratchpad, so a later session re-exports before the sandbox clone. Next half: see the following paragraph.

**Sandbox Basic clone from the bundle with file copy (build-order item 4, second half), built Session 537 (2026-09-23/24).** [VERIFIED via owner-run live sandbox execute, its receipt (`evidence/test-request-factory/bundle-clone-receipt-2026-09-24.json`), and read-only `--inspect --receipt`, 2026-09-24 UTC; source at `36c55db8c`, 33 focused unit tests plus write-boundary pins, mutation checks per fence, types and Dataverse gates] `scripts/rehearse-test-request-sandbox.mjs --prepare --bundle=<v2 bundle> --source-request-number=<n>` writes a v4 manifest that embeds the validated bundle (so execute never reads production from the sandbox client) and refuses a bundle whose Request number differs from the attested `--source-request-number`, whose export is older than six hours or future-dated, or whose Grant type differs from the sandbox Grant option; execute re-checks the bundle hash, request number, revision, host, freshness and the copy-policy digest, and the manifest expires at the earlier of its one-hour TTL and the bundle window. Writes are governed by the named `SANDBOX_REHEARSAL_COPY_POLICY` in `lib/services/test-requests/bundle-file-copy.js` (7 files, 25 MB each, 50 MB total, PDF/XLSX; version and SHA-256 digest bound in the manifest), which is distinct from the admin preview's read-only hashing ceilings; the production executor's limits remain an owner decision. After the Request and its document location are proven, `copyBundleFiles` runs per file in this order: destination (resolved from the server-assigned request number through the Basic file plan) journaled in the receipt before any Graph write; Graph caches cleared and the registered `akoyago-shared` site plus source and `akoya_request` drives re-resolved and compared with preflight; source item re-read by stable ID and refused on name/size/MIME/eTag/version drift, then downloaded and refused on SHA-256 mismatch; destination subfolder ensured and the destination path confirmed absent; upload attempt journaled, then one `conflictBehavior: 'fail'` PUT. `GraphService.uploadFile` gained an additive `onItemCreated` hook (shared runtime Graph code, `lib/services/graph/writes.js`) awaited right after the create response is parsed and before its own metadata read-back, and the copy module journals the stable item ID there; `itemJournaledAt` is set only after the receipt write returns, a captured-but-unjournaled ID fails without any further network call, and a read-back failure after a journaled create is verified by exact ID. A thrown PUT with no journaled ID is classified: 409 refuses, another definitive 4xx verifies absence and fails, anything else is recovered only by reading the exact destination path with matching size and SHA-256, never retried. The created item is read back by ID, downloaded and hashed; after the observation window every journaled item is re-verified by stable ID (metadata before and after a fresh download must equal the verified identity and bytes must hash to the bundle), and the folder listing must equal the journal exactly. `--inspect --receipt` reconciles every journaled stable ID read-only. Three Codex adversarial rounds (`gpt-5.6-sol`) on `8a82db600`, `47972f5f8` and `a5bf60cbc` found four, two and one defects respectively, all fixed in the next commit; the round-three fix `36c55db8c` was **accepted by the owner without a fourth review round (Session 537, 2026-09-24)**, closing build-order item 4. Session 537 re-exported Request 1003222 (same GUID, revision `W/"98622844"` and two SHA-256 prefixes as Session 536), prepared and read-only-inspected a v4 manifest successfully (both destinations template until the number is assigned), and confirmed the attestation, stale-policy and freshness refusals live; the agent harness's safety classifier blocked the agent from issuing `--execute --bypass-goverify`, so the owner ran it by hand (the bypass matches the two proven sandbox creates, 1000338 and 1000339). Result: HTTP 201 created sandbox Request **1000340** (`5d54abc5-7f74-4d23-b4be-39599147e674`, December 2026 / 2026-12-11, no purpose, requested amount copied), GoVerify restored and verified, one app-owned location (`98a7e0e3-c122-4d65-9484-56b562477882`) on folder `1000340_5D54ABC57F744D23B4BE39599147E674`, and two files created with no read-back error or path recovery: `AI Materials/ProposalNarrative_1000340.pdf` (item `01G4GVMS34H6SGDJZCGNF2NLBTIUMXWRAW`, 287,771 bytes) and `Reviewer Materials/Proposal_1000340.pdf` (item `01G4GVMS6NXN2G3W7ZKZHKAWXU5NS6DEZG`, 2,841,448 bytes). Per-file verification, the post-observation stable-ID re-check and the folder listing all passed; zero payment or regarding-email rows; Foundation account and Contact unchanged. `--inspect --receipt` then reconciled both items by stable ID with size and SHA-256 matching. This proves the combined fresh create, meeting-date handling, folder/location and byte-exact file copy path in the sandbox; nothing was promoted to production.

**Durable run ledger (build-order item 5, first half: slice 5a), built Session 537 (2026-09-24).** [SOURCE-BUILT on `codex/test-request-preview-integration`; 71 focused tests pass including 11 live against a throwaway local Postgres 16 container; migration 054 is applied to NO shared database (the repo's `POSTGRES_URL` is the shared Production/Preview Neon database and its apply needs owner authorization)] Migration `lib/db/migrations/054_test_request_runs.sql` (manifest entry, `scripts/setup-database.js` V55 mirror, pure-JS CHECK mirror test) creates `test_request_runs` (run identity = the value written to `wmkf_testcreationrunid`; actor + idempotency key unique; status, separate current step; source host/GUID/number/revision, bundle SHA-256 and export time, copy-policy version and digest, plan digest, create-body SHA-256; preallocated destination Request and location GUIDs, unique; expected app user, organization, Graph site and drive; `version`, `lease_token`, `lease_generation`, `locked_until`, attempt count, sanitized reason/error) and `test_request_run_resources` (gapless per-run sequence, step, kind, system, planned identity before dispatch, source provenance, dispatched time, response status, readback after dispatch, outcome). `lib/services/test-requests/run-ledger.js` takes an injected `{ query, transaction }` interface (`run-ledger-db.js` supplies a `@vercel/postgres` adapter and a lazily imported `pg` adapter for local proofs): `reserveRun` is INSERT … ON CONFLICT DO NOTHING then SELECT in one transaction, returns the stored row so a same-key retry never yields a second destination GUID and a differing plan digest is a 409; `claimLease` fences on version, working status and expired-or-null lease and bumps the generation; `advanceStep`/`markReady`/`markNeedsAttention`/`recordError` fence on token, generation, version and a live lease and return null on a miss; resource writes lock the run row and check token, generation and database-clock expiry, throwing 409 on a miss; `markReady` and `markNeedsAttention` release the lease; `needs_attention` runs re-claim and their reason clears on leaving the state; every error string and every JSONB value is scrubbed of bearer tokens, `Authorization` values, SAS credentials, download links, bodies and purpose text. Atlas page `docs/atlas/postgres-test-request-runs.md`. Review record: Sonnet built; Opus review found a `needs_attention` run could never recover (CHECK blocked clearing its reason), the `completed_at` CHECK blocked both retirement paths, `markReady` left the lease live, and the resource fence used the app clock; all fixed with live tests. Root's pass found the sequence query's invalid `FOR UPDATE` on an aggregate (mocked tests passed with it), a Jest environment mismatch for `pg`, and a pool leak in the adapter's success path; all fixed. Design decisions taken by root: dedup stays per actor; `advanceStep` may record the server-assigned request number; `last_error` is kept as a receipt; `markNeedsAttention` releases the lease; redaction widened and applied to JSONB. Codex adversarial round one found the JSONB scrubber was a fail-open denylist (an `Authorization: Basic …` credential survived), that `markReady` could complete a run without a request number, and that the live PostgreSQL suite was optional; root replaced the scrubber with `assertLedgerReceipt` (allowlisted keys, flat objects, bounded and credential-free values, validated before any SQL, rejecting rather than redacting), added a shared request-number validator plus the `test_request_runs_ready_request_number` CHECK and an atomic WHERE requirement in `markReady`, and added a required CI job (`ledger-postgres`, PostgreSQL 16 service) in which the live suite fails instead of skipping; Codex round two found that allowlisted label keys (`workflowName`, `reason`, comparison values) still admitted arbitrary text, that quoted `Authorization` values survived the sanitizer, that the ready CHECK accepted an empty or malformed number, and that the new CI job would fail on Opus's live ready transition; root replaced the single string rule with per-key value grammars (identifiers, filename and app-chosen path grammars, enum tokens, date-like values; no free-text key remains, and `workflowName`/`reason` were removed from receipts), made the `Authorization` rule quote-aware, added the `test_request_runs_request_number_shape` CHECK and made the ready CHECK explicitly non-null (the live suite exposed that `NULL ~ regex` passes a CHECK), matched `markReady`'s predicate to the digit shape, and repaired the live test. Codex round three found the quoted-`Authorization` rule still leaked Digest parameters and JSON-escaped values, and that several character-class grammars (status, folder, filename, `Month YYYY` dates, eTag) still admitted prose; root now redacts everything after an `Authorization` field to end of line and replaced those grammars with semantic shapes: folders are the request-folder family `<number>_<32 hex>` with the Basic recipe's three subfolders only, filenames are the file plan's exact families, eTags are the Dataverse `W/"n"` and Graph `"{GUID},n"` forms, versions are numeric or token forms, dates are ISO or an explicit month name plus year, and the `status` string key is gone (numeric `statusCode`/`responseStatus` remain). Codex round four found that raw upstream error text still reached `last_error`, `needs_attention_reason` and resource `error`; root removed message text from the ledger entirely: those columns now hold an allowlisted code token or a classification with HTTP status (`describeLedgerError` / `ledgerReasonOrThrow`), prose is rejected, and full messages stay in the operator's private receipt. Codex round five found the reason gate was a shape check that accepted token-shaped secrets; root replaced it with the finite exported `LEDGER_REASON_CODES` set (ledger-internal codes, four foreign-error classifications, and the runner's step-outcome codes), copies a foreign error's own code only when it is a member, and added token-shaped-secret regressions through all three writers. Codex round six found `current_step` and resource `step` were unrestricted text; root added the finite exported `LEDGER_STEPS` set (fence_source, create_request, correct_meeting_date, provision_location, copy_file, observe, verify, ready), pre-SQL enum checks for resource kind, system and outcome, and `assertReservePlan`, which validates every text column of a reservation (actor, idempotency key, recipe, hosts, revision, digests, policy version, Graph identities, fiscal year, meeting date, and a bounded operator label with no URL separators) before any SQL. Codex round seven found the operator label was still a free-text sink; the ledger now derives `test_label` from validated fields (`TEST <recipe> clone of <source number> run <run id prefix>`) and ignores caller text, which stays in the private manifest. Codex round eight found the idempotency key (and by extension the actor id) could still carry a URL or JWT; both now have factory-owned grammars (keys: lowercase tokens joined by hyphens; actors: `cli|admin|user:` plus a lowercase local part without dots and an optional domain), so neither a scheme nor dot-separated base64 segments can be expressed. Codex round nine found that grammar alone still let a lowercase password or codename persist as an actor id or idempotency key; the ledger now stores nothing caller-typed in those columns: `idempotency_key` holds the SHA-256 of the caller's key (same key still maps to the same run), and `actor_id` is either an authenticated principal GUID (`admin:`/`user:` prefix) or `cli:` plus a 16-hex digest of the operating-system username (`cliActorId`). Codex round ten found that Graph identity keys (`itemId`, `driveId`, `siteId`, the reservation's expected site and drive) accepted credential-shaped strings such as a GitHub token verbatim; the ledger now requires the exact Microsoft Graph shapes (site `<host>.sharepoint.com,<guid>,<guid>`, drive `b!<base64url>`, item `01` plus 32 base32 characters) and rejects recognized credential prefixes (GitHub, OpenAI, Slack, AWS access key, JWT header, GitLab, Google API) anywhere a string is accepted. Codex round eleven found the credential screen could not see past the Graph drive prefix (`b!ghp_…` fit the base64url body) and that PostgreSQL enforced none of the JS text grammars, so a direct or version-skewed writer could still store a URL or prose; the screen now applies with and without the `b!` wrapper, and migration 054 (with its fresh-install mirror) gained CHECK constraints for every grammar-bound text column (actor id, key digest, step and recipe enums, hosts, source number and revision, digests, Graph site and drive, fiscal year, derived label, and reason codes on the three error columns, the enum lists generated from the JS exports), proven by a live test that direct SQL writes of URLs, tokens, codenames and prose are rejected. Codex round twelve found four things: the three JSONB receipt columns were unconstrained in PostgreSQL (and the run-level drive CHECK missed a `b!`-wrapped token); a re-entered `create_request`, `provision_location` or `copy_file` could dispatch a second POST or PUT after an earlier invocation had journaled a dispatch attempt whose result never became readable; the runner reported `needs_attention` when the stop transition had lost the fence; and in-place edits to migration 054 do not reach a database that already applied it. Dispositions: migration 054 now defines an IMMUTABLE SQL function `test_request_receipt_ok(jsonb)` (allowlisted keys or `<stem>At` timestamps, scalars only except the four array keys, every per-key grammar, no URL, control character or credential prefix with or without the `b!` wrapper) as a CHECK on all three receipt columns, with the credential exclusion added to the run-level Graph identifiers; a unit test proves every allowlisted key is named in the function and that the fresh-install mirror carries the identical text, and a live test replays the six receipts of Request 1000341 through the function and proves it rejects what the JS validator rejects and refuses a direct INSERT. The runner now applies a dispatch-marker rule: a prior `createAttemptedAt`, `locationCreateAttemptedAt` or `uploadAttemptedAt` with no readable remote identity stops the run (`ambiguous_create_outcome`, `location_readback_mismatch`, `file_ambiguous_unrecovered`) instead of dispatching again; absence after an attempt is never treated as definitive because the earlier worker may be stalled past its lease. The runner does not renew leases: every step is bounded well under the 300-second lease and the marker rule, not a shorter window, is what prevents a duplicate dispatch. The step-failure path is one fenced UPDATE (`markNeedsAttention` now records `last_error` in the same statement); when it loses the fence the runner reports `lease_lost` with the durable row as it is. Migration 054 is applied to no durable database (only CI's per-run PostgreSQL service and the owner's throwaway local container), so its edits stay in place until the first shared apply, after which changes go in new numbered migrations; both live suites now fail loudly when a throwaway database's constraint set predates the migration file. Codex round thirteen found that the create step's response write replaced the readback and so erased the `createAttemptedAt` marker (a definitive POST error followed by a resume would POST again), and that the credential screen only looked at position zero, so a token behind an eTag quote or a drive-id separator passed both layers. Fixes: `recordResourceReadback` now merges into the stored receipt (`readback || new`) and keeps an earlier response status, the runner accumulates create receipt state across callbacks and treats any recorded response, non-planned outcome or attempt timestamp as an attempt; the credential regex is anchored at the start or after any non-alphanumeric character in JavaScript, the SQL receipt function and the run-level CHECK alike (identical text), with JavaScript and live PostgreSQL rejections for quoted eTags, `W/"ghp_…"`, an AWS key as a version id and a drive id carrying `-ghp_`; a live test proves a merged readback keeps its marker, and a live runner test proves a definitive POST error followed by a resume performs zero further POSTs; the stale-schema guard now also compares the live receipt function body with the migration text. Codex round fourteen accepted the runner's re-entry, GoVerify and fencing paths and found one remaining ledger gap: a credential marker preceded by an alphanumeric character (`b!AAAAgithub_pat_…`) passed the boundary-anchored screen. The screen now matches a marker anywhere inside a value in JavaScript, the SQL receipt function and the run-level CHECK (identical text; a genuine identifier that happens to contain a marker is refused, fail closed), with JavaScript, live-function and direct-SQL rejections for alphanumeric-prefixed markers. Codex round fifteen: **approve**, no material findings ("alphanumeric-prefixed credential markers are rejected consistently by JavaScript, the SQL receipt function, and run-level CHECKs; migration/fresh-install SQL remain aligned, and runner behavior is unaffected"). Build-order item 5 is complete on the Factory branch and was **accepted by the owner in Session 538 (2026-09-24)**; migration 054 remains unapplied to any shared database, and its first shared apply still needs separate owner authorization.

**Build-order item 6 blocker (found 2026-09-24, Session 537; RESOLVED 2026-09-24, Session 538).** Resolution: rather than a solution import, the sandbox was brought to production `wmkf_` schema parity from this repository by replaying every schema wave plus two waves generated from production metadata; every production `wmkf_` table, column and choice value now exists in the sandbox, reads of the new tables return 200, and sandbox record creates are not yet exercised [ASSUMED until the first recipe run]. Recorded deviations: two computed columns exist as plain columns, and three choice labels differ from production (owner: leave them), so a recipe must not depend on those labels. Record and replay recipe: PR #331, `docs/plans/SANDBOX_SCHEMA_PARITY_2026-09-24.md` on `claude/sandbox-schema-parity`. <!-- doc-symbol-refs:ignore reason=other-branch --> The original finding follows. [VERIFIED via read-only sandbox metadata probe; names in `evidence/test-request-factory/sandbox-schema-gap-2026-09-24.md`] The registered sandbox has no `wmkf_requestdocument` entity, no `wmkf_currentinitialassessment` or `wmkf_ai_fieldprimer` column on `akoya_request`, and none of the review tables (`wmkf_appreviewersuggestion`, `wmkf_appreviewanswer`, `wmkf_reviewquestion`), AI-run tables or the other application-added entities the later-stage recipes write. The Initial Assessment recipe and everything after it therefore cannot be proven live until the application solution is imported into the sandbox, an owner action outside this repository. Reconnaissance for the IA recipe is complete and recorded here for when that lands: the app files a new IA through `generateInitialAssessment` (`lib/services/initial-assessment/artifact-service.js`), whose identity (`buildInitialAssessmentIdentity`) hashes request GUID, number, title, institution, cycle code and proposal inputs, so a synthetic seed must fix deterministic stand-ins for the proposal inputs; path is `<request folder>/Artifacts/Initial Assessment` and filename `<number> Initial Assessment <key8>-<claim8>.docx`; bytes come from the pure `renderInitialAssessmentDocx` (`template.js`) fed a synthetic `generated` object satisfying `REQUIRED_OUTPUTS`; the hash is `hashGovernedDocxContent`; `commitReadyLineage` sets READY, the SharePoint identity fields and the request pointer `wmkf_CurrentInitialAssessment@odata.bind` in one If-Match changeset; the board snapshot (`createInitialAssessmentBoardSnapshot`, injectable dependencies) requires the pointer first. The artifact service uses module singletons (`DynamicsService`, `GraphService`) bound to the configured production host, so a sandbox CLI step must write through the sandbox client under the ledger's journal, and the production form (item 7) must route through `requestDocumentAdapter` with a registered seam in `scripts/check-request-document-writers.js`; the seed must never write the immutable origin fields. Ledger additions the recipe needs: a recipe token, steps `seed_initial_assessment` and `seed_initial_assessment_snapshot`, resource kind `dataverse_request_document`, receipt keys for the generation key and claim id, folder and filename grammar families for the IA path, and reason codes for claim loss and pointer mismatch.

**Bounded resumable runner and CLI (build-order item 5, second half: slice 5b), built Session 537 (2026-09-24).** [SOURCE-BUILT on `codex/test-request-preview-integration`; 33 focused unit tests plus 9 live runner tests and the ledger's 11 live tests pass against the throwaway local Postgres container; proven live against the sandbox Dataverse as Request 1000341, see the end of this paragraph] `lib/services/test-requests/basic-clone-steps.js` holds the step bodies extracted from the rehearsal script's `--execute` path (preflight, source fence, body compilation, GoVerify bypass pairing, meeting-date correction, location provisioning, observation, verification), and `lib/services/test-requests/run-runner.js` (`advanceRun`) performs exactly one step per call against the ledger: it refuses a manifest or bundle whose digests and bound GUIDs differ from the reserved run before claiming any lease, claims the lease once, runs the handler for `current_step` (fence_source → create_request → correct_meeting_date → provision_location → copy_file, one document per call → observe → verify), journals each resource before its dispatch and records readbacks after, then advances and releases, or records a coded error and marks `needs_attention`; a lost fence returns `lease_lost` without claiming anything was recorded. Re-entry rules: `create_request` first refuses if the run's last GoVerify bypass row is not confirmed restored, then reads the preallocated GUID (absent → POST; present and owned → recovered, no POST; present and not owned → permanent stop); `provision_location` recovers only a single owned location whose folder equals the expected request folder; `copy_file` refuses an index whose journaled item id was never verified (`file_journal_unverified`) and reuses a planned row that never dispatched; a resumed run whose bundle has passed its 6-hour window stops with `bundle_stale`. The CLI modes `--reserve` (writes the private manifest first, then reserves; actor defaults to `cliActorId` of the OS username, `--actor` accepts only `admin:`/`user:` GUIDs or an OS username, keys are pre-checked with `idempotencyKeyDigest`), `--advance` (one step; the full error text goes to stdout and a 0600 sidecar beside the manifest, never to Postgres) and `--run-inspect` (ledger only, no Dataverse client) require `TEST_REQUEST_LEDGER_URL` and refuse unset, neon.tech, or shared `POSTGRES_URL*`/`DATABASE_URL` values. The CI job `ledger-postgres` now runs both live suites. Review record: Sonnet built; Opus review found twelve defects, all fixed with live-ledger tests and four mutation checks: GoVerify readbacks used outcomes the ledger rejects (every bypass run failed at its first journal), Dataverse version numbers were journaled as numbers, file readbacks carried camelCase kinds the grammar rejects and a constant index, `--reserve` sent a full URL as the destination host, the unrestored-bypass check only ran when the GUID was absent, the pending-file lookup matched on the run-wide sequence so recovery never fired, a non-matching meeting-date readback was recorded `verified`, a present preallocated GUID was coded `unknown_error`, `--reserve` reserved before writing the manifest, a recovered location was not checked against the expected folder, a null `recordError` was reported as `needs_attention`, and recovered request numbers were numeric. Root's pass relabelled the location row's system as Dataverse, widened the ledger-URL guard, coded the stale-bundle stop, and added the runner suite to CI. Known limits recorded for later: a same-key `--reserve` retry after a lost reserve response is a 409 (the plan digest includes the fresh run id; recover with `--run-inspect` on the manifest's run id); a GoVerify refusal has no operator-clear action (start a new run after restoring the workflow by hand); recovered creates and crashed steps leave superseded `planned`/`dispatched` rows; `observe` is one 60-second call within the 300-second lease. Live proof (2026-09-24, Session 537): run `3f83c1e2` reserved from the 1003222 bundle against the throwaway local ledger, then advanced across four separate CLI invocations (fence_source; create_request with the paired GoVerify bypass, journaled `verified` with `restoreVerified`; date, location and first copy; second copy, observe, verify) to `ready` as sandbox Request 1000341 with two copied documents, six resource rows, no message text in any column; evidence `docs/plans/evidence/test-request-factory/ledger-run-1000341-2026-09-24.json`. Next: build-order items 6 and 7.

**Build-order item 6, Initial Assessment recipe — plan (Session 538, 2026-09-24; revised after Codex plan review rounds 1–4).** [PLANNED; source facts VERIFIED via reads of `run-runner.js`, `artifact-service.js`, `artifact-lineage.js`, `controls-service.js`, `artifact-reader.js` and `lib/services/dynamics/changeset.js` in this session] Owner decisions carried: synthetic IA content only (Session 537); one new sandbox Request per run, up to two creates for this proof; production reads authorized for this task (bundle re-export only). Two slices, each with Codex adversarial review before acceptance, mirroring 5a/5b.

*Slice 6a — recipe dimension (ledger + runner, no IA bodies).* `test_request_runs.recipe` already exists with the single token `basic` (`054_test_request_runs.sql` CHECK, `run-ledger.js` `RECIPES`), and the Admin preview and policy compiler emit and require `basic`. Keep `basic` exactly and add `initial_assessment` to the SQL CHECK (in-place edit of unapplied migration 054), its `scripts/setup-database.js` mirror and the JS set, pinned by the existing parity test. Bind the recipe into idempotency: include it in the manifest and the reservation plan digest, and compare it with the ledger row in the runner's pre-lease manifest check, so a same-key retry naming a different recipe conflicts instead of returning the first run. Replace the runner's flat `STEP_ORDER` and hardcoded `nextStepIndex` values with a per-recipe step order: `basic` is unchanged; `initial_assessment` is the Basic steps, then `seed_initial_assessment`, `seed_initial_assessment_snapshot`, `verify_initial_assessment`, where the Basic `verify` advances instead of calling `markReady` and only the recipe's final step marks the run ready. Add the step names, resource kind `dataverse_request_document`, receipt keys (generation key, claim token, content hash, request-document id, source version id) and reason codes (`ia_claim_lost`, `ia_pointer_mismatch`, `ia_upload_ambiguous`, `ia_snapshot_stale`, `ia_verification_failed`) to the ledger's finite sets in JavaScript and in the SQL lists; every value keeps the no-text invariant (ids, digests, fixed tokens). `--reserve` gains `--recipe=`. Live PostgreSQL suites cover routing for both recipes, every terminal transition, and a same-key cross-recipe reservation conflict, and show that a `basic` run's persisted rows are unchanged.

*Slice 6b — IA step bodies.* The IA services reach Dataverse through module-level adapters whose host is `process.env.DYNAMICS_URL`, read on each call (`lib/services/dynamics/read-ops.js`, `changeset.js:109`); the sandbox steps must not change that process-wide. Instead:
1. **Injection seams, default-preserving.** `createInitialAssessmentBoardSnapshot` already takes `dependencies`. Give `commitReadyLineage` (and its `assertOwnedClaim` / `rereadByGenerationKey` helpers) and `resolveCanonicalInitialAssessment` the same optional `dependencies` parameter (request-document create/update/find, request read, changeset runner). Production callers pass nothing and behave exactly as today; unit tests pin that.
2. **One target-bound sandbox service.** `executeChangeset` (`lib/services/dynamics/changeset.js`) gets its token from `svc.getAccessToken()` but its URL from `process.env.DYNAMICS_URL`, and the runner's `createClient` does not expose the service interface it needs. Add a sandbox Dataverse service (`lib/services/test-requests/ia-sandbox-deps.js`) whose constructor refuses any host other than the registered sandbox and binds that one host to its token, its interlock-checked `_writeFetch`, `_withCallerId` and an explicit base URL. `executeChangeset` uses an explicit base URL when the service supplies one and never falls back to the environment for such a service; the module singleton keeps today's behavior. The lineage commit stays one `$batch` changeset. All sandbox deps (request-document create/update/find, request read, changeset) are built from this one service, run inside a trusted DAL context, and the writer seam is registered in `scripts/check-request-document-writers.js`. Tests set `DYNAMICS_URL` to the production host and prove every sandbox-path request, including the `$batch`, goes to the sandbox; a unit test proves the changeset is one request whose embedded failure yields a failed result with no separate writes.
3. **Journal before every remote mutation.** Because the deps are injected, the step wraps **every mutating dependency** with ledger journaling instead of changing app code: `createDocument`, `updateDocument` (the seed's hash PATCH and every snapshot claim, failure and Ready PATCH), `ensureFolderPath`, `uploadFile` and the changeset. Each wrapper records an operation-specific planned identity and an attempt marker before dispatch and a readback after. In particular, the `createDocument` wrapper records the planned identity (generation key, claim token digest, folder and filename families) and a create-attempt marker before the POST; the `uploadFile` wrapper records an upload-attempt marker before the PUT and passes `onItemCreated` to persist the item id the moment the PUT commits (the hook runs only after commit, so the attempt marker is what a crash between PUT and callback leaves behind); the changeset wrapper records a commit-attempt marker before the `$batch`. The snapshot step gets the same wrappers, so `createInitialAssessmentBoardSnapshot`'s internally computed generation key, row and file are journaled before each write without changing its code. Resume rules: an attempt marker with no readable result stops the run (`ia_upload_ambiguous` or a matching reason), never re-dispatches. Tests simulate process death before and after each Dataverse POST and PATCH (including the snapshot's claim, failure and Ready PATCHes), folder create, Graph PUT, item-id callback, changeset response and ledger write.
4. **Synthetic fixture**, tracked in the repo: a `generated` object satisfying `INITIAL_ASSESSMENT_REQUIRED_OUTPUTS` (`summary`, `significance_impact`, `research_plan`, `team_expertise`) plus a stand-in narrative (at least 60 characters) and stand-in filename. Identity is `buildInitialAssessmentIdentity` over the destination request's number, title, institution and cycle and the stand-in narrative. This reconciles the two earlier statements: the live seed uses the stand-in; the internal contract test mocks `getAiProposalNarrativeText` to return the same stand-in and asserts a normal generation call returns `reused: true` with no provider invocation. A later real Generate on the test request reads the copied narrative, gets a different key and creates a new IA, which the Later-stage recipes table already accepts. The ledger stores the fixture's digest, never its text.
5. **`seed_initial_assessment`**: create the registry row as Generating with the same fields the producer writes (`artifact-service.js` create body); render with `renderInitialAssessmentDocx` and hash with `hashGovernedDocxContent`; ETag-update the owned row with `wmkf_contenthash` and the contract's prompt name and version, as the producer does before upload (`commitReadyLineage`'s changeset does not write the hash), and reacquire the claim; ensure the `Artifacts/Initial Assessment` folder and upload; then `commitReadyLineage(row, { metadata, claimToken }, sandboxDeps)`. Re-entry resolves by generation key; an upload marked attempted with no readable item stops the run (`ia_upload_ambiguous`), never re-uploads (dispatch-marker rule).
6. **`seed_initial_assessment_snapshot`**: `createInitialAssessmentBoardSnapshot` with sandbox deps and the canonical file's current version id; journal the snapshot row and file.
7. **`verify_initial_assessment`**, the recipe's terminal verifier, repeats the Basic safety verification after the IA writes (the operation contract requires verification of all requested resources and forbidden links immediately before readiness): re-fence the source; compare fresh Foundation-account and Foundation-Contact snapshots against a **durable pre-IA baseline** (for the `initial_assessment` recipe the Basic `verify` step records a SHA-256 digest of its Foundation/Contact identity-and-version snapshot as a ledger receipt value, which keeps the no-text invariant; `verifyClone`'s own before/after pair is taken seconds apart inside one step and cannot see a change made between steps); re-run the Basic verifier (`verifyClone`: owner, location, prohibited payment/email/Foundation/Contact checks) with a folder census that expects exactly the Basic copies plus the IA and snapshot files; re-verify every copied file by stable id and hash. Then it resolves the canonical IA through the sandbox-backed reader; confirm the request pointer, Ready state, a non-null `wmkf_contenthash` equal to the hash of the downloaded bytes, and a Ready board snapshot whose source version and source hash match; then read the snapshot file's metadata by its persisted drive and item id, download it, read the metadata again, require the two reads to agree, and require the downloaded bytes' governed hash to equal both the snapshot row's `wmkf_contenthash` and its `wmkf_sourcecontenthash` (the same check `readStableRetainedSnapshotFile` applies); only then `markReady`. Tests prove that a copied-file mutation, an unexpected file in the folder, a Foundation-account or Foundation-Contact change, or another forbidden side effect introduced after the Basic `verify` step prevents readiness. The baseline digest is one more receipt key in slice 6a's ledger additions.

*Proof.* Re-export the 1003222 bundle immediately before the run (six-hour window), reserve one `initial_assessment` run and advance it to `ready`: one new sandbox Request. Evidence file under `evidence/test-request-factory/`. Start `wmkf-ledger-pg` and drop the three ledger objects before the live suites, since 054 changes.

*Plan review.* Codex adversarial plan review (`gpt-5.6-sol`): round 1 needs-attention (sandbox target binding and atomic changeset, journal-before-dispatch, content hash never persisted, recipe token and idempotency), round 2 (unwrapped PATCH and folder mutations, snapshot bytes not hashed), round 3 (Basic safety checks ran before the IA writes), round 4 (no durable pre-IA Foundation/Contact baseline); each resolved in this paragraph. Round 5: **approve**, no material findings (2026-09-24).

*Slice 6a built (2026-09-24, Session 538).* [SOURCE-BUILT on this branch: `86c8549dc`, `a13b7b885`, `178d56d00`, `38c2a34f6`; 544 focused tests pass, including live suites against a throwaway local PostgreSQL 16; mutation checks on the baseline comparison, insert-time digest, one-baseline index, SQL filename grammar and legacy-execute guard] Recipe `initial_assessment` added beside unchanged `basic`, bound into the plan digest and the pre-lease manifest check; per-recipe step order (`nextStepFor`), with only a recipe's final step marking the run ready and the three IA steps stopping with `recipe_step_not_built` until 6b. The Basic `verify` of an IA run records the Foundation/Contact baseline digest as its own `foundation_baseline` resource, written in the inserting call and limited to one per run by a partial unique index; a repeated verify compares against it (`ia_verification_failed` on mismatch). The ledger grammars accept the IA folder, its `Board Milestones` child and the two IA DOCX filename families, identically in JavaScript and SQL. The legacy one-shot `--execute` refuses non-`basic` manifests. Root review also fixed a pre-existing red `check:secret-scan` on this branch (token-shaped test fixtures now assembled at runtime). Codex adversarial review: round 1 needs-attention (lost-response baseline, IA grammar), round 2 needs-attention (legacy `--execute` accepted IA manifests; fixed by Codex rescue, reviewed by Claude), round 3 **approve**. Slice 6a was **accepted by the owner in Session 539 (2026-09-24)**; slice 6b (IA step bodies) is next.

*Slice 6b Stage A built (2026-09-24, Session 539; plan items 1–2).* [SOURCE-BUILT on this branch: `2bf5904a9`, `10e0a5e4f`, `4919e2d37`, `e74225c94`; 243 focused tests across 15 suites plus mutation checks] Sonnet built, Opus reviewed (round 1 needs-changes, round 2 approve), Fable final review. `commitReadyLineage`, its `assertOwnedClaim` / `rereadByGenerationKey` / private `verifyReadyLineage` helpers, and `resolveCanonicalInitialAssessment` take an optional positional `dependencies` argument (third for `commitReadyLineage`, second for the reader, matching the pre-site-visit and Pre-RP siblings) defaulting to today's module adapters; production callers pass nothing and mutation checks prove the defaults are load-bearing. `lib/services/test-requests/ia-sandbox-deps.js` builds one frozen sandbox-bound service (`https://` bare origin whose hostname is in `SANDBOX_HOSTS`; token fetched per call because `lib/dataverse/client.js` exposes no expiry) and derives `findByGenerationKey` / `findByRequest` / `getRequest` / `runChangeset` from it: reads go through `lib/dataverse/client.js` `createClient` with the annotations Prefer header and `processAnnotations`, so rows carry `_etag` and `*_formatted` exactly as the production read path; the write goes through `executeChangeset`, which now prefers an explicit string `svc.baseUrl` over `process.env.DYNAMICS_URL` (the `DynamicsService` singleton has none and keeps env behavior). Every dependency asserts a trusted DAL context; `getRequest` refuses a non-GUID id; a page-cap overrun throws `ia_sandbox_deps_pagination_cap_exceeded` rather than truncating. Tests set `DYNAMICS_URL` to the production host with the interlock on and prove every request, including each embedded line of the `$batch` body, targets the sandbox; an end-to-end test drives the real `commitReadyLineage` over the sandbox deps to a stateful `$batch` responder. One named `EXEMPT_FILES` entry in `scripts/check-dataverse-access-layer.js` covers the module. Deviations recorded: no create/update keys in the defaults (the four functions never call them; Stage B adds them at the step site), and the plan's pointer to a `createClient` in `sandbox-clone.js` was wrong (it lives in `lib/dataverse/client.js`). Carried to Stage B: `createRecord` / `updateRecord` in `lib/services/dynamics/write-core.js` read `process.env.DYNAMICS_URL`, so the sandbox `createDocument` / `updateDocument` deps need their own bound path; never pass `actingUserSystemId` (MSCRMCallerID) to the sandbox; `requestDocumentSelect()` picks optional columns from env readiness flags that describe the `DYNAMICS_URL` org, to be confirmed against the sandbox at the live run; writer-seam registration happens at the step site that calls `dependencies.createDocument(`.

*Slice 6b Stage B built (2026-09-24, Session 539; plan items 3–5).* [SOURCE-BUILT on this branch: `646a1707e` … `09db93e30` (nine Sonnet commits), `06e34859b`; full unit suite 15,622 tests at Sonnet's last run, focused suites plus 15 Opus mutations killed, one caught by the writer gate] Sonnet built, Opus reviewed (round 1 needs-changes: four P1, two P2; round 2 approve), Fable final review. `createRecord` / `updateRecord` / `deleteRecord` / `disassociate` in `lib/services/dynamics/write-core.js` prefer an explicit string `svc.baseUrl` exactly as `executeChangeset` does; `requestDocumentAdapter.create` / `update` take an optional `options.svc` (default path unchanged, pinned). `createIaSandboxDeps` gained `createDocument` / `updateDocument` and an optional `graph` whose `ensureFolderPath` / `uploadFile` / `getFileMetadataById` / `downloadFile` are forwarded under the `controls-service.js` dependency key names; it re-exports `processAnnotations` so the runner's own sandbox reads take the transform from the registered transport module rather than through a facade allowlist change. Synthetic fixture `lib/services/test-requests/fixtures/initial-assessment-synthetic.js` (the four required outputs plus a 114-character stand-in narrative); its contract test proves a Ready row at the fixture's generation key returns `reused: true` with no provider, upload, create or update call. `stepSeedInitialAssessment` replaces the `seed_initial_assessment` stub: `runPreflight` first and `preflight_identity_changed` on any site/drive mismatch with the run's expected ids; destination request read through `processAnnotations` (a raw read left the institution empty and would have failed every live run); one `dataverse_request_document` resource per run whose receipt merges resend the full current state; every mutating call journaled before dispatch; a create marker with no readable row stops with `ambiguous_create_outcome` (never a second POST; the create catch always rereads by generation key and adopts or stops, never matches on status or message; the HTTP status and cause reach only the CLI message); explicit site and drive ids passed to the folder create and the PUT; an upload marker with no journaled item stops `ia_upload_ambiguous`; a journaled item is re-read by stable id (a 404, or a drive other than the verified one, stops and never re-PUTs); the ledger `contentHash` is the hex of the governed `gdc1:` digest (render-stable, equal to `wmkf_contenthash`) and is never re-merged on a no-upload resume; then `commitReadyLineage(row, opts, sandboxDeps)`; the step never calls `markReady`. New actor policy `REQUEST_DOCUMENT_ACTOR_POLICY.SANDBOX_REHEARSAL` (resolved without a system-user read, excluded from `isActorNotCaptured` like `external-contributor`) is the only policy the step passes, which closes both the adapter's lost-response recovery reread through the production DAL and the shared `operational_events` insert; it is reachable from exactly one runtime site and the writer gate pins the policy per seam (10 seams). Decision: a stopped run leaves its row Generating (the plan requires a stop, not Failed; `markFailedIfOwned` would make resume fail `ia_claim_lost`; a stale Generating row blocks nothing later). Carried to Stage C: forward `getFileMetadataByPath` / `getFileVersionMetadata` / `downloadFileVersion` / `hashDocx` / `newClaimToken` / `now` and build `resolveCanonical` over the sandbox deps; export `controls-service.js` `DEFAULT_DEPENDENCIES` if needed; the rehearsal Graph wrapper lacks `getFileVersionMetadata` / `downloadFileVersion`; `requestDocumentSelect()` env-flag columns and real Graph behavior remain unverified until the live run.

*Slice 6b Stage C built (2026-09-24, Session 539; plan items 6–7); Opus rounds closed and Fable final review done (Session 542).* [SOURCE-BUILT on this branch: `936b5e008`, `41c549165`, `9aa8a6d63`, `57935a658`, then round-2 fixes `46f4a9457`, `e96c7d6c6`, `91b02249b`, then round 3 `aa87c3c1d` (tests only, Fable); full unit suite 15,649 at Sonnet's last run; every named stop rule mutation-killed by Sonnet; Opus round 1 needs-changes (two P1, P2 test gaps, P3s), round 2 needs-changes with no source change required (the eTag arm of the twin snapshot-metadata read and each single arm of the snapshot bytes-versus-row-hash check lacked an isolating test; the verify fixture manifest was not fully validator-shaped), round 3 closed by Fable under the three-rounds rule: the eTag test now asserts the arm's own message and that the step stops before reverifyClone's third metadata read, a versionId-arm test and message assertions on the size and lastModified arms were added, V11a corrupts only the snapshot row's own content hash and V11b gives bytes that hash to the row's own hash but not its source hash, and the verify fixture is validator-shaped with a `validateCloneManifest(..., { allowExpired: true })` self-check; five mutations killed (eTag arm, versionId arm, row-hash arm, source-hash arm, fixture `target` removed); verify suite 20 tests, focused 7 suites / 101 tests, live-Postgres runner suite 15/15 after the throwaway ledger schema was reset to the current 054 file; Fable final review of `1499b0fdb..aa87c3c1d` confirmed the census `+2` is a shallow clone that bumps only `expectedSharePointFiles`, `enterDynamicsBypassForScript` has one call site inside `runAdvance` with the CLI modes mutually exclusive, and the sandbox `createDocument` substitutes `SANDBOX_REHEARSAL` and strips `actingUserSystemId` one layer below the production seam; Opus confirmed the census `+2` cannot be satisfied by two wrong files because `verifyCopiedFiles` matches by stable id, and the script-scoped bypass cannot leak into `--reserve` / `--run-inspect`; Codex adversarial review NOT done; no live run] `createIaSandboxDeps` completes the board-snapshot dependency contract (`resolveCanonical` over the sandbox reads, `getFileMetadataByPath` / `getFileVersionMetadata` / `downloadFileVersion` forwarded, `hashDocx` / `newClaimToken` / `now`; `restoreFileVersion` not forwarded because the snapshot function never calls it) and its `createDocument` always substitutes `SANDBOX_REHEARSAL` for the caller's policy and strips `actingUserSystemId`, because `createInitialAssessmentBoardSnapshot` hardcodes `REQUIRED` at its registered seam (Opus: correct layer; unreachable from production; the writer gate still pins the production seam as `REQUIRED`). `stepSeedInitialAssessmentSnapshot` calls the unmodified `createInitialAssessmentBoardSnapshot` over journal-before-dispatch wrappers (create, update, folder, upload with injected site/drive ids and `onItemCreated`, path lookup that must resolve to the journaled item id), plans the resource on the first create because the generation key is computed inside the function (an update-only path plans it too), passes the seed step's journaled `sourceVersionId` as `expectedCurrentVersionId` so the function's stale check has teeth, refuses to invoke the function when an upload marker has no item id, and rereads by generation key on any create error (adopt or `ambiguous_create_outcome`); `mapSnapshotErrorCode` maps the function's error codes to `ia_snapshot_stale` / `ia_claim_lost` / `ia_verification_failed` (`ia_claim_lost` inside the function's 15-minute lease after a crash is expected and self-resolving). `stepVerifyInitialAssessment` is the only IA step that marks ready: preflight and site/drive check, source re-fence, fresh Foundation/Contact digest against the durable `foundation_baseline` resource, sandbox `resolveCanonical` with the seed's `requestDocumentId` (its codes become `ia_pointer_mismatch`), IA bytes hashed against `wmkf_contenthash`, snapshot Ready with matching source version and source hash, two metadata reads around one download of the retained snapshot required to agree with bytes hashing to both row hashes, row item ids cross-checked against the journaled receipts, then the Basic census and re-verification over the ledger's copy_file resources plus exactly the IA and snapshot entries against a manifest whose expected file count is the Basic count plus two, then `markReady` after asserting the step is terminal. Bugs found by building against the real verifier: `basic-clone-steps.js` `sha256()` JSON-stringifies its argument (a local raw-byte hash is used for file bytes); `hashDocx` throws on non-DOCX bytes (now `ia_verification_failed`); the verifier's census count would always have failed by two on a real manifest; the CLI `--advance` path never entered a trusted DAL context (now `enterDynamicsBypassForScript` inside `runAdvance` only, pinned from both sides); a pre-existing red live test still asserted the 6a stubs. Known limits: the census walks three folder levels; a 412 on the Ready PATCH stops as `upstream_http`. Next: merge `main` into the Factory branch, Codex adversarial review of the whole slice, then the live proof.

*Slice 6b Codex adversarial round 1 (2026-09-24, Session 542; `--model gpt-5.6-sol`, `--base b2afbe919^` from a detached worktree at `b70e42fc3` so the later `main` merge did not pollute the diff).* Verdict needs-attention, four findings; Fable adjudicated. [SOURCE-BUILT on this branch: `13e9d8d71`] F1 (high, confirmed against source): the seed step's Ready-row recovery journaled only the request-document id and generation key, so a crash between `commitReadyLineage` and the final receipt merge left a run the snapshot step stops on permanently (`ia_pointer_mismatch`, no `sourceVersionId`); recovery now refuses a Ready row missing its item, drive, version or content hash, refuses one whose item disagrees with an already-journaled item, and journals the full receipt from the Ready row before advancing. F2 (high, confirmed): the snapshot step's path-lookup wrapper guarded only a non-null mismatch, so a null path read with a journaled item fell through to the production function's upload branch and a second PUT; it now stops `ia_upload_ambiguous`. F3 (high, partly accepted): the verify step compared rows only to bytes read now; it now anchors both rows' content hashes and SharePoint versions to the seed and snapshot step receipts (mandatory since `46411dc29`: every producer path journals them, so a missing field fails closed), so an in-place file replacement plus a consistent row-hash rewrite no longer verifies. Codex's further ask, recomputing the deterministic synthetic fixture's governed hash in the verify step as an anchor independent of the seed step's own receipt, is design-changing and goes to the owner; it is cheap if chosen [VERIFIED 2026-09-24: two independent renders of the synthetic fixture differ in bytes but produce the same governed hash]. F4 (medium, not acted on): no lease renewal on a fixed 300-second lease while the IA steps run several sequential Dataverse and Graph calls; `run-ledger.js` already exposes `renewLease`. This challenges the recorded 6b decision (no lease renewal) and goes to the owner. Eight mutations killed (recovery drops `sourceVersionId`, recovery drops `contentHash`, null-path guard disabled, each of the four receipt anchors disabled, anchors made optional again); focused 7 suites / 110 tests, live-Postgres runner suite 15/15, changed-surface gates green. **Owner decisions (2026-09-24, Session 542), built in `e0f79838a`:** (1) the verify step re-renders the synthetic fixture from the destination Request's own fields (`readIaRenderInputs`, shared with the seed step) and requires the Initial Assessment row's governed hash to equal the fresh render's, an anchor independent of every receipt; (2) no lease renewal stays decided, and the CLI's `--advance` passes the ledger's maximum 900-second lease for the `initial_assessment` recipe (300 s otherwise; `renewLease` stays unused, source-pinned by test), to be reconsidered only if a live run shows longer phases. **Codex adversarial round 2 (`--base 16b172152`): needs-attention, two findings.** F5 (high, confirmed, built in `d564af8aa`): Ready-row recovery adopted any Ready row at the deterministic generation key; it now requires this run's pre-create receipt, requires the row's retained `wmkf_claimtoken` to hash to the journaled `claimTokenSha256`, checks every already-journaled identity against the row, and never overwrites a journaled anchor (three mutations killed; focused 8 suites / 127 tests, live-Postgres 15/15). F6 (medium, owner decision): `hashGovernedDocxContent` excludes every non-`word/` package part by design, so a DOCX carrying a foreign `customXml` part hashes identically to the fresh render (Codex demonstrated it in memory: 9,424 to 32,299 bytes, same governed hash); the fresh-render, receipt, snapshot and reverification checks are all governed-hash or self-derived, so no check attests the complete package. **Owner decision (2026-09-24): journal the bytes, built in `af65a24bd`.** New `bytesSha256` receipt key (HEX64; `run-ledger.js` KEY_RULES and the unapplied 054 receipt CHECK, edited in place); the seed step journals the raw SHA-256 of the rendered DOCX and the snapshot step's upload wrapper the SHA-256 of the bytes it hands to Graph, each in the same merge as `uploadAttemptedAt` before the PUT; the verify step requires each downloaded file to match its step's digest (mandatory). A second render of the fixture (same governed hash, different bytes) now stops for both files; four mutations killed; focused 9 suites / 175 tests; live-Postgres 15/15. Codex could not run Jest (read-only sandbox EPERM); its verdicts are source-based. **Codex adversarial round 3 (`--base 16b172152`, the cap): needs-attention, two findings; Fable closes the loop, built in `26f5a64c0`.** F7 (high, confirmed): ownership was asserted only for Ready rows, so a foreign Generating row at the deterministic key (a staff Generate in progress) fell through to the resume path, which copied the row's claim-token digest into the receipt and then patched, uploaded and committed it; `assertIaRowOwnedByThisRun` now runs on every row the seed step resolves (first read and post-create reread), requiring this run's pre-create receipt and a matching `wmkf_claimtoken` digest before any read or write, and the resume merge never derives the digest from the row (three tests assert no write reaches Dataverse; three mutations killed). F8 (high, confirmed): `scripts/setup-database.js` mirrors the 054 receipt CHECK for fresh installs and lacked `bytesSha256`; fixed (mirror test 44/44). Codex's forward-migration ask is declined under the recorded decision that 054 is unapplied on every shared database and edited in place; the throwaway ledger was reset to the edited file. Focused 10 suites / 222 tests, live-Postgres 15/15, gates green. Slice 6b review loops are closed. **Live proof (2026-09-24, owner re-confirmed): sandbox Request 1000342 reached `ready`** through all eleven steps against real Dataverse and Graph (`docs/plans/evidence/test-request-factory/ia-recipe-live-proof-2026-09-24.md`). Two things only the live run could show, both fixed on the branch: the GoVerify deactivation PATCH outran the 15 s bypass bound on run 1 (no Request created; workflow re-activated by hand; bound raised to 60 s, `53960b008`), and SharePoint property promotion rewrites every uploaded DOCX so the raw `bytesSha256` anchor could never match a download; it is replaced by `docx-package-attestation.js` (`9f061fe92`), a part-by-part comparison with the fresh render that normalizes only the characterized SharePoint mutations (customXml items recognized by SharePoint root element or relationship type only, not inspected further; a tenant promotion change fails closed), keeping the F6 intent (foreign customXml payloads and unknown parts still fail; 12 unit cases, 4 verify-suite cases, 4 mutations killed). `bytesSha256` stays journaled as evidence only. **Owner accepted the attestation in place of the raw byte digest (2026-09-24): same user-facing content proven by the governed hash, plus no unexplained parts anywhere in the package.** **Slice 6b accepted by the owner (2026-09-24), with Fable's self-review accepted for the post-cap Claude-authored code (F7 ownership assertion, fresh-install receipt mirror, 60 s bypass bound, package attestation); owner directed the merge to `main`.** **Merged 2026-09-24 PT as PR #336 (`b63803951`) after a CI fix round (`4c5539ee7`: the branch's email-guard fixture against PR #335's mandatory recipients; hostname checks for eleven CodeQL substring warnings in test fetch mocks); production deployment `dpl_F3XxWLs9Kcg8feoXVgzbkmcpHdfJ` (build of `18a2c94e6`) Ready and owning `applications.wmkeck.org`.** Migration 054 remains unapplied until item 7; nothing applies on deploy.

*Open checks at build time.* Whether the Basic clone's copied `ProposalNarrative_<number>.pdf` lands where `resolveActiveAiMaterialsFolder` expects (affects only the later-Generate note above); whether the sandbox app user can create `wmkf_requestdocument` rows (reads are proven, creates are not).

**Build-order item 6, synthetic reviewers and reviews recipe — plan (Session 543, 2026-09-25).** [PLANNED; source facts VERIFIED in this session via reads of `run-runner.js`, `run-ledger.js`, `source-bundle.js`, `lib/dataverse/adapters/{potential-reviewer,reviewer-suggestion,review-answer}.js`, `lib/services/external-review/submit-service.js`, `lib/services/review-upload.js`, `lib/services/test-requests/request-test-state.js`, `lib/dataverse/core/entity-registry.js` and `scripts/check-reviewer-engagement-boundary.js`] Owner decisions carried: 3 (reviews cloned onto synthetic reviewers seeded 1:1), 4 (real staff-controlled throwaway addresses, editable per run, never a real reviewer's address as a default, assignment recorded in the run ledger), 9 (email only to synthetic reviewers; Contact promotion, payments and scheduled jobs always blocked), 10 (never write to an existing request). Same cadence as 6a/6b: Codex plan review, then slices with Sonnet build / Opus review / Codex adversarial / Fable final / live proof / owner acceptance. Branch `claude/factory-synthetic-reviewers` off `main` (`0e19f4293`).

*Decisions the plan needed before the recipe dimension is built* — **all six taken by the owner 2026-09-25 (Session 543)**; the recommendation was adopted in each case, with D-R3 reshaped after the owner clarified the source's review form:

- **D-R1 Person marker.** [VERIFIED] `wmkf_potentialreviewers` has no test/synthetic column (entity registry and adapter select lists); the amendment requires a person-level marker because the address is not a safety boundary. **[OWNER DECISION 2026-09-25: adopted.]** A new Boolean `wmkf_issyntheticreviewer` on `wmkf_potentialreviewers` under the decision-11 pattern (platform owner consulted; existing rows keep empty; only the factory writes it, always `true` in the initial INSERT; `assertTestRequestMarkerNotWritten` extended to refuse the field in any other write body or URL), applied to the sandbox before the proof and to production before the recipe's production release. The alternative, a `TEST · ` name prefix plus a ledger lookup, does not satisfy the amendment and is not recommended. [VERIFIED] the marker write guard runs in `write-core.js:133,179,293` and in `lib/dataverse/client.js:152`, and only the factory CLI's raw client opts out (`allowTestRequestMarkerWrites: true`, `rehearse-test-request-sandbox.mjs:738-743`); the person create carrying the new marker therefore goes through that same opted-out raw client, never through `write-core`, and the new field name is folded into the write guards only: [VERIFIED] `TEST_REQUEST_ISOLATION_FIELDS` also builds the ordinary-request OData/FetchXML filters and the request select (`isolation.js:31-45,143`), so a person field must never join it; instead a separate `SYNTHETIC_REVIEWER_MARKER_FIELDS` constant is folded into `MARKER_FIELD_NAMES` (`isolation.js:48`) and into the CommonJS parity set `TEST_REQUEST_MARKER_FIELDS` in `lib/dataverse/client.js:130`, with a parity test. The column ships as a new tracked schema wave beside `lib/dataverse/schema/wave29-test-request-isolation/` (same `extensions-on-existing` shape, Boolean, creation-only, no trigger), applied to the sandbox by the owner before the live proof; the production apply and the decision-11 platform-owner consult are the owner's actions, [ASSUMED pending] until recorded here. Until the column exists in production, the bundle exporter treats every 1003222 reviewer as real: no address is exported and every address comes from the reserve flag (D-R5).
- **D-R2 Address storage versus the ledger's no-text invariant.** Decision 4 makes the per-run address assignment the recipient-confinement authority; the ledger receipts forbid free text (`assertString` rejects credential shapes and anything outside a finite grammar). **[OWNER DECISION 2026-09-25: adopted.]** One sanctioned exception: a `test_request_run_reviewer_assignments` table (run id, source person GUID, destination person GUID, address, `sequence`), created by an in-place edit of unapplied migration 054 with the `setup-database.js` mirror, holding the address as plain text and nothing else free-form; receipts carry only its row id and the address's SHA-256 (the row id is the assignment's own `sequence`, carried as receipt key `assignmentSequence`, added 2026-09-25 for 6c-ii to reference; a database CHECK ties `address_sha256` to `address` so a miscomputed or forged digest is rejected at the database, not trusted from the application layer).
- **D-R3 Review files: copy and link [OWNER DECISION].** [VERIFIED] a completed review is either portal-submitted (answer rows; the scheduled filer later renders a DOCX and writes the `wmkf_reviewsharepointfolder` / `wmkf_reviewfilename` pointers, `individual-file-service.js`) or uploaded (`review-upload.js:223-272`: 1–5 PDF, DOCX or legacy DOC files ([VERIFIED] `file-magic.js:143-159` and `review-upload.js:62-66` accept `.doc` with OLE2 bytes as `application/msword`), 25 MB each, stored under `<requestFolder>/Reviewer_Uploads/<reviewerSubfolder>/attempt_<32 hex>/`, the pointers plus received state PATCHed on the suggestion, and only the rating and multiselect answers written as rows). The owner reports 1003222's two reviews were **uploaded**, so the scheduled filer is irrelevant and the recipe copies and links, as the IA recipe does: each uploaded file is copied byte-for-byte into the destination request's `Reviewer_Uploads/<destination reviewer subfolder>/attempt_<new id>/`, the synthetic suggestion gets the new folder and primary filename pointers, and the rating/multiselect rows are seeded. Received source reviews without a file (none in 1003222) are seeded as answer rows and receipt only, with no file, and the filer's cron skip stands. The supersedes the recipes table's "review DOCX is produced by the app's filing path" line.
- **D-R4 Email exception scope.** The Stage 1 simplification deferred the reviewer-engagement send exception (trusted server-owned purpose bound to one suggestion and synthetic person, every recipient equal to the recorded address) to this recipe. It threads through `assertRequestEmailAllowed`, `createEmailActivity` and the `sendEmail` dispatch recheck (Stage 1b-i code). **[OWNER DECISION 2026-09-25: adopted; no preference on acceptance point.]** Two slices: 6c seeds accepted, received reviews with their copied files and no email; 6d adds the exception and an invite/remind proof through a throwaway inbox. The recipe is accepted per slice, as 6a/6b were. 6d's binding contract (Codex plan round 3, high; adjudicated by Fable as the 6d design requirement, not a 6c blocker): [VERIFIED] `createEmailActivity` persists recipients, the regarding record and an optional generic `subcategory` from `correlationKey` (`lib/services/dynamics/email.js:144-189`), and the dispatch recheck re-reads only the activity's regarding request (`assertSendNotAboutTestRequest`, `email.js:105-127`), so today no create-time authorization survives to dispatch. 6d must persist a versioned reviewer-email purpose bound to the suggestion, the run and the assignment row on the activity itself (a dedicated field or a structured `subcategory` token, decided in 6d's plan), and both the create-time check and the dispatch recheck must reload the activity parties, the request and run, the assignment, the suggestion and the synthetic person, failing closed on a missing, duplicate, stale or mismatched binding; tests cover a tampered recipient, a mismatched suggestion, crash-then-retry, and a pre-existing activity. 6d gets its own Codex plan review before it is built.
- **D-R5 Production read scope [OWNER DECISION 2026-09-25].** Authorized: read-only production reads of Request 1003222's `wmkf_appreviewersuggestion`, `wmkf_appreviewanswer` and `wmkf_potentialreviewers` rows and its `Reviewer_Uploads` files, for the bundle export only. Owner-stated facts, [ASSUMED] until the first export reads them back: 1003222 is a test request whose accepted reviewers use throwaway inboxes the owner controls, and it has two uploaded reviews. Because production has no person marker until D-R1 is applied there, the exporter still treats every source reviewer as real: no address is exported and the owner supplies the throwaway addresses on the reserve flag.
- **D-R6 Thank-you stamp.** **[OWNER DECISION 2026-09-25: adopted.]** With email denied for the request, stamp `wmkf_thankyousentat` together with `wmkf_reviewreceivedat` in the seed changeset so the thank-you sweep never has a candidate; the amendment allows this "when the thank-you email is not wanted".

*Recipe dimension (slice 6c-i, mirrors 6a).* Steps: `seed_reviewers`, `copy_review_file` (one file per call), `seed_review_answers`, `verify_reviews`. New recipe token `reviews`, cumulative: its step order is the `initial_assessment` order, then `seed_reviewers` (one source reviewer per call: person then suggestion), `copy_review_file` (one uploaded file per call), `seed_review_answers` (one suggestion per call: answers, pointers and received stamp), `verify_reviews` (terminal; the only step that marks ready). Add it to the 054 CHECK, the `setup-database.js` mirror and `LEDGER_RECIPES` (parity test), bind it into the plan digest and the pre-lease manifest check exactly as `initial_assessment` is. Resource kinds `dataverse_potential_reviewer`, `dataverse_reviewer_suggestion`, `dataverse_review_answer_set` (copied review files reuse `sharepoint_folder` / `sharepoint_file`); the ledger's grammars stay finite ([VERIFIED] `FILENAME` at `run-ledger.js:97` is an exact regex applied to `filename` and `name`, and `buildReviewerSubfolder` yields `<≤30 alphanumerics>_<8 hex>` or the bare 8 hex, `review-upload.js:398-408`; upload filenames are reviewer-chosen and unbounded): the folder grammar gains `Reviewer_Uploads/(?:[A-Za-z0-9]{1,30}_)?[0-9a-f]{8}/attempt_[0-9a-f]{32}` and the copied files are **renamed deterministically** to `Review_<n>.<pdf|docx|doc>` (`n` = the file's ordinal within its review, primary file first), the same move as decision 1 for the numbered PDFs; the seed PATCH writes the renamed primary filename into `wmkf_reviewfilename`, whose readers ([VERIFIED] download, briefing page, portal context, token verifier, filing service, merge and remove) all resolve the file through the pointer pair and never parse the name; identically in JavaScript and SQL; the copy policy gains the DOCX and `application/msword` MIME types (each copied file re-validated by extension plus magic bytes through `validateReviewFile`) and a per-review file count of 5 at 25 MB (the uploader's own limits, `review-upload.js:50-51`), with the version and digest re-bound in the manifest; receipt keys for the source and destination person GUIDs, suggestion GUID, address digest, answer count, suggestion eTag before and after the answers changeset; reason codes `reviewer_person_not_synthetic`, `reviewer_person_conflict`, `reviewer_person_provenance_mismatch`, `reviewer_person_projection_drift`, `synthetic_reviewer_not_bindable`, `reviewer_suggestion_present_not_owned`, `reviewer_answers_ambiguous`, `reviewer_source_changed`, `reviews_verification_failed`; every value keeps the no-text invariant. The three steps stop with `recipe_step_not_built` until 6c-ii. [VERIFIED] `verify_initial_assessment` refuses to run as a non-terminal step (`run-runner.js:1822-1827`, `nextStepFor` must be null); for the cumulative recipe it takes the Basic `verify`'s branch (`run-runner.js:736-742`): advance when a next step exists, `markReady` only when none does, pinned by a test for each recipe.

*Source projection (bundle v3).* [VERIFIED] `buildSourceBundle` carries the Request row and documents only. Extend the exporter and `readSourceBundle` with `reviewers[]`: per source suggestion the person projection (`wmkf_name`, first/last, `wmkf_areaofexpertise`, `wmkf_primaryaffiliation`, `wmkf_academicrank`, `wmkf_primarydepartment`, `wmkf_maininstitution`, and `wmkf_emailaddress` **only when the source person carries the synthetic marker**; no ORCID — Codex plan round 6: [VERIFIED] the identity-binding contract requires ORCID id and URL as a pair with lineage (`reviewer-identity-binding-contract.js:184-193,274-280`), so a synthetic person carries neither), the suggestion's candidate content and lifecycle (`wmkf_suggestionlabel`, `wmkf_programarea`, `wmkf_relevancescore`, `wmkf_matchreason`, `wmkf_sources`, `wmkf_selected`, `wmkf_invited`, `wmkf_accepted`, `wmkf_declined`, `wmkf_responsetype`, response/materials/received/completed timestamps, `wmkf_reviewstatus`, `wmkf_revieweraffiliation`, `wmkf_reviewuploadedbystaff` (Codex plan round 2: upload provenance, carried into the strict validator, the digest, the seed PATCH and the verifier's exact comparison), the reviewer-supplied name/title fields, `wmkf_applicantdisposition`), and its answer rows: `wmkf_questionkey` plus every field `answerRowBody` writes (`review-answer.js:280-295`: question order/text/type, answer html/text/value/values, the stored option snapshot), so the seeded row is the same shape the portal writes. Each uploaded review's files are exported and hashed exactly as the proposal documents are (kind `reviewerUpload`, keyed to its suggestion, folder and filename copied from the source pointers, inventory identity including eTag and version id), so `copyBundleFiles` and the file verifier serve them unchanged. The bundle validator **rejects** a bundle carrying `_wmkf_contact_value`, `wmkf_addresstruststatejson`, `wmkf_emailsource`, any `wmkf_externaltoken*` field, `wmkf_proposalfirstaccessed`, the proposal URL/password fields, `wmkf_honorariumrequest`, or a real person's address (the source's `wmkf_reviewsharepointfolder` / `wmkf_reviewfilename` are carried only as the file manifest's source provenance, never written to the destination); the bundle digest covers the reviewer section so a same-bundle retry is bound to the same reviewers. Consistency fence for the reviewer rows (Codex plan round 2, medium): [VERIFIED] the exporter already fences the parent Request revision and repeats strict document discovery, comparing the complete canonical inventory (`source-bundle.js:110-171`, `assertCompleteInventory` / `assertSameInventory`); bundle v3 applies the same two-pass rule to the child rows: snapshot the complete bounded set of suggestion, answer and person identities with their eTags (and the upload files' eTags and version ids) before hydration, re-read the complete set afterwards, and reject any addition, removal, field or eTag drift as `reviewer_source_changed` (an exporter reason, surfaced by the CLI; no bundle is written), pinned by a concurrent-mutation test. The bundle stays a private local file, as today.

*Reserve.* `--reserve --recipe=reviews` takes `--reviewer-address=<sourcePersonGuid>=<address>` once per source reviewer (the admin form of item 7 replaces the flag); a source reviewer whose bundle entry has a synthetic address defaults to it, a real source reviewer requires the flag, and reservation refuses when any reviewer lacks an address or two reviewers share one. Identity contract (Codex plan round 1, high): the destination person is keyed by normalized address, and Dataverse enforces that key ([VERIFIED] `wmkf_emailaddress_unique` alternate key, `potential-reviewer.js:515-516`). Reservation resolves each address through the synthetic-only lookup **before** writing the assignment: an existing marker-`true`, active, Contact-less row whose fields equal `syntheticPersonProjection` for this source and address (null preserved; a drifted row refuses the reservation as `reviewer_person_projection_drift`, since the factory never updates an existing row — decision 10's rule applied to persons) is bound by its GUID with `reused: true` **only if** every prior assignment row naming that destination GUID (any run, any status) names the same source person GUID — the assignments table is the cross-run provenance authority, since the person row carries none (Codex plan round 4: an address recycled from Alice to Bob must not bind Bob to `TEST · Alice`); a destination previously bound to a different source refuses the reservation (`reviewer_person_provenance_mismatch`, operator picks another alias address); an existing row that is not synthetic, inactive or Contact-linked refuses the reservation (`reviewer_person_not_synthetic`); none → a preallocated GUID with `reused: false`. Assignments are written in the reservation transaction (D-R2) and are immutable afterwards; the seeder never chooses a person: it creates exactly the preallocated GUID, or re-reads exactly the bound GUID and requires marker `true` and the assigned address. A create rejected on the email alternate key (a concurrent run created the address between reserve and seed) stops the run `reviewer_person_conflict` for the operator to retire and re-reserve; the run never adopts the winner. An ambiguous create is recovered by the preallocated GUID and refused if not owned. Tests: a fresh person is created with exactly the projection; two reservations with the same address and source resolve to the same existing GUID; a same-source reused row whose metadata changed refuses; the same address for two different source GUIDs across runs refuses the second; a conflict on create stops without adoption; a bound GUID whose marker or address changed stops.

*Seeder (slice 6c-ii).* [VERIFIED] `scripts/check-reviewer-engagement-boundary.js` limits generic suggestion writers to `lib/services/reviewer-engagement/`, and `reviewer-suggestion.js` `upsert` finds by (person, request) and cannot preallocate a GUID; so the seeder is a new named command `lib/services/reviewer-engagement/seed-synthetic-review.js` with a positional `dependencies` argument (person find-by-email and create, suggestion create and re-read, changeset, request read) whose defaults are the production adapters, exactly as `commitReadyLineage` defaults (6b Stage A); the sandbox path passes deps built from `createIaSandboxDeps`'s service, whose `write-core` calls already prefer the explicit `svc.baseUrl`, plus the opted-out raw client for the marker-carrying person create (D-R1). Order per reviewer, every mutation journaled before dispatch: (1) resolve the assignment row: `reused` → re-read the bound GUID and require marker `true`, active, no Contact link and the assigned address (else `reviewer_person_not_synthetic`); not reused → create with the journaled preallocated `wmkf_potentialreviewersid` and the body returned by one pure, exported `syntheticPersonProjection(bundleReviewer, address)` in the seeder module (Codex plan round 5): `wmkf_name` = `TEST · <source wmkf_name>`, `wmkf_firstname` / `wmkf_lastname` = the source's, `wmkf_emailaddress` = the assigned address, expertise, affiliation (both `wmkf_primaryaffiliation` and its `wmkf_organizationname` shadow), rank, department and institution copied verbatim with null preserved, both ORCID fields null, the marker `true`, and no `wmkf_emailsource`, trust-state JSON or Contact link (so the row never reads as staff-attested); alternate-key rejection → `reviewer_person_conflict`; (2) create the suggestion with a journaled preallocated `wmkf_appreviewersuggestionid` from an explicit **allowlist** (Codex plan round 6): `wmkf_suggestionlabel`, `wmkf_programarea`, `wmkf_relevancescore`, `wmkf_matchreason`, `wmkf_sources`, `wmkf_selected`, `wmkf_invited`, `wmkf_accepted`, `wmkf_declined`, `wmkf_responsetype`, `wmkf_emailsentat`, `wmkf_responsereceivedat`, `wmkf_materialssentat`, `wmkf_revieweraffiliation`, the reviewer-supplied name/title fields, `wmkf_applicantdisposition`, the two binds and `wmkf_grantcyclecode`; **never** `wmkf_reviewreceivedat`, `wmkf_reviewstatus`, `wmkf_completedat`, `wmkf_thankyousentat`, `wmkf_reviewuploadedbystaff`, either file pointer, or any token, honorarium or proposal-access field — those completion fields are written only by the answers changeset, with a crash-boundary test proving they are null after the create alone, `wmkf_grantcyclecode` from the destination, bound to the destination Request and the synthetic person; an ambiguous create is recovered by the preallocated GUID and refused if not owned (`reviewer_suggestion_present_not_owned`). Then, per suggestion: (3a) for an uploaded review, copy its files one per call through `copyBundleFiles`' create-only path into the destination `Reviewer_Uploads/<subfolder>/attempt_<journaled id>/` folder (journal the planned folder and each file before the write, recover an ambiguous write by exact item, never retry); (3b) one atomic changeset, [VERIFIED] the same `atomicParentWithChildren` shape the portal's `submitReview` commits (`submit-service.js:180-191`; N `answerUpsertDescriptor` upserts by alternate key plus an If-Match parent PATCH carrying `wmkf_reviewreceivedat`, `wmkf_reviewstatus`, `wmkf_completedat`, per D-R6 `wmkf_thankyousentat`, and for an uploaded review the new `wmkf_reviewsharepointfolder` / `wmkf_reviewfilename` pointers and `wmkf_reviewuploadedbystaff`, so pointers and received state commit together as the uploader's PATCH does); this strengthens the amendment's sequential "answers then stamp" order, since the zero-answer state cannot be observed at all. [VERIFIED] core `runChangeset` dispatches on the `DynamicsService` singleton (`lib/dataverse/core/changeset.js:116-118`), so the seeder builds the operations with `atomicParentWithChildren` + `buildOperations` and dispatches them through `executeChangeset(sandboxService, operations)` (`lib/services/dynamics/changeset.js:86-130`, which honors `svc.baseUrl`), exactly as the IA lineage commit does. `wmkf_grantcyclecode` is `meetingDateToCycleCode` over the destination's meeting date, the rule the IA seed (`run-runner.js:800`) and the producer (`my-candidates-service.js:217-224`) both use. A changeset with no readable outcome stops (`reviewer_answers_ambiguous`); re-entry resolves by the preallocated suggestion GUID and never re-dispatches (dispatch-marker rule). The review **form** is derived from pointer provenance and the received stamp only (Codex plan rounds 7–9): the exporter classifies a complete pointer pair with the repository's existing server-owned path classifier (`shared/utils/review-file-provenance.js` `classifyReviewFileProvenance`, already used by `reviewers-service.js:51` and `context-service.js:43`), so `Reviewer_Uploads/…/attempt_*` pointers ([VERIFIED] `review-upload.js:234,267-271`) are `uploaded`, while pointers the filing service attached to a portal or manual receipt under `<request>/Reviews` ([VERIFIED] `individual-file-service.js:217-228,653-663`) are `received_no_file` with the generated DOCX **not** exported or copied (D-R3: the filer's output is never cloned); no pointers with a received stamp is `received_no_file`; a partial or unrecognized pointer state fails the export; no received stamp is `unreceived`; answer cardinality never enters the classification, because [VERIFIED] `markReceivedNoFile` writes zero or many answer rows atomically with a staff-provenance receipt (`mark-received-no-file-service.js:76-138`) and manual staff entry creates the same no-pointer, answer-bearing staff receipt (`manual-review-entry-service.js:149-175`), while the portal writes the same shape with `wmkf_reviewuploadedbystaff` false. For every received form the completion fields are the source's, `wmkf_reviewuploadedbystaff` copied verbatim; the **write shape** alone depends on answer presence: answers present → the atomic answers-plus-parent changeset; zero answers → a journaled, If-Match parent-only completion PATCH. `unreceived` gets neither. The bundle records the form, and fixtures cover an uploaded review, a portal receipt before and after filing attached `<request>/Reviews` pointers (proving it stays `received_no_file` with no file copied), a zero-answer no-file receipt and an answer-bearing manual-entry receipt (only the first is exercised by 1003222's live proof).

*Verify (`verify_reviews`).* Re-fence the source; compare the Foundation/Contact snapshot against the durable baseline; re-run the Basic verifier and the IA verifier's row checks; then for every seeded reviewer, through the sandbox-bound reader: the person row equal, field-by-field with null preserved, to `syntheticPersonProjection(bundleReviewer, assignedAddress)` (so the name is verified against its prefixed value, never the raw source name) plus marker `true` and no Contact link; suggestion bound to the destination Request and that person, every copied candidate and lifecycle field equal to the bundle field-by-field with null preserved, the derived fields exact (`wmkf_grantcyclecode` from the destination; `wmkf_reviewreceivedat`, `wmkf_completedat`, `wmkf_thankyousentat` and `wmkf_reviewstatus` equal to the journaled seed values, and null where the source review was unreceived), no token, honorarium or proposal-access fields, eTag equal to the journaled post-changeset eTag (Codex plan round 4: the eTag alone only proves the row did not change after a faulty write), with one mutation test per person and suggestion field family; answer rows compared field-by-field against the bundle keyed by question key (Codex plan round 2, high): every field `answerRowBody` projects (order, text, type, html, text, value, values, options) with null preserved and JSON arrays compared canonically, refusing a missing, extra, duplicate or content-mismatched row, with one mutation test per field family; receipt verified independently of the answer count — received, status, completed, thank-you and upload provenance equal to the journaled seed values for the `uploaded` and `received_no_file` forms and, for `unreceived`, received/status/completed/thank-you null and `wmkf_reviewuploadedbystaff` null **or** `false` (Codex plan round 10: [VERIFIED] the tracked schema defaults the Boolean to `false`, `wave2-existing/wmkf_appreviewersuggestion-extensions.json:46-50`, so a fresh create reads back `false`), with a verifier fixture for a newly created suggestion at the Dataverse default and verifier mutations for the zero-answer and the answer-bearing no-file fixtures; then one of the mutually exclusive file branches by the bundle's review form (Codex plan round 1, medium; round 7): a **received_no_file** or **unreceived** review requires `wmkf_reviewsharepointfolder` and `wmkf_reviewfilename` both null; an **uploaded** review requires `wmkf_reviewsharepointfolder` equal to the journaled destination folder (never the source's path), `wmkf_reviewfilename` equal to the journaled primary filename, `wmkf_reviewuploadedbystaff` equal to the bundle's value, every copied file re-verified by stable id and integrity against the bundle (exact SHA-256 for PDF and legacy DOC; for DOCX, a **new source-to-destination package comparator** `attestDocxPackageAgainstSource(destinationBytes, sourceBytes)` beside the IA attestor — Codex plan rounds 11–12: [VERIFIED] `copyBundleFiles` rejects any size or hash change on readback; SharePoint rewrites every uploaded DOCX; and the existing `attestDocxPackageAgainstRender` (`docx-package-attestation.js:40,75-102`) assumes a fresh-render baseline and requires every `customXml/item*.xml` to match one of three SharePoint roots, so it rejects an uploader-accepted source DOCX that already carries its own custom XML (`validateReviewFile` accepts any ZIP-magic `.docx`, `file-magic.js:143-159`). The new comparator requires every part present in the source to be byte-identical in the destination unless a characterized SharePoint transformation explicitly permits that part's delta, normalizes only parts SharePoint demonstrably adds or rewrites (the property-promotion set already characterized for the IA), and fails closed on any other addition, removal or change; tests use an uploader-accepted source DOCX with pre-existing non-SharePoint custom XML (unchanged copy passes; mutation or removal fails) across copy readback, ambiguous recovery, final re-verification and inspection. Both attestors read packages under a fail-closed budget (Codex plan round 13: [VERIFIED] the existing reader inflates every ZIP member into memory with no entry, per-part or total limit, `docx-package-attestation.js:52-58`): the raw ZIP central directory is validated **before** JSZip materializes its keyed file map (entry count, OPC-normalized part names with exact and path-alias duplicate detection, declared per-part and total uncompressed sizes), and parts are then inflated through a streaming reader that aborts the moment either byte ceiling is exceeded — never `file.async('nodebuffer')` followed by a length check (Codex plan round 14) — enforced at copy readback, ambiguous recovery, final verification and inspection, with fixtures for an exact duplicate name, a normalized path-alias duplicate, and a compressed bomb whose stream is proven to stop at the ceiling; the copy step gets a per-MIME integrity mode, the receipt journals the attested digest, and the first copied reviewer DOCX is characterized live before the grammar is frozen), and the folder census counting exactly the seeded files; a suggestion whose pointers match neither branch fails (`reviews_verification_failed`), with a verifier test per branch and a mutation that swaps the branches; Foundation account and Contact rows unchanged; no `wmkf_appreviewersuggestion` on the destination Request beyond the seeded set.

*Isolation additions.* [VERIFIED] the Stage 1c sweeps (thank-you, synthesis drain, reminder, stale-invite, DOCX filing, email reconciler) resolve each suggestion's `_wmkf_request_value` through `createRequestTestStateLookup`, so seeded rows are skipped by the request marker; the acceptance drain cancels on a test request (Stage 1b-ii). The recipe adds: the person-marker write guard (D-R1), and `upsertByEmail`'s reuse path (`potential-reviewer.js:286-300`) refusing, unconditionally, to merge into a marker-`true` row (the seeder never calls it: it finds, then creates by preallocated GUID), so a Reviewer Finder save using a throwaway address cannot silently adopt a synthetic person. Read-side fan-out of the marker (Codex plan round 1, high): [VERIFIED] ordinary identity resolution reaches person rows through `lookupReviewerIdentity`'s ORCID, exact-email and name paths (`reviewer-identity-lookup.js:380-416`: `findByOrcidCandidates`, `findByEmailCandidates`, then `searchByName`), and its result is bound to a suggestion without any further person check by manual add (`manual-reviewer-service.js:143-237` → `ensureStaffManualCandidate`), save-candidates (`save-candidates-service.js:1575`), applicant recommendations (`applicant-reviewers-service.js:119` → `ensureApplicantRecommended`) and merge (`reviewer-merge.js:429`). A synthetic person bound to an ordinary request would be treated as ordinary by every request-marker guard. Therefore: (a) the three adapter lookups (`findByEmailCandidates`, `findByOrcidCandidates`, `searchByName`) select the marker and exclude marker-`true` rows unconditionally, so ordinary resolution never sees a synthetic person; (b) every suggestion-creating adapter op (`upsert`, `ensureStaffManualCandidate`, `ensureApplicantRecommended`, and `repointToPotentialReviewer`) reads the person's marker and refuses a marker-`true` person (`synthetic_reviewer_not_bindable`), a second, independent fence at the binding seam; (b′) the merge path (Codex plan round 11, high): [VERIFIED] `planMerge` reads keeper and loser by client-supplied GUID (`reviewer-merge.js:223-232`) and `executeMerge` updates the keeper person (`:391,502`) before any suggestion repoint (`:410`) or applicant-slot bind (`:429`), so the marker joins `MERGE_FIELD_SELECT` and both `planMerge` and `executeMerge`'s execute-time re-read refuse a synthetic keeper **or** loser before the first write, with tests proving zero writes in either position, including merges with no repoint and with applicant-slot references; (c) the factory gets its own synthetic-only lookup (`findSyntheticByEmail`, exact alternate-key read requiring marker `true`) that no ordinary path imports, pinned by the access-layer gate's allowlist; (d) by-id hydration (`reviewers-service.js:705-718`, `my-candidates-service.js:408-420`) is unchanged, since it only names suggestions already bound; the `TEST` label is rendered wherever a person row is shown with its own identity. Tests: an ordinary manual add, save-candidates and applicant recommendation against a synthetic exact-email and ORCID match must each refuse, and the lookup tests must construct a marker-`true` row that would match.

*Sandbox unknowns, resolved only by the proof.* Creates on `wmkf_potentialreviewerses`, `wmkf_appreviewersuggestions` and `wmkf_appreviewanswers` and the `$batch` changeset by the sandbox app user (reads proven by parity); whether the sandbox holds `wmkf_reviewquestion` rows (parity replayed schema, not data), which affects only the Reviews tab's question labels, not the recipe; the [ASSUMED] client-supplied primary key on these three creates (proven for `akoya_request`).

*6c-i build notes.* A v2 bundle (no reviewer section) stays valid for `basic` and `initial_assessment`; `reviews` requires the section. The address digests and the assignment count are bound into the plan digest, so a same-key retry with different addresses conflicts. The copy-policy version bump changes the digest bound into every manifest; the live suites drop four ledger objects (the assignments table joins the three). `verify_initial_assessment`'s terminal assertion takes the Basic verify's advance-or-ready branch, one test per recipe. **Deviation (Codex adversarial round 1, 2026-09-25):** `SANDBOX_REHEARSAL_COPY_POLICY` is NOT widened with the DOCX/`application/msword` MIME types or a raised `maxFiles` in 6c-i, since this slice builds no `copy_review_file` step body and the widening had no accompanying kind↔extension↔MIME binding or magic-byte check (a `proposalNarrative` document with a DOCX-shaped MIME under a `.pdf` destination would have been accepted by `planBundleFileCopies`); 6c-ii introduces a separate review-file policy with the cap, binding and validation described above, bound into its own digest.

*Proof.* Re-export the 1003222 bundle as v3 (D-R5), reserve one `reviews` run with owner-supplied throwaway addresses, advance to `ready`: one new sandbox Request with its IA and N synthetic reviewers. Evidence under `evidence/test-request-factory/`.

*Codex adversarial plan review (`gpt-5.6-sol`, 2026-09-25).* Round 1 needs-attention: exact-email/ORCID identity resolution could bind a synthetic person to an ordinary request (high); reservation-bound destination GUID versus find-by-email reuse (high); verifier forbade and required the upload pointers (medium) — all three closed in place. Round 2 needs-attention: answer verification by identity only (high); `wmkf_reviewuploadedbystaff` missing from bundle v3 (medium); no two-pass fence over reviewer child rows (medium) — closed in place. Round 3 needs-attention: the 6d email exception had no durable dispatch-time binding (high) — recorded as 6d's contract above and scoped out of 6c. Round 4 (6c only) needs-attention: address-only reuse could bind a new source reviewer to an old synthetic identity; the verifier did not check projected person/suggestion fields; legacy `.doc` uploads were excluded — all three closed in place. Round 5 (6c only) needs-attention: the round-4 verifier demanded the raw source name while the seed prefixes it — closed by one shared `syntheticPersonProjection` for seed, reservation-time reuse check and verify. Round 6 (6c only) needs-attention: a one-sided ORCID identity on the synthetic person; completion fields could be published by the suggestion create before answers — closed (no ORCID; explicit create allowlist). Round 7 (6c only) needs-attention: zero-answer no-file receipts (the live `markReceivedNoFile` state) were downgraded to unreceived — closed by deriving the review form from the source lifecycle with a parent-only completion PATCH for that form. Round 8 (6c only) needs-attention: answer-bearing staff no-file receipts were misclassified as portal submissions — closed by classifying on pointer state and receipt only, copying provenance verbatim, and choosing the write shape by answer presence. Round 9 (6c only) needs-attention: filed portal/manual receipts carry complete `<request>/Reviews` pointers and would have been exported as uploads — closed by classifying pointers with the existing path-provenance classifier and never exporting generated files. Round 10 (6c only) needs-attention: the unreceived verifier demanded a null upload-provenance Boolean whose schema default is `false` — closed by null/false equivalence for `unreceived`. Round 11 (6c only) needs-attention: the merge-by-ID path could mutate or bind a synthetic person; byte-exact DOCX copy verification conflicts with SharePoint property promotion — closed (merge fence before the first write; per-MIME integrity with DOCX package attestation). Round 12 (6c only) needs-attention: reusing the fresh-render attestor would reject uploader-accepted DOCX with its own custom XML (Codex probed it) — closed by a distinct source-to-destination comparator. Round 13 (6c only) needs-attention: the package reader had no decompression budget — closed (bounded entries, per-part and total bytes, duplicate-name rejection, bomb fixture). Round 14 (6c only) needs-attention: the budget had to bind before inflation with a streaming abort and duplicate-name fixtures — closed. Round 15 (6c only): **approve**, no material findings (2026-09-25). The 6c plan is cleared for build; 6d gets its own plan review.

*Slice 6c-i built (2026-09-25, Session 543).* [SOURCE-BUILT on `claude/factory-synthetic-reviewers`: `9e531310b` … `36d85b381` (Sonnet), Opus round 1 fixes `ae04ebdb7`, `93bea0839`, `812f25745`, Opus round 2 fixture `9a057b913`; Codex round 1 fixes `2a4bc52e9`, `29e0783c4`, `6e254a461`; Fable closures for Codex rounds 2–7 `7afdb2399`, `6c0ee34de`, `c91eff298`, `bae3d70fb`, `2da21340e`, `3bf3e4dba`; Fable final-review fix (canonical entity-set derivation) in the record commit; full unit suite 16,533 passed / 0 failed; every changed-surface gate and self-test green] Sonnet built, Opus reviewed (round 1 needs-changes: ledger-side assignment binding, `reviews` lease 900 s, per-CHECK SQL enum coverage, live grammar fixtures, `reused: true` refused until 6c-ii, digest CHECK, `Review_[1-5]`, copy-policy ceiling reverted; round 2 approve with one fixture). Codex adversarial round 1 (`gpt-5.6-sol`, base `b23863ab2`): four findings, two accepted (operation-scoped marker-write opt-out on the raw client; Word MIME types removed from the Basic copy policy until 6c-ii's review-file policy exists, after Codex proved a DOCX could be planned under a `.pdf` name) and two **declined by Fable**: (1) "CLI retries conflict before the assignment check" — every CLI `--reserve` retry has conflicted on its freshly minted run id since slice 5b ([VERIFIED by Opus] `buildCloneManifest`, `basic-clone-steps.js:475`), recovery is by the reserved manifest, and the ledger-side comparison exists for item 7's admin form; **item 7 must give reservations a deterministic identity per actor and idempotency key** so its retries reach that check; (2) "ship the delta as migration 055" — 054 is edited in place by owner decision (2026-09-24, do-not-reopen) and is unapplied on every shared database until item 7; the assignments query is nevertheless skipped for non-`reviews` retries so a pre-branch schema cannot break Basic or IA runs. Codex rounds 2–8 (each a probed finding, closed in place by Fable): array-nested markers made the sanctioned-write check vacuous; percent-encoded, backslash-separated and fragment-suffixed property paths bypassed both marker write guards (both now canonicalize the target with the WHATWG URL parser, decode and re-split segments, failing closed on malformed input); the assignments `address` CHECK admitted arbitrary text (now the JS validator's email shape with credential and URL exclusions, and the JS validator made case-insensitive and URL-aware to match). Round 8: **approve**, no material findings (2026-09-25). Fable final review of `b23863ab2..HEAD`: one further gap of the same class fixed (the opted-in POST's entity-set derivation now uses the same canonical pathname, so a dot-segment path cannot be classified as a sanctioned entity set); the reservation retry comparison, the per-recipe lease helper, the IA verifier's advance-or-ready branch, and the wave30 definition read as specified. **Slice 6c-i accepted by the owner (2026-09-25), including Fable's self-reviewed entity-set fix; merge as a standalone PR (the marker-guard hardening has standalone production value).** [VERIFIED 2026-09-25] PR #337 merged to `main` as `39f641bac` with every CI check green (Jest, the live ledger suite, CodeQL, Semgrep, Gitleaks, Trivy, Vercel preview, claude-review); production deployment `dpl_81tWrHRMwc166t69D42oT2qwB3Mn` is Ready and owns `applications.wmkeck.org`, and GitHub's Production deployment record for that commit (`6670033612`, state `success`) names the same deployment. The marker write guards are therefore hardened in production; migration 054 and wave30 remain unapplied everywhere. Doc claims rechecked against the 6c-i tree at `9fcc5c234`: [RECHECKED after lib/services/test-requests/run-ledger.js change: `assertReviewerAssignments` / `assertReviewerAssignmentsUnchanged` / `reviewerAddressSha256` at run-ledger.js:422-490, retry site :725-750] [RECHECKED after lib/services/test-requests/bundle-file-copy.js change: `SANDBOX_REHEARSAL_COPY_POLICY` restored to version `sandbox-rehearsal-2026-09-23` with PDF/XLSX only, bundle-file-copy.js:43-52] [RECHECKED after lib/dataverse/client.js change: marker parity set at client.js:130, `pathSegmentsForMarkerCheck` / `targetEntitySet` / `isSanctionedMarkerWrite` and the guard block at client.js:158-275; the plan's `client.js:152` citation now reads at :262-275]

*Contract-reconcile review (Mode A, Session 543, Fable).* Surface: this plan paragraph; entry points CLI `--reserve`/`--advance`, exporter, seeder command; persistence Postgres ledger (054, in place) and Dataverse `wmkf_potentialreviewers` / `wmkf_appreviewersuggestion` / `wmkf_appreviewanswer`; consumers Reviews tab, Reviewer Finder hydration and name search, Stage 1c sweeps, acceptance drain, Pre-Site referee section. Findings: (1) CONFIRMED the boundary gate forces the seeder into `lib/services/reviewer-engagement/` (`check-reviewer-engagement-boundary.js:156-166`); (2) CONFIRMED the portal's atomic answers-plus-received changeset shape (`submit-service.js:180-191`, `review-answer.js:306-313`); (3) CONFIRMED core `runChangeset` is singleton-bound (`core/changeset.js:116-118`) and the sandbox path must dispatch through `executeChangeset(svc, …)` (`services/dynamics/changeset.js:86-130`); (4) CONFIRMED Stage 1c sweeps skip by the suggestion's request marker (`reviewer-thankyou-sweep.js:170-180` and the five sibling importers of `createRequestTestStateLookup`); (5) CONFIRMED the marker write guard has one sanctioned opt-out, the CLI raw client; (6) CONFIRMED the IA verifier refuses to run non-terminally. New issues fixed in place: the answer projection omitted five `answerRowBody` fields; the destination cycle source was unnamed; the person marker had no read-side fan-out (name search); a caller-identity exemption on `upsertByEmail` was removed. Partial success: one reviewer or one suggestion per `--advance`, each journaled with the gapless resource sequence; the answers changeset is all-or-nothing. Async: none beyond the runner's lease. Durable surfaces: 054 in-place edit + `setup-database.js` mirror + `migration-054-test-request-runs.test.js`; Atlas page for the new assignments table; `check:request-document-writers` unaffected; the boundary gate's recorded set unchanged (the seeder lives inside the directory). Verdict: **NEEDS OWNER DECISIONS** (D-R1 to D-R6) before the Codex plan review; no code change until D-R1.

### Stage 1 decisions and plan (2026-09-23, Session 535)

8. **[OWNER DECISION] Visibility.** Test requests appear everywhere in the Workbench (dashboard, search, lists) with a clear TEST badge. Reports, exports and cycle totals exclude them. This supersedes the original "excluded from normal queues, dashboards … by default" rule in *Isolation before the first create*.
9. **[OWNER DECISION] Actions.** Workflows behave normally on test requests, including Reviewer Finder and staff-clicked AI generation (which spends credits). Always blocked: payments/BILL onboarding, CRM Contact promotion, email other than to synthetic reviewers, and scheduled jobs. This supersedes the original denials of ordinary actions and provider generation on marked requests.

**[VERIFIED via a read-only sandbox query, 2026-09-23] Existing requests read back `null`, not `false`.** In the sandbox, where `wmkf_istestrequest` is applied, 5,000+ `akoya_request` rows (the count caps at 5,000) have a null marker, zero are `false`, and two (the rehearsal requests) are `true`; the default did not populate existing rows. [ASSUMED] Production will behave the same once the field is added. Because only the factory ever writes the marker, and always `true` in the initial INSERT, a null or false marker with a null run ID is classified ordinary. `unknown` (fail closed) is reserved for a missing projection, a failed read or an invalid value. Production does not yet have the field and any query selecting it would fail, so the production schema apply (owner authorization required) must precede deploying code that selects it.

**Stage 1 simplification.** No synthetic reviewer can exist before the synthetic-reviewer recipe, which follows the run ledger. Stage 1 therefore denies **all** email concerning a marked request at the delivery seam; the reviewer-engagement exception, synthetic-person marker and address matching ship with that recipe.

10. **[OWNER DECISION] Never write to an existing request.** The factory only creates new requests and writes to those. No backfill, normalization or other change to existing request rows, under any circumstances.
11. **[OWNER DECISION, platform owner Connor consulted 2026-09-23] Marker columns approved.** Adding `wmkf_istestrequest` and `wmkf_testcreationrunid` to the `akoya_request` table is acceptable: existing rows keep an empty value, only factory-created requests are written, and the values will only ever be empty, yes, or "No". [ASSUMED] Requests created after the column exists without the field (intake, AkoyaGO) likely take the column's `false` ("No") default; the owner accepted this on the condition that "No" and empty are functionally equivalent, which the classifier and the ordinary-only query fragments guarantee (false or null with no run ID is ordinary). Connor will not surface the fields anywhere ordinary staff work in AkoyaGO; staff reach Dataverse only through the WMKF apps or AkoyaGO, so Dataverse field-level security is not needed. The production schema apply remains a deliberate, owner-confirmed rollout step.

**Marker write guard (Stage 1a follow-ups, answering two Codex Stage 1a reviews).** The legacy-null rule is safe only if no app path can clear the fields on a test request or forge them on an ordinary one. Because these field names exist only on `akoya_request`, the guard is independent of the write target: `assertTestRequestMarkerNotWritten` (`lib/services/test-requests/isolation.js`) refuses any write body naming either field at any depth (including deep inserts and annotated keys) and any write URL whose path names either field (property-level PUT/DELETE). `DynamicsService.createRecord`, `updateRecord`, `deleteRecord` and every changeset operation call it, and the CommonJS raw client `lib/dataverse/client.js` applies a tested parity copy to every non-GET call. The factory's local operator CLI (`scripts/rehearse-test-request-sandbox.mjs`) is the one sanctioned writer and opts out with `allowTestRequestMarkerWrites: true` on its own client; an in-app factory create must add an explicit, reviewed exception. After three correction rounds (design rule: stop after two for a root decision), root closed Stage 1a to further medium findings; only a high-severity finding reopens it. The parked intake portal's `dataverseFields` path could otherwise have forged the marker on a create; that wider injection risk is recorded in `docs/INTAKE_PORTAL_DESIGN.md`. Tampering outside the apps is not a modeled threat per decision 11; the run ledger becomes a second, Dataverse-independent signal when it exists.

**Stage 1a accepted** (Codex approve on `9a3b81e4f`, 2026-09-23).

**[OWNER DECISION 2026-09-23] Rollout switch.** The read-side guards select the marker columns, which production does not have until its schema apply; a fail-closed guard deployed first would refuse every request-regarding email. They are therefore gated by `TEST_REQUEST_ISOLATION` (only the literal `on` enables them, like `DATAVERSE_DAL_ENFORCEMENT`), turned on per environment only after that environment's schema apply: sandbox-backed Preview first, production after its apply. The marker write guard does not depend on the columns and is always active. The design's "no configuration flag claiming success" rule concerns stage acceptance, not this rollout switch.

**Stage 1b-i (email), built 2026-09-23.** With the switch on: `createEmailActivity` refuses any email regarding an `akoya_request` that is not a verified ordinary request (test, anomaly or unreadable marker), before any write; `assertRequestEmailAllowed` refuses early in the grantee invite (before the deliverable write, link mint or send) and in materials create/invite/remind (before loading the request or collection); invitations and acceptance confirmations whose request cannot be resolved are refused instead of being sent unlinked. After the Stage 1b Codex review (2026-09-23) the slice also covers: a dispatch-time recheck in `sendEmail`, which reads an existing activity's regarding record and refuses a test request (or an unreadable activity or regarding type) before the SendEmail action, so activities created earlier cannot be sent later; scheduled grantee delivery checks the request (see the round-2 record below); the automatic materials reminder sweep skips test-request rows before any read, preparation or claim, and the manual remind refuses before loading anything (materials emails carry no regarding record, so the seam cannot see them); and Review Manager refuses a test request before reviewer-token minting, which would otherwise invalidate an existing portal link. `tests/unit/test-request-email-sender-census.test.js` derives every direct sender and every `lib/` file reaching one through its exported send helpers, requires each named entry function to contain its early refusal, and fails on any unrecorded change.

**Stage 1b-ii (Contact and payment), built 2026-09-23.** With the switch on, the reviewer acceptance job (`lib/services/reviewer-acceptance-drain.js`) checks the request right after its existing acceptance checks. For a test request it cancels the job (`test_request_followup_skipped`, the same cancel path as a released reviewer), so no Contact promotion, identity capture, honorarium record or confirmation email runs. The accept itself was already recorded by the portal. An unreadable marker retries instead of cancelling. [VERIFIED via source] `ensureHonorariumOnboarding` and `ensureAcceptedReviewerContact` are reached only from that job, and the internal `/api/bill/onboard-reviewer` route acts on the honorarium request that job creates, so this single gate also blocks BILL onboarding.

**Stage 1b correction round 2 (2026-09-23).** Codex's second review found guard placements that could harm ordinary requests or still act on test ones. Fixed: (1) every dispatch-recheck refusal or read failure in `sendEmail` carries `dispatched: false`, so an ordinary send whose check read failed is a retryable definite non-send, not "possibly sent"; (2) `sendReminderEmail` no longer re-reads the marker after the at-most-once claim, since both callers check before claiming and the marker is immutable; (3) `deliverScheduledEmail` resolves the request before `claimSend`, so an unreadable marker throws with no durable write and a test request's row is never claimed; (4) the `grantee-deliverable-reminders` cron skips test requests in `processRow` before any recipient read or ledger write (counters `skippedTestRequest`, `testStateUnknown`), pulled forward from Stage 1c. Root decisions: a verified-unsent test request's pre-existing scheduled row is **stopped with one terminal Postgres write** (`cancelForSource`, reason `test_request`) rather than left inert, because an inert row would reappear in the due list and every PD digest indefinitely; this is app state, not a Dataverse request write. If a row already carries Dynamics transport state, the pre-claim guard instead reads the existing activity: accepted transport is receipted and finalized without resend, an unreadable or ambiguous dispatched state performs no write, and only a readable unaccepted activity with no send intent falls through to the one cancel write. Accepted residual: a row created before the switch was turned on may appear in PD digests until its due date stops it (a per-row marker read on every digest run is disproportionate). Finalization of a reminder already sent before the switch updates that test request's own deliverable, which the owner's "all actions work on test requests" rule allows. The reviewer acceptance drain now resolves isolation from the freshly read suggestion's request relationship rather than the queued payload snapshot, and its test-request cancellation must mutate under the current lease or report lease loss. Per the two-round stop rule, remaining medium findings after this round are closed by root decision. The round-2 re-review raised one medium finding, a case-sensitive PD GUID comparison in the new pre-claim ownership check that could refuse an ordinary send-now; it was fixed with a regression test, and **Stage 1b is accepted by root decision** without a third review round.

**Stage 1c (scheduled jobs), built 2026-09-23.** [VERIFIED via source census of all 23 `pages/api/cron` routes, 21 scheduled in `vercel.json`] With the switch on, every job that acts on request-linked rows skips test requests at candidate selection, before claims, token mints, provider calls, writes or sends; an unreadable marker skips that row for this run only. Query-time exclusion (`TEST_REQUEST_ORDINARY_OData_FILTER`): grantee title generation, and the cycle dossier roster (**owner decision**: a cycle dossier is a cycle-wide report, so test requests are left out, and items queued before the switch fail as no longer on the roster). Per-row skips through `createRequestTestStateLookup`: reviewer thank-you sweep (previously its claim stamped the row before the email seam refused), respond and review-due reminder sweeps (unscheduled but callable; previously token mint or claim preceded the seam), stale-invite sweep, reviewer email reconcile (no address write onto a person row, repoint or admin alert), automatic review synthesis (no job enqueued; a job enqueued before the switch is cancelled before any provider call, unknown takes the retryable failure path), and review DOCX filing (the discovery select now carries `_wmkf_request_value`). Previously guarded: grantee reminders, the reviewer acceptance drain, and the materials reminder sweep. **Owner decision:** AI review panels are staff-launched on chosen requests and do run on test requests. Not guarded, recorded: `drain-submissions` creates new applicant requests and the app cannot write the marker; operational jobs (health, secrets, logs, spend, pricing, auth-bypass, identity reconcile, IRS BMF refresh, and the maintenance retention/GC sub-tasks) take no per-request action. Residuals: maintenance's BILL onboarding sweep acts on honorarium requests, which the acceptance drain no longer creates for test requests, so only rows from before the switch are possible; the acceptance drain's declined/released honorarium cleanup runs before its isolation check but only deletes or cancels honoraria that exist; spend totals include test-request usage until Stage 1d's report exclusion. **Stage 1c correction round 1 (2026-09-23).** Codex's review found that skipped test rows could crowd out ordinary work and that one maintenance write was unrecorded. Fixed: an automatic synthesis job whose marker cannot be read is requeued with the claim's attempt given back (`releaseReviewSynthesisJob`), so transient read failures can never exhaust an ordinary job's retries; reviewer email reconcile excludes each non-ordinary request from its newest-first candidate query and re-reads, so test rows never occupy the batch; the materials reminder sweep counts its batch only after the test-request check. Root decisions: a synthesis job enqueued before the switch is still claimed (its own Postgres row) and then cancelled, since the claim is a single SQL statement with no request state; maintenance's close of expired materials collections is **allowed** on test requests, because it is Postgres-only lifecycle expiry of the request's own collection with no external effect, and skipping it would leave the collection open and block a new one on that request; upload-staging GC and review-draft GC likewise only clean the request's own app state. The round-1 re-review raised one medium finding: a permanently unreadable request (`request_not_found`, `request_id_invalid`) would requeue its synthesis job forever; such jobs are now cancelled, and only transient read failures requeue. **Stage 1c is accepted by root decision** under the two-round rule. `tests/unit/test-request-scheduled-job-census.test.js` records every cron route with its classification, matches the schedule against `vercel.json`, and requires each guarded job's selecting file to keep its skip.

**Stage 1d census and implementation record (2026-09-23).** [VERIFIED via source census] “Badge” means the server selects the marker/run columns and emits only the derived `isTestRequest` boolean, and the client renders the shared TEST badge. “Exclude” means an ordinary-only fragment is composed into the server query/aggregate before paging or counting. Both happen only when `testRequestIsolationEnabled()` is true; when false, the existing select/filter and DTO shape are preserved and no marker column is named.

| Surface class | Classification | Authoritative source/caller |
|---|---|---|
| Workbench cycle rows and Review Manager follow-up rows | Badge; cycle/rollup counts exclude | `lib/services/workbench/dashboard-service.js:45-59,107-161,205-285`; `shared/components/workbench/RequestListPanel.js:342-355`; `shared/components/workbench/ReviewerFollowUpPanel.js:45-64` |
| Workbench request search, including exact-number and Project Leader joins | Badge; search result rows remain visible | `lib/services/workbench/request-search-service.js:49-67,101-125,170-230,309-371`; `shared/components/workbench/RequestLocator.js:570-585` |
| Workbench request header | Badge | `lib/services/workbench/resolve-request-service.js:23-47,61-124`; `pages/workbench/[requestId].js:191-200` |
| Initial Assessments, Staff Deliberations and Final Writeups cross-request lists | Badge; their displayed cycle/queue counts exclude | `lib/services/initial-assessment/artifact-reader.js:120-220`; `lib/services/initial-assessment/artifact-model.js:18-35,96-128`; `lib/services/pre-site-visit/cycle-list-service.js:44-54,198-239,252-392`; `lib/services/final-writeup/dashboard-service.js:57-71,162-227,301-343,705-746` |
| Reviewer Finder / Expertise Finder historical-proposal table | Badge; its `totalCount` excludes while rows remain visible | `lib/services/expertise-finder/proposals-service.js:39-82`; `pages/expertise-finder.js:921-927` |
| Virtual Review Panel roster | Badge; launch remains allowed; run history is N/A because it is a selected workflow ledger, not a request report | `lib/services/review-panel-service.js:52-69,175-206`; `pages/review-panel.js:226-231` |
| Meeting Tracker request list and proposal selectors | Badge; inherited cycle rollups exclude | `lib/services/meeting-tracker/dashboard-service.js:168-211`; `shared/components/meeting-tracker/MeetingTrackerList.js:47-52`; `shared/components/meeting-tracker/SessionEditor.js:486-503` |
| Dynamics Explorer `get_entity(request)`, `query_records(akoya_request[s])`, Search request hits, and `get_related` request lists | Badge in the derived result only; raw marker/run fields are removed. Search and relationship text results carry the same derived `TEST` signal. | `lib/services/dynamics-explorer/tools/get-entity.js:18-31,89-157`; `lib/services/dynamics-explorer/tool-executor.js:43-61`; `lib/services/dynamics-explorer/tools/composite.js:66-157`; `lib/services/dynamics-explorer/tools/get-related.js:35-60,157-184,500-570`; `pages/dynamics-explorer.js:914-940` |
| Dynamics Explorer `count_records`, `aggregate` and `export_csv` over `akoya_request[s]`; `find_reports_due` and `get_related` report populations | Exclude. Request-payment report queries qualify the canonical ordinary predicate through the documented `akoya_requestlookup/...` navigation property before paging/counting. The assistant's server-owned completion metadata reports the isolation state; while isolation is on, the page-only CSV action is hidden for every markdown table, independent of LLM-authored headers, so staff use the server-filtered `export_csv` tool instead. Off mode preserves the prior local-CSV behavior. | `lib/services/dynamics-explorer/chat-session.js`; `lib/services/dynamics-explorer/tool-executor.js:63-110,145-146`; `lib/services/dynamics-explorer/tools/export.js:35-137`; `lib/services/dynamics-explorer/tools/composite.js:20-60`; `lib/services/dynamics-explorer/tools/get-related.js:35-60,270-365`; `pages/dynamics-explorer.js`; `docs/INTAKE_PORTAL_BUDGET_ROSTER_RECONCILE_STATUS.md:94-103` |
| Admin Test Request preview source | Badge (the page is deliberately test-factory-specific); operational-event rows are N/A, because they are incident records and do not carry a trusted request projection | `lib/services/test-requests/admin-preview-service.js:141-222`; `shared/components/admin/TestRequestPreviewSection.js:30-285`; `shared/components/admin/OperationalEventsSection.js:1-250` |
| Grantee Awardees list/count and awardee cycle picker | Exclude | `lib/services/workbench/grantee-deliverables/awardees-service.js:74-105,115-177` |
| Grantee combined cycle export | Exclude | `lib/services/workbench/grantee-deliverables/cycle-export-service.js:57-102` |
| Dataverse bulk XLSX data and parallel FetchXML true-count export | Exclude independently of the legacy applicant-name `excludeTestRecords` option; the preview waterfall shows marked Test Requests as their own counted step (Session 536). The signed preview token binds that preview's marked-isolation policy; the run route returns a pre-stream 409 re-preview response, without creating a blob, if the current server policy differs. Legacy tokens without the policy field mean isolation off. | `lib/services/dataverse-export/compiler.js:383-512`; `lib/services/dataverse-export/result-token.js`; `pages/api/dataverse-export/preview.js`; `pages/api/dataverse-export/run.js` |
| Cycle dossier DOCX/PDF | Exclude, already implemented in Stage 1c | `lib/services/cycle-dossier-service.js:24-39` |
| Candidate XLSX and combined-review DOCX downloads | Exclude at their request resolver before document generation | `lib/services/workbench/export-candidates-service.js:32-76`; `lib/services/review-manager/export-reviews-service.js:31-93` |
| Grant Reporting lookup, full extraction, field regeneration and goal regeneration | N/A (Session 536): single-request actions on one looked-up request; `requestGuid` only attributes the AI-run audit row. Available for test requests by owner decision 2026-09-23. | `lib/services/grant-reporting/lookup-grant-service.js`; `lib/services/grant-reporting/extract-service.js`; `pages/api/grant-reporting/extract.js` |
| Per-request proposal/material downloads and staff-created IA/brief/review-panel/final-writeup artifacts | N/A: workflow artifacts for one deliberately selected request, not report population exports; actions remain normal by decision 9 | `pages/api/workbench/download-proposal-document.js:36-105`; `pages/api/review-panel/download.js:1-39`; `pages/api/review-manager/download-review.js:1-62`; `pages/api/cycle-dossier/download.js:1-20` (the last is covered by the excluded dossier roster) |
| Workbench request-search status/cycle aggregates and dashboard cycle totals | Navigation includes test requests (Session 536): search cycle/status options and dashboard cycle discovery keep them so they stay reachable; dashboard cycle counts and the proposal rollup exclude them. When a program has no cycle with an ordinary request, `defaultCycleCode` and `lastDecidedCycleCode` are null and the Workbench asks staff to choose one of the still-listed test-only cycles explicitly. | `lib/services/workbench/request-search-service.js:237-272`; `lib/dataverse/adapters/grant-request.js:284-342`; `lib/services/workbench/dashboard-service.js`; `shared/components/workbench/WorkbenchShell.js` |
| Admin cost dashboard and hourly spend check, review-panel portion | Admin dashboard: test-request attempts (by the entry's `request_id`) are left out of totals and shown as one separate line; if any request cannot be classified nothing is removed and the dashboard says so. Spend check: counts all spend, test requests included (Session 536) | `pages/api/admin/stats.js:164-212`; `pages/api/cron/spend-check.js:68-170`; `lib/services/review-panel-store.js:363-449` |
| `api_usage_log` spend dashboards and cron sum | Attribution gap: `api_usage_log.request_id` is an Explorer/API correlation ID, not an `akoya_request` ID, so existing rows cannot be assigned to an ordinary/test request. The total remains inclusive and the admin stats response labels it unattributable (the spend check simply counts it); future request-linked paid calls must add trusted request attribution before they can be excluded. | `lib/utils/usage-logger.js:21-43,98-142`; `pages/api/admin/stats.js:47-161`; `pages/api/cron/spend-check.js:71-90` |

Dataverse Search inventory: Workbench text search (`grantRequestAdapter.searchRequests`) cannot be proven to accept a server-side marker predicate, so Stage 1d does not claim that it does. Search is allowed to show test requests; every returned ID is canonically hydrated with the gated marker projection and the normal program/cycle/status OData filter before the row is returned. The Search service's raw `totalCount` can include stale/unavailable hits and is already not presented as a complete canonical count (`unavailableCount`/`capped` describe that gap). Generic Dynamics Explorer Search likewise has no verified server-side marker-filter contract, but it is a visibility surface rather than an exclusion/count source: request hits are hydrated by ID and receive the derived TEST signal, while its provider `totalCount` remains a search-provider count. No report, export or cycle total uses a post-filtered Search page. `TEST_REQUEST_ORDINARY_OData_FILTER` and `TEST_REQUEST_ORDINARY_FETCHXML_FILTER` (including the OData constant qualified through a trusted navigation property) are the only marker exclusions used by report/export/count callers.

Known attribution gaps: `api_usage_log.request_id` is an Explorer/API correlation ID rather than a Grant Request ID, and Grant Reporting requests that omit `requestGuid` cannot be assigned to a request. Stage 1d labels the former as unattributable in the admin stats response; the latter is moot since Session 536, because Grant Reporting is a single-request action that is not excluded; it does not infer request ownership from filenames or other untrusted inputs.

Open classification question carried conservatively: a download belonging to one explicitly selected request is treated as a workflow artifact and stays available, while any file assembled from a request population is an export and excludes test requests. **Owner decision (2026-09-23, Session 536): confirmed.** Single-request Word/PDF artifacts are normal workflow actions, not reports, and stay available for test requests; only population files and named exports exclude them.

**Stage 1d verification record (2026-09-23).** [VERIFIED via local source, Jest, TypeScript, ESLint and required gates] The exact final tree passed 1,078 Jest suites / 16,011 tests / one snapshot, `check:types`, and changed-file ESLint with zero errors (15 pre-existing hook warnings on touched components). Each required gate and self-test passed sequentially: API routes (226 route files), route/service boundary, Dataverse access layer, OData escape (1,097 files), Atlas (58 Postgres tables / 38 Dataverse entity sets), doc currency, fact consistency (808 durable files), and trust-boundary GUID (226 routes). Mutation checks proved the badge classifier (2 failures), OData report/count exclusion (5 failures), attributable-spend exclusion (1 failure), and badge census (1 failure). The first census mutation exposed a weak filename-only assertion; the test was tightened to require the canonical import binding and JSX use, then the same mutation failed. No environment, deployment, schema or Dataverse operation was performed. The branch implementation is complete locally; production remains gated by the existing schema-before-switch rollout contract. <!-- fact-consistency:ignore fact=api-route-file-count reason=historical -->

**Stage 1d root review (2026-09-23) — all six fixed in Session 536; see the fix record after this paragraph.** Claude's review of `4c0a9e86f..4b0c53246` found three defects to fix before Stage 1d is accepted: (1) `pages/api/cron/spend-check.js` now excludes test-request review-panel spend (and, through `excludeTestRequestSpendRows`, any request whose state is unknown) from the runaway-spend alarm; the alarm is a safety guard on real money, so it must count all spend. **Owner decision (2026-09-23):** the admin usage dashboard (`pages/api/admin/stats.js`) may exclude test-request spend from its totals but must show that spend as one separate line. (2) Dynamics Explorer `searchRecords` (`lib/services/dynamics-explorer/tools/composite.js`) throws when any request hit cannot be classified, which breaks ordinary search on a stale index hit; the badge is informational, so an unclassified hit should render without it. (3) Workbench `listCycles` (`lib/services/workbench/dashboard-service.js`) now excludes test requests from cycle discovery, so a cycle containing only test requests would vanish from the dropdown and those requests would be unreachable, contrary to "visible everywhere"; navigation must include them (status/count aggregates may still exclude). A Codex adversarial review of the same range (base `a69545697`) returned needs-attention with three high findings, also open: (4) `aggregateMeetingDateCycles` in `lib/dataverse/adapters/grant-request.js` appends the ordinary-only FetchXML as a second entity-level `<filter>`, which Dataverse rejects, so the Workbench cycle/status picker fails as soon as the switch is on; compose the conditions inside the existing filter and add a structural XML test (root note: overlaps defect 3, since cycle discovery should include test requests anyway). (5) Grant Reporting's exclusion (`lib/services/grant-reporting/extract-service.js`) is skipped when the client omits `requestGuid` and the GUID is not bound to the report/proposal references; root note: Grant Reporting extraction on one chosen request may be a normal single-request action rather than a report, so classify it before building a server-bound reference. (6) Spend classification in `lib/services/test-requests/spend-isolation.js` reads one request at a time and silently drops spend for unreadable requests while reporting `available: true`; batch the reads and mark totals unavailable when any request cannot be classified (this also resolves defect 1's unknown-exclusion half).

**Stage 1d fix record (Session 536, 2026-09-23) — Stage 1d ACCEPTED by the owner after the fixes below.** Root fixes, two Codex adversarial re-review rounds from base `a69545697`, and Codex-rescue fixes for the second round's findings, reviewed by Claude. The owner accepted without a fourth review round.
- Defects 3–4 (`f06b5d3a8`): Workbench cycle discovery (`listCycles`) and request-search cycle/status options include test requests; `listCycles` leaves them out of counts. Removing the search-option filter removed the invalid second FetchXML filter, and the adapter's unused extra-filter parameters were reverted. Status options were included too, for the same navigation reason.
- Defects 1 and 6 (`7c8edcf45`): `spend-check` reverted to count all spend. `excludeTestRequestSpendRows` batch-reads markers (50 IDs per read); if any request is missing, unreadable or not a GUID it removes nothing and reports `available: false`. The admin dashboard shows test spend as one separate line, or says it could not be separated.
- Defect 2 (`6f2d56c48`): Explorer search renders an unclassified request hit without the badge instead of throwing.
- Defect 5 (`008612629`): owner confirmed single-request actions stay available for test requests. Grant Reporting lookup and extraction are single-request actions (`requestGuid` only attributes the AI-run audit row), so both exclusions were removed; the visibility census records them as `single-request` and the Workbench search options as `navigation`.
- Codex re-review (Session 536), high: a test-only cycle could become the Workbench default. Fixed (`ee9901c59`): default and last-decided cycles are chosen from cycles holding ordinary requests, and a cycle's meeting-date anchor comes from its ordinary requests; test-only cycles stay in the list.
- Codex re-review (Session 536), medium: the bulk-export preview compiled its comparison counts with the marker filter, so the waterfall hid marked Test Request removals. Fixed: matched and after-operational counts omit the marker filter, and while isolation is on the preview adds a separate `excludedMarkedTestRequests` step (with `markedTestRequestsApplied`) before the legacy Foundation-applicant step; the invariant now checks all four counts.
- Codex Stage 1d isolation fix (`6a293fe12`): the bulk-export preview token now signs the preview's marked-isolation policy, and `/run` rejects policy skew before SSE setup or blob creation with a clear re-preview response; legacy tokens are treated as isolation-off.
- Codex Stage 1d isolation fix (`744bde88d`): the Explorer assistant completion now carries the server-owned isolation state. While isolation is on (or metadata is absent), the page-local CSV action is unavailable for every markdown table rather than trusting an LLM-authored marker header; the guarded server `export_csv` tool remains available, and off-mode behavior is unchanged.
- Codex Stage 1d isolation fix (`53260f1af`): when a program has only test-request cycles, the dashboard returns null default/last-decided cycle codes and the shell renders an explicit pick-a-cycle state while preserving every test-only cycle in the selector.

Stage 1 slices, each a separate commit with tests, relevant gates and a Codex adversarial review:

| Slice | Scope |
|---|---|
| 1a. Marker read contract | Classifier rule above; a server resolver that reads marker and run ID for a request ID and returns the classification; tests including null legacy rows, missing projection and failed read. No behavior change for callers yet. |
| 1b. Hard blocks | Default-deny email at the shared delivery seam for marked requests, with early denial in direct and scheduled grantee paths and materials invite/remind; the mechanically derived sender-census gate; acceptance-drain Contact promotion and BILL onboarding skipped for marked requests; honorarium orchestrator refuses marked requests. |
| 1c. Scheduled jobs | Census of every scheduled job that selects requests or reviewer suggestions (`vercel.json` crons); marked requests skipped at candidate selection, before claims, tokens, provider calls or sends. |
| 1d. Visibility | TEST badge on the Workbench request header, dashboard rows and search results; marked requests excluded from reports, exports and cycle totals (for example awardees and cycle export). Lists and search show them. |

Rollout: sandbox-backed Preview first; production schema apply (owner-authorized) before any production deploy that selects the marker; then production deploy.

## Outcome and user flow

From an existing request, an administrator selects **Create test request**. A short wizard asks for a test label, starting point, selected documents, and staff-controlled test identities. It previews exactly what will be copied, reset, created, and omitted, including the destination environment. One **Create test request** action produces a new request and opens it in the Workbench with a persistent TEST banner and a link to its creation receipt.

A bounded source/script search found existing tests using prebuilt requests, but no reusable admin clone tool. **[VERIFIED via Luna source reconnaissance; not a tenant feature inventory.]**

The tool supplies the repetitive setup. The user should not need to construct a Dataverse request, arrange SharePoint folders, repair document pointers, or send an invitation to establish a test fixture. Defaults come from a configured test organization and test personas; no contact is silently inherited from the source. (Amended 2026-09-23: synthetic reviewers are seeded from the source's reviewers with per-run editable throwaway addresses; see Owner amendment decision 4.)

The owner explicitly chose fresh destination Requests rather than resetting existing fixtures. SharePoint documents and version history survive Dataverse state resets and can affect later behavior. Every factory run therefore owns a fresh Request GUID/number/location/folder/history under W. M. Keck Foundation; resetting an existing Request is not the default or a fallback.

Proposed starting points:

| Preset | Result | Preconditions / limits |
|---|---|---|
| Basic request | New request identity and allowed proposal attributes; selected proposal files in an independent destination; no reviews or generated artifacts. | V1 minimum useful delivery. Does not claim readiness for version restore or external materials. |
| Initial Assessment ready | Basic request plus a supported synthetic IA document, fresh registry row and canonical pointer. Optional two-version fixture for restore testing. | Requires an identity-safe artifact builder and final domain readback; no AI generation during cloning. Two target versions are newly created test history, not transplanted source history. |
| Site-visit materials open | Basic request plus a synthetic scheduled visit and an open collection, one empty upload slot, fresh short-lived contributor link restricted to test personas. | Requires an explicit prepare-without-send operation plus at least one approved test PI/liaison email, active scheduled visit, eligible request stage and valid date/window. The current collection creation service sends an invitation; do not call it unchanged. |

The materials recipe provisions/assigns dedicated test-persona contacts rather than bypassing recipient validation. Extract a named preparation operation that retains eligibility, scheduling, date/window and recipient checks but never calls the send step; ordinary collection creation keeps its existing send behavior.

The IA recipe must supply the new request number, title, test institution and meeting-date cycle. To remain usable by the existing generation flow later, its canonical narrative must yield at least 60 characters of text (`initial-assessment/artifact-service.js` validation); fixture construction itself must not call that paid generation path. Compute identity through `buildInitialAssessmentIdentity` over the new request context and narrative returned by `getAiProposalNarrativeText`, including the current prompt/template contract versions. Create the fresh row through `requestDocumentAdapter.create`, upload and validate fixture bytes, then activate with `commitReadyLineage(row, { metadata, actingUserSystemId })`. Do not directly PATCH the canonical pointer. A mocked normal generation call must reuse the completed fixture (`reused: true`) without any provider invocation or second Ready row. The public paid-generation action remains denied for test requests in V1; this is an internal contract test, not a live AI test.

Proposal-dependent presets must also satisfy the exact canonical narrative filename/location for the **new** request number (currently `AI Materials/ProposalNarrative_<number>.pdf`); arbitrary copied attachments do not establish that contract.

Presets are recipes with checked prerequisites, not a dropdown that blindly writes arbitrary lifecycle labels. A request already at a later source stage can seed a basic earlier-stage copy. Current-stage cloning of Review, Final, leadership, payment, or closed workflows is deferred until each has a valid recipe. (Amended 2026-09-23: the owner amendment's *Later-stage recipes* supply those recipes through Final Writeup; leadership, payment and closed workflows remain deferred.) Do not forge historical approvals or completion timestamps merely to light up a UI button.

## Grounded current behavior

All items below are **[VERIFIED via source inspection]**, not claims about current tenant configuration:

| Existing surface | Design consequence |
|---|---|
| `lib/dataverse/adapters/grant-request.js` exports `getById`, `create`, and `updateById`; `create` passes through to Dynamics. | Reuse the adapter under trusted post-auth DAL context. It is not a validated clone service. |
| `docs/atlas/dataverse-akoya-request.md` records vendor-owned request identity/status, contact/account lookups, and governed artifact pointers. | Use an explicit field manifest; do not POST the source row or its formatted annotations. Live required/createable fields and numbering must be verified before implementation acceptance. |
| Intake `lib/services/cron/drain-submissions-service.js` `handleScanning` uses a preallocated request GUID and account binding, expects a server-generated request number, and recovers duplicate-PK creation by readback. | Reuse these proven source patterns, not the whole intake drain. This skeleton does not prove every target tenant/plugin create requirement. |
| `lib/utils/sharepoint-buckets.js` resolves request document locations; the Graph facade reads/writes files separately from Dataverse. | Create and verify independent request-bound locations and files; never reuse the source's path or drive/item pointers as writable clone destinations. Governed writes require resolved parent locations (`requireResolvedParents: true`), not the read-only fallback library. |
| `lib/dataverse/adapters/request-document.js` owns actor attribution and generation-key recovery. | Create new rows bound to the new request, with new generation keys and server-resolved actor context. |
| IA `artifact-reader.js` resolves a unique Ready, non-superseded artifact through the request's canonical pointer; `controls-service.js` checks version identity and governed content during restore. | A copied file alone is not an IA-ready fixture. New file, registry row, pointer, content identity, and version checks must agree. |
| `lib/services/site-visit-materials/collection-service.js` creates a collection from an eligible request/active site visit and sends an invitation. `contributor-service.js` chooses canonical filenames and can supersede a previous slot row. | Add a named no-send fixture preparation path; allocate a fresh collection/slot and never inherit real predecessor artifacts. |
| `lib/external/verify-materials-token.js` verifies token audience/digest/request binding/expiry/collection state. | Do not copy tokens. A test-persona access restriction is additional proposed enforcement, not provided by the existing bearer link alone. |
| `shared/config/workbenchVisibility.js` filters request status/triage, not a universal test marker. Historical test probes recognize Foundation-as-applicant. | A Foundation applicant or TEST title is useful labeling, but does not prove exclusion from queues, reports, or automations. |
| `lib/dataverse/core/interlock.js` checks deployment/target/operation. | A synthetic marker must not bypass target restrictions. Dataverse target isolation does not isolate Graph, Blob, email, or external Power Automate flows. |

Read also: `docs/CLAUDE_REMEDIATION_PLAN.md`, `docs/APPLICATION_STATE_ATLAS.md`, `docs/SYSTEM_MODEL.md`, and `docs/CAMPAIGN_RELEASE_AND_DATAVERSE_TEST_STRATEGY.md`. Memory context read: `project-test-residue-cleanup-is-for-data-mining.md` (structured test residue must not masquerade as real data) and `project-dynamics-sandbox-state.md` (historical sandbox schema gaps; not current live proof). The original design used source evidence only. Subsequent metadata and bounded sandbox app-owned provisioning evidence are recorded in [the Stage 0 contract](TEST_REQUEST_FACTORY_PLATFORM_CONTRACT_2026-09-19.md); production automation suppression remains unverified.

## Copy policy

**[PROPOSED]** Maintain a versioned server-side allowlist, with per-field transformations and preset overrides. Anything unlisted is omitted. The browser submits source ID and approved choices, never an arbitrary Dataverse payload.

| Data | Rule |
|---|---|
| Title, narrative, abstract, program/type and request amount | Allowlist-selected copies; title visibly marked TEST. Requested amount may remain for realistic display; it is not an award or payment authorization. Destination lookup values must be validated. |
| Cycle, meeting date, request status and triage | For the Basic clone, default fiscal year and meeting date from the server-resolved source Request so current-cycle fixtures retain their visibility. Ask the administrator for either missing value and allow an explicit override. Validate both before creation and verify the final date after Dataverse processing. Status and triage follow a supported preset; do not inherit a closed/awarded state automatically. |
| Applicant/payee and PI/liaison/Co-PI/CEO/contact memberships | Replace with approved test organization/personas where needed; omit unused roles. Never mutate shared real contacts/accounts or copy portal memberships. Staff PD/PC selection must resolve active authorized staff server-side. |
| Request GUID and request number | Fresh destination identity; never retain the source number, calculate max+1, or fabricate a number. Use the platform's verified allocation mechanism. |
| Creator and owner | Owner-decided app-suite application user. Create without staff impersonation; omit `createdby`, `ownerid`, and `owneridtype`, then verify assigned identities by readback. Record the initiating staff actor in the run ledger. |
| Selected proposal/supporting documents | Copy bytes into new files after source access and version/hash validation; preserve source untouched. Bounded count/size and supported types only. Never recursively copy the entire SharePoint tree by default. |
| Numbered proposal PDFs | Copy bytes and rename to the destination request number (owner amendment decision 1); no content transformer. |
| Governed IA/Pre-Site/Final artifacts | Follow the owner amendment's *Later-stage recipes*: copy-and-link with recomputed identity and hash (IA), seed-and-render (Pre-Site), or app-driven steps (board snapshot, Pre-RP brief, Final Writeup). Embedded source request references inside document bodies are accepted for test fixtures; hashes are always recomputed over the destination bytes. |
| Pointers, registry identities, generation keys, source links, AI-run links | Reset. Rebuild only links among newly owned fixture resources. A source ID is allowed solely as admin-only provenance, never as an operational current-document pointer. |
| Reviewers and reviews | Clone onto synthetic reviewers with per-run throwaway addresses (owner amendment decisions 3–4 and *Synthetic reviewers*). Never copy portal tokens, proposal credentials or honorarium request links. |
| Honoraria, payments, approval/leadership/finalization history, email/calendar activities | Do not copy. Payment authorization and paid totals remain absent/zero as permitted by verified schema. |
| Tokens, signatures, sessions, invitations, reminder schedules, leases, queues, completion timestamps | Do not copy. Explicitly set `wmkf_respondreminderenabled` and `wmkf_reviewduereminderenabled` false in the initial INSERT after schema verification; omission inherits documented true defaults. Create only fresh preset-specific records, with automation disabled by enforced policy. |

Copying narrative or document bytes does **not** anonymize them. Preview must say that source content may contain real names/confidential information. Access remains at least as restrictive as the source; content-bearing external test fixtures require approved internal test actors and an enforced access boundary. Ordinary external bearer-link behavior is insufficient for this guarantee. Until that boundary exists, external presets use synthetic replacement content only and do not copy source documents into an externally visible location.

## Isolation before the first create

**[PROPOSED; enablement blocker]** Add an explicit server-readable synthetic-request marker and creation-run identifier to `akoya_request` using reviewed WMKF extension schema. Exact logical names are reserved only after metadata validation; suggested names are `wmkf_istestrequest` and `wmkf_testcreationrunid`. Record provenance in the admin operation ledger, not in public request DTOs. Keep the established test-organization convention where compatible, but do not use organization name or title matching as the safety boundary.

The marker must be present in the initial INSERT. A later patch is too late for create-triggered automation. Before production enablement, inventory and test every relevant create/update consumer: Dataverse plugins/business rules, Power Automate, AkoyaGO/vendor processes, local cron/workers, reporting/export selectors, email/calendar transport, payment/honorarium paths, and AI automation. Repository inspection cannot establish off-platform suppression. Obtain a named platform-owner review and disconfirming tests demonstrating marked requests cannot trigger those effects. If vendor create logic cannot safely accept/exclude these requests, production cloning stays disabled; use a separately verified sandbox.

Server enforcement:

- `requireSuperuser(req, res)` plus normal source-request read authorization on preview, create, resume, inspect and retire. Creating a clone must not bypass source program/access restrictions.
- Keep business eligibility functions `isVisibleRequestRow`, `buildVisibilityFilter`, and `assertSchedulableRequest` unchanged: eligible test fixtures must still pass their stage/date rules. Apply separate synthetic exclusion fragments at ordinary OData list and FetchXML aggregate callers, and a server-side request resolver for direct-ID access. Inventory Dataverse Search separately: server-side marker filtering requires verified field support/indexing; if unavailable, use a bounded supported query with correct paging/count semantics or keep that capability blocked—never fetch a page then silently filter it while claiming complete counts. The historical Quick Find change failure means indexing cannot be assumed.
- (Superseded by owner decision 8, 2026-09-23: test requests are shown in lists and search with a TEST badge; only reports, exports and cycle totals exclude them.) Test requests excluded from normal queues, dashboards, aggregates and exports by default; admin Test Requests view and explicit admin test mode expose them. Direct-ID routes also enforce test visibility; hiding list rows alone is insufficient. Unknown marker state fails closed in the test tool.
- Background workers skip test requests by default. Deliberate AI/payment testing is outside V1; no generic client `testMode` escape hatch. Test collection setup creates no send job or email activity. (Amended 2026-09-23: reviewer email to the run's recorded throwaway addresses is in scope; see *Isolation additions required by this amendment*.)
- (Amended by owner decision 9, 2026-09-23: ordinary actions and staff-clicked provider generation work on test requests; payments/BILL, Contact promotion, non-synthetic email and scheduled jobs stay blocked.) Ordinary synchronous calendar/payment/provider-generation actions on marked requests are denied in V1. Reviewer invite/remind actions are confined to synthetic reviewers' recorded throwaway addresses (amended 2026-09-23; see *Isolation additions required by this amendment*); other email on marked requests remains denied. Resolve marker state before dispatch; an unavailable required check fails closed. Deploy schema/readiness support before enabling these guards, preserving normal behavior for verified non-test requests. No client-controlled exemption.
- Each supported synchronous test action verifies the request marker, operation ownership and allowed test identity/target again. Synthetic status does not weaken ordinary auth or target interlocks.
- Retiring a fixture first blocks new activity and revokes its links, then reconciles exact owned resources. No delete-by-prefix, source traversal, or deletion of shared test personas.

Named Stage 0 external consumers include the documented Power Automate writers of `AI Materials/ProposalNarrative_{#}.pdf` and `Reviewer Materials/Proposal_{#}.pdf` (`docs/REVIEWER_MATERIALS_FOLDER_SPEC.md`), request-status-driven automation described in the request Atlas, and AkoyaGO OnCreate defaults used by the intake drain. These are documented dependencies, not freshly verified live flows. Enumerate initial INSERT and every subsequent field/pointer PATCH trigger. For each, obtain owner/config evidence and an approved disconfirming scenario (e.g. marker-present create/update must cause no narrative overwrite, send, paid generation or payment action). Do not perform a production create merely to discover its unsafe effects.

This is the largest prerequisite. It must be delivered and tested before a convenient production “clone” button is enabled.

## Operation contract and recovery

**[PROPOSED]** One eventual execution service owns the workflow. Suggested files: `lib/services/test-requests/{policy,service,store}.js`, `shared/config/testRequestPresets.js`, admin routes under `pages/api/admin/test-requests/`, and an Administration Test Requests panel. The inert policy/file-plan compilers and a separate read-only Admin preview service, route, and panel are locally built and mock-verified. The ledger, execution service, create/copy controls, and operational status/resume/retire surfaces remain planned, not built.

Use a small durable Postgres operation ledger (operational orchestration, not a competing request master): proposed `test_request_runs` and `test_request_run_resources`. Define through numbered migrations/manifest and fresh-install schema together. Dataverse remains the request/document-registry authority; SharePoint remains file authority.

A run stores actor, source/destination environment IDs, source request ID and revision, preset/policy version, selected file versions/hashes, a bounded approved plan, idempotency key, preallocated destination GUID, current step, lease/fencing generation, and sanitized result/error. Resources store exact destination IDs/pathnames and ownership evidence plus source→destination provenance. Record planned identity before dispatch and readback after dispatch. No credentials, bearer links, document bodies, or unrestricted source snapshots in the ledger.

1. **Preview:** read source and selected resources; validate schema/config/preset prerequisites; produce a short-lived server-stored plan and digest. No request, file, token or email creation. Include realistic size/count estimates and missing prerequisites; unavailable presets explain why.
2. **Confirm:** reauthorize and compare request revision plus selected document identities/versions. Reject stale plans rather than silently copying changed data. Atomically reserve `(actor, idempotencyKey)` with plan digest; identical retries return the same run, differing payloads conflict. A deliberate “create another” uses a new key.
3. **Create:** persist intended destination GUID/marker before request POST. On timeout, read that exact GUID and verify run ownership before retrying. Platform numbering is read back; inability to identify the create outcome leaves `needs_attention`, never blindly creates a second request.
4. **Provision/copy:** use the sandbox-proven app-owned path: resolve the one `akoya_request` parent and registered Graph drive, create the exact new folder, then POST a preallocated `sharepointdocumentlocation` bound to the new Request and parent. Read back one exact location and physical folder. Journal both identities before dispatch and never blindly repeat a POST after an ambiguous result. A preexisting or second location requires explicit ownership and duplicate resolution; production vendor provisioning may race this step, so that contract is an enablement gate. Governed file writes still require exactly one Dynamics-tracked request bucket with resolved parents. Process selected files in bounded steps; reject unsupported/oversized items at preview. Use create-only/conflict semantics and recover ambiguous success by exact item/path ownership and byte/version evidence.
5. **Apply preset:** build fresh registry rows and relations; set canonical pointers only after file/content readback. Each cross-system mutation is individually journaled. Reauthorize resumption and fence stale workers before subsequent writes. Destination conflicts or uncertain ownership stop the run.
6. **Verify:** use the same domain readers as the Workbench/portal; validate all requested resources, identity map, source unchanged, absence of real contact/token/payment links, and preset-specific invariants. Only then mark `ready` and expose launch links.

Run states: `prepared → creating → ready`, with `needs_attention` for partial/ambiguous failure and `retiring → retired` for explicit retirement. Store step identity separately. Use an admin-driven, lease-based step endpoint with the dedicated `test_request_runs` ledger. Each authenticated call advances bounded work and persists progress; closing the browser pauses advancement safely and Resume continues from stored state. No cron registration, detached HTTP promises, or reuse of intake `submission_jobs`/other domain queues. Chunk/substep file work must fit request time/memory limits; unsupported files are rejected at preview rather than requiring a new generic scheduler. Server-owned fencing and destination idempotency both matter: an expired lease alone cannot cancel an already dispatched remote write.

The UI shows partial resources and a **Resume** action, not “failed—try creating again.” Preserve receipts on failure. Retirement is separately previewed, only acts on proven run-owned resources, retains tombstones/audit mapping, and respects actual deletion privileges. Inactivity/expiry never initiates a broad cascade delete. Retirement must not claim success if protected records or files remain; show those explicitly.

## Build stages and verification gates

Every accepted stage leaves existing builds green. Offline implementation and mocked validation may proceed while live platform evidence is pending, but all create/preset execution remains server-disabled unless its exact readiness requirements are verified. A failing or unknown platform gate is not an accepted live stage, and cannot be replaced by a configuration flag claiming success. Luna builds/reconnoiters, fresh Sol reviews each stage, root accepts. (Superseded 2026-09-23: Claude root builds and Codex adversarially reviews each stage; see owner amendment decision 7.) Stop after two correction rounds for a root decision on remaining material issues; avoid cosmetic loops.

| Stage | Work and files | Tests required before enabling the stage / exit evidence |
|---|---|---|
| 0. Prove platform contract | Read-only metadata/automation census; verify create privileges/required fields, numbering, folder provisioning, test organization/personas and schema availability. Produce exact field/consumer matrix and probe receipts. | No create experiment until the initial-insert suppression contract and target are approved. Mock metadata tests reject unknown/noncreateable fields. Unknown platform consumers keep production disabled. |
| 1. Test identity and isolation | Add marker schema, server test-request resolver, admin visibility and applicable worker/report/transport exclusion guards. No clone UI enabled. | Before rollout: non-admin/direct-ID denial, marked/unmarked/unknown truth table, every enumerated reader/worker/transport consumer, false-positive normal-request regression, platform create/update trigger exclusion. Existing gates plus schema/Atlas/security updates. |
| 2. Pure preview | Implement copy allowlist/preset validation and admin preview route/panel. | Before exposing: unauthorized source rejection; malicious fields ignored/rejected; reset matrix; no writes/providers in preview; stale source/file detection; content/access disclosure; missing persona/config rejection. |
| 3. Basic clone | Add ledger/migrations, reservation, bounded runner, new request and independent file locations; basic preset only. | Before first controlled fixture: concurrent same-key requests; response lost after every write; no source mutation; deterministic GUID/number readback; ownership checks; count/size bounds; retries without extra request/file; lease expiry and unknown success; exact cleanup inventory. Domain readback and approved controlled rehearsal. |
| 4. Later-stage recipes (amended 2026-09-23) | Add the owner amendment's *Later-stage recipes* as separate slices in dependency order: IA → synthetic reviewers and reviews → site-visit materials and transcripts → Pre-Site seed and render → app-driven Pre-RP brief, site-visit start and Final Writeup. Reuse domain contracts, never direct arbitrary lifecycle patches. | IA: correct new request/file/registry pointers, valid governed content, two-version restore, no inherited audit links. Materials: eligible synthetic visit, fresh collection/token, no send/queue, token confinement/expiry, canonical empty slot, staging and registry reconciliation. Source-ID/link mutation tests must fail. |
| 5. Admin operations and release | Finish list/status/resume/retire; retention policy; documentation and Tier 2 rehearsals. | Interrupted run resumes; retirement revokes links and reports residue; active worker/retire race; no deletion of source/shared resources; ordinary staff/reporting exclusions; first-time admin usability test; full relevant regressions/build and fresh adversarial review. |

After each stage, a fresh-context reviewer receives only the accepted design, current diff and evidence paths. Re-read actual callers → adapters → persisted IDs → consuming UI/jobs, and deliberately disconfirm isolation and retry claims. Record assumptions that remain unknown. Stage 0's platform census must be refreshed if schemas, flows, selected presets, target environment or field policies change.

Required durable updates when implementing: relevant Atlas pages, API route security matrix, schema records, migration manifest, service catalog and test-runbook. Run touched gates with each self-test sequentially. Do not treat these structural gates as proof that remote Power Automate is disabled.

## Decisions and open prerequisites

Decided: admin-only, test use, source preserved, independent destination identity, explicit starting-point recipes, no email/AI/payment action during creation. Build order (owner amendment 2026-09-23, superseding the earlier basic → IA-ready → materials-open recommendation): Stage 1 isolation first; then the read-only production source bundle and sandbox Basic-clone proof with file copy under the sole existing CLI receipt/exact-ID-recovery exception; then the durable run ledger, migrations, reservation and resumable runner; then later-stage recipes in dependency order (IA → synthetic reviewers and reviews → site-visit materials and transcripts → Pre-Site seed and render → app-driven Pre-RP brief, site-visit start and Final Writeup); then the Admin creation form and production release. No synthetic-reviewer or later-stage recipe and no production execution precedes the ledger/runner. Email during creation remains out of scope; reviewer email afterwards is confined through the trusted server-owned purpose and ledger-authorized synthetic address described in *Isolation additions required by this amendment*.

Before live enablement/acceptance, determine: approved deployment/data targets; live create/numbering/folder contracts; platform automation exclusions and owner; schema logical names; test personas (reviewers resolved 2026-09-23 by per-run throwaway addresses; test organization, applicant contacts and consultants remain open); exact source-field allowlist; the open Pre-Site AI-run link choice (source run or stub run); content-access policy; maximum file/count limits; retention period. These are implementation prerequisites, not reasons for the user to hand-build test requests again. The engineering work should resolve discoverable metadata and source questions and bring only actual policy/authorization choices back to the owner.

Prior review status (original design): **ACCEPTED AS A DESIGN WITH EXPLICIT PLATFORM GATES** by fresh Sol `sol_clone_design` after a bounded correction recheck. The reviewer identified the materials recipient/scheduling prerequisite and IA narrative/context prerequisites; those corrections are incorporated. Root checked the create/recovery path and isolation limitations. This is a source-grounded design with explicit platform verification gates, not a claim that production cloning is currently safe. That original review performed no live calls or implementation; at that checkpoint no request creation, schema mutation, push or deployment had occurred. Subsequent implementation and metadata evidence are recorded below.

Original design validation: doc-currency, fact-consistency and their sequential self-tests, plus docs-catalog passed; `git diff --check` passed. Evidence: `/private/tmp/test-request-design-gates.log`. The new worktree initially lacked parser resolution; the successful rerun used the existing main-checkout dependency through `NODE_PATH`, with no dependency change. Runtime tests/build were not run for the original design-only commit.

## Opus amendment disposition and implementation handoff

Claude Opus 5 reviewed `daaf565d` via OAuth and returned **READY WITH NAMED CHANGES**. Review: `/private/tmp/test-request-opus-review-daaf565d/REVIEW.md`. All seven findings are incorporated: explicit provisioner decision/readback blocker; exclusion separate from eligibility; production IA identity/lineage functions; dedicated step runner; named off-platform create/update consumers; synchronous transport denial; exact admin guard and explicit reminder disabling. The runner choice is an architectural decision; the review's statement that intake is the only existing runner is not adopted as a repository-wide fact.

Implementation owner: Luna; independent stage reviewer: fresh Sol; final acceptance: root. Worktree/branch: `/Users/gallivan/.codex/worktrees/test-request-design/WMKF_Apps`, `codex/test-request-design`. (Historical. From 2026-09-23 Claude root builds on `codex/test-request-preview-integration` in worktree `WMKF_Apps-factory`, with Codex adversarial review per stage.) Stage 0 first produces a reproducible source/metadata contract and records unresolved platform ownership. No write probe or schema apply is required to begin offline development. If production safety/provisioning remains unknown, deliver reviewed, tested, disabled capability with a precise blocker receipt, not a working-creation claim. No existing Graph release branch changes belong to this workstream.


## Current implementation boundary

Plan amendment accepted by Sol and committed as `ade5018a`. Source census and sanitized read-only metadata receipts are committed as `d7abaa30`; Sol accepted them as **partial Stage 0 evidence**, not a passed live stage. Production still lacks the proposed marker/run fields; sandbox now has them plus the two reminder controls from the isolated parity wave. Production and sandbox differ in application-required fields, and sandbox still lacks the triage control. The verified applicant relationship is `akoya_applicantid@odata.bind`, so intake's different binding must not be copied blindly. See the Stage 0 contract for exact evidence and unresolved gates.

Luna built the initial offline basic-request draft compiler and unit tests; root completed the bounded corrections and Sol accepted the result. The sandbox-only rehearsal branch added the sandbox-required numeric `akoya_requesttype`, a two-field reminder-control parity wave, a manifest-bound create operator, and read-only recovery/failure probes. Sandbox schema is live. The first create failed transactionally in GoVerify; a temporary bypass then created Request 1000338 and proved server numbering plus marker/run/reminder persistence without payment/email/account/Contact effects. Its location appeared later under a staff identity. A second bounded run created Request 1000339 and proved the app can create its exact Graph folder and Dataverse location directly, with the app-suite principal as creator and owner. Both requests had the meeting date rewritten.

The branch now also contains a pure, non-writing Basic-clone preview composition: the existing request draft plus an explicit selected-file plan over trusted source identity/version/hash and server-supplied count/size/MIME limits. Trusted server state and browser choices are separate function arguments. The file plan allowlists the four Phase I source slots for direct copy, and any blocker removes all actionable request/file payloads while retaining separately labeled preview-only values. It exposes canonical reviewer/narrative/bibliography filename templates before allocation and resolves exact names only from a trusted future destination Request number; those numbered PDFs are planned as a byte copy renamed to the destination number (owner amendment decision 1, 2026-09-23; this replaced the earlier `requires-transform` block).

A superuser-only `GET`/`POST /api/admin/test-requests/preview` route and Administration **Test Requests → Preview** panel now provide the bounded runtime caller for that pure compiler. The service refuses Production/unknown Dataverse targets and unregistered SharePoint sites before any source read; the accepted document source is explicitly the shared akoyaGO site, not a sandbox-isolated SharePoint store. It resolves the Request, W. M. Keck Foundation, Grant type, live create metadata, SharePoint buckets, and opaque document inventory server-side; follows Graph children pagination; treats inventory truncation as a blocker; and hashes selected stable file eTag/version identities under technical read ceilings. The browser can supply only the source selector and bounded preview choices. The UI exposes no Create control, invalidates stale responses, distinguishes the two environments, and labels the result non-authoritative. Signed-in smoke on the branch-scoped Vercel Preview invoked the route against sandbox Dataverse and the registered shared SharePoint site. Request 996142 loaded successfully and a POST produced a blocked non-authoritative preview; the deployed UI then named `ownerid`, `owneridtype`, and `filePolicy` as unresolved. Subsequent readback of sandbox Request 1000338 proved app-user ownership after omitting both owner fields; the owner chose the app suite principal as creator and owner for future fixtures. The pure compiler now recognizes only those two system-required server defaults, but the external Preview has not been redeployed or live-smoked after this source change. Request 1000338 also loaded from sandbox. Neither source exposed allowlisted proposal files. A bounded read-only search of 150 sandbox Requests with linked folders found no canonical allowlisted proposal-document set, so the selected-file hashing path has not been live-smoked. The technical read ceilings are explicitly not an approved copy policy. Every response strips active request/file plans; the displayed request fields are a non-authoritative field/value list with destination IDs and OData bindings removed, not a POST body. There is still no ledger, copy executor, document-ready fixture, or accepted operational Stage 2 capability. Stage 1 isolation is complete locally but not rolled out; Stages 3–5 remain unaccepted. Platform suppression, deterministic defaults, production duplicate-location handling, approved file-policy values, the production marker schema apply, and the per-environment isolation switch remain before operational wiring can be enabled. (The generated-document transformer prerequisite was removed for the three numbered proposal PDFs by owner amendment decision 1, 2026-09-23.) A future executor must verify the assigned owner and creator after create.

[VERIFIED via the Basic preview source/route/client path and focused tests, 2026-09-23] The source load displays its fiscal year and meeting date as editable defaults. When unchanged, the browser omits them from POST and the server compiles the values from its freshly resolved source Request. Missing source values require administrator entry; the compiler blocks a missing or invalid date/cycle. Explicit edits remain bounded overrides. The `2099` date in sandbox receipts was a synthetic rehearsal input, not a factory default or current-cycle requirement. This branch change has not been redeployed or live-smoked.

[VERIFIED 2026-09-23 via sandbox rule metadata and one guarded retained-fixture PATCH] `WMKF_Set Meeting Date` in sandbox contains server-side hard-coded December/June 2024 assignments. An ETag-guarded, one-field PATCH restored Request 1000339's intended `2099-12-01` date without changing marker/owner or creating payment/email rows; later readback retained it. The rehearsal script now includes this conditional one-time correction after create and checks the final date. The combined path has not been exercised by another fresh create, and production did not expose a workflow with that exact rule name. See the Stage 0 contract and `evidence/test-request-factory/meeting-date-rule-and-patch-2026-09-23.json`.

[SOURCE-BUILT on `codex/test-request-preview-integration`; focused Jest tests, scoped ESLint, and doc gates passed; no new live Request created] The local sandbox rehearsal CLI prepares a Basic clone from exactly one server-resolved Grant Request selected by bounded request number. Fiscal year and meeting date default from that source, with explicit overrides required for absent source values. The private v3 manifest binds destination IDs and the compiled body digest to the source GUID/revision; execute re-reads and fences the source before any remote write and immediately before the single Request POST. It copies only allowlisted purpose/requested amount, replaces applicant and visible TEST title, uses Grant type, marker/run and disabled reminders, and verifies final cycle/content/ownership plus exact empty location/folder and no payment/email effects. The exclusive receipt is atomically updated and fsynced at create, date-correction, folder, location, and observation milestones so `--inspect` can guide exact-GUID recovery without retrying. During an explicitly requested GoVerify bypass, bounded calls receive an interrupt fence. SIGINT/SIGTERM cancels client-side fetch work and awaits that client promise before restoration; the remote server may still commit an aborted or timed-out PATCH. If the deactivation PATCH was attempted but inactive state was never positively verified, the receipt records `restoreVerified:false` and a manual recheck reason with the exact workflow ID; the command stops without treating an active readback as proof or continuing post-create steps. A Request POST timeout may likewise be ambiguous if the server committed it; use `--inspect` and reconcile the preallocated GUID without retrying. If verified deactivation later fails restoration, post-create work is recorded as skipped. A hard process kill or power loss cannot run `finally`; operators must inspect the receipt and verify GoVerify state manually. Old v1/v2 manifests remain available for inspection and are not executable. This operator is sandbox-only and does not add a deployed Admin Create route or establish production readiness.

Validation for the initial offline slice: 30 focused Jest tests passed; scoped ESLint passed; `npm run build` passed without copying live credentials; source-census self-test and syntax check passed; doc-currency/fact-consistency with sequential self-tests and docs-catalog passed. Build log: `/private/tmp/test-request-factory-build.log`. Initial build attempts exposed worktree permissions and an external dependency symlink; the final successful build used an independent APFS dependency copy and the required worktree write permission. The migration manifest was regenerated without a tracked change; no database migration was applied. The integrated preview branch later passed 46 focused tests plus a 35-suite/475-test regression set, scoped ESLint, canonical build, API-route/DAL/trust-boundary/Atlas/lifecycle/route-service/docs/fact gates and their required self-tests. The final blocker-field display change passed its 3-test component suite and scoped ESLint. Vercel build and signed-in read-only smoke passed. These checks verify the read-only surface; they do not supply the missing document-bearing sandbox fixture or authorize writes.

Fresh review checkpoints remain mandatory for each subsequent stage. Stage 1 isolation is now source-built and locally verified; its environment rollout remains contingent on the marker schema being present before `TEST_REQUEST_ISOLATION=on`, and production apply/enablement requires explicit owner authorization. The next creation milestone is the durable ledger/resumable runner before any deployed create route. The app-owned folder/location path is proven in sandbox; production still needs duplicate-location handling. The deployed preview does not create requests, expose a clone control, or make any live creation-ready claim.
