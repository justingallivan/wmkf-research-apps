# Dependencies
/node_modules
/.pnp
.pnp.js

# Testing
/coverage

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.tgz
*.tar.gz

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files, pulled env, and backups (may contain secrets) — never commit
.env*.local
.env
.env.*
.env*.bak
!.env.example

# Vercel
.vercel

# IDE
.vscode/
.idea/

# Source documents
*.docx
# Migration data files
proposals-for-migration.csv
# Generated schema output (may contain tokens)
scripts/dynamics-schema-output.json
scripts/dynamics-schema-diff.json
# Generated artifacts (presentation/deck builds, etc.)
outputs/
# Cron scheduler runtime state (session-scoped)
.claude/scheduled_tasks.json
.claude/scheduled_tasks.lock
# Smoke-test helper state (live prod GUIDs + tester email — never commit)
scripts/.smoke-test-candidate.json
.git.nosync
# iCloud-exclusion artifacts (local-only build/dep dirs)
*.nosync/
tmp/
.playwright-mcp/
# Codex CLI artifacts. .agents/ stays ignored (corrupted s/Claude/Codex/ skill
# mirrors). AGENTS.md is NOT ignored — it is a tracked symlink to CLAUDE.md so
# Codex loads identical current content (Codex verified: it ingests literal
# AGENTS.md bytes as user_instructions; a thin pointer would leave it
# context-blind). The only writer is the manual `migrate-to-codex` skill, which
# unlink()s + recreates (Codex-verified cli.py:452-470) — that SEVERS the
# symlink into a regular file = visible tracked diff, never a silent CLAUDE.md
# mutation. Restore with `git checkout AGENTS.md`. Do not run migrate-to-codex
# here. Background: docs/DOCS_GROUND_TRUTH_AUDIT_2026-05-19.md.
.agents/

# appresearcher collapse backfill snapshot (local rollback insurance, S213)
scripts/.appresearcher-snapshot.jsonl

# scholar-identity audit output (contains person data; local-only, S214)
scripts/.scholar-identity-audit.jsonl

# ORCID resolution measurement output (contains person data; local-only, S215)
scripts/.orcid-resolution.jsonl

# ORCID backfill checkpoints (contain person data; local-only, S215)
scripts/.orcid-backfill.jsonl
scripts/.orcid-backfill-applied.jsonl

# Contact-ORCID back-prop backfill checkpoints (PR2; contain person data; local-only, S217)
scripts/.contact-orcid-backfill.jsonl
scripts/.contact-orcid-backfill-applied.jsonl

# Lone-ORCID + Scholar corroboration backfill checkpoints (contain person data; local-only, S219)
scripts/.lone-orcid-scholar.jsonl
scripts/.lone-orcid-scholar-applied.jsonl
scripts/.lone-orcid-scholar-conflicts.jsonl
scripts/.lone-orcid-scholar-*.out

# ORCID cross-store match probe artifact (contains person data; local-only, S216)
scripts/.orcid-cross-store.json
scripts/.orcid-contact-direct.json
scripts/.orcid-contact-provenance.json
