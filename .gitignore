# Dependencies
/node_modules
/.pnp
.pnp.js

# Testing
/coverage

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.tgz
*.tar.gz

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env*.local

# Vercel
.vercel

# IDE
.vscode/
.idea/

# Source documents
*.docx
# Migration data files
proposals-for-migration.csv
# Generated schema output (may contain tokens)
scripts/dynamics-schema-output.json
scripts/dynamics-schema-diff.json
# Generated artifacts (presentation/deck builds, etc.)
outputs/
# Cron scheduler runtime state (session-scoped)
.claude/scheduled_tasks.json
.claude/scheduled_tasks.lock
# Smoke-test helper state (live prod GUIDs + tester email — never commit)
scripts/.smoke-test-candidate.json
.git.nosync
# iCloud-exclusion artifacts (local-only build/dep dirs)
*.nosync/
tmp/
.playwright-mcp/
# Codex CLI artifacts. .agents/ stays ignored (corrupted s/Claude/Codex/ skill
# mirrors). AGENTS.md is NOT ignored — it is a tracked symlink to CLAUDE.md so
# Codex loads identical current content (Codex verified: it ingests literal
# AGENTS.md bytes as user_instructions; a thin pointer would leave it
# context-blind). The only writer is the manual `migrate-to-codex` skill, which
# unlink()s + recreates (Codex-verified cli.py:452-470) — that SEVERS the
# symlink into a regular file = visible tracked diff, never a silent CLAUDE.md
# mutation. Restore with `git checkout AGENTS.md`. Do not run migrate-to-codex
# here. Background: docs/DOCS_GROUND_TRUTH_AUDIT_2026-05-19.md.
.agents/
